we need to make workspace modes fluid & dynamic like blender

1. each viewport should have a -/e/+ button at the top left so that the user can right click this and control this viewport. 
- this button will act as a navagation for the viewport. if the user wants to switch the viewport from the browser to the editor. it will change only that viewport. this means we will need to ne able to see 2 spaghetti editors at once. or 2 browsers at once. or two model viewports at once.
-Model viewport
-Spaghetti Editor viewport
-Browser Viewport

. this is primarly for split view. but i think it would be best to organize it this way so that we can use the viewports dynamically & copy them around if the user chooses to. - blender has this set up well. 

refrence 1 - docs\Human-Plans\Architecture\Workspace-Modes\img\IMGref_1.png

in refrence one you will see two viewports. the top one the model viewport. the bottom is the timeline viewport. (in our case we will use the spaghetti editor) - on the top left there is a button that the user can click. to change the viewport type. 

in refrence image 2 -docs\Human-Plans\Architecture\Workspace-Modes\img\IMGref_2.png

you can see the menu that pops up when the user clicks the top left button. it allows them to choose the viewport. 
