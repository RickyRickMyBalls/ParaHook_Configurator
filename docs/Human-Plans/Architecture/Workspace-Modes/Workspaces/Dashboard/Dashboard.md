# Dashboard

## Doc Header

### Doc History
79. 2026-04-04 19:30: Closed `Future/Dashboard_Phase Dashboard-12 - Lane Layout Tools And Smart Align.md` `Phase 12.2 - Smart Align Toggle For Non-Overlapping Stack Alignment` after shipping the lane-local toggle plus non-overlapping align widening through `DashboardSurface.tsx` and focused AppShell regressions, then updated this umbrella doc so the `Dashboard-12` layout-tools ladder now reads as landed through both the grid action and smart-align follow-on
78. 2026-04-04 19:33: Tightened `Future/Dashboard_Phase Dashboard-12 - Lane Layout Tools And Smart Align.md` by preparing `Phase 12.2 - Smart Align Toggle For Non-Overlapping Stack Alignment` for implementation around the live lane-local align seam plus the shipped real-note bounds contract in `DashboardSurface.tsx`, then updated this umbrella doc so the next dashboard layout-tools command now reads as that explicit visible-toggle smart-align widening pass
77. 2026-04-04 19:10: Closed `Future/Dashboard_Phase Dashboard-12 - Lane Layout Tools And Smart Align.md` `Phase 12.1 - Arrange Notes Into A Lane Grid` after shipping the lane-local grid arrange action through `DashboardSurface.tsx` plus focused AppShell coverage, then updated this umbrella doc so the next dashboard layout-tools command now reads as `Phase 12.2 - Smart Align Toggle For Non-Overlapping Stack Alignment`
76. 2026-04-04 19:08: Added the dedicated future doc `Future/Dashboard_Phase Dashboard-12 - Lane Layout Tools And Smart Align.md` so the next dashboard lane-layout-tool follow-on now has one canonical planning home for one-shot lane grid arrangement plus the later smart-align toggle widening, and updated this umbrella doc so the next `Dashboard-12` planning read points there instead of leaving those new layout actions only in chat
75. 2026-04-04 18:37: Added `Phase 12 - Dashboard Top Shell Cleanup` inside `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` so the remaining dashboard top-panel and board-shell cleanup now has its own explicit later phase after the sticky-note attachment and sizing ladder, and updated this umbrella doc so that future dashboard-shell polish is now named directly instead of living only as a loose earlier `Phase 7` note
74. 2026-04-04 14:29: Closed `Future/Dashboard_Phase Dashboard-7.10 - Sticky Attachment Bounds And Resizable Notes.md` `Phase 10.5 - Sticky Note Focus Lift` after shipping the temporary double-click lifted-note override on top of the attachment-aware stack order, then updated this umbrella doc so the next dashboard attachment follow-on now returns to `Phase 11` direct-drag child detach
73. 2026-04-04 14:25: Tightened `Future/Dashboard_Phase Dashboard-7.10 - Sticky Attachment Bounds And Resizable Notes.md` by preparing `Phase 10.5 - Sticky Note Focus Lift` for implementation around the shipped attachment-aware stack order in `DashboardSurface.tsx`, the sticky-note shell interactions in `DashboardStickyNoteCard.tsx`, and the focused AppShell overlap regressions, then updated this umbrella doc so the next dashboard attachment-and-resize command now reads as one explicit temporary double-click front-lift override pass
72. 2026-04-04 13:33: Closed `Future/Dashboard_Phase Dashboard-7.10 - Sticky Attachment Bounds And Resizable Notes.md` `Phase 10.4 - Attachment Stack Layering Polish` after shipping attachment-aware sticky-note render ordering so children stay visually above parents at rest and during parent-subtree drag, then updated this umbrella doc so the next dashboard attachment-and-resize command now reads as `Phase 10.5 - Sticky Note Focus Lift`
71. 2026-04-04 13:25: Reworked `Future/Dashboard_Phase Dashboard-7.10 - Sticky Attachment Bounds And Resizable Notes.md` by splitting the old broad post-resize `Phase 10.4` placeholder into one implementation-ready `Phase 10.4 - Attachment Stack Layering Polish` pass plus a later `Phase 10.5 - Sticky Note Focus Lift` pass, then updated this umbrella doc so the next dashboard attachment-and-resize command now reads as the default child-above-parent stack-order cleanup before the later double-click front-lift override
70. 2026-04-04 13:17: Closed `Future/Dashboard_Phase Dashboard-7.10 - Sticky Attachment Bounds And Resizable Notes.md` `Phase 10.3 - Resizable Sticky Notes Foundation` after shipping dashboard-owned persisted sticky-note size plus explicit edge and corner resize handles through the dashboard model, surface, card chrome, and focused regressions, then updated this umbrella doc so the next dashboard attachment-and-resize command now reads as `Phase 10.4 - Resize Polish And Attachment Compatibility`
69. 2026-04-04 13:05: Tightened `Future/Dashboard_Phase Dashboard-7.10 - Sticky Attachment Bounds And Resizable Notes.md` by preparing `Phase 10.3 - Resizable Sticky Notes Foundation` for implementation around the newly centralized sticky-note geometry seam plus the dashboard-owned layout model and sticky-note chrome, then updated this umbrella doc so the next dashboard attachment-and-resize command now reads as one explicit persisted-size plus resize-handle foundation pass before later mixed-size polish
68. 2026-04-04 13:01: Closed `Future/Dashboard_Phase Dashboard-7.10 - Sticky Attachment Bounds And Resizable Notes.md` `Phase 10.2 - Attachment Bounds Refactor For Variable Note Size` after shipping the internal sticky-note geometry-seam cleanup through `DashboardSurface.tsx` without changing current runtime behavior, then updated this umbrella doc so the next dashboard attachment-and-resize command now reads as `Phase 10.3 - Resizable Sticky Notes Foundation`
67. 2026-04-04 12:55: Tightened `Future/Dashboard_Phase Dashboard-7.10 - Sticky Attachment Bounds And Resizable Notes.md` by preparing `Phase 10.2 - Attachment Bounds Refactor For Variable Note Size` for implementation around the live fixed-size sticky-note geometry helpers in `DashboardSurface.tsx`, then updated this umbrella doc so the next dashboard attachment-and-resize command now reads as one internal bounds-seam cleanup before later resize persistence and handles land
66. 2026-04-04 12:48: Closed `Future/Dashboard_Phase Dashboard-7.10 - Sticky Attachment Bounds And Resizable Notes.md` `Phase 10.1 - Parent Full-Body Attachment Hit Area` after shipping the parent-body attachment-hit widening through `DashboardSurface.tsx` plus focused AppShell coverage, then updated this umbrella doc so the next dashboard attachment-and-resize command now reads as `Phase 10.2 - Attachment Bounds Refactor For Variable Note Size`
65. 2026-04-04 12:40: Tightened `Future/Dashboard_Phase Dashboard-7.10 - Sticky Attachment Bounds And Resizable Notes.md` by preparing `Phase 10.1 - Parent Full-Body Attachment Hit Area` for implementation around the live drop-time attachment helper plus focused AppShell regressions, then updated this umbrella doc so the next dashboard attachment-and-resize command now reads as one narrow parent-hit-area cleanup before the later bounds refactor and resize phases
64. 2026-04-04 12:30: Added the dedicated future doc `Future/Dashboard_Phase Dashboard-7.10 - Sticky Attachment Bounds And Resizable Notes.md` to pull the post-`Dashboard-7 / Phase 10` attachment-hit and sticky-note resizing ladder into its own planning home, then updated this umbrella doc so the next dashboard attachment-and-resize work now points at that new `7.10` doc with `Phase 10.1` staged first
63. 2026-04-04 12:30: Tightened `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` again by locking the post-`Phase 10` order into explicit `Phase 10.1` through `Phase 10.4` subphases for full-body parent attachment hit area, variable-bounds refactor, resizable sticky-note foundation, and resize-plus-attachment compatibility polish, then updated this umbrella doc so the next `Dashboard-7` command now reads as `Phase 10.1` instead of jumping straight to direct-drag detach
62. 2026-04-04 12:13: Closed `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` `Phase 10 - Move Attached Note Subtrees` after shipping parent-led sticky-note subtree movement through the existing dashboard drag seam plus one batched dashboard placement commit that preserves attachment links across cross-lane subtree movement, then updated this umbrella doc so the next `Dashboard-7` command now reads as `Phase 11` direct-drag child detach
61. 2026-04-04 12:03: Tightened `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` by preparing `Phase 10 - Move Attached Note Subtrees` for implementation around the shipped attachment-tree model plus the live sticky-note drag-preview and drop-commit seam in `DashboardSurface.tsx`, then updated this umbrella doc so the next `Dashboard-7` command now reads as one explicit parent-led subtree-movement pass before the later direct-drag detach phase
60. 2026-04-04 11:58: Closed `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` `Phase 9 - Attach Notes On Drop By Title-Bar Overlap` after shipping same-lane drop-time title-bar-overlap attachment through the existing dashboard drag seam and focused AppShell regressions, then updated this umbrella doc so the next `Dashboard-7` command now reads as `Phase 10` attached subtree movement
59. 2026-04-04 11:50: Tightened `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` by preparing `Phase 9 - Attach Notes On Drop By Title-Bar Overlap` for implementation around the live sticky-note drag preview plus drop commit seam in `DashboardSurface.tsx`, the shipped title-bar drag contract in `DashboardStickyNoteCard.tsx`, the landed dashboard-owned `parentNoteId` groundwork, and the focused `AppShell.test.tsx` regressions, then updated this umbrella doc so the next `Dashboard-7` command now reads as one explicit same-lane drop-time attachment pass before subtree movement or detach work
58. 2026-04-04 11:41: Closed `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` `Phase 8 - Sticky Attachment Tree Contract` after landing the first dashboard-owned attachment-tree model groundwork through optional persisted `parentNoteId` note relationships plus store and persistence normalization for invalid links, then updated this umbrella doc so the next `Dashboard-7` command now reads as `Phase 9` drop-time attachment creation by title-bar overlap
57. 2026-04-04 11:38: Tightened `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` by preparing `Phase 8 - Sticky Attachment Tree Contract` for implementation around the shipped sticky-note drag seam, sticky-note card title-bar contract, and focused `AppShell.test.tsx` dashboard regressions, then updated this umbrella doc so the next `Dashboard-7` command now reads as one explicit attachment-tree contract pass with the later create/move/detach runtime slices locked behind it
56. 2026-04-04 11:35: Reworked the later `Dashboard-7` sticky-note attachment idea inside `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` from one small `Phase 8` placeholder into a multi-phase attachment-tree ladder that now separates contract locking, drop-time attachment creation, subtree movement, and child detachment, then updated this umbrella doc so the backlog reflects that larger drag-semantics direction more honestly
55. 2026-04-04 11:30: Added `Phase 8 - Sticky Title-Bar Attachment Stacks` inside `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` to capture the new board-organization idea where title-bar overlap on sticky-note drop can create one attached stack that later moves together, then updated this umbrella doc so the `Dashboard-7` backlog now includes that later drag-semantics follow-on after the current burger-menu, selection-aware action, and shell-polish slices
54. 2026-04-04 11:24: Closed `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` `Phase 5 - Sticky Note Burger Menu Foundation` after the sticky-note chrome cleanup shipped through a new top-right burger menu, moved `Open in Notepad` and visible color access into that overflow surface, and added focused AppShell regressions, then updated this umbrella doc so the next `Dashboard-7` ladder now reads as having the first five board-tool slices landed
53. 2026-04-04 11:17: Tightened `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` by preparing `Phase 5 - Sticky Note Burger Menu Foundation` for implementation around the live sticky-note title-bar action row, right-click color-menu seam, and focused `AppShell.test.tsx` dashboard regressions, then updated this umbrella doc so the next `Dashboard-7` command now reads as one explicit sticky-note chrome cleanup pass instead of a looser overflow-menu idea
52. 2026-04-04 11:04: Closed `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` `Phase 4 - Align Selected Notes` after the first board-local dashboard align pass shipped through temporary lane-header align controls, lane-local vertical and horizontal edge alignment, and focused AppShell regressions, then updated this umbrella doc so the next `Dashboard-7` ladder now reads as having the first four board-tool slices landed
51. 2026-04-04 10:56: Tightened `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` by preparing `Phase 4 - Align Selected Notes` for implementation around the shipped board-local selection plus lane-local group-move seams in `DashboardSurface.tsx`, the existing sticky-note selected-state affordance in `DashboardStickyNoteCard.tsx`, and the focused `AppShell.test.tsx` dashboard regressions, then updated this umbrella doc so the next `Dashboard-7` command now reads as one explicit align pass instead of a looser backlog item
50. 2026-04-04 10:47: Closed `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` `Phase 3 - Group Move And Selection Polish` after the dashboard lane-local group-move widening shipped through the existing drag seam, sticky-note selection visuals, and focused AppShell regressions, then updated this umbrella doc so the next `Dashboard-7` ladder now reads as having the first three board-tool slices landed
49. 2026-04-04 10:40: Tightened `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` by preparing `Phase 3 - Group Move And Selection Polish` for implementation around the newly shipped board-local selection seam in `DashboardSurface.tsx`, the sticky-note selected-state affordance in `DashboardStickyNoteCard.tsx`, and the focused `AppShell.test.tsx` dashboard regressions, then updated this umbrella doc so the next `Dashboard-7` command now reads as one explicit group-move widening instead of a broad movement idea
48. 2026-04-04 10:37: Closed `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` `Phase 2 - Multi-Note Selection Foundation` after the dashboard board-local selection pass shipped through `DashboardSurface.tsx`, sticky-note selected-state visuals, and focused AppShell regressions, then updated this umbrella doc so the next `Dashboard-7` ladder now reads as having both the zoom-fit correction and the first selection slice landed
47. 2026-04-04 10:28: Tightened `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` by preparing `Phase 2 - Multi-Note Selection Foundation` for implementation around the live `DashboardSurface.tsx`, `DashboardStickyNoteCard.tsx`, and focused `AppShell.test.tsx` seams, then updated this umbrella doc so the next `Dashboard-7` command now reads as one explicit board-selection pass instead of a broad selection idea
46. 2026-04-04 09:20: Reworked `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` so the remaining board-tool backlog is now an explicit multi-phase ladder after shipped `Phase 1`, splitting the next work into ordered multi-note selection, group move, align, burger-menu, and shell-polish slices, then updated this umbrella doc so the `Dashboard-7` bucket reads as a real execution sequence instead of one loose feature list
45. 2026-04-04 09:14: Closed `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` `Phase 1 - Zoom-Unlocked Fit All Notes In Lane` after the dashboard fit action shipped with true zoom-plus-pan fit-to-bounds only for zoom-unlocked lanes while locked lanes keep the simpler current recovery behavior, then updated this umbrella doc so the next backlog bucket now reads honestly as having that first `Dashboard-7` camera correction landed
44. 2026-04-04 09:00: Tightened `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` again by locking `Phase 1 - Zoom-Unlocked Fit All Notes In Lane` as an implementation-ready slice around true unlocked-lane zoom-plus-pan fit-to-bounds, explicit exclusions, and focused dashboard verification, then updated this umbrella doc so the next dashboard backlog now reads as a real next command instead of a loose suggestion
43. 2026-04-04 08:58: Tightened `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` by adding `Phase 1 - Zoom-Unlocked Fit All Notes In Lane`, locking the first `Dashboard-7` slice around the magnifying-glass action fitting all sticky notes in an unlocked lane vertically and horizontally instead of only solving the left-to-right spread, and updated this umbrella doc so the next dashboard backlog now starts with one explicit camera-fit correction
42. 2026-04-04 08:58: Added `Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md` as the next dashboard-family backlog surface after shipped `Dashboard-6`, preserving the requested next feature list around zoom polish, multi-note selection, alignment, burger-menu cleanup, and thinner top-panel board chrome in one canonical future doc
41. 2026-04-04 08:52: Closed `Phase 4 - Optional Lane Zoom Unlock` inside `Future/Dashboard_Phase Dashboard-5.2 - Real Lane Canvas Camera.md` after the lane-header lock-button zoom follow-on shipped through `DashboardSurface.tsx`, focused dashboard regressions, and verification, then updated this umbrella doc so the dashboard lane-camera ladder now reads as fully landed through the optional zoom-unlock control and noted that the runtime slice was logged in `docs/CHANGELOG.md`
40. 2026-04-04 08:44: Added `Phase 4 - Optional Lane Zoom Unlock` inside `Future/Dashboard_Phase Dashboard-5.2 - Real Lane Canvas Camera.md` so the dashboard lane-camera ladder now captures one small post-ship follow-on where each lane header can expose a lock button beside the fit action, default to locked pan-only behavior, and only allow lane-local zoom after explicit user unlock
39. 2026-04-04 08:32: Closed `Phase 3 - Add Resizable Lane Widths And Layout Polish` inside `Future/Dashboard_Phase Dashboard-6 - User-Managed Board Lanes And Resizable Columns.md` after the vertical splitter runtime pass shipped through the dashboard store, surface, styling, and focused regressions, then updated this umbrella doc so the dashboard lane-management follow-on now reads as fully landed through user-managed lanes plus persisted width resizing and logged the matching runtime slice in `docs/CHANGELOG.md`
38. 2026-04-04 08:25: Prepared `Phase 3 - Add Resizable Lane Widths And Layout Polish` inside `Future/Dashboard_Phase Dashboard-6 - User-Managed Board Lanes And Resizable Columns.md` for implementation by grounding it in the live lane-width persistence seam in `src/app/dashboard/useDashboardStore.ts`, the dynamic board-grid rendering seam in `src/app/workspace/DashboardSurface.tsx`, the dashboard styling owner in `src/app/theme/foundation/base.css`, and the existing dashboard integration coverage in `src/app/AppShell.test.tsx`, then tightening this umbrella doc so the next dashboard lane follow-on now reads as a narrow splitter-and-width-balancing runtime pass instead of a loose polish idea
37. 2026-04-04 08:21: Closed `Phase 2 - Implement User-Managed Lanes` inside `Future/Dashboard_Phase Dashboard-6 - User-Managed Board Lanes And Resizable Columns.md` after the dynamic-lane runtime widening shipped through the dashboard types, store, persistence, surface, and focused regressions, then updated this umbrella doc so the dashboard lane-management follow-on now reads as having user-managed lanes live with only the later width-resize polish still open in `Phase 3`
36. 2026-04-04 08:07: Prepared `Phase 2 - Implement User-Managed Lanes` inside `Future/Dashboard_Phase Dashboard-6 - User-Managed Board Lanes And Resizable Columns.md` for implementation by grounding it in the live fixed-lane store, persistence, surface, and dashboard-regression seams, then tightening this umbrella doc so the next dashboard runtime widening now reads as a direct move from hardcoded lanes to stable `laneId`-backed lane records
35. 2026-04-04 08:06: Closed `Phase 1 - Lock Dynamic Lane Contract` inside `Future/Dashboard_Phase Dashboard-6 - User-Managed Board Lanes And Resizable Columns.md` by marking the dynamic-lane questions answered and the first-cut lane-model contract locked, then updated this umbrella doc so the dashboard lane-management follow-on now reads as having a finished contract pass with runtime work staged next in `Phase 2`
34. 2026-04-04 08:00: Prepared `Phase 1 - Lock Dynamic Lane Contract` inside `Future/Dashboard_Phase Dashboard-6 - User-Managed Board Lanes And Resizable Columns.md` for implementation by grounding it in the live fixed-lane seams in `src/app/dashboard/useDashboardStore.ts`, `src/app/dashboard/dashboardPersistence.ts`, and `src/app/workspace/DashboardSurface.tsx`, then tightening this umbrella doc so the next dashboard follow-on now reads as a real contract-lock pass before the dynamic-lane runtime widening
33. 2026-04-04 07:59: Reworked `Future/Dashboard_Phase Dashboard-6 - User-Managed Board Lanes And Resizable Columns.md` into an explicit three-phase ladder so the next dashboard follow-on now stages dynamic-lane work across a contract lock, a user-managed lane runtime pass, and a later resizable-width polish slice instead of treating all of that board-structure work as one implementation bucket
32. 2026-04-04 07:56: Added the dedicated `Dashboard-6 - User-Managed Board Lanes And Resizable Columns` future phase doc at `Future/Dashboard_Phase Dashboard-6 - User-Managed Board Lanes And Resizable Columns.md`, and updated this umbrella doc so the next dashboard follow-on now promotes fixed `TO DO` and `Completed` lanes into a user-managed lane system with add/remove/rename/resize questions plus suggested answers
31. 2026-04-04 07:52: Closed the shipped `Dashboard-5.2 / Phase 3 - Lane Fit-To-Notes Action` follow-on by updating `Future/Dashboard_Phase Dashboard-5.2 - Real Lane Canvas Camera.md` and this umbrella doc so the dashboard family now records the lane-header recovery buttons for `TO DO` and `Completed`, and logged the matching runtime slice in `docs/CHANGELOG.md`
30. 2026-04-04 07:46: Prepared `Phase 3 - Lane Fit-To-Notes Action` inside `Future/Dashboard_Phase Dashboard-5.2 - Real Lane Canvas Camera.md` for implementation by grounding it in the live `DashboardSurface.tsx` lane-header plus local lane-camera seam and the existing dashboard camera regressions in `src/app/AppShell.test.tsx`, then tightening this umbrella doc so that small post-ship recovery slice now reads as implementation-ready
29. 2026-04-04 07:44: Added `Phase 3 - Lane Fit-To-Notes Action` inside `Future/Dashboard_Phase Dashboard-5.2 - Real Lane Canvas Camera.md` so the dashboard lane-camera ladder now includes one small post-ship usability follow-on for recovering off-screen sticky notes through a lane-local fit action, and updated this umbrella doc so the shipped real-camera work now explicitly leaves that tiny recovery action open
28. 2026-04-04 07:39: Closed the shipped `Dashboard-5.2` implementation by updating `Future/Dashboard_Phase Dashboard-5.2 - Real Lane Canvas Camera.md` and this umbrella doc so the dashboard family now records real per-lane camera movement instead of DOM-scroll lane panning, while keeping sticky-note placement ownership unchanged and logging the runtime slice in `docs/CHANGELOG.md`
27. 2026-04-04 07:34: Prepared `Phase 2 - Implement Real Lane Camera` inside `Future/Dashboard_Phase Dashboard-5.2 - Real Lane Canvas Camera.md` for implementation by grounding it in the live `DashboardSurface.tsx` scroll-based pan plus drag-math seam and the existing dashboard regression coverage in `src/app/AppShell.test.tsx`, then tightening this umbrella doc so the open `Phase 5.2` runtime slice now reads as a narrow implementation-ready camera conversion rather than a broad follow-on idea
26. 2026-04-04 07:34: Closed `Phase 1 - Lock Lane Camera Contract` inside `Future/Dashboard_Phase Dashboard-5.2 - Real Lane Canvas Camera.md` by marking the lane-camera contract questions answered and the first-cut decisions locked, then updated this umbrella doc so `Phase 5.2` now reads as having a finished contract-lock slice with the real runtime work still staged in `Phase 2`
25. 2026-04-04 07:31: Prepared `Phase 1 - Lock Lane Camera Contract` inside `Future/Dashboard_Phase Dashboard-5.2 - Real Lane Canvas Camera.md` for implementation by grounding it in the live `DashboardSurface.tsx` lane-scroll plus visible-rect clamp seam and the `SpaghettiCanvas.tsx` camera-transform reference path, then tightening this umbrella doc so the later real lane-camera follow-on now reads as an explicit implementation-prep contract rather than a looser research idea
24. 2026-04-04 07:20: Added the dedicated `Dashboard-5.2` future phase doc at `Future/Dashboard_Phase Dashboard-5.2 - Real Lane Canvas Camera.md`, and updated this umbrella doc so the next architectural dashboard follow-on now captures the Spaghetti-style lane-camera direction plus the key design questions and recommended answers before implementation
23. 2026-04-04 07:11: Added the dedicated `Dashboard-5.1` future phase doc at `Future/Dashboard_Phase Dashboard-5.1 - Pannable Sticky Note Lane Canvases.md`, and updated this umbrella doc so the next implementation-ready dashboard follow-on now targets middle-mouse pannable `TO DO` and `Completed` lane canvases while keeping sticky-note note ownership and placement persistence unchanged
22. 2026-04-04 06:51: Tightened `Phase 5` into an implementation-ready dashboard execution slice by grounding it in the live `DashboardSurface.tsx` and `useNotepadStore.ts` seams, locking the first cut to a hero `Add Sticky Note` action, one extracted sticky-note card component, blur-commit inline editing with visually seamless fields, and a dedicated drag handle while keeping extra widget work staged later
21. 2026-04-04 06:47: Added the dedicated `Dashboard-5` future phase doc at `Future/Dashboard_Phase Dashboard-5 - Dashboard Sticky Note Creation And Inline Editing.md`, updated this umbrella doc so the next implementation-ready dashboard slice now reflects dashboard-created sticky notes plus inline title/body editing while keeping `Notepad` as the canonical text editor, and pushed extra dashboard widgets to a later `Phase 6`
20. 2026-04-04 06:40: Refreshed this umbrella doc after the immediate post-`Dashboard-4.1` cleanup so the current family status no longer claims sticky-note lane buttons remain, and now reads honestly as drag-only lane movement with the same dashboard-owned lane-plus-position persistence
19. 2026-04-04 06:35: Updated this umbrella doc after the shipped `Dashboard-4.1` implementation so the family status now records sticky notes dragging directly between `TO DO` and `Completed` with dashboard-owned lane-plus-position drop persistence and retained fallback lane buttons, while extra widgets, console workspace-modes adoption, and popup-local shell adoption remain later follow-ons
18. 2026-04-04 06:30: Added the dedicated `Dashboard-4.1` future phase doc at `Future/Dashboard_Phase Dashboard-4.1 - Drag Sticky Notes Between Board Lanes.md`, and updated this umbrella doc so the next dashboard follow-on is now explicitly cross-lane sticky-note drag and drop while keeping lane ownership in the dashboard widget model and the shared note model unchanged
17. 2026-04-03 22:14: Updated this umbrella doc after the shipped `Dashboard-4` implementation so the family status now records sticky notes living in real `TO DO` and `Completed` dashboard lanes with dashboard-owned lane persistence and explicit lane-move actions, while style presets, extra widgets, console workspace-modes adoption, and popup-local shell adoption remain later follow-ons
16. 2026-04-03 22:10: Tightened the new `Dashboard-4` future phase into a real implementation-ready next slice by grounding it in the live dashboard store and board-render seams, locking the first board-organization pass to two lanes named `TO DO` and `Completed`, and choosing explicit lane-move card actions instead of widening immediately into full cross-lane drag behavior
15. 2026-04-03 22:04: Added the dedicated `Dashboard-4` future phase doc at `Future/Dashboard_Phase Dashboard-4 - Add To Do And Completed Board Lanes For Sticky Notes.md`, and updated this umbrella doc so the next implementation-ready slice is now explicitly two dashboard board lanes named `TO DO` and `Completed`, with lane ownership staying in the dashboard widget model instead of widening the shared notepad note model
14. 2026-04-03 21:58: Updated this umbrella doc after the shipped `Dashboard-3` implementation so the family status now records sticky notes as the first live dashboard widget with dashboard-owned placement persistence layered cleanly over the shared notepad note model, while sticky-note styling, extra widgets, console workspace-modes adoption, and popup-local shell adoption remain later follow-ons
13. 2026-04-03 21:43: Added the dedicated `Dashboard-3` future phase doc at `Future/Dashboard_Phase Dashboard-3 - Add Sticky Notes As The First Dashboard Widget.md`, and updated this umbrella doc so the next implementation-ready slice is now explicitly the first real dashboard widget path through sticky-note cards, dashboard-owned placement persistence, and an `open in Notepad` handoff without widening into styling or extra widgets
12. 2026-04-03 20:22: Updated this umbrella doc after the shipped `Dashboard-2` implementation so the family status now records that `Notepad` is live as a real secondary workspace surface with one shared note model and dedicated note persistence, while sticky notes, console workspace-modes adoption, and popup-local shell adoption remain later follow-ons
11. 2026-04-03 20:08: Added the dedicated `Dashboard-2` future phase doc at `Future/Dashboard_Phase Dashboard-2 - Create Notepad Workspace And Shared Note Model.md`, and updated this umbrella doc so the next implementation-ready slice is now explicitly `Notepad` surface onboarding plus one feature-owned shared note model and note persistence seam before sticky-note widget work
10. 2026-04-03 20:05: Updated this umbrella doc after the shipped `Dashboard-1` implementation so the family status now records that the main-workspace `dashboard` surface foundation is live through the shared workspace seams, while console workspace-modes adoption, popup-local shell adoption, `Notepad`, and widget work remain later follow-ons
9. 2026-04-03 19:51: Tightened the `Dashboard-1` future phase into a real implementation-ready slice after checking the live `useAppShellViewportActions`, `PopupWorkspaceShell`, and console workspace-modes seams, and updated this umbrella doc so Phase 1 now explicitly means main-workspace `dashboard` surface-kind adoption while console staged-navigation and popup-local shell adoption stay deferred
8. 2026-04-03 19:44: Added the first dedicated dashboard-family future phase doc at `Future/Dashboard_Phase Dashboard-1 - Create Dashboard Workspace Foundation.md`, moved the immediate implementation-planning handoff there, and updated this umbrella doc so `Architecture/Dashboard/` is now the canonical home for dashboard execution planning instead of pointing back at the older workspace-family promotion doc
7. 2026-04-03 19:38: Promoted this wish doc into the real workspace-family execution lane by pointing implementation planning at `docs/Human-Plans/Architecture/Workspace-Modes/Future/Workspace_Phase Workspace-7.5-17 - Dashboard And Notepad Surface Onboarding.md`, while keeping this file as the product-shape source for `Dashboard`, `Notepad`, and `Sticky Notes`
6. 2026-04-03 17:42: Prepared `Phase 0 - Research New Workspace Foundation` for implementation by grounding it in the live workspace-mode architecture, locking the current owner seams around `AppShell`, `useWorkspaceStore`, `workspaceShellTypes`, `ViewportFrame`, and the workspace surface-action layer, and adding a concrete research sequence, file targets, and verification shape so the dashboard plan can start from the real `Workspace 7.x` foundation instead of a speculative parallel mode system
5. 2026-04-03 17:32: Added a new `Phase 0` research step ahead of implementation so the dashboard plan explicitly starts by studying how new workspace or viewport foundations should be introduced before building `Dashboard` and `Notepad`
4. 2026-04-03 17:29: Added an implementation phase ladder at the end of the document using one `##` section per phase, mirroring the future-doc planning style more closely, and cleaned up the leftover duplicate `Notepad` widget header now that `Notepad` is defined as an editor workspace rather than a dashboard widget
3. 2026-04-03 17:22: Reframed `Notepad` so it is no longer treated as a dashboard widget, but instead as the dedicated editor or workspace surface behind notes, while keeping `Sticky Notes` as the first real dashboard widget and preserving the possibility that `Notepad` could later support more than one note-based feature
2. 2026-04-03 17:18: Restructured the doc so `Dashboard` is the main feature section and each planned widget gets its own section, with `Notepad` and `Sticky Notes` captured as widgets `01` and `02`, while clarifying that both `Dashboard` and `Notepad` are their own viewport or workspace modes
1. 2026-04-03 17:10: Created this wish-feature planning doc to define `Dashboard` as a new ParaHook workspace direction, with `Notepad` and `Sticky Notes` treated as core features inside the dashboard rather than as separate top-level workspace modes

### Purpose

This doc defines the future `Dashboard` direction for ParaHook.

Use it to answer:
- what `Dashboard` should be
- how `Dashboard` should work as its own viewport or workspace mode
- which widgets belong inside `Dashboard`
- how `Notepad` and `Sticky Notes` should relate to it
- what the first useful version should include

### Why This Doc Exists

The idea has now settled into a clearer shape:
- `Dashboard` is its own viewport or workspace mode
- `Notepad` is also its own viewport or workspace mode
- `Dashboard` should also act as the home for widgets
- `Sticky Notes` should be treated as a widget
- `Notepad` should be treated as the editor surface behind notes rather than as a widget

This doc exists to hold that structure in a clean way before architecture planning begins.

The goal here is to:
- define the main `Dashboard` mode
- define the first widgets inside it
- keep the feature organized as more widgets are added later

### Promotion Status

This doc is still the product-shape source for `Dashboard`.

The canonical dashboard planning home now lives in:
- `docs/Human-Plans/Architecture/Dashboard/`

The first implementation-ready phase doc now lives in:
- `docs/Human-Plans/Architecture/Dashboard/Future/Dashboard_Phase Dashboard-1 - Create Dashboard Workspace Foundation.md`

The most recently shipped phase doc now lives in:
- `docs/Human-Plans/Architecture/Dashboard/Future/Dashboard_Phase Dashboard-5.2 - Real Lane Canvas Camera.md`

The next implementation-ready phase doc now lives in:
- `docs/Human-Plans/Architecture/Dashboard/Future/Dashboard_Phase Dashboard-7.10 - Sticky Attachment Bounds And Resizable Notes.md`

The next backlog phase doc now lives in:
- `docs/Human-Plans/Architecture/Dashboard/Future/Dashboard_Phase Dashboard-7 - Dashboard Board Tools And Surface Polish Backlog.md`

The next attachment-and-resize phase doc now lives in:
- `docs/Human-Plans/Architecture/Dashboard/Future/Dashboard_Phase Dashboard-7.10 - Sticky Attachment Bounds And Resizable Notes.md`

The next dashboard layout-tools phase doc now lives in:
- `docs/Human-Plans/Architecture/Dashboard/Future/Dashboard_Phase Dashboard-12 - Lane Layout Tools And Smart Align.md`

The previously implementation-ready phase doc now lives in:
- `docs/Human-Plans/Architecture/Dashboard/Future/Dashboard_Phase Dashboard-5.2 - Real Lane Canvas Camera.md`

That first shipped phase now means:
- main-workspace `dashboard` surface-kind adoption
- one minimal dashboard shell
- later follow-ons for console `Workspace Modes` adoption and popup-local child-window surface switching

That second shipped phase now means:
- main-workspace `notepad` surface-kind adoption
- one shared note model with dedicated note persistence outside workspace layout snapshots
- one calm note-writing surface with create, rename, edit, delete, active-note, and pin-state behavior
- later follow-ons for sticky notes, dashboard widget rendering, console `Workspace Modes` adoption, and popup-local child-window surface switching

That third shipped phase now means:
- `Dashboard` now renders pinned notes as the first real sticky-note widget family
- sticky-note layout persists through a separate dashboard-owned placement store instead of workspace layout or note-content persistence
- sticky notes can unpin in place, drag on the board, and open back into `Notepad` from both slotted and detached dashboard hosts
- sticky-note styling, board organization, extra widgets, console `Workspace Modes` adoption, and popup-local child-window surface switching still remain later follow-ons

That fourth shipped phase now means:
- sticky notes now live in two real board lanes named `TO DO` and `Completed`
- lane assignment persists through the dashboard-owned widget store and stays out of the shared notepad note model
- lane changes happen through explicit card actions while drag remains scoped to placement inside the current lane
- sticky-note styling, extra widgets, console `Workspace Modes` adoption, and popup-local child-window surface switching still remain later follow-ons

That `Phase 4.1` shipped follow-on now means:
- sticky notes can now drag directly between `TO DO` and `Completed`
- drop commits lane assignment and lane-local placement together through the dashboard-owned widget store
- lane movement now happens through drag only
- extra widgets, console `Workspace Modes` adoption, and popup-local child-window surface switching still remain later follow-ons

That `Phase 5` shipped follow-on now means:
- users can create sticky notes directly from `Dashboard`
- sticky-note title and body can be edited inline on the board
- sticky-note color can be changed from a title-bar right-click color menu
- `Notepad` still remains the larger editor surface for the same shared note records
- lane-board panning and extra widgets still remain later follow-ons

That `Phase 5.1` shipped follow-on now means:
- dashboard lane boards now support middle-mouse pannable viewport movement through DOM scroll
- sticky-note coordinates and placement persistence still stay lane-local and unchanged
- a later real lane-camera follow-on still remains open if the product wants the board to feel more like the Spaghetti canvas than a scroll container

That `Phase 5.2` shipped follow-on now means:
- dashboard lane boards now use real per-lane camera movement instead of DOM scroll as the main viewport model
- sticky notes render inside transformed lane stages while keeping dashboard-owned lane plus lane-world placement persistence
- middle-mouse lane panning now behaves like a real surface-camera movement in both directions
- sticky-note drag, inline edit, and title-bar color-menu behavior still remain intact under the new camera model
- each lane header now also includes a small fit-to-notes recovery action so users can recenter lost notes without widening into zoom tooling
- a later follow-on can now honestly widen the fixed two-lane board into user-managed lanes with resizing instead of staying locked to `TO DO` and `Completed`

That `Phase 6` next follow-on now means:
- the next board-structure widening is no longer one giant bucket
- `Phase 1` has now locked the lane model and delete rules
- `Phase 2` has now landed user-managed lanes with stable `laneId` ownership
- `Phase 3` has now landed vertical lane-width resizing after the dynamic lane model stabilised
- the live prep seam for `Phase 1` is the current fixed-lane truth split across `useDashboardStore.ts`, `dashboardPersistence.ts`, and `DashboardSurface.tsx`
- the live prep seam for `Phase 2` is the runtime move from fixed-lane store plus persistence plus surface rendering into dynamic lane records without breaking existing sticky-note flows
- the live prep seam for `Phase 3` was the already-persisted lane `width` data in `useDashboardStore.ts` plus the current `gridTemplateColumns` board rendering in `DashboardSurface.tsx`
- user-managed lane create, rename, delete, note migration, and width splitters are now live as one shipped dashboard board system

That `Phase 7` next backlog now means:
- the next dashboard polish and board-tool bucket is now captured in one dedicated future doc instead of only in chat
- `Phase 1` has now shipped as the unlocked-zoom fit correction where the magnifying-glass action frames all lane notes top-to-bottom as well as left-to-right when zoom is unlocked
- that first slice now lands true zoom-plus-pan fit-to-bounds for unlocked lanes while locked lanes keep the simpler current recovery behavior
- `Phase 2` has now shipped as the board-local multi-note selection foundation around click-select, lane-local box selection, and clear-on-empty-board behavior
- `Phase 3` has now shipped as the lane-local group-move widening on top of that shipped selection model
- `Phase 4` has now shipped as the first board-local align pass on top of the shipped selection and group-move seams
- that align slice landed lane-local vertical and horizontal edge alignment only through temporary lane-header controls, with burger-menu exposure still staged later
- `Phase 5` has now shipped as the sticky-note chrome cleanup pass that introduces the real overflow menu surface
- that burger-menu slice moved `Open in Notepad` plus visible color access into one top-right overflow menu while keeping `Unpin` visible and leaving align-menu work staged for `Phase 6`
- `Phase 8` has now shipped as the dashboard-owned attachment-tree groundwork pass
- that contract slice landed one optional persisted `parentNoteId` relationship per sticky note plus automatic invalid-link cleanup for missing parents, cross-lane links, and cycles, while leaving live attachment behavior itself staged
- `Phase 9` has now shipped as the first live same-lane attachment runtime pass
- that runtime slice now creates one parent/child link on drop when the directly dragged sticky-note title bar overlaps another same-lane title bar, chooses the strongest overlap winner deterministically, and keeps ordinary no-overlap drops detached
- `Phase 10` has now shipped as the first attached-subtree movement runtime pass
- that subtree slice now carries the full descendant tree whenever the directly dragged sticky note owns attached descendants, preserves relative offsets through live preview and final drop, and keeps cross-lane subtree movement intact through one batched dashboard placement commit
- the post-`Phase 10` attachment-hit and sticky-note resizing ladder now lives in the dedicated future doc `Future/Dashboard_Phase Dashboard-7.10 - Sticky Attachment Bounds And Resizable Notes.md`
  - `Phase 10.1` has now shipped there so same-lane drop-time attachment can land when the dragged note title bar overlaps any visible part of the parent sticky note instead of only the parent title bar
  - `Phase 10.2` has now shipped there as the internal sticky-note geometry-seam cleanup that keeps current fixed-size runtime behavior unchanged while preparing later per-note size support
  - `Phase 10.3` has now shipped there with dashboard-owned persisted note-specific width and height plus explicit edge and corner resize handles
  - `Phase 10.4` has now shipped there for default attachment stack layering so children stay visually above parents even while the parent subtree is dragged
  - `Phase 10.5` has now shipped there for the temporary double-click focus-lift override without permanently breaking the attachment-derived stack order
- `Phase 11` is now the implementation-ready next command after the attachment-hit and resizing ladder stabilised
- `Phase 12` now follows in `Dashboard-7` as the later dashboard top-shell cleanup bucket for thinning and calming the top board chrome after the sticky-note stack work
- the next dedicated `Dashboard-12` layout-tools follow-on now lives in `Future/Dashboard_Phase Dashboard-12 - Lane Layout Tools And Smart Align.md`
  - `Phase 12.1` there has now shipped as the one-shot lane grid arrange action
  - `Phase 12.2` there has now shipped as the smart-align toggle widening for non-overlapping vertical and horizontal stack alignment
  - the remaining backlog is now split into:
    - `Phase 6` for selection-aware burger-menu actions
    - `Phase 7` for dashboard shell and global lane-height polish
    - `Dashboard-7.10` for sticky attachment bounds and resizable notes
    - `Phase 11` for child detachment by direct drag

Use this file for:
- feature intent
- feature identity
- widget and rollout direction

Use the future phase doc for:
- code-backed owner seams
- implementation order
- future execution slices

### Scope

This doc covers:
- `Dashboard` as its own viewport or workspace mode
- the widget structure inside `Dashboard`
- `Notepad` as the editor or note workspace behind note-based content
- `Sticky Notes` as widget `01`
- the relationship between `Dashboard` mode and `Notepad` mode
- future widget expansion such as time or weather

This doc does not cover:
- final visual design details
- cloud sync decisions
- final plugin architecture for widgets
- deep collaboration features
- a full generic productivity suite

## Doc Body

## Dashboard

### Short Version

ParaHook could eventually have a `Dashboard` viewport or workspace mode that acts as a personal board, utility surface, and project thinking area.

`Dashboard` should be the place where the user can:
- see pinned note content
- organize sticky notes
- access lightweight widgets
- later add simple utility surfaces like time or weather

### Core Direction

`Dashboard` should be treated as a real workspace mode, not only as a floating panel.

It should feel like:
- a board
- a personal workspace surface
- a home for lightweight widgets
- a place for thought and organization near the main ParaHook work

It should not feel like:
- a generic operating-system desktop clone
- an overloaded productivity suite
- a random collection of unrelated widgets with no center of gravity

### Workspace Rule

Two important workspace rules should be preserved:

- `Dashboard` is its own viewport or workspace mode
- `Notepad` is also its own viewport or workspace mode

That means `Dashboard` is not replacing `Notepad`.

Instead:
- `Dashboard` is the organization and widget surface
- `Notepad` is the focused writing surface

### Widget Structure

This doc should use one section per widget so the feature can grow in an organized way.

Current first widget:
- `01 - Sticky Notes`

Possible later widgets:
- `02 - Time`
- `03 - Weather`
- later other small project or utility widgets

### Why This Feels Worth Adding

- it gives ParaHook a more human workspace for ideas, reminders, and planning
- it keeps lightweight project thinking inside the app
- it adds a personal studio-like layer without replacing the core design tools
- it creates one place where smaller supporting tools can live

### Good First Non-Goals

- giant widget ecosystems in v1
- making `Dashboard` more important than the main design workspace
- turning ParaHook into a generic desktop shell
- stuffing every utility idea into the first version

## Notepad

### Role

`Notepad` should be a focused text-writing workspace mode and editor surface.

It should be useful for:
- quick thoughts
- scratch notes
- project TODOs
- planning text
- pasted references
- idea capture during work

### Workspace Direction

`Notepad` should have its own viewport or workspace mode because writing deserves a cleaner and more focused surface than a small widget card alone.

That means:
- the user can enter `Notepad` directly as a mode
- the user can write in a larger, calmer editor view
- notes can still relate back to `Dashboard`

### Product Direction

`Notepad` should not be treated as a widget.

It makes more sense as:
- the main note-writing surface
- the larger text-editor view of any note that needs more room
- a reusable text-editing workspace that could later support more than one note-based feature

That means `Notepad` can stay valuable even if ParaHook later adds:
- other note types
- richer text surfaces
- project-linked writing features

### Good First Behaviors

- create note
- edit title
- edit body
- autosave
- reopen saved notes
- send or pin a note to `Dashboard`

### Product Rule

`Notepad` should stay simple in the first pass:
- plain text
- fast editing
- no heavy rich-text or document suite complexity

And it should feel closer to:
- a calm Windows-Notepad-style text editor

than to:
- a sticky-note-only sidecar tool

## Widget 01 - Sticky Notes

### Role

`Sticky Notes` should be the visual note-card widget system inside `Dashboard`.

This widget gives the user a way to:
- pin notes
- see them as cards
- arrange them spatially
- keep important reminders visible

### Good First Behaviors

- add sticky note directly from dashboard
- pin note to dashboard
- unpin note
- drag note
- edit sticky note title inline
- edit sticky note body inline
- open sticky note back into `Notepad`

### Relationship To Notepad

The strongest version of this idea is:
- notes can be created in `Notepad`
- notes can also be created directly from `Dashboard`
- notes can be shown in `Dashboard` as `Sticky Notes`
- the same note can still open in `Notepad` for larger editing

That keeps writing and organization connected without making them the exact same surface.

### Shared Note Model

There should not be one note system for `Notepad` and a different unrelated card system for `Sticky Notes`.

Instead there should be one note model with shared properties such as:
- title
- body text
- created date
- modified date
- pinned state
- color or style preset
- maybe tags later

This would support a natural workflow:
- write note in `Notepad`
- pin it to `Dashboard`
- or create it directly as a sticky note from `Dashboard`
- move it around as a sticky note
- edit it inline on the board
- reopen it in `Notepad`
- unpin it without deleting the note

## Future Widgets

### Expansion Direction

Once `Dashboard`, `Notepad`, and `Sticky Notes` feel solid, more widgets can be added carefully.

Good later examples:
- `Time`
- `Weather`
- maybe a small project status or reminder widget later

Healthy rule:
- each widget should earn its place
- notes and note-organization should remain the main identity at first

## Suggested Rollout

- `v1`
  - add `Dashboard` as its own workspace mode
  - add `Notepad` as its own workspace mode
  - add `Sticky Notes` as a dashboard widget
  - create, edit, and autosave simple notes
  - pin notes from `Notepad` into `Dashboard`
  - drag and arrange sticky notes

- `v2`
  - add tags or categories
  - add multiple dashboard boards or named areas
  - improve note search and filtering
  - maybe add the first utility widget such as time

- `v3`
  - add weather or other optional widgets
  - add richer board organization
  - maybe add project-linked notes later

## Recommended First Cut

The best first cut is probably:
- one `Dashboard` mode
- one `Notepad` mode
- one shared note model
- one `Sticky Notes` widget inside `Dashboard`
- no large widget system yet

That proves the workspace relationship cleanly without overbuilding.

## Implementation Phases

The phases below are meant to capture a sensible implementation order for getting `Dashboard`, `Notepad`, and `Sticky Notes` into ParaHook without trying to build every widget idea at once.

Important rule:
- `Dashboard` and `Notepad` should land as honest workspace or viewport modes first
- `Sticky Notes` should land as the first real dashboard widget
- future widgets should build on the same dashboard structure instead of forcing a redesign later

## [ ] `Phase 0` - `Research New Workspace Foundation`

### Summary

`Phase 0` should study how ParaHook currently creates, owns, and switches workspace or viewport modes so `Dashboard` and `Notepad` can be added on top of a real foundation instead of being bolted on blindly.

This is a research phase, not a feature-build phase.

### Goals

- inspect how current workspace or viewport modes are defined
- identify the real owner seams for:
  - mode registration
  - mode switching
  - surface rendering
  - layout/state persistence
- determine what is reusable versus what must be expanded to support `Dashboard` and `Notepad`
- identify whether one generalized workspace foundation improvement should land before either new mode is added

### Current Live Read

The current codebase already has a real workspace-mode and viewport-slot foundation.

The strongest current owner seams appear to be:
- `src/app/AppShell.tsx`
  - high-level workspace composition
  - slot rendering
  - viewport frame wiring
  - slot-level surface switching callbacks
- `src/app/workspace/useWorkspaceStore.ts`
  - workspace slot tree state
  - split, float, popout, redock, and slot-surface mutation
  - viewport chrome and detached surface ownership
- `src/app/workspace/workspaceShellTypes.ts`
  - canonical workspace types
  - current `WorkspaceSurfaceKind` union
  - slot and detached-surface ownership shapes
- `src/app/workspace/ViewportFrame.tsx`
  - local viewport header and slot chrome
  - split, float, popout, close, and kind-switch affordances
- `src/app/workspace/ViewportSurfaceRegistry.tsx`
  - surface-kind to rendered-surface resolution
- `src/app/workspace/workspaceSurfaceActions.ts`
  - workspace-aware float, popout, redock, and split helper actions
- `src/app/workspace/workspacePersistence.ts`
  - persisted workspace snapshot normalization and restore

Important read:
- `Dashboard` and `Notepad` probably should not invent a second unrelated mode system
- they should most likely ride the existing `Workspace 7.x` slot and surface architecture if they are meant to be honest workspace or viewport modes

### Locked Direction

The locked direction for `Phase 0` should be:
- study the current workspace-slot system first
- determine whether `Dashboard` and `Notepad` should become new `WorkspaceSurfaceKind` values, a higher-level workspace wrapper, or one slotted surface plus one related editor surface
- prefer extending the current workspace seam over inventing a parallel dashboard-specific mode framework

Important non-goal:
- `Phase 0` should not silently turn into implementation of `Dashboard` or `Notepad`
- it should only make the next implementation seam obvious and safer

### Research Questions

`Phase 0` should answer at least these questions:

- should `Dashboard` be a new `WorkspaceSurfaceKind`
- should `Notepad` also be a new `WorkspaceSurfaceKind`, or should it be a specialized editor surface under a different ownership pattern
- how should these new surfaces appear inside:
  - the slot type picker
  - the viewport header
  - split, float, popout, and redock flows
- what state belongs in shared workspace state versus note-feature state
- what persistence should workspace layout own versus what note content persistence should own
- whether `Dashboard` and `Notepad` should support all host modes immediately or stage that parity later

### Proposed File Targets

The first research pass should read and annotate these files directly:

- `src/app/workspace/workspaceShellTypes.ts`
  - confirm where new surface kinds would be introduced
- `src/app/workspace/useWorkspaceStore.ts`
  - confirm which slot and detached-surface flows would need widening
- `src/app/workspace/ViewportFrame.tsx`
  - confirm how a new surface kind enters the local viewport header and kind picker
- `src/app/workspace/ViewportSurfaceRegistry.tsx`
  - confirm where a new surface kind is actually rendered
- `src/app/workspace/workspaceSurfaceActions.ts`
  - confirm float, popout, split, and restore expectations for a new surface kind
- `src/app/workspace/workspacePersistence.ts`
  - confirm snapshot and restore implications
- `src/app/AppShell.tsx`
  - confirm current high-level slot rendering and special-case assumptions that may still be viewer, browser, console, or spaghetti specific

Useful supporting docs:
- `docs/Human-Plans/Architecture/Workspace-Modes/Workspace-Modes-Index.md`
- the live `Workspace 7.x` family docs under `docs/Human-Plans/Architecture/Workspace-Modes/`

### Important Rule

- do not start building `Dashboard` or `Notepad` until the workspace foundation is understood clearly enough
- this phase should reduce implementation risk, not drift into speculative redesign

### Recommended Implementation Order

1. Audit the current workspace surface family and write down the existing live kinds:
   - `modelViewer`
   - `browser`
   - `console`
   - `spaghettiEditor`
2. Trace how one new surface kind would flow through:
   - type definitions
   - slot creation
   - slot switching
   - rendering
   - float and popout
   - persistence and restore
3. Identify all current special-case assumptions in `AppShell.tsx`, `ViewportFrame.tsx`, and workspace action helpers that would block `Dashboard` or `Notepad` from behaving like first-class surfaces.
4. Decide whether the next safest build seam is:
   - adding `Dashboard` first as one new workspace surface kind
   - or first extracting one more generalized workspace registration seam before any new surface kind is added
5. Record the result back into this doc so `Phase 1` starts from one concrete owner model instead of reopening the same research.

### Verification Shape

This is a research phase, so verification should focus on truthfulness of the read rather than shipping behavior.

Useful verification outputs:
- one clear owner inventory for the workspace foundation
- one explicit list of files likely to change first
- one explicit list of current blockers or special cases
- one recommended first implementation seam for `Phase 1`

### Acceptance Shape

- there is a clear read on how a new workspace mode should be introduced in ParaHook
- the likely file owners and architecture seams are known
- the later dashboard phases can proceed without guessing about workspace ownership
- the team can say whether `Dashboard` and `Notepad` should extend the existing surface-kind system directly or whether one smaller workspace generalization cut is needed first

## [x] `Phase 1` - `Create Dashboard Workspace Foundation`

### Summary

`Phase 1` should create the base `Dashboard` workspace surface inside the existing main workspace shell.

This phase is about making `Dashboard` real as a destination inside ParaHook before any meaningful widget system exists.

Implementation-ready execution note:
- use the dedicated future doc as the real code-facing contract for this phase
- keep the first command scoped to main-workspace surface-kind onboarding only

### Goals

- add `Dashboard` as a real workspace surface kind
- expose it in the in-app slot picker and viewport labels
- define the base shell where widgets will later live
- establish the first board-like layout area without starting note-model work

### Important Rule

- do not try to solve note editing, sticky behavior, and utility widgets all in this first phase
- the goal is to create the home surface first
- do not silently widen this phase into console staged-navigation or popup-local child-window adoption

### Acceptance Shape

- the user can open `Dashboard` as a real main-workspace surface
- the surface has a stable slot, host-mode, and persistence ownership model
- later widgets have a clear place to attach

Current shipped status:
- `dashboard` now exists as a real secondary workspace surface kind
- the main workspace supports slot switch, split, float, popout, redock, and persistence for `dashboard`
- console workspace-modes and popup-local shell switching are still intentionally deferred

## [x] `Phase 2` - `Create Notepad Workspace And Shared Note Model`

### Summary

`Phase 2` should add `Notepad` as its own workspace or viewport mode and introduce the shared note model behind the feature.

This is the phase that makes note creation and editing real.

Current shipped status:
- `notepad` now exists as a real secondary workspace surface kind
- the main workspace supports note create, rename, edit, delete, pin-state, split, float, popout, redock, restore, and note persistence for `notepad`
- console workspace-modes and popup-local shell switching are still intentionally deferred

Implementation-ready execution note:
- use the dedicated future doc as the real code-facing contract for this phase
- keep the first command scoped to `notepad` surface onboarding plus one feature-owned shared note store and note persistence seam

### Goals

- add `Notepad` as a focused writing workspace
- support creating and editing notes
- add autosave
- define one shared note model that later dashboard widgets can read

### Important Rule

- `Notepad` should not be implemented as only a dashboard card editor
- it should be its own useful writing surface

### Acceptance Shape

- the user can enter `Notepad`
- the user can create, edit, and reopen notes
- notes are stored through one shared data model rather than through widget-local state only

## [x] `Phase 3` - `Add Sticky Notes As The First Dashboard Widget`

### Summary

`Phase 3` should connect `Notepad` and `Dashboard` by introducing `Sticky Notes` as the first real widget.

This is where the dashboard starts to become useful rather than only structural.

Implementation-ready execution note:
- use the dedicated future doc as the real code-facing contract for this phase
- keep the first command scoped to sticky-note card rendering, dashboard-owned placement persistence, drag placement, and one honest `open in Notepad` handoff

### Goals

- allow notes to be pinned from `Notepad` into `Dashboard`
- render pinned notes as sticky-note cards
- support drag placement on the dashboard
- support opening a sticky note back into `Notepad`

### Important Rule

- `Sticky Notes` should read from the shared note model
- do not create a second isolated card-only note system

### Acceptance Shape

- a note can be written in `Notepad`
- that note can be pinned into `Dashboard`
- the pinned note appears as a movable sticky note
- the user can reopen it for editing

Current shipped status:
- pinned notes now appear inside `Dashboard` as draggable sticky-note cards
- sticky-note placement now persists through a separate dashboard-owned widget-layout store
- cards can unpin in place and open back into `Notepad` without duplicating note content ownership

## [x] `Phase 4` - `Add To Do And Completed Board Lanes For Sticky Notes`

### Summary

`Phase 4` should give the dashboard board its first real task-flow structure by adding two sticky-note lanes: `TO DO` and `Completed`.

Implementation-ready execution note:
- use the dedicated future doc as the real code-facing contract for this phase
- keep the first command scoped to two fixed board lanes plus dashboard-owned lane persistence
- use explicit card actions for lane changes instead of widening immediately into cross-lane drag behavior

### Goals

- add one `TO DO` lane
- add one `Completed` lane
- let sticky notes move between those two lanes through explicit card actions
- persist lane placement in the dashboard-owned widget model without widening the notepad note model

### Important Rule

- keep lane ownership in the dashboard store, not the shared note model
- do not turn the first board-organization pass into a full task-management system

### Acceptance Shape

- sticky notes can live in either `TO DO` or `Completed`
- the board is easier to scan and reason about
- lane placement persists cleanly without polluting the shared note model
- the first lane pass stays lightweight and avoids full task-system complexity

Current shipped status:
- sticky notes now render in `TO DO` and `Completed` board lanes
- newly pinned notes default into `TO DO`
- notes move between lanes through explicit card actions while drag remains lane-local for placement
- lane ownership stays in the dashboard widget model instead of the shared note model

## [x] `Phase 4.1` - `Drag Sticky Notes Between Board Lanes`

### Summary

`Phase 4.1` should upgrade the shipped two-lane board so sticky notes can move between `TO DO` and `Completed` by drag and drop instead of only through explicit card actions.

### Goals

- allow dragging a sticky note from `TO DO` into `Completed`
- allow dragging a sticky note from `Completed` back into `TO DO`
- persist both the new lane and the dropped position through the dashboard-owned widget model

### Important Rule

- keep lane ownership in the dashboard store, not the shared note model
- treat this as an interaction upgrade on top of shipped `Dashboard-4`, not as a new task-system phase

### Acceptance Shape

- users can drag sticky notes across the lane boundary
- the dropped note keeps a stable local position in the target lane
- the new lane persists after reload
- the shared note model remains unchanged

Current shipped status:
- users can now drag sticky notes directly between `TO DO` and `Completed`
- drop persists both the target lane and lane-local position through the dashboard widget store
- the active target lane highlights during drag and lane movement now happens through drag only

## [ ] `Phase 5` - `Dashboard Sticky Note Creation And Inline Editing`

### Summary

`Phase 5` should make sticky notes feel truly native to `Dashboard` while keeping `Notepad` as the larger plain-text editor for the same notes.

Implementation-ready execution note:
- use the dedicated future doc as the real code-facing contract for this phase
- keep the first command scoped to dashboard-created notes, inline sticky-note title/body editing, and the drag-handle shift only

### Goals

- add an `Add Sticky Note` action directly in `Dashboard`
- let users edit sticky-note title directly on the card
- let users edit sticky-note body directly on the card
- keep `Notepad` as the larger editor view of the same note
- keep board visibility optional so not every note has to be shown on the dashboard

### Important Rule

- do not split sticky notes into a second unrelated note system
- keep note-content ownership in the shared note model while dashboard continues to own board placement and lane metadata
- make inline edit visuals nearly identical to preview visuals so sticky-note editing feels seamless
- do not let title/body editing fight the drag interaction; drag should move to a dedicated non-text handle zone

Locked first-cut truth:
- use the current notepad-store creation and update seams instead of inventing a new dashboard note service in this phase
- let dashboard-created notes keep the current shared `activeNoteId` behavior in the first pass unless live implementation proves that side effect is too disruptive

### Acceptance Shape

- users can create a sticky note directly from `Dashboard`
- users can edit sticky-note title and body inline on the board
- the same note can still open in `Notepad`
- `Notepad` still reads as the larger plain-text text editor, not as a separate note database
- dashboard and notepad stay linked through one note model instead of copies

## [ ] `Phase 5.1` - `Pannable Sticky Note Lane Canvases`

### Summary

`Phase 5.1` should make each dashboard sticky-note lane feel more like a real canvas by letting the user middle-mouse drag-pan the visible area of `TO DO` and `Completed`.

Implementation-ready execution note:
- use the dedicated future doc as the real code-facing contract for this phase
- keep the first command scoped to middle-mouse lane-board panning only
- do not widen the first cut into zooming, persistent camera state, or whiteboard-scale navigation features

### Goals

- let the user middle-mouse drag-pan the `TO DO` lane viewport
- let the user middle-mouse drag-pan the `Completed` lane viewport
- keep sticky-note `x/y` placement exactly as lane-local note coordinates
- keep note drag, inline edit, and title-bar color-menu behavior working

### Important Rule

- keep panning as temporary lane-board viewport state, not persisted note or dashboard model state
- do not turn this first pass into a generalized infinite-canvas subsystem

### Acceptance Shape

- each lane feels like a pannable sticky-note canvas
- middle-mouse drag changes lane scroll position without moving sticky-note coordinates
- sticky-note drag, editing, and color-menu behavior all still work after the pan layer lands

## [x] `Phase 5.2` - `Real Lane Canvas Camera`

### Summary

`Phase 5.2` should upgrade the first shipped lane-panning pass from a scroll-container interaction into a real canvas-camera interaction closer to the Spaghetti editor surface.

Implementation-prep execution note:
- use the dedicated future doc as the real code-facing contract for this phase
- `Phase 1` in that future doc is now closed as the contract-lock slice
- the next runtime work is the `Phase 2` implementation pass
- keep that runtime pass narrow around local lane camera state, transformed stage rendering, and lane-world drag math

Implementation-ready runtime note:
- the open runtime seam is `DashboardSurface.tsx`
- the open verification seam is the existing dashboard interaction coverage in `AppShell.test.tsx`
- the first code pass should replace DOM-scroll panning with a local per-lane camera without widening into zoom or persistence

### Goals

- replace DOM-scroll lane panning with lane-local camera state
- support real up/down and left/right canvas navigation
- keep sticky-note positions as lane-world coordinates
- keep sticky-note drag, edit, and color-menu behavior working under the new camera model

### Important Rule

- keep note placement and camera state as separate concerns
- avoid widening the first real camera pass into zoom, persistence, or generalized whiteboard tooling

Locked first-cut truth:
- start from `DashboardSurface.tsx` as the lane-camera owner seam
- use `SpaghettiCanvas.tsx` as the conceptual pan-and-transform reference without forcing a shared abstraction
- keep the first camera pass pan-only with one independent transient camera per lane

Current status:
- the lane-camera contract is now locked in the dedicated future doc
- the `Phase 2` runtime slice has shipped through a local per-lane camera implementation in `DashboardSurface.tsx`
- dashboard lanes now pan through camera movement rather than DOM scroll while sticky-note placement stays dashboard-owned lane-world state
- a small `Phase 3` lane-header fit-to-notes follow-on is still open if the product wants a quick “find my notes” recovery action

Implementation-ready follow-on note:
- the open recovery seam is still `DashboardSurface.tsx`
- the open verification seam is still the dashboard camera regressions in `AppShell.test.tsx`
- the next narrow follow-on should stay pan-only and update only the local lane camera rather than note placement or zoom state

Later status update:
- the `Phase 3` lane-header fit-to-notes follow-on has now shipped too
- the `Phase 4` lane zoom-unlock follow-on has now shipped as well, so the current `Dashboard-5.2` camera ladder is fully landed through optional lane-local zoom unlock

### Acceptance Shape

- the lane feels like a real canvas surface rather than a scrolled box
- panning happens through lane camera movement
- sticky-note placement remains stable and understandable under camera offset

## [ ] `Phase 6` - `Expand Dashboard With Additional Widgets`

### Summary

`Phase 6` should add the first non-note widgets once the sticky-note workflow already feels complete.

Good first candidates:
- `Time`
- `Weather`

### Goals

- add one or two small utility widgets
- confirm that the dashboard structure scales beyond sticky notes
- preserve the note-centered identity of the feature

### Important Rule

- new widgets should not displace notes as the main reason the dashboard exists
- each added widget should justify its presence clearly

### Acceptance Shape

- dashboard can host more than one widget type
- notes still remain the central feature
- the feature is ready for slower future expansion without redesigning the whole system
