# Catalog Gen0 Index

## Doc Header

### Doc History
24. 2026-04-18 14:01:44: Completed `Catalog-Gen0 - Phase 6 - Generation 1 Start Boundary Cleanup`, closing `Generation 0` now that the Browser reference-baseline retirement and Catalog-owned asset-home migration lanes are already complete, marking the remaining handoff checklist items done, and making the first real family start explicit by routing the next true execution cut to the existing `Catalog-1 - Workspace Foundation And Catalog Contract` future doc while keeping later `Generation 2` widening out of that start boundary
23. 2026-04-18 14:01:44: Prepped `Catalog-Gen0 - Phase 6 - Generation 1 Start Boundary Cleanup` for implementation, renaming the remaining handoff lane from the older `Phase 00` wording onto the active `Phase 6` read and grounding the next pass in the now-complete Browser-baseline retirement plus Catalog-owned asset-home cleanup, the still-Gen0 wording in `Catalog-Vision.md`, the more detailed `Generation 1` umbrella in `Catalog-Index.md`, and the still-missing explicit "Catalog has now started" checklist that should close Gen0 honestly before the first true `Catalog-1` execution cut
22. 2026-04-18 13:57:35: Reconciled the Gen0 tracking state in this `Catalog-Gen0` index, closing `Phase 2.1 - Browser References Baseline Removal` plus `G0-HLG-1` now that the earlier `Catalog-Gen0.2.1` runtime-removal ladder had already landed, marking the umbrella `Phase 5` migration lane complete after the later family moves and retirement work, and aligning the Generation 1 handoff wishlist lane naming onto the current `Phase 00` read so the remaining open Gen0 work now matches the real shipped state more cleanly
21. 2026-04-18 13:49:29: Completed `Catalog-Gen0 - Phase 5.6 - Legacy ReferenceModels Retirement` by removing the old `public/ReferenceModels` family folders after the family migrations and shared path rewires were already in place, then closing the final `Phase 5` honesty read so `Gen0` can now claim one Catalog-owned repo asset home for `shoes`, `hooks`, and `footpads`
20. 2026-04-18 13:49:29: Completed `Catalog-Gen0 - Phase 5.5 - Shared Consumer Path Rewire` by removing the remaining `src/...` `ReferenceModels/...` assumptions across the shared manifest, Catalog seed, and test-fixture seams so the surviving migrated-family reads now point at `Catalog/shoes/...`, `Catalog/hooks/...`, and `Catalog/footpads/...` instead of carrying old dual-home assumptions forward
19. 2026-04-18 13:49:29: Completed `Catalog-Gen0 - Phase 5.4 - Footpads Asset Migration` by copying the legacy footpad family assets plus companion `.mtl` files into `public/Catalog/footpads`, repointing the manifest and Catalog seed assembly path to `Catalog/footpads/XR_Footpad_PubPad_Full_Assembly.obj`, and keeping the broader family asset set resident in the Catalog-owned home without widening into preview creation or broader metadata redesign
18. 2026-04-18 13:49:29: Completed `Catalog-Gen0 - Phase 5.3 - Hooks Asset Migration` by copying the legacy repo-backed hook `.step` assets into `public/Catalog/hooks`, repointing the hook manifest paths to `Catalog/hooks/...`, expanding the Catalog repo seed so `hook-large`, `hook-medium`, `hook-small`, and `hook-xl` now read from Catalog-owned homes, and leaving legacy-folder retirement for the later dedicated cleanup phase
17. 2026-04-18 13:32:46: Completed `Catalog-Gen0 - Phase 5.2 - Shoes Asset Migration` by copying the legacy repo-backed `Shoe_1.glb`, `Shoe_2.glb`, and `Shoe_3.glb` assets into `public/Catalog/shoes`, repointing the manifest-owned shoe asset paths to `Catalog/shoes/...`, expanding the Catalog repo seed so the migrated shoes family now exposes `shoe-1`, `shoe-2`, `shoe-3`, and `vans-high-top-low` from Catalog-owned homes, and leaving legacy `public/ReferenceModels/shoes` retirement for the later dedicated cleanup phase
16. 2026-04-18 13:22:24: Completed `Catalog-Gen0 - Phase 5.1 - Catalog Family Folder Contract` as one docs-and-contract pass, re-reading the live `public/Catalog` and `public/ReferenceModels` family layout, locking `public/Catalog/shoes`, `public/Catalog/hooks`, and `public/Catalog/footpads` as the stable migration homes, locking one shared path and slug contract that later manifest and Catalog-seed rewires should target without widening into immediate runtime rewires, and marking `Phase 5.2`, `Phase 5.3`, and `Phase 5.4` implementation-ready against that folder contract
15. 2026-04-18 13:18:49: Added one explicit `Phase 5.1` worker checklist to this `Catalog-Gen0` index so a single dispatch Codex can prep and implement the Catalog family-folder contract through smaller `5.1A` through `5.1D` slices instead of improvising the order inside the broader `ReferenceModels/...` migration ladder
14. 2026-04-18 13:00:51: Expanded `Catalog-Gen0 - Phase 5` from a loose folder-organization follow-up into a full reference-model migration ladder, adding explicit sub-phases for the Catalog family folder contract, per-family `shoes`, `hooks`, and `footpads` migration, shared consumer-path rewiring, and final `ReferenceModels/...` retirement so `Generation 0` can finish with one clean Catalog-owned repo asset home instead of stopping halfway through the asset-home cleanup
13. 2026-04-18 13:00:51: Started `Catalog-Gen0 - Phase 5 - Catalog Folder Organization Follow-Up` by copying the external `Vans-High-Top-low.glb` shoe model into `public/Catalog/shoes/vans-high-top-low.glb` and adding one matching Catalog seed entry so the new Catalog-owned shoes home now has one real family resident without widening into the broader `ReferenceModels/...` migration
12. 2026-04-18 12:58:14: Added `Catalog-Gen0 - Phase 5 - Catalog Folder Organization Follow-Up` to this `Catalog-Gen0` index so the already-recorded asset-home drift now has one explicit next prep lane for organizing stable `public/Catalog/...` family homes for `footpads`, `shoes`, and `hooks` instead of leaving the newer Catalog-owned area as one lightly used folder beside the older `ReferenceModels/...` layout
11. 2026-04-18 11:44:03: Added `Catalog-Gen0 - Phase 2.1 - Browser References Baseline Removal` to this `Catalog-Gen0` index and linked it to one new standalone future doc so the already-landed `Phase 2` boundary rule now has an explicit runtime-removal follow-on for taking the Browser `References` startup baseline out of first-load behavior without widening into asset-home migration or broader `Catalog-1` work
10. 2026-04-18 11:35:47: Completed `Catalog-Gen0 - Phase 3 - Curated Asset And Metadata Prep` as a docs-and-research prep pass, recording the broader manifest-versus-seed family inventory, the current preview gaps, the surviving `ReferenceModels` asset-home drift, the current id and slug drift between manifest items and Catalog seed items, the not-yet-grounded environment asset path, and the still-light `Imports` metadata read so later migration and `Catalog-1` work can start from one explicit asset-and-metadata baseline instead of rediscovering the same gaps
9. 2026-04-18 11:33:09: Prepped `Catalog-Gen0 - Phase 3 - Curated Asset And Metadata Prep` for implementation, grounding the next `Generation 0` cut in the current mismatch between the broader manifest asset inventory and the much smaller Catalog repo seed, the partial `CatalogPreviews` coverage, the surviving `ReferenceModels` asset-home paths, the thin current catalog item contract, the not-yet-grounded environment asset path, and the still-light `Imports` metadata read so the next prep lane can stay explicit about asset-home and metadata readiness instead of widening into the first real `Catalog-1` runtime
8. 2026-04-18 11:26:37: Completed `Catalog-Gen0 - Phase 2 - Ownership Boundary Cleanup` as a docs-and-research boundary pass, locking that Browser-side manifest preload is only the legacy owner to retire, Catalog browse owns curated repo-backed entries while `Imports` only mirrors already-imported reuse items, preview session state stays Catalog-owned and temporary, repo-backed add-to-project stays a downstream Browser-project commit handoff, environment apply stays a downstream viewer-environment handoff, and Catalog already counts as a real hosted workspace surface instead of a Browser subsection
7. 2026-04-18 11:19:18: Prepped `Catalog-Gen0 - Phase 2 - Ownership Boundary Cleanup` for implementation, grounding the next `Generation 0` cut in the still-live Browser-side manifest preload seeding plus Browser tree consumer seams, the Catalog repo-seed and imports snapshot split, the Catalog preview-session, repo-backed commit, and viewer-environment apply seams, and the already-shipped workspace-surface hosts so the boundary-cleanup pass can stay one explicit owner-read correction instead of widening into asset migration or new runtime behavior
6. 2026-04-18 11:12:54: Completed `Catalog-Gen0 - Phase 1 - Existing Catalog-Like Drift Inventory` as a docs-and-research pass, inventorying the live Browser-side manifest preload owner in `useAppStore.ts`, the shared Browser tree consumer in `useBrowserPanelController.ts`, the Catalog repo-seed plus imports snapshot seams in `catalogSeedItems.ts` and `catalogSource.ts`, the shipped Catalog surface host plus proof in `CatalogSurface.tsx` and `CatalogSurface.test.tsx`, the viewer-environment apply path through `catalogEnvironmentApply.ts` plus `envPreset`, and the current public asset split where geometry still lives under `ReferenceModels`, previews live under `CatalogPreviews`, and the newer `Catalog` folder is only partially in use
5. 2026-04-18 11:10:07: Prepped `Catalog-Gen0 - Phase 1 - Existing Catalog-Like Drift Inventory` for implementation, grounding the first `Generation 0` cut in the live repo-backed reference manifest, Catalog seed-item seam, shared Catalog surface host, Catalog surface proof, and surviving `ReferenceModels` asset-path plus environment-action reads so the opening prep pass can stay one explicit current-state inventory instead of drifting into early boundary cleanup or runtime rewiring
4. 2026-04-18 11:08:04: Reformatted the `Catalog-Gen0` index phase naming so the tracked prep lanes and matching read sections now use the clearer `Catalog-Gen0 - Phase N - ...` pattern instead of the older compact `Catalog-Gen0-N` labels, keeping the `Generation 0` wishlist surface aligned with the stronger family-phase naming used elsewhere in the Catalog planning area
3. 2026-04-18 11:03:38: Expanded this `Catalog-Gen0` index into a more explicit wishlist-tracking surface, reframing the prep lanes as one cleaner `Generation 0` tracking map with lane targets, stronger answers for what prep wishes are worth tracking before `Catalog-1`, and a newly explicit repo-backed asset-home cleanup item so the old `ReferenceModels` path drift can be treated as Catalog prep instead of getting rediscovered later
2. 2026-04-16 17:10:00: Tightened `Generation 0` so the prep lane now explicitly includes moving the current preloaded reference models out of `Browser`, clarifying that `foothooks`, `shoes`, and `footpads` should stop reading as default Browser-resident content during cleanup and instead become later optional add-ins the user can choose intentionally
1. 2026-04-16 16:55:23: Added this dedicated `Catalog-Gen0-Index.md` planning surface so the `Catalog` family can stay honest that it has not started yet, defining `Generation 0` as the cleanup-and-prep band before `Catalog-1` and splitting that prep into explicit cleanup phases for drift inventory, ownership-boundary cleanup, curated asset or metadata prep, and the `Generation 1` start-boundary lock

### Purpose

This file is the focused planning index for `Catalog Generation 0`.

Use it to answer:
- what `Generation 0` means for the `Catalog` family
- which cleanup or prep work should happen before `Catalog-1`
- how to keep pre-start cleanup separate from the first real catalog implementation phase
- which `Generation 0` wishlist items are currently worth tracking
- how those prep wishes cluster into coherent `Generation 0` lanes
- what the first `Generation 0` cleanup phases should be

### Scope

This doc covers:
- current catalog-like behavior inventory
- ownership-boundary cleanup before the family starts
- first curated asset and metadata prep
- the full `ReferenceModels/...` to `Catalog/...` migration ladder for the first Catalog-owned reference families
- wishlist tracking for those `Generation 0` prep lanes
- start-boundary cleanup between `Generation 0`, `Generation 1`, and `Generation 2`

This doc does not cover:
- the first real `Catalog` workspace implementation
- the `Catalog-1` runtime shell
- reference-family or `HDRI` loading behavior
- curated external-source widening that belongs to `Generation 2`

## Doc Body

### Short Version

`Catalog Generation 0` is now complete.

This generation was the cleanup and prep band before the first real family start.

That cleanup work is now complete enough that the next true execution owner should be read as `Catalog-1`.

The most important completed cleanup reads are:
- the old Browser-resident reference baseline has been retired
- `foothooks`, `shoes`, and `footpads` no longer read as default Browser content
- repo-backed reference assets now live under Catalog-owned homes instead of older Browser-era path assumptions
- those families can now return only as later intentional optional add-ins

Important rule:
- `Generation 0` should now stay closed
- if work adds a real `Catalog` surface, manifest runtime, preview flow, or load behavior, that work belongs to `Catalog-1` or later

This doc now exists mainly as the completed closeout record for the prep band and the handoff surface into the first real family phase.

### Why This Doc Exists

The `Catalog` family already has a strong `Generation 1` and `Generation 2` planning shape.

That is useful, but it becomes misleading if the family is described as already being in `Generation 1` before the family has actually started.

This doc exists so the current state can stay honest:
- `Generation 0`
  - prep and cleanup before the family start
- `Generation 1`
  - the first real repo-backed catalog implementation lane
- `Generation 2`
  - later widening such as curated external sources and stronger part-system normalization

It also exists so `Generation 0` can act as a working prep map instead of only a short summary.

The family already has stronger later tracking surfaces such as `Catalog-Index.md` and `Catalog-Gen2-Index.md`.

Without a matching `Generation 0` wishlist area, the prep lane stays too easy to hand-wave even when it still owns real cleanup such as:
- Browser-era preload retirement
- Catalog-versus-Browser boundary cleanup
- repo-backed asset-home cleanup
- manifest and preview-media prep for the first real catalog families

### Generation 0 Summary

`Generation 0` was responsible for:
- inventory where catalog-like behavior already exists today
- clean up owner and boundary reads before a real `Catalog` workspace lands
- move the current preloaded reference models out of the `Browser` baseline
- move repo-backed reference assets toward Catalog-owned pathing instead of leaving the old asset-home drift unowned
- prepare the first curated asset and metadata baseline
- lock what should count as the actual `Generation 1` start

Important rule:
- `Generation 0` should reduce ambiguity
- it should not quietly start the real `Catalog` runtime under cleanup language

Current closeout read:
- that ambiguity reduction work is now complete enough that the next executable lane should be treated as the first real `Generation 1` family work
- the first standalone execution owner should now be read as [Catalog_Phase Catalog-1 - Workspace Foundation And Catalog Contract.md](./Future/Catalog_Phase%20Catalog-1%20-%20Workspace%20Foundation%20And%20Catalog%20Contract.md)

### Generation 0 High Level Goals

- [x] `G0-HLG-1. Retire The Browser-Owned Reference Baseline So Optional Repo Families Stop Reading As Default Browser Content`
- [x] `G0-HLG-2. Finish Gen0 With One Clean Catalog-Owned Repo Asset Home For The First Reference Families`
- [x] `G0-HLG-3. Hand Off Into Catalog-1 Without Carrying Legacy ReferenceModels Drift Forward`

## Wishlist Tracking

Use the `Generation 0` lanes to organize the current wishlist like this:

### `Catalog-Gen0 - Phase 1 - Existing Catalog-Like Drift Inventory`
  - [x] `G0-1. Preloaded Reference Models In Browser Inventory`
  - [x] `G0-2. HDRI Entry And Consumer Inventory`
  - [x] `G0-3. Catalog-Like UI Touchpoint Inventory`
  - [x] `G0-4. Current Asset Owner And Consumer Map`
  #### - lane target:
    - identify where catalog-like behavior already leaks through preload, Browser, viewer, or shell seams
    - create one honest current-state inventory before the family starts

### `Catalog-Gen0 - Phase 2 - Ownership Boundary Cleanup`
  - [x] `G0-5. Browser Versus Catalog Boundary Cleanup`
  - [x] `G0-6. Remove Preloaded Reference Models From Browser Baseline`
  - [x] `G0-7. Import Versus Catalog Reuse Boundary Cleanup`
  - [x] `G0-8. Preview Versus Commit Ownership Cleanup`
  - [x] `G0-9. Workspace-Surface Boundary Cleanup`
  #### - lane target:
    - restate and tighten the owner split between `Catalog`, `Browser`, import, viewer state, and shared workspace hosting
    - make the user stop starting with browser-resident `foothooks`, `shoes`, and `footpads` as implied default project content
    - prevent `Catalog-1` from reopening the same boundary questions during its first runtime cut

### `Catalog-Gen0 - Phase 2.1 - Browser References Baseline Removal`
  - [x] `G0-6A. Remove Browser References Root From Startup Baseline`
  - [x] `G0-6B. Retire Manifest-Backed Startup Seeding`
  - [x] `G0-6C. Preserve Imported Reference Behavior After Real Intake`
  #### - lane target:
    - execute the already-landed `Phase 2` boundary rule through one narrow runtime removal pass
    - remove the Browser `References` startup baseline that still appears because of manifest preload
    - keep later optional repo-backed reference access routed through `Catalog` instead of Browser preload
  #### - future doc:
    - [Catalog_Phase Catalog-Gen0.2.1 - Browser References Baseline Removal.md](./Future/Catalog_Phase%20Catalog-Gen0.2.1%20-%20Browser%20References%20Baseline%20Removal.md)

### `Catalog-Gen0 - Phase 3 - Curated Asset And Metadata Prep`
  - [x] `G0-10. Later Optional Reference Family Inventory`
  - [x] `G0-11. Preview Media Gap Inventory`
  - [x] `G0-12. Catalog-Owned Repo Asset Home Cleanup`
  - [x] `G0-13. Stable Item Id And Slug Prep`
  - [x] `G0-14. Manifest-Field Prep For First Families`
  - [x] `G0-15. Imports Area Readiness Notes`
  #### - lane target:
    - prepare `foothooks`, `shoes`, and `footpads` as later optional add-in families instead of default Browser preload
    - prepare repo-backed reference assets to live under clearer Catalog-owned homes instead of older preload-era folder assumptions
    - prepare the first `HDRIs` and `Imports` reads so the later item contract can start from explicit prep instead of scattered filenames and assumptions

### `Catalog-Gen0 - Phase 6 - Generation 1 Start Boundary Cleanup`
  - [x] `G0-16. First Real Family Start Definition`
  - [x] `G0-17. Catalog-1 Entry Checklist`
  - [x] `G0-18. Keep Generation 2 Widening Out Of Generation 1 Start`
  - [x] `G0-19. First Standalone Doc Routing`
  #### - lane target:
    - lock what should count as "the `Catalog` family has started"
    - lock that later optional reference add-ins are not the same thing as old Browser preload
    - keep `Generation 0` prep, `Generation 1` baseline work, and `Generation 2` widening from blurring together

### `Catalog-Gen0 - Phase 5 - ReferenceModels To Catalog Migration Ladder`
  - [x] `G0-20. Clean Catalog-Owned Repo Asset Home`
  - [x] `G0-21. Family-By-Family Reference Asset Migration`
  - [x] `G0-22. Shared Consumer Path Rewire`
  - [x] `G0-23. Legacy ReferenceModels Retirement`
  - [x] `G0-HLG-2. Finish Gen0 With One Clean Catalog-Owned Repo Asset Home For The First Reference Families`
  #### - lane target:
    - fully migrate the first repo-backed reference families out of `public/ReferenceModels/...` and into stable `public/Catalog/...` homes before `Gen0` closes
    - break that migration into narrow sub-phases so one Codex can land each family move and the final shared cleanup without widening into `Catalog-1` browse work

### `Catalog-Gen0 - Phase 5.1 - Catalog Family Folder Contract`
  - [x] `G0-20A. Stable Catalog Family Folder Names`
  - [x] `G0-20B. Target Path And Slug Contract`
  #### - lane target:
    - lock the target `public/Catalog/shoes`, `public/Catalog/hooks`, and `public/Catalog/footpads` family homes before the broader migration lands
    - make later family moves target one stable destination layout instead of improvising folder names mid-migration
    - mark `Phase 5.2`, `Phase 5.3`, and `Phase 5.4` implementation-ready against that same folder contract instead of rediscovering naming and path rules mid-move

### `Catalog-Gen0 - Phase 5.2 - Shoes Asset Migration`
  - [x] `G0-21A. Move Repo Shoes Into Catalog-Owned Home`
  - [x] `G0-21B. Repoint Shoe Manifest And Seed Paths`
  #### - lane target:
    - migrate all repo-backed shoe geometry from `public/ReferenceModels/shoes` into the Catalog-owned shoes family home
    - keep the shoes family usable through the manifest and Catalog seed after the move
    - target the locked `Phase 5.1` contract: runtime geometry home `public/Catalog/shoes`, manifest ids unchanged, and readable shoe slugs such as `shoe-1`, `shoe-2`, `shoe-3`, and `vans-high-top-low` kept as the shared human-facing naming rail

### `Catalog-Gen0 - Phase 5.3 - Hooks Asset Migration`
  - [x] `G0-21C. Move Repo Hooks Into Catalog-Owned Home`
  - [x] `G0-21D. Repoint Hook Manifest And Seed Paths`
  #### - lane target:
    - migrate all repo-backed hook geometry from `public/ReferenceModels/hooks` into the Catalog-owned hooks family home
    - keep the hooks family usable through the manifest and Catalog seed after the move
    - target the locked `Phase 5.1` contract: runtime geometry home `public/Catalog/hooks`, manifest category id stays `premade-foothooks` for this migration ladder, and readable hook slugs such as `hook-large`, `hook-medium`, `hook-small`, and `hook-xl` stay as the shared naming rail

### `Catalog-Gen0 - Phase 5.4 - Footpads Asset Migration`
  - [x] `G0-21E. Move Repo Footpads Into Catalog-Owned Home`
  - [x] `G0-21F. Repoint Footpad Manifest And Seed Paths`
  #### - lane target:
    - migrate all repo-backed footpad geometry from `public/ReferenceModels/footpads` into the Catalog-owned footpads family home
    - keep the footpads family usable through the manifest and Catalog seed after the move
    - target the locked `Phase 5.1` contract: runtime geometry home `public/Catalog/footpads`, manifest ids unchanged, and the current `pubpad-full-assembly` readable slug stays as the shared naming rail even if source-authored geometry filenames remain longer during the move

### `Catalog-Gen0 - Phase 5.5 - Shared Consumer Path Rewire`
  - [x] `G0-22A. Remove Remaining ReferenceModels Path Assumptions`
  - [x] `G0-22B. Normalize Shared Catalog-Owned Family Reads`
  #### - lane target:
    - clean up any remaining shared consumers that still assume the old `ReferenceModels/...` home after the per-family moves land
    - make the surviving manifest, seed, Browser, and Catalog runtime seams read one shared Catalog-owned path contract

### `Catalog-Gen0 - Phase 5.6 - Legacy ReferenceModels Retirement`
  - [x] `G0-23A. Remove Or Empty Legacy ReferenceModels Family Folders`
  - [x] `G0-23B. Final Repo-Cleanup Honesty Pass`
  - [x] `G0-HLG-3. Hand Off Into Catalog-1 Without Carrying Legacy ReferenceModels Drift Forward`
  #### - lane target:
    - retire the old `public/ReferenceModels/...` family homes once the migration and consumer rewires are complete
    - leave `Gen0` with one clean Catalog-owned repo asset layout instead of a dual-home compromise

## Lane Reads



## [x] Catalog-Gen0 - Phase 1 - Existing Catalog-Like Drift Inventory

### Phase 1 Summary
#### Purpose

Inventory the current catalog-like behavior already scattered across preload, Browser, viewer, or shell seams so the family starts from one explicit current-state read.

#### Owns

- current preloaded reference entry inventory
- where those reference models still read as `Browser`-resident defaults today
- current `HDRI` entry and consumer inventory
- current catalog-like UI touchpoint inventory
- the first owner and consumer map for reusable assets already implied by the repo

#### Does Not Own

- the real `Catalog` workspace onboarding
- the first manifest runtime
- preview or commit behavior implementation

#### Current Live Read

The strongest current seams already show that the catalog-like baseline is spread across both older reference infrastructure and newer Catalog-owned surfaces:

- `src/app/references/referenceManifest.ts`
  - already owns the repo-backed manifest categories plus reference entries for `footpads`, `shoes`, and `premade-foothooks`
  - still points those entries at the older `ReferenceModels/...` asset-home path
  - is therefore one primary owner for the current preloaded-reference inventory and old asset-home drift
- `src/app/catalog/catalogSeedItems.ts`
  - already seeds the first curated Catalog-facing entries for `footpads`, `shoes`, `foothooks`, and the first `environment` item
  - still points its repo-backed reference assets at the same older `ReferenceModels/...` paths while preview media lives under `CatalogPreviews/...`
  - is therefore one primary owner for the current Catalog-facing asset-home split
- `src/app/workspace/CatalogSurface.tsx`
  - already hosts the shared Catalog source snapshot, preview-session ownership, add-to-project handoff, and environment-apply handoff
  - is the strongest nearby runtime host for inventorying where Catalog behavior has already become real instead of staying hypothetical
- `src/app/workspace/CatalogSurface.test.tsx`
  - already proves the browse shell, temporary preview session, downstream repo-backed commit handoff, imports reuse path, and environment apply path
  - is the strongest nearby proof surface for inventorying which behaviors are already shipped versus still only planned
- live asset layout
  - repo-backed geometry still lives under `public/ReferenceModels/...`
  - Catalog preview media already lives under `public/CatalogPreviews/...`
  - `public/Catalog/...` has started to exist for newer Catalog-owned content, but the reference-family runtime does not yet read from it

So the first honest implementation read is:
- `Phase 1` should stay inventory-first
- it should classify current owners, paths, and touchpoints explicitly
- it should stop before trying to fix the drift it discovers

The live inventory is now:

- Browser-side preload owner
  - `src/app/store/useAppStore.ts` still seeds the initial `referenceWorkspace` from `REFERENCE_MANIFEST_ITEMS` through `buildInitialReferenceRecords(...)`
  - those seeded records keep `sourceKind: 'manifest'`, preserve the manifest category ids, and resolve their URLs through `resolveReferenceAssetPath(...)`
- Browser-side consumer surface
  - `selectReferenceWorkspaceBrowserTree(...)` in `src/app/store/useAppStore.ts` still projects those manifest-backed records into the Browser `References` root with explicit `footpads`, `shoes`, and `premade-foothooks` categories
  - `src/app/panels/useBrowserPanelController.ts` still consumes that Browser tree directly, so the old manifest-backed preload remains a real Browser-facing surface rather than only dormant data
- Catalog repo-backed source seam
  - `src/app/catalog/catalogSeedItems.ts` already carries the first curated repo-backed Catalog entries for `footpads`, `shoes`, `foothooks`, and one `environment`
  - `src/app/catalog/catalogSource.ts` turns those repo seeds plus imported references from `referenceWorkspace` into the shared Catalog snapshot, so Catalog already has one real browse-owned source seam instead of only a future plan
- Catalog runtime host and shipped touchpoints
  - `src/app/workspace/CatalogSurface.tsx` already hosts the real Catalog runtime through the shared source snapshot, preview-session state, repo-backed add-to-project handoff, and environment-apply handoff
  - `src/app/workspace/CatalogSurface.test.tsx` already proves the shipped browse shell, temporary preview session, imports reuse, repo-backed commit path, and viewer-environment apply path, so these are live Catalog touchpoints rather than only design intent
- Environment entry and consumer path
  - `src/app/catalog/catalogSeedItems.ts` already exposes `environment:studio` as the first Catalog environment item
  - `src/app/catalog/catalogActionPlan.ts` and `src/app/catalog/catalogEnvironmentApply.ts` already classify that entry onto the `viewer-environment` downstream owner path
  - `src/app/workspace/CatalogSurface.tsx` applies that handoff through `setViewKey('envPreset', ...)`, while `src/app/components/ViewToolbar.tsx` still exposes the shared environment preset owner on the broader viewer side
- Public asset-home split
  - `public/ReferenceModels/` still holds the live repo-backed geometry assets for `footpads`, `hooks`, and `shoes`
  - `public/CatalogPreviews/` already holds the current Catalog-facing preview media for `footpads`, `hooks`, `shoes`, and `environments`
  - `public/Catalog/` now exists, but the current reference-family runtime does not yet read those repo-backed reference assets from that folder, so the asset-home migration remains prep work rather than shipped truth

So the current owner-and-consumer map reads like this:

- repo-backed reference manifest truth
  - owner:
    - `src/app/references/referenceManifest.ts`
  - current consumers:
    - `src/app/store/useAppStore.ts`
    - Browser reference tree selectors and controllers
    - Catalog reference commit and preview-source helpers through resolved asset paths
- Catalog browse and preview choice
  - owner:
    - `src/app/catalog/catalogSeedItems.ts`
    - `src/app/catalog/catalogSource.ts`
    - `src/app/catalog/catalogPreviewSession.ts`
    - `src/app/workspace/CatalogSurface.tsx`
  - current consumers:
    - `CatalogShell`
    - `CatalogSurface.test.tsx`
- downstream repo-backed commit
  - owner:
    - `src/app/catalog/catalogReferenceCommit.ts`
    - `src/app/workspace/CatalogSurface.tsx`
  - current consumers:
    - `referenceWorkspace` imported-reference path in `useAppStore`
    - Browser imported reference tree after Catalog commit
- viewer environment apply
  - owner:
    - `src/app/catalog/catalogEnvironmentApply.ts`
    - shared `envPreset` view setting
  - current consumers:
    - `src/app/workspace/CatalogSurface.tsx`
    - `src/app/components/ViewToolbar.tsx`

#### First Pass Decisions

- treat this phase as a current-state inventory pass, not as the first cleanup implementation
- inventory reference-style assets, environment entries, and Catalog shell/runtime touchpoints together so later `Phase 2` and `Phase 3` can work from one shared map
- explicitly record where older Browser-era preload assumptions still survive through pathing, naming, or runtime seams
- keep the output focused on owner classification and drift inventory instead of jumping ahead into migration or redesign

### Phase 1 Implementation Spec
#### Exact First Code Cut

1. Inventory the current repo-backed reference families that already exist across `referenceManifest.ts`, `catalogSeedItems.ts`, and the public asset folders.
2. Inventory the current environment entry and consumer path so the `HDRI` or environment lane is included in the same current-state read instead of being rediscovered later.
3. Inventory the current Catalog UI and runtime touchpoints that already ship through `CatalogSurface.tsx`, related Catalog source seams, and `CatalogSurface.test.tsx`.
4. Record one explicit owner-and-consumer map that distinguishes:
   - Browser-era preload or reference seams
   - Catalog-owned browse or preview seams
   - downstream Browser-project commit seams
   - viewer-environment apply seams
5. Stop after the inventory is explicit enough that `Phase 2` and `Phase 3` can reuse it directly.

#### Likely Files

- `src/app/references/referenceManifest.ts`
- `src/app/catalog/catalogSeedItems.ts`
- `src/app/catalog/catalogSource.ts`
- `src/app/workspace/CatalogSurface.tsx`
- `src/app/workspace/CatalogSurface.test.tsx`
- `public/ReferenceModels/`
- `public/CatalogPreviews/`
- `public/Catalog/`
- this `docs/Human-Plans/Architecture/Workspace-Modes/Workspaces/Catalog/Catalog-Gen0-Index.md`

#### No-Widening Rule

- do not start moving assets or changing runtime paths in this phase
- do not reopen Browser-versus-Catalog boundary decisions that belong to `Phase 2`
- do not start manifest-field redesign or item-contract widening that belongs to `Phase 3`
- do not treat the inventory pass as the first real `Catalog-1` runtime cut

#### Implementation Risks

- mixing inventory with cleanup and losing the honest current-state read
- under-inventorying the already-shipped environment or imports-adjacent seams because the phase focuses only on shoes, hooks, and footpads
- listing files without classifying the real owner and consumer relationship, which would force later phases to redo the discovery work

#### Checklist

- [x] the current preloaded reference families are inventoried across manifest, seed, and public asset seams
- [x] the current environment entry and consumer path are inventoried alongside the reference lanes
- [x] the current Catalog UI and runtime touchpoints are inventoried from the shipped surface host and proof seams
- [x] one explicit owner-and-consumer map exists for later `Phase 2` and `Phase 3`

#### Verification Shape

Minimum verification for this phase should cover:

- the inventory names the live repo-backed reference owners and asset-home paths
- the inventory names the current Catalog-facing source, preview, and commit owners
- the inventory names the current viewer-environment apply owner
- the resulting read is explicit enough that the next cleanup phases do not need to rediscover where the drift currently lives

#### Done Shape

`Phase 1` is done when:

- the repo has one explicit current-state inventory of catalog-like drift
- later `Generation 0` cleanup phases can point at concrete owners instead of broad guesses
- the family can move into boundary cleanup and asset-home prep without reopening the basic "what already exists today?" question

`Phase 1` is now complete on that inventory basis.

## [x] Catalog-Gen0 - Phase 2 - Ownership Boundary Cleanup

### Phase 2 Summary
#### Purpose

Clean up the owner split before the family starts so `Catalog-1` can build on explicit boundaries instead of re-litigating them during the first runtime slice.

#### Owns

- `Browser` versus `Catalog` boundary cleanup
- removing preloaded reference models from the default `Browser` baseline
- import versus catalog-reuse boundary cleanup
- preview-versus-commit ownership cleanup
- workspace-surface boundary cleanup

#### Does Not Own

- the first actual workspace registration or slot-switching proof
- asset-family loading behavior
- the later `Generation 2` widening lanes

#### Current Live Read

The `Phase 1` inventory now makes the surviving boundary drift fairly concrete:

- Browser-side manifest preload still survives as real runtime truth
  - `src/app/store/useAppStore.ts` still seeds `referenceWorkspace` directly from `REFERENCE_MANIFEST_ITEMS`
  - `selectReferenceWorkspaceBrowserTree(...)` still turns those manifest-backed records into the Browser `References` root
  - `src/app/panels/useBrowserPanelController.ts` still consumes that Browser tree directly
  - that means `foothooks`, `shoes`, and `footpads` still read as live Browser-resident default references instead of only later optional Catalog add-ins
- Catalog already exists as a real hosted surface
  - `src/app/workspace/ViewportSurfaceRegistry.tsx` already routes the `catalog` render family to `CatalogSurface`
  - `src/app/AppShell.tsx` already hosts detached floating Catalog surfaces
  - so the workspace-surface question is not whether Catalog exists; it is whether the remaining owner reads still blur Catalog with older Browser preload behavior
- Catalog source boundaries are already partly explicit, but still mixed
  - `src/app/catalog/catalogSeedItems.ts` owns repo-backed curated Catalog entries
  - `src/app/catalog/catalogSource.ts` keeps imports reuse separate by only projecting `sourceKind: 'imported'` records from `referenceWorkspace` into the Catalog `Imports` area
  - that means the imports-versus-curated split already exists, but `Phase 2` still needs to lock that this is reuse-only and not a second import owner
- Preview and commit seams are already distinct in code
  - `src/app/catalog/catalogPreviewSession.ts` owns temporary preview-loaded item state
  - `src/app/catalog/catalogReferenceCommit.ts` only resolves repo-backed reference items onto the downstream `browser-project` handoff
  - `src/app/workspace/CatalogSurface.tsx` then hands that request to `addImportedReference(...)`
  - so the code already contains the right split, but the prep lane still needs to lock that split as the boundary truth the family should preserve
- Environment apply is also already separate from reference commit
  - `src/app/catalog/catalogEnvironmentApply.ts` resolves only `environment` items onto the `viewer-environment` downstream owner
  - `src/app/workspace/CatalogSurface.tsx` applies that through `setViewKey('envPreset', ...)`
  - so the environment lane already has a separate owner path and should stay out of Browser-project reference truth

So the next honest implementation read is:
- `Phase 2` should not invent the owner split from scratch
- it should tighten and document the surviving boundary drift where the runtime still contradicts the intended Catalog-vs-Browser read
- it should stop before changing the asset home or widening metadata work that belongs later

#### First Pass Decisions

- treat this phase as an owner-read cleanup contract first, not as a broad asset migration
- explicitly lock that repo-backed reference preload should stop reading as Browser baseline truth and should instead belong to later optional Catalog browsing
- explicitly lock that Catalog `Imports` is reuse-only, not the import intake owner
- explicitly lock that temporary preview state, downstream repo-backed commit, and viewer-environment apply are separate systems that should not collapse back into one generic “load” meaning
- keep the phase grounded in the already-shipped workspace surface instead of reopening whether Catalog is a real workspace mode

#### Boundary Cleanup Read

The owner split is now explicit enough to treat as the shared boundary contract:

- `Browser` owner read
  - `src/app/store/useAppStore.ts` still seeds `referenceWorkspace.importedReferencesById` and `importedReferenceOrder` from `REFERENCE_MANIFEST_ITEMS` through `buildInitialReferenceRecords(...)` and `INITIAL_REFERENCE_RECORD_ORDER`
  - `selectReferenceWorkspaceBrowserTree(...)` still projects those manifest-backed records into the Browser `References` root, and `src/app/panels/useBrowserPanelController.ts` still renders that tree directly
  - this means the Browser-side manifest preload is still the live legacy owner that later cleanup must retire
  - this does not mean those repo-backed families belong to Browser long term; it means the old preload bridge is still the surviving contradiction that `Generation 0` must remove
- `Catalog` browse owner read
  - `src/app/catalog/catalogSeedItems.ts` owns the current curated repo-backed Catalog entries
  - `src/app/catalog/catalogSource.ts` builds the Catalog browse snapshot from those repo items first, then appends reuse-only imports items as a second source lane
  - this means curated repo-backed entries already belong to Catalog browse even while the old Browser preload still survives elsewhere
- `Imports` reuse owner read
  - `createCatalogImportsSourceSnapshotFromReferenceWorkspace(...)` only projects records whose `sourceKind` is `imported`
  - manifest-backed preload records are intentionally excluded from the Catalog `Imports` lane
  - this means Catalog `Imports` is not an intake system and not a second source of curated repo truth; it is only a Catalog-facing reuse mirror after Browser-project intake has already happened
- preview versus commit owner read
  - `src/app/catalog/catalogPreviewSession.ts` owns temporary per-surface preview-loaded item ids
  - `CatalogSurface.tsx` keeps that preview session separate from downstream commit and only persists the temporary preview state per surface instance
  - `src/app/catalog/catalogReferenceCommit.ts` does not own preview; it only resolves an explicit repo-backed `add-to-project` handoff into the downstream `browser-project` request shape
  - `CatalogSurface.tsx` then forwards that handoff through `addImportedReference(...)`, which means Browser-project truth still starts at the downstream commit seam rather than inside temporary Catalog preview state
- environment owner read
  - `src/app/catalog/catalogEnvironmentApply.ts` only resolves `environment` items onto the downstream `viewer-environment` owner path
  - `CatalogSurface.tsx` applies that via `setViewKey('envPreset', ...)`
  - this means environment apply is not project-reference commit and should stay outside Browser-project reference truth
- workspace-surface owner read
  - `src/app/workspace/ViewportSurfaceRegistry.tsx` already treats `catalog` as its own render family and routes it to `CatalogSurface`
  - `src/app/AppShell.tsx` already hosts detached floating Catalog surfaces through `SimpleFloatingSurfaceHost`
  - this means Catalog already counts as a real hosted workspace surface in both slotted and floating form
  - the remaining cleanup problem is the old Browser preload read, not whether Catalog is merely a Browser subsection

So the boundary-cleanup result is:

- later cleanup should retire Browser-side manifest preload as baseline truth
- later cleanup should preserve Catalog as the owner of curated repo-backed browse
- later cleanup should preserve Catalog `Imports` as reuse-only
- later cleanup should preserve temporary preview, Browser-project commit, and viewer-environment apply as separate downstream meanings
- later cleanup should treat Catalog as an existing workspace surface instead of reopening its host legitimacy

### Phase 2 Implementation Spec
#### Exact First Code Cut

1. Re-read the live Browser-side manifest preload seams in `useAppStore.ts` and `useBrowserPanelController.ts`, then document exactly which owner assumptions must be retired so `foothooks`, `shoes`, and `footpads` stop reading as default Browser-resident content.
2. Re-read the Catalog source split in `catalogSeedItems.ts` plus `catalogSource.ts`, then lock the boundary that repo-backed curated entries belong to Catalog browse while imported entries only reappear there as reuse surfaces after intake.
3. Re-read the preview, commit, and environment seams in `catalogPreviewSession.ts`, `catalogReferenceCommit.ts`, `catalogEnvironmentApply.ts`, and `CatalogSurface.tsx`, then record one explicit boundary read that keeps:
   - preview temporary and Catalog-owned
   - repo-backed commit downstream to Browser-project truth
   - environment apply downstream to viewer-environment truth
4. Record one explicit workspace-surface boundary read that says Catalog is already a real hosted surface and should not be treated as a Browser subsection just because Browser preload drift still survives elsewhere.
5. Stop once the owner cleanup rules are explicit enough that later `Phase 3` asset-home and metadata prep can execute against stable boundaries instead of re-arguing them.

#### Likely Files

- `src/app/store/useAppStore.ts`
- `src/app/panels/useBrowserPanelController.ts`
- `src/app/catalog/catalogSeedItems.ts`
- `src/app/catalog/catalogSource.ts`
- `src/app/catalog/catalogPreviewSession.ts`
- `src/app/catalog/catalogReferenceCommit.ts`
- `src/app/catalog/catalogEnvironmentApply.ts`
- `src/app/workspace/CatalogSurface.tsx`
- `src/app/workspace/ViewportSurfaceRegistry.tsx`
- `src/app/AppShell.tsx`
- this `docs/Human-Plans/Architecture/Workspace-Modes/Workspaces/Catalog/Catalog-Gen0-Index.md`

#### No-Widening Rule

- do not start the repo asset-home migration in this phase; that belongs to `Phase 3`
- do not widen into the first full Catalog runtime or family onboarding behavior; that belongs to `Catalog-1` and later
- do not reopen the basic inventory work from `Phase 1` unless one boundary note truly cannot be expressed without a tiny inventory correction
- do not turn this phase into external-source, builder, or compatibility planning

#### Implementation Risks

- solving the boundary drift by jumping straight into file moves or runtime deletions before the owner rules are explicit
- treating imports reuse as if it were the same thing as repo-backed curated Catalog content
- blurring temporary preview state back into commit semantics because both already exist in the Catalog runtime
- overstating the workspace-hosting problem even though Catalog is already a real hosted surface

#### Checklist

- [x] the Browser-versus-Catalog boundary is explicit enough to retire the old Browser preload read later without confusion
- [x] the imports-versus-Catalog-reuse boundary is explicit enough that Catalog stays a reuse surface instead of becoming the import owner
- [x] the preview-versus-commit-versus-environment owner split is explicit enough that later runtime work can preserve it without guesswork
- [x] the workspace-surface boundary is explicit enough that Catalog is treated as a real hosted surface, not a Browser subsection

#### Verification Shape

Minimum verification for this phase should cover:

- the phase names the surviving Browser preload owner that later cleanup must retire
- the phase names the current Catalog source and imports reuse owners separately
- the phase names the current preview, Browser-project commit, and viewer-environment apply owners separately
- the resulting boundary read is explicit enough that later cleanup or migration work can proceed without reopening the same owner questions

#### Done Shape

`Phase 2` is done when:

- the repo has one explicit owner-boundary cleanup read for Browser, Catalog, imports reuse, preview, commit, and environment apply
- later `Generation 0` work can retire the old Browser preload and asset-home drift against that shared boundary contract
- `Catalog-1` no longer needs to start by rediscovering what each early catalog-adjacent seam is actually allowed to own

`Phase 2` is now complete on that owner-boundary-contract basis.

## [x] Catalog-Gen0 - Phase 2.1 - Browser References Baseline Removal

This follow-on implementation slice executes the already-landed `Phase 2` boundary rule through one narrow runtime removal pass.

It should:
- remove the Browser `References` startup baseline that still appears because `referenceWorkspace` is seeded from manifest items on first load
- retire manifest-backed Browser startup seeding without widening into asset-home migration
- preserve true imported-reference behavior after real intake instead of deleting the whole reference runtime blindly

This runtime-removal slice is now complete on that basis and remains linked to the standalone shipped-read doc here:

- [Catalog_Phase Catalog-Gen0.2.1 - Browser References Baseline Removal.md](./Future/Catalog_Phase%20Catalog-Gen0.2.1%20-%20Browser%20References%20Baseline%20Removal.md)

Future doc:
- [Catalog_Phase Catalog-Gen0.2.1 - Browser References Baseline Removal.md](./Future/Catalog_Phase%20Catalog-Gen0.2.1%20-%20Browser%20References%20Baseline%20Removal.md)

## [x] Catalog-Gen0 - Phase 3 - Curated Asset And Metadata Prep

### Phase 3 Summary
#### Purpose

Prepare the first curated asset baseline and manifest-adjacent metadata reads so `Catalog-1` can lock a real item contract without starting from scattered ad hoc asset assumptions.

#### Owns

- later optional reference-family inventory for `foothooks`, `shoes`, and `footpads`
- the first `HDRI` inventory
- preview-media gap inventory
- repo-backed asset-home cleanup for the first catalog-owned reference families
- stable id or slug prep
- first manifest-field prep
- `Imports` area readiness notes

#### Does Not Own

- the final item contract itself
- manifest runtime loading
- the first visible catalog shell

#### Current Live Read

The current asset and metadata baseline is concrete enough that `Phase 3` can now prep against real repo drift instead of broad guesses:

- manifest inventory is broader than the current Catalog repo seed
  - `src/app/references/referenceManifest.ts` currently exposes one footpad, three shoes, and four foothooks through the older `ReferenceModels/...` pathing
  - `src/app/catalog/catalogSeedItems.ts` currently surfaces only one footpad, one shoe, one foothook, and one environment entry through the Catalog repo seed
  - that means the later optional-family inventory is still partial on the Catalog side even before any item-contract widening starts
- preview coverage is partial and family-by-family inconsistent
  - `public/CatalogPreviews/footpads/` currently has one PubPad preview
  - `public/CatalogPreviews/shoes/` currently has only `shoe-1.svg` even though the manifest already knows about `shoe-2` and `shoe-3`
  - `public/CatalogPreviews/hooks/` currently has only `hook-large.svg` even though the manifest already knows about `medium`, `small`, and `xl`
  - that means the preview-media gap inventory is real and should be tracked separately from the asset-home move
- repo-backed geometry still lives in the legacy reference home
  - `public/ReferenceModels/footpads`, `public/ReferenceModels/shoes`, and `public/ReferenceModels/hooks` still hold the live reference-family geometry
  - `referenceManifest.ts` plus `catalogSeedItems.ts` still point those items at `ReferenceModels/...`
  - `public/Catalog/` now exists, but it is only lightly used for newer board content and not yet for the current reference families
- environment prep is not yet grounded cleanly
  - `catalogSeedItems.ts` points the first environment entry at `Environments/studio.hdr`
  - the live public folder currently contains `public/HDRI/citrus_orchard_road_puresky_2k.exr`
  - there is no current `public/Environments/studio.hdr` file in the repo
  - so the first `HDRI` inventory and path normalization still need an honest prep read before `Catalog-1`
- the current Catalog item contract is still intentionally thin
  - `src/app/catalog/catalogItemContract.ts` currently exposes `itemId`, `label`, `familyKey`, `sectionKey`, `tags`, `description`, `assetKind`, `actionKind`, `source`, and `previewMedia`
  - that is enough for the current shell, but it does not yet encode stronger prep concepts such as stable slug conventions, preview completeness expectations, richer family metadata, or later compatibility-oriented manifest fields
- the current `Imports` read is also intentionally light
  - `catalogSource.ts` currently maps imported references into Catalog using `referenceId`, `categoryId`, `label`, and `assetPath`
  - imports entries still inherit their `sectionKey` from the Browser-side category read and carry no preview media
  - that means `Imports` readiness notes belong in this phase before the first real catalog item contract tries to absorb that lane

So the next honest implementation read is:
- `Phase 3` should stay prep-first and inventory-backed
- it should tighten the repo asset-home and metadata baseline without claiming the final item contract already exists
- it should stop before manifest runtime loading, first visible shell expansion, or broader `Catalog-1` browse behavior

#### First Pass Decisions

- treat this phase as the first explicit asset-home and metadata-read prep lane, not as the final migration itself
- inventory and name the missing reference-family members, missing previews, and missing environment grounding before deciding what `Catalog-1` can honestly rely on
- explicitly prep the move from `ReferenceModels/...` toward Catalog-owned paths, but keep the actual runtime cut separate from this prep doc
- explicitly prep stable ids or slugs and first manifest-field additions only to the point where `Catalog-1` can start from a clearer baseline
- keep `Imports` readiness scoped to the fields and assumptions the first item contract will need, not to a broader import redesign

#### Asset And Metadata Prep Read

The prep baseline is now explicit enough to treat as the shared asset-and-metadata contract for later work:

- later optional-family inventory read
  - `referenceManifest.ts` currently exposes:
    - `footpad:pubpad-full-assembly`
    - `shoe:shoe-1`
    - `shoe:shoe-2`
    - `shoe:shoe-3`
    - `hook:large`
    - `hook:medium`
    - `hook:small`
    - `hook:xl`
  - `catalogSeedItems.ts` currently surfaces:
    - `reference:footpad-pubpad-full-assembly`
    - `reference:shoe-1`
    - `reference:hook-large`
    - `environment:studio`
  - so the current curated seed already covers the first footpad member, but it still omits `shoe-2`, `shoe-3`, `hook-medium`, `hook-small`, and `hook-xl` even though those repo-backed assets already exist in manifest truth
- preview-gap read
  - current preview coverage exists for:
    - `pubpad-full-assembly.svg`
    - `shoe-1.svg`
    - `hook-large.svg`
    - `studio-environment.svg`
  - current preview coverage is missing for:
    - `shoe-2`
    - `shoe-3`
    - `hook-medium`
    - `hook-small`
    - `hook-xl`
  - so later curated browse should not pretend those missing members are already preview-ready just because the geometry files exist
- repo asset-home read
  - live reference-family geometry still sits under `public/ReferenceModels/footpads`, `public/ReferenceModels/shoes`, and `public/ReferenceModels/hooks`
  - the newer `public/Catalog/` area currently only shows lightly used board content such as `public/Catalog/boards/adv/ADV_Full Assembly_parts.step`
  - so the asset-home drift is real: the first Catalog reference families are still physically owned by the legacy reference folder while the newer Catalog folder is not yet their runtime home
- stable id and slug read
  - manifest ids currently follow family-member ids such as `shoe:shoe-2` and `hook:medium`
  - Catalog seed ids currently follow Catalog-facing item ids such as `reference:shoe-1` and `reference:hook-large`
  - preview filenames already imply a narrower shared slug pattern such as `shoe-1`, `hook-large`, and `pubpad-full-assembly`
  - so the minimum prep conclusion is that later Catalog work needs one explicit member-slug convention that can bridge:
    - manifest item ids
    - Catalog item ids
    - preview filenames
    - later Catalog-owned asset paths
- first manifest-field prep read
  - the current item contract already depends on `itemId`, `familyKey`, `sectionKey`, `assetKind`, `actionKind`, `source`, and `previewMedia`
  - the next narrow prep baseline should preserve those fields and add only what later migration and `Catalog-1` actually need to stop improvising:
    - stable member slug
    - explicit preview-readiness expectation
    - explicit repo-family membership read
  - this phase does not need to invent wider compatibility or external-source metadata yet
- `HDRI` prep read
  - the current seeded environment item points at `Environments/studio.hdr`
  - the live repo currently exposes `public/HDRI/citrus_orchard_road_puresky_2k.exr`
  - there is no current `public/Environments/studio.hdr`
  - so the first environment lane is not yet normalized enough for `Catalog-1` to assume a settled environment asset home or one final filename pattern
- `Imports` readiness read
  - `catalogSource.ts` currently derives imports items from `referenceId`, `categoryId`, `label`, and `assetPath`
  - the derived Catalog imports item then adds:
    - `itemId` as `imports:<referenceId>`
    - `familyKey` as `imports`
    - `sectionKey` from the Browser-side category
    - `tags` as `['imports', categoryId]`
    - empty `previewMedia`
  - so the current imports lane is ready only for a light reuse read, not for curated slugs, preview completeness, or richer family metadata

So the prep result is:

- later migration should treat the manifest inventory as broader than the current Catalog seed
- later migration should treat preview coverage as a separate readiness lane from geometry availability
- later migration should treat `ReferenceModels/...` as the current legacy asset home to move away from
- later Catalog item-contract work should normalize one member-slug bridge across manifest ids, Catalog item ids, preview files, and future Catalog-owned paths
- later `Catalog-1` work should assume only a light imports metadata baseline unless a narrower follow-on phase expands it

### Phase 3 Implementation Spec
#### Exact First Code Cut

1. Re-read the current manifest inventory in `referenceManifest.ts`, then record which reference-family members already exist in repo truth but are still missing from the current Catalog repo seed.
2. Re-read `catalogSeedItems.ts`, `catalogItemContract.ts`, and `catalogSource.ts`, then document the minimum stable id, slug, family, section, source, and preview assumptions the first Catalog item contract should inherit from prep instead of improvising later.
3. Re-read the public asset folders, then document the current split between:
   - live geometry under `public/ReferenceModels/...`
   - preview media under `public/CatalogPreviews/...`
   - lightly used newer Catalog-owned assets under `public/Catalog/...`
   - current environment assets under `public/HDRI/...` or any later normalized environment home
4. Record one explicit preview-gap read for the first optional families so `Catalog-1` knows which items already have preview coverage and which ones still need preview assets before they can browse honestly as curated entries.
5. Record one explicit `Imports` readiness read that names what import-derived fields are currently available and what should remain out of scope until the later real item contract.
6. Stop once the asset-home and metadata baseline is explicit enough that later migration or `Catalog-1` work can execute against a stable prep contract.

#### Likely Files

- `src/app/references/referenceManifest.ts`
- `src/app/catalog/catalogSeedItems.ts`
- `src/app/catalog/catalogItemContract.ts`
- `src/app/catalog/catalogSource.ts`
- `src/app/catalog/ui/catalogShellShared.ts`
- `public/ReferenceModels/`
- `public/CatalogPreviews/`
- `public/Catalog/`
- `public/HDRI/`
- this `docs/Human-Plans/Architecture/Workspace-Modes/Workspaces/Catalog/Catalog-Gen0-Index.md`

#### No-Widening Rule

- do not implement the full repo asset-home migration in this phase; prep the baseline for it
- do not turn this prep lane into the final Catalog item contract or manifest runtime
- do not widen into the first visible `Catalog-1` shell or browse behavior
- do not reopen the Phase 2 owner split except where a tiny metadata note truly depends on it
- do not fold later compatibility, external-source, or builder metadata into this prep baseline

#### Implementation Risks

- pretending the current Catalog repo seed already represents the full optional-family baseline when the manifest and public folders are broader
- mixing preview-media gap inventory with the actual asset-home move and losing clarity about which problem is being solved
- letting the existence of `public/Catalog/` imply the current reference families already live there when runtime still points at `ReferenceModels/...`
- overdesigning the future item contract instead of only documenting the minimum prep fields that `Catalog-1` needs to start honestly
- treating the current environment path as settled even though the live repo does not yet contain the seeded `studio.hdr` asset

#### Checklist

- [x] the later optional-family inventory is explicit enough that `Catalog-1` will know which repo-backed reference members already exist versus which ones are still missing from the current Catalog seed
- [x] the preview-media gap read is explicit enough that later curated entries do not silently ship without honest preview coverage
- [x] the repo asset-home drift is explicit enough that later migration work can move from `ReferenceModels/...` toward Catalog-owned paths without rediscovering the file layout
- [x] the first stable id, slug, and manifest-field prep read is explicit enough that `Catalog-1` can start from a narrow real baseline instead of scattered assumptions
- [x] the `Imports` readiness notes are explicit enough that the first item contract can include that lane without becoming the import owner

#### Verification Shape

Minimum verification for this phase should cover:

- the phase names the current manifest inventory and the smaller current Catalog seed separately
- the phase names which preview assets currently exist and where the gaps still are
- the phase names the live public asset-home split across `ReferenceModels`, `CatalogPreviews`, `Catalog`, and environment assets
- the phase names the thin current item-contract fields and the current imports-ready fields separately
- the resulting prep read is explicit enough that later migration or `Catalog-1` work does not need to rediscover the same asset and metadata gaps

#### Done Shape

`Phase 3` is done when:

- the repo has one explicit prep read for current curated asset coverage, preview gaps, asset-home drift, stable id or slug needs, first manifest-field needs, and imports readiness
- later migration or `Catalog-1` work can start from an honest asset and metadata baseline instead of broad file-folder assumptions
- the family can move into the `Generation 1` start-boundary cleanup without pretending the current repo asset and metadata situation is already normalized

`Phase 3` is now complete on that asset-and-metadata-prep basis.


### Purpose

Lock the handoff from prep into the first real family phase so the repo can say clearly when `Catalog` has actually moved from cleanup into implementation.

### Owns

- the definition of the first real `Catalog` family start
- the `Catalog-1` entry checklist
- the rule that the old Browser preload behavior is gone before later optional add-ins arrive
- the rule that `Generation 2` widening should stay out of the `Generation 1` baseline start
- first standalone doc routing for the family start

### Does Not Own

- executing `Catalog-1`
- executing `Catalog-Gen2`
- later builder or compatibility work

### Summary

The `Generation 0` cleanup direction is now:
- stay honest that the `Catalog` family has not started yet
- inventory the current catalog-like drift before new runtime work begins
- move the preloaded reference models out of `Browser` so `foothooks`, `shoes`, and `footpads` stop reading as default Browser-resident content
- clean up owner boundaries between `Catalog`, `Browser`, import, viewer state, and shared workspace hosting
- prepare `foothooks`, `shoes`, and `footpads` as later optional add-ins, alongside the first `HDRI` and `Imports` baseline prep
- give repo-backed reference assets one clearer Catalog-owned home instead of letting older `ReferenceModels` path drift quietly survive into later family phases
- lock a clean handoff so `Catalog-1` can start as the first real family phase and `Catalog-Gen2` can stay a later widening lane

## [x] Catalog-Gen0 - Phase 5 - ReferenceModels To Catalog Migration Ladder

### Purpose

Finish the `ReferenceModels/...` cleanup for the first reference families by landing one explicit migration ladder that ends with a clean Catalog-owned repo asset home before `Gen0` closes.

### Owns

- the full `ReferenceModels/...` to `Catalog/...` migration ladder for `shoes`, `hooks`, and `footpads`
- the stable family-folder contract for those first repo-backed reference families
- per-family migration sequencing that can be implemented by one Codex-sized slice at a time
- the final shared path rewires and legacy-folder retirement needed to leave one clean repo asset home behind

### Does Not Own

- new Catalog browse UI or `Catalog-1` behavior
- wider catalog item-contract expansion beyond what migration strictly requires
- preview creation beyond the narrow preview preservation needed to keep migrated items usable

### Current Live Read

The current asset-home read still shows a gap between the intended Catalog home and the live repo layout:

- `public/ReferenceModels/footpads`, `public/ReferenceModels/shoes`, and `public/ReferenceModels/hooks` still hold the live reference-family geometry
- `public/Catalog/` now has one direct shoes-family resident at `public/Catalog/shoes/vans-high-top-low.glb`, but it still does not yet replace the broader `ReferenceModels/...` family homes
- `src/app/references/referenceManifest.ts` and `src/app/catalog/catalogSeedItems.ts` still point the first reference families at the older `ReferenceModels/...` layout
- `Phase 3` already made that asset-home drift explicit, and the first `Phase 5` seed proves the Catalog-owned shoes home can start to exist, but the repo still needs a full family-by-family move plus final cleanup before `Gen0` can honestly call the asset-home drift retired

So the next honest prep read is:

- this lane should become a real migration ladder instead of stopping at folder prep
- it should still split the work into narrow family-sized phases instead of one giant migration pass
- it should stop before broader `Catalog-1` browse changes or unrelated item-contract widening

### First Pass Decisions

- keep `Phase 5` as one migration umbrella with explicit sub-phases instead of treating the whole migration as one Codex-sized pass
- land the family-folder contract first, then migrate `shoes`, `hooks`, and `footpads` in separate phases
- reserve one shared follow-through phase for the remaining consumer-path rewires after the family moves land
- reserve one final retirement phase so `Gen0` can end with the old `ReferenceModels/...` family homes truly gone or intentionally emptied

### Phase 5 Sub-Phase Ladder

#### [x] `Phase 5.1 - Catalog Family Folder Contract`

Owns:
- locking the stable target folder names under `public/Catalog/...`
- locking the path and slug contract later migration should target
- making the next three family-migration phases implementation-ready against one shared folder contract

Does not own:
- moving all family assets yet
- rewiring every runtime consumer yet
- renaming manifest category ids or widening into broader metadata cleanup

Implemented `5.1A. Re-read the live public asset layout and confirm the exact target family homes under public/Catalog`:
- re-read `public/Catalog` and confirmed the folder currently has only `boards` plus the newer `shoes` family home
- re-read `public/ReferenceModels` and confirmed the live reference-family geometry still resides under `footpads`, `hooks`, and `shoes`
- re-read `src/app/references/referenceManifest.ts` and `src/app/catalog/catalogSeedItems.ts` and confirmed the runtime asset-path drift is still the same shared issue across both seams: manifest truth still points at `ReferenceModels/footpads`, `ReferenceModels/shoes`, and `ReferenceModels/hooks`, while the Catalog-owned asset home has only started to exist through `public/Catalog/shoes/vans-high-top-low.glb`
- so the exact target family homes for the migration ladder are now locked as:
  - `public/Catalog/shoes`
  - `public/Catalog/hooks`
  - `public/Catalog/footpads`

Implemented `5.1B. Lock the stable family folder names for shoes, hooks, and footpads`:
- locked `shoes` as the stable Catalog folder name for the wearable reference family
- locked `hooks` as the stable Catalog folder name for the premade foothook geometry family
- locked `footpads` as the stable Catalog folder name for the footpad geometry family
- explicitly did not force the metadata layer to rename at the same time:
  - manifest category id can remain `premade-foothooks`
  - current Catalog seed `familyKey` and `sectionKey` can remain `foothooks`
- this phase only locks the runtime asset-home folder contract so later migration phases stop improvising between `hooks` versus `foothooks`

Implemented `5.1C. Lock the shared path and slug contract that later manifest and seed rewires must target`:
- later `5.2`, `5.3`, and `5.4` rewires should change the asset home from `ReferenceModels/<family>/...` to `Catalog/<family-folder>/...` without widening into unrelated identity or browse redesign
- the locked family-folder mapping is:
  - manifest `shoes` and Catalog `shoes` items -> `Catalog/shoes/...`
  - manifest `premade-foothooks` and current Catalog `foothooks` items -> `Catalog/hooks/...`
  - manifest `footpads` and Catalog `footpads` items -> `Catalog/footpads/...`
- the locked shared readable slug rail is:
  - shoes: `shoe-1`, `shoe-2`, `shoe-3`, `vans-high-top-low`
  - hooks: `hook-large`, `hook-medium`, `hook-small`, `hook-xl`
  - footpads: `pubpad-full-assembly`
- later migration phases should keep using those readable slugs in docs, preview naming, and seed-entry reasoning instead of inventing a second folder-specific naming system
- later migration phases do not need to rename the source-authored geometry filenames just to satisfy this contract; the required move is the family-home parent path change, not a forced file-basename rewrite
- later migration phases also do not need to rename manifest ids in `5.1`; current id rails such as `shoe:shoe-1`, `hook:large`, and `footpad:pubpad-full-assembly` can stay stable while the asset-home parent path changes underneath them

Implemented `5.1D. Mark Phases 5.2, 5.3, and 5.4 implementation-ready against that folder contract`:
- `Phase 5.2` is implementation-ready to move `Shoe_1.glb`, `Shoe_2.glb`, and `Shoe_3.glb` into `public/Catalog/shoes` and repoint manifest plus Catalog-seed asset paths to that locked home while preserving the already-added `vans-high-top-low.glb`
- `Phase 5.3` is implementation-ready to move `large.step`, `medium.step`, `small.step`, and `xl.step` into `public/Catalog/hooks` and repoint manifest plus Catalog-seed asset paths without reopening the separate `premade-foothooks` versus `foothooks` metadata naming question
- `Phase 5.4` is implementation-ready to move the footpad family into `public/Catalog/footpads`, repoint the manifest plus Catalog-seed asset paths for the published assembly, and carry any directly referenced companion footpad files that the moved asset still needs to load honestly

Done read:
- `Phase 5.1` is complete when the repo has one explicit target folder contract for `public/Catalog/shoes`, `public/Catalog/hooks`, and `public/Catalog/footpads`, one stable shared naming rule for later family moves, and the next three family-migration phases can implement against that contract without reopening folder-layout decisions
- `Phase 5.1` is now complete on that docs-and-contract basis

#### [x] `Phase 5.2 - Shoes Asset Migration`

Owns:
- moving the repo-backed shoe geometry into the Catalog-owned shoes home
- repointing shoe manifest and Catalog seed paths to the new home
- expanding the Catalog repo-backed shoes slice so the migrated family is represented honestly after the move

Does not own:
- hooks or footpads migration
- final shared cleanup for every remaining consumer
- retirement of the legacy `public/ReferenceModels/shoes` folder

Implemented internal slice `confirm live shoe inventory and contract-target destination under public/Catalog/shoes`:
- re-read the live family inventory and confirmed `public/ReferenceModels/shoes` still held `Shoe_1.glb`, `Shoe_2.glb`, and `Shoe_3.glb`
- re-read `public/Catalog/shoes` and confirmed the Catalog-owned shoes home only contained the previously added `vans-high-top-low.glb`
- re-read `referenceManifest.ts` and `catalogSeedItems.ts` and confirmed both runtime seams still pointed the migrated shoes at `ReferenceModels/shoes/...`
- kept the `Phase 5.1` contract unchanged:
  - target runtime home remains `public/Catalog/shoes`
  - manifest ids remain `shoe:shoe-1`, `shoe:shoe-2`, and `shoe:shoe-3`
  - readable slug rail remains `shoe-1`, `shoe-2`, `shoe-3`, and `vans-high-top-low`

Implemented internal slice `move remaining legacy shoe assets into the Catalog-owned shoes home per the 5.1 contract`:
- copied `Shoe_1.glb`, `Shoe_2.glb`, and `Shoe_3.glb` from `public/ReferenceModels/shoes` into `public/Catalog/shoes`
- left legacy `public/ReferenceModels/shoes` in place for now so `Phase 5.6` can own the final retirement cleanly instead of hiding retirement work inside the family migration pass
- the live Catalog-owned shoes family home now contains:
  - `Shoe_1.glb`
  - `Shoe_2.glb`
  - `Shoe_3.glb`
  - `vans-high-top-low.glb`

Implemented internal slice `repoint manifest paths for shoes`:
- updated `src/app/references/referenceManifest.ts` so `shoe:shoe-1`, `shoe:shoe-2`, and `shoe:shoe-3` now resolve from `Catalog/shoes/Shoe_1.glb`, `Catalog/shoes/Shoe_2.glb`, and `Catalog/shoes/Shoe_3.glb`
- left shoe ids and category ownership unchanged so the migration stayed on the parent asset-home move only

Implemented internal slice `repoint Catalog seed paths and expand Catalog repo-backed shoe entries as needed so Catalog reflects the migrated repo-backed shoe set`:
- updated the existing `reference:shoe-1` Catalog seed entry to read from `Catalog/shoes/Shoe_1.glb`
- added `reference:shoe-2` and `reference:shoe-3` so the Catalog repo-backed shoes slice now reflects the migrated repo-backed shoe set instead of only surfacing one of the three legacy shoe assets
- kept the already-added `reference:vans-high-top-low` entry in the same `shoes` family so the Catalog-owned shoes home now reads as one coherent family home
- preserved the current preview honesty:
  - `shoe-1` keeps its existing preview asset
  - `shoe-2`, `shoe-3`, and `vans-high-top-low` still have empty `previewMedia` until a later preview lane adds coverage

Done read:
- `Phase 5.2` is complete when all legacy repo-backed shoe assets live in `public/Catalog/shoes`, manifest shoe paths point to Catalog-owned homes, and the Catalog seed exposes the migrated shoes family from Catalog-owned paths without reopening folder-contract questions
- `Phase 5.2` is now complete on that migration basis

#### [x] `Phase 5.3 - Hooks Asset Migration`

Owns:
- moving the repo-backed hook geometry into the Catalog-owned hooks home
- repointing hook manifest and Catalog seed paths to the new home
- expanding the Catalog repo-backed hooks slice so the migrated family is represented honestly after the move

Does not own:
- shoes or footpads migration beyond preserving already-landed behavior
- final legacy-folder retirement

Implemented internal slice `confirm live hook inventory and the locked destination public/Catalog/hooks`:
- re-read the live hook family inventory and confirmed `public/ReferenceModels/hooks` held `large.step`, `medium.step`, `small.step`, and `xl.step`
- confirmed the locked `Phase 5.1` destination remained `public/Catalog/hooks`
- re-read the shared manifest and Catalog seed seams and confirmed they still pointed the hooks family at the legacy `ReferenceModels/hooks/...` home before this subphase landed

Implemented internal slice `move or copy remaining legacy hook assets into public/Catalog/hooks`:
- created `public/Catalog/hooks`
- copied `large.step`, `medium.step`, `small.step`, and `xl.step` into the Catalog-owned hooks home
- left legacy retirement to `Phase 5.6` so the family migration did not silently absorb the final cleanup pass

Implemented internal slice `repoint manifest paths for hooks`:
- updated `src/app/references/referenceManifest.ts` so `hook:large`, `hook:medium`, `hook:small`, and `hook:xl` now resolve from `Catalog/hooks/large.step`, `Catalog/hooks/medium.step`, `Catalog/hooks/small.step`, and `Catalog/hooks/xl.step`
- preserved the existing manifest category id `premade-foothooks` per the `Phase 5.1` contract

Implemented internal slice `repoint and expand Catalog seed hook entries as needed`:
- updated the existing `reference:hook-large` seed entry to read from `Catalog/hooks/large.step`
- added `reference:hook-medium`, `reference:hook-small`, and `reference:hook-xl` so the Catalog repo-backed hooks slice now reflects the migrated repo-backed hook set instead of only surfacing one of the four legacy assets
- preserved preview honesty:
  - `hook-large` keeps its existing preview asset
  - `hook-medium`, `hook-small`, and `hook-xl` currently keep empty `previewMedia`

Done read:
- `Phase 5.3` is complete when all legacy repo hook assets live in `public/Catalog/hooks`, hook manifest paths point to Catalog-owned homes, and the Catalog seed exposes the migrated hook family from Catalog-owned paths
- `Phase 5.3` is now complete on that migration basis

#### [x] `Phase 5.4 - Footpads Asset Migration`

Owns:
- moving the repo-backed footpad geometry into the Catalog-owned footpads home
- repointing footpad manifest and Catalog seed paths to the new home
- carrying the required companion files honestly into the Catalog-owned home

Does not own:
- broader family metadata redesign
- final shared cleanup beyond the footpad move itself

Implemented internal slice `confirm live footpad inventory and the locked destination public/Catalog/footpads`:
- re-read the live footpad family inventory and confirmed `public/ReferenceModels/footpads` held:
  - `PubPad.mtl`
  - `PubPad.obj`
  - `XR_Footpad_PubPad_Full_Assembly.mtl`
  - `XR_Footpad_PubPad_Full_Assembly.obj`
- confirmed the locked `Phase 5.1` destination remained `public/Catalog/footpads`
- re-read the manifest and Catalog seed seams and confirmed the published assembly still pointed at the legacy `ReferenceModels/footpads/...` home before this subphase landed

Implemented internal slice `move or copy the footpad asset set and required companion files into public/Catalog/footpads`:
- created `public/Catalog/footpads`
- copied the full visible legacy footpad asset set into the Catalog-owned footpads home
- kept the `.mtl` companions with the `.obj` geometry so the published assembly can continue loading from one honest family home

Implemented internal slice `repoint manifest and Catalog seed footpad paths`:
- updated `src/app/references/referenceManifest.ts` so `footpad:pubpad-full-assembly` now resolves from `Catalog/footpads/XR_Footpad_PubPad_Full_Assembly.obj`
- updated `src/app/catalog/catalogSeedItems.ts` so `reference:footpad-pubpad-full-assembly` now reads from `Catalog/footpads/XR_Footpad_PubPad_Full_Assembly.obj`
- kept the current readable slug and current single seeded family member intact while carrying the broader family asset set into the new home

Done read:
- `Phase 5.4` is complete when the legacy footpad asset set and required companions live in `public/Catalog/footpads`, the published assembly manifest path points to the Catalog-owned home, and the Catalog seed footpad path points to the same Catalog-owned home
- `Phase 5.4` is now complete on that migration basis

#### [x] `Phase 5.5 - Shared Consumer Path Rewire`

Owns:
- removing the last shared runtime assumptions that still point at `ReferenceModels/...`
- normalizing the surviving shared reads onto the Catalog-owned family homes

Does not own:
- new browse behavior
- unrelated runtime cleanup outside migration follow-through

Implemented internal slice `confirm remaining shared ReferenceModels consumers after 5.3 and 5.4`:
- swept `src/...` for surviving `ReferenceModels/...` reads after the family migrations landed
- confirmed the remaining seams were no longer the primary manifest and Catalog seed only; a set of shared test fixtures and path assumptions still carried the old parent family home

Implemented internal slice `rewire surviving shared reads to Catalog-owned homes`:
- normalized the remaining `src/...` `ReferenceModels/...` parent paths onto the new Catalog-owned homes:
  - `Catalog/shoes/...`
  - `Catalog/hooks/...`
  - `Catalog/footpads/...`
- this shared rewire covered the shared manifest or seed seams plus the surviving Catalog and Browser-facing test-fixture paths so the repo no longer encoded a dual-home assumption for the migrated families

Done read:
- `Phase 5.5` is complete when the remaining shared `src/...` consumers no longer assume `ReferenceModels/...` for `shoes`, `hooks`, or `footpads`
- this phase now completes with zero surviving `ReferenceModels/...` matches under `src/...`

#### [x] `Phase 5.6 - Legacy ReferenceModels Retirement`

Owns:
- retiring the old `public/ReferenceModels/...` family homes
- locking the final repo-cleanup read so `Gen0` closes with one Catalog-owned asset home

Does not own:
- new `Catalog-1` runtime work
- later family widening beyond the migrated baseline

Implemented internal slice `confirm which legacy family folders can now be removed or intentionally emptied`:
- confirmed `shoes`, `hooks`, and `footpads` had already been copied into their Catalog-owned homes
- confirmed the shared `src/...` consumers no longer depended on the old `ReferenceModels/...` parent paths
- with both conditions true, the old family folders no longer held active runtime ownership

Implemented internal slice `retire the old ReferenceModels family folders for the migrated families`:
- removed the old `public/ReferenceModels` tree after the family migrations and shared consumer rewires were already complete
- this retirement includes the migrated `shoes`, `hooks`, and `footpads` family folders, leaving the repo without the old dual-home asset compromise

Implemented internal slice `final repo-cleanup honesty pass`:
- the repo now carries the migrated family assets only from:
  - `public/Catalog/shoes`
  - `public/Catalog/hooks`
  - `public/Catalog/footpads`
- the old `public/ReferenceModels/...` family layout is no longer present

Done read:
- `Phase 5.6` is complete when the migrated family folders no longer depend on the old `public/ReferenceModels/...` homes and `Gen0` can honestly claim one clean Catalog-owned repo asset home for these families
- `Phase 5.6` is now complete on that retirement basis

### Phase 5 Implementation Spec
#### Exact First Code Cut

1. Land `Phase 5.1` first so the stable Catalog-owned family-folder contract is explicit.
2. Migrate `shoes`, `hooks`, and `footpads` through separate family phases instead of one broad multi-family cut.
3. After the family moves land, clean up the remaining shared runtime path assumptions in one follow-through phase.
4. Retire the legacy `ReferenceModels/...` family homes in one final cleanup phase so `Gen0` closes with a single Catalog-owned repo asset home.

#### Likely Files

- `public/Catalog/`
- `public/ReferenceModels/`
- `src/app/references/referenceManifest.ts`
- `src/app/catalog/catalogSeedItems.ts`
- this `docs/Human-Plans/Architecture/Workspace-Modes/Workspaces/Catalog/Catalog-Gen0-Index.md`

#### No-Widening Rule

- do not collapse all family moves into one giant migration pass
- do not widen into preview-media reorganization or `Catalog-1` browse behavior
- do not reopen the earlier Browser-baseline retirement work except where migration follow-through needs one tiny boundary reminder
- do not let the final cleanup phase silently absorb unrelated Catalog runtime redesign

#### Implementation Risks

- leaving `Phase 5` too vague and ending `Gen0` with a half-migrated dual-home asset layout
- letting one family move quietly reintroduce different naming or path conventions than the others
- finishing the per-family copies but forgetting the last shared runtime consumers that still read `ReferenceModels/...`
- declaring the migration done before the legacy family folders are truly retired

#### Checklist

- [x] the target Catalog-owned family-folder contract is explicit enough that every later move targets the same destination layout
- [x] `shoes`, `hooks`, and `footpads` all finish the move out of `ReferenceModels/...`
- [x] the remaining shared consumers no longer assume `ReferenceModels/...` paths
- [x] the old `public/ReferenceModels/...` family homes are retired cleanly enough that `Gen0` can honestly claim one Catalog-owned repo asset home

#### Verification Shape

Minimum verification for this phase should cover:

- the phase names the migration end-state clearly enough that `Gen0` has one clean Catalog-owned repo asset home
- the sub-phase ladder is explicit enough that each family move can be dispatched as one narrow Codex-sized implementation slice
- the final retirement step is explicit enough that the repo does not stop at a dual-home compromise

#### Done Shape

`Phase 5` is done when:

- the repo has moved the first reference families into Catalog-owned asset homes
- the surviving manifest, seed, and runtime consumers no longer depend on `ReferenceModels/...`
- the old `ReferenceModels/...` family layout is retired cleanly enough that `Gen0` ends with one honest Catalog-owned repo asset home

`Phase 5` is now complete on that migration-and-retirement basis.

## [x] Catalog-Gen0 - Phase 6 - Generation 1 Start Boundary Cleanup

### Phase 6 Summary
#### Purpose

Close `Generation 0` honestly now that the earlier Browser-baseline retirement and Catalog-owned asset-home cleanup lanes are complete, and make the first real `Generation 1` start condition explicit before the next `Catalog-1` implementation doc widens the family.

#### Owns

- defining what should count as "the `Catalog` family has now started"
- locking the minimum `Catalog-1` entry checklist after the completed `Gen0` cleanup lanes
- tightening the doc boundary between completed `Gen0` cleanup, the first real `Generation 1` runtime cut, and later `Generation 2` widening
- routing the next standalone execution doc to the right family home instead of leaving the handoff implicit

#### Does Not Own

- implementing new Catalog runtime behavior
- adding or widening browse, preview, commit, or environment features
- reopening the already-complete Browser baseline removal or Catalog-owned asset-home migration
- pulling `Generation 2` external-source or compatibility widening into the first `Generation 1` start

#### Current Live Read

The strongest current handoff seams are now in the planning docs, not in one missing runtime cleanup:

- `docs/Human-Plans/Architecture/Workspace-Modes/Workspaces/Catalog/Catalog-Gen0-Index.md`
  - already shows the earlier cleanup lanes as complete:
    - `Phase 2.1 - Browser References Baseline Removal`
    - `Phase 5 - ReferenceModels To Catalog Migration Ladder`
  - still carries one final open handoff lane under this `Phase 6` section
  - therefore still owns the honest "is Gen0 actually done yet?" answer
- `docs/Human-Plans/Architecture/Workspace-Modes/Workspaces/Catalog/Catalog-Vision.md`
  - still states the family is in `Generation 0` today and that `Generation 1` is the first real Catalog baseline
  - is therefore the strongest north-star source for what must become true before the family can stop calling itself prep-only
- `docs/Human-Plans/Architecture/Workspace-Modes/Workspaces/Catalog/Catalog-Index.md`
  - already carries the more detailed `Generation 1` umbrella and the `Catalog-1` through `Catalog-5` ladder
  - therefore already answers what the first real family lane should be once the `Gen0` handoff is made explicit
- `docs/Human-Plans/Architecture/Workspace-Modes/Workspaces/Catalog/Future/Catalog_Phase Catalog-1 - Workspace Foundation And Catalog Contract.md`
  - already exists as the first real standalone `Catalog-1` execution doc
  - therefore the missing piece is not a lack of a `Generation 1` doc home, but a still-soft handoff rule between completed `Gen0` prep and that first true family execution lane

So the honest live read for `Phase 6` is:

- the cleanup work that justified `Generation 0` is now largely complete
- the docs already have a `Generation 1` umbrella and first standalone future doc
- what is still missing is one explicit close-out read that says:
  - why `Gen0` is done
  - what must stay out of the first `Catalog-1` start
  - which doc becomes the first true execution owner once the family starts

#### First Pass Decisions

- keep `Phase 6` as a docs-and-boundary cleanup pass, not a runtime feature phase
- treat the completed Browser-baseline retirement and Catalog-owned asset-home cleanup as the key reasons `Gen0` can now close
- prefer tightening the handoff in `Catalog-Gen0-Index.md` first rather than rewriting the broader `Catalog-Vision.md` or `Catalog-Index.md` unless a small wording correction is truly needed
- make the first real `Generation 1` start definition explicit enough that later work stops reopening whether a cleanup task still belongs to `Gen0`
- route the next executable family work to the existing `Catalog-1` future doc home instead of inventing a second competing start lane

### Phase 6 Implementation Spec
#### Exact First Code Cut

1. Rename the remaining open handoff lane consistently onto `Catalog-Gen0 - Phase 6 - Generation 1 Start Boundary Cleanup` wherever the older `Phase 00` wording still survives.
2. Add one explicit current-live-read section under `Phase 6` that names which earlier `Gen0` cleanup lanes are already complete and why the remaining work is now a handoff problem rather than another prep inventory.
3. Add one explicit `Catalog-1` entry checklist that states the minimum truths required for the family to stop reading as `Generation 0`.
4. Add one explicit boundary read that keeps `Generation 2` widening and any new cleanup drift out of the first `Generation 1` execution slice.
5. Add one explicit routing read that points the first true execution handoff at the existing `Catalog-1` standalone future doc.

#### Likely Files

- `docs/Human-Plans/Architecture/Workspace-Modes/Workspaces/Catalog/Catalog-Gen0-Index.md`
- `docs/Human-Plans/Architecture/Workspace-Modes/Workspaces/Catalog/Catalog-Index.md`
- `docs/Human-Plans/Architecture/Workspace-Modes/Workspaces/Catalog/Catalog-Vision.md`
- `docs/Human-Plans/Architecture/Workspace-Modes/Workspaces/Catalog/Future/Catalog_Phase Catalog-1 - Workspace Foundation And Catalog Contract.md`

#### No-Widening Rule

- do not turn `Phase 6` into new runtime Catalog work
- do not reopen the shipped `Phase 2.1` Browser-baseline removal or the shipped `Phase 5` asset-home migration unless a one-line boundary reminder is truly required
- do not widen into `Generation 2` source onboarding, compatibility metadata, or builder work
- do not create a second competing `Generation 1` start doc if the existing `Catalog-1` future doc can remain the first execution owner

#### Implementation Risks

- leaving the family in a vague "still Gen0, but also maybe started" state even after the cleanup lanes are done
- accidentally treating another cleanup wish as required before `Catalog-1` can begin
- letting `Generation 2` widening language bleed back into the first `Generation 1` runtime start
- routing the next execution cut to the wrong doc and creating two overlapping family starts

#### Checklist

- [x] the first real `Catalog` family start condition is explicit
- [x] the minimum `Catalog-1` entry checklist is explicit
- [x] the doc boundary between completed `Gen0`, the first `Generation 1` cut, and later `Generation 2` widening is explicit
- [x] the first standalone execution owner is routed explicitly enough that the family no longer has two competing starts

#### Verification Shape

Minimum verification for this phase should cover:

- the `Gen0` index now answers whether cleanup is complete enough for the family to start
- the handoff to `Catalog-1` is explicit enough that the next runtime phase does not need to rediscover where it begins
- the wording is narrow enough that `Phase 6` still reads as boundary cleanup rather than hidden implementation

#### Done Shape

`Phase 6` is done when:

- `Catalog-Gen0` can honestly say the cleanup band is complete enough for the first real family phase to begin
- the first `Catalog-1` execution owner is explicit
- the repo no longer has an ambiguous gap between "Gen0 prep" and "Catalog family has started"
- later widening work is clearly kept out of that first start

#### Implementation Read

This phase is now landed as a docs-and-boundary closeout:

- `Generation 0` now reads as complete instead of current
- the first real family start condition is now:
  - Browser reference-baseline retirement is complete
  - Catalog-owned repo asset-home cleanup is complete
  - the first true runtime work should therefore move to `Catalog-1`
- the minimum `Catalog-1` entry checklist is now:
  - treat `Catalog` as a real workspace-surface family start, not more prep cleanup
  - keep optional repo-backed families intentional instead of restoring Browser preload behavior
  - keep preview-versus-commit and downstream ownership honest by asset type
  - keep `Generation 2` external-source and compatibility widening out of the first baseline runtime cut
- the first standalone execution owner is now:
  - [Catalog_Phase Catalog-1 - Workspace Foundation And Catalog Contract.md](./Future/Catalog_Phase%20Catalog-1%20-%20Workspace%20Foundation%20And%20Catalog%20Contract.md)

`Catalog-Gen0` is now complete on that closeout-and-handoff basis.
