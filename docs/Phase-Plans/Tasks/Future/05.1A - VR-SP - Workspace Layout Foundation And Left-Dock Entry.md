# 05.1A - VR-SP - Workspace Layout Foundation And Left-Dock Entry

## Doc Header

### Doc History
1. 2026-03-17 12:46: Created this future task doc as the first dedicated execution-planning surface under the re-homed `[5.1] VR / SP - Workspace Modes` lane, locking the shared hybrid-workspace foundation around one app-level layout owner, one canonical tile-tree model, one required main viewer region, and the existing left-dock `[]` split button as the first tiled-entry seam

### Purpose

This file is the implementation-ready execution spec for:
- `[5.1A] VR / SP - Workspace Layout Foundation And Left-Dock Entry`

Use it to lock:
- the shared hybrid-workspace model
- the first workspace-layout state owner
- the canonical split-tree shape
- the first required main-viewer anchor
- the first left-dock-only tiled entry behavior

### Scope

This file covers:
- one shared `Hybrid Workspace` model
- `Windowed` versus `Tiled` as presentation styles
- the canonical workspace tile-tree structure
- active-pane ownership
- the first left-dock `[]` entry button behavior
- the first default left-dock-only tiled entry state
- first-pass non-goals for the workspace system

This file does not cover:
- deeper divider authoring behavior
- pane header menus
- clone-versus-move surface hosting policy
- saved workspace modes
- full migration off the old special-case split renderer

## Doc Body

### Summary

`5.1A` establishes the shared shell foundation for workspace modes.

This phase should deliver:
- one app-level `Hybrid Workspace` system
- one canonical pane tree for tiled layout
- one required `Model Viewer` anchor region
- one first tiled-entry control using the existing left-dock `[]` button
- one default entry state that only splits out the left dock while leaving the main model viewport intact

### Locked Outcome

At the end of `5.1A`, ParaHook should have a locked architecture target for workspace layout ownership:
- the workspace system is not feature-local
- the workspace system is not Spaghetti-only
- `Windowed` and `Tiled` are placement styles of one shared system
- the first tiled entry uses the already-shipped left-dock split affordance

Important rule:
- `5.1A` defines the shell model and its first entry seam
- it does not yet need full pane-authoring UX depth

### Current State And Source Inputs

Primary planning sources:
- `docs/Human-Plans/Architecture/Workspace-Modes.md`
- `docs/Human-Plans/roadmap/roadmap.md`
- `docs/Phase-Plans/Tasks/Future/02.1D - VR-SP - Spaghetti Floating Window Controls And View Modes.md`
- `docs/Phase-Plans/Tasks/Future/02.1E - VR-SP - Dockable Left Panels And In-App Floating Panel Shell.md`

Current code seams:
- `src/app/AppShell.tsx`
- `src/app/store/useAppStore.ts`
- `src/app/spaghetti/store/useSpaghettiStore.ts`

Current shipped proof:
- the app already has a narrow split proof through the Spaghetti editor shell
- the app already has a left-dock split toggle that moves the current dock stack into its own constrained region
- `5.1A` should generalize those ideas into one shared layout owner instead of adding more one-off shell branches

### Locked Model

The workspace system should be described as:
- one `Workspace Layout System`
- with two placement styles:
  - `Windowed`
  - `Tiled`

Important rule:
- these are not global app modes
- they are surface-placement styles inside one hybrid workspace

### Locked State Ownership

The first shared owner should live near `AppShell`, not in a feature store.

Recommended ownership:
- `AppShell`
- plus a dedicated workspace-layout state surface under:
  - `src/app/workspace/`

Avoid:
- putting the layout tree into `useSpaghettiStore`
- making workspace layout an accidental side effect of editor viewport state

### Locked Tile-Tree Shape

The canonical tiled-layout model is a tree.

Branch node owns:
- split direction
- split ratio
- split priority

Leaf node owns:
- hosted tool-surface instance identity
- pane-local metadata

This should be the minimum planned type family:
- `WorkspaceLayoutState`
- `WorkspacePaneNode`
- `WorkspaceBranchNode`
- `WorkspaceLeafNode`
- `WorkspaceSurfaceKind`
- `WorkspacePresentationMode`
- `ActiveWorkspacePaneId`

### Locked Viewer Anchor Rule

The first pass should require one main `Model Viewer` region.

Rules:
- the workspace must keep one main viewer alive
- the first split anchors against that viewer region
- the user should not be able to accidentally remove the only viewer in `5.1A`

Why:
- too much of the current app still assumes one honest 3D working area

### Locked Left-Dock Entry Rule

The existing `[]` left-dock split button becomes the first tiled-entry control.

Entry behavior:
- split out the current left dock
- keep the main model viewport intact
- treat that state as the clean first visible proof of the shared workspace system

Important rule:
- do not invent a second new entry control before this one is reused

### Active Pane Rule

The shared workspace should define one active tiled pane.

Active pane should own:
- keyboard-targeted layout actions
- focus-sensitive workspace actions
- contextual pane/surface commands

Important rule:
- active-pane ownership belongs to the shell, not any one feature surface

### Important Interfaces And Types To Lock

- `WorkspaceLayoutState`
  - root layout node
  - active pane id
  - required main viewer pane id
- `WorkspacePaneNode`
  - discriminated union for branch versus leaf
- `WorkspaceBranchNode`
  - `direction`
  - `ratio`
  - `priority`
  - `firstChild`
  - `secondChild`
- `WorkspaceLeafNode`
  - `surfaceInstanceId`
  - pane-local metadata
- `WorkspaceSurfaceKind`
  - first-pass kinds should cover:
    - `modelViewer`
    - `browser`
    - `console`
    - `gizmoView`
    - `spaghettiEditor`
- `WorkspacePresentationMode`
  - `windowed`
  - `tiled`
- `ActiveWorkspacePaneId`
  - shell-owned id for the current active tiled pane

### Acceptance And Done Shape

`5.1A` is done when:
- the workspace architecture uses one shared hybrid-layout model
- one canonical tile-tree shape is locked
- one app-level state seam near `AppShell` is chosen
- one required main viewer anchor rule is explicit
- the existing left-dock `[]` button is explicitly owned as the first tiled-entry control
- the default first tiled-entry state is locked as left-dock-only split
- the phase clearly says what it does not yet solve

### Likely Files

- `src/app/AppShell.tsx`
- `src/app/store/useAppStore.ts`
- `src/app/spaghetti/store/useSpaghettiStore.ts`
- `src/app/workspace/`
