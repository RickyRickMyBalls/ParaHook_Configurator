# 01.07 - GE - Phase 11B - Cached Graph Lifecycle

## Progress CheckList

### Active TaskList

- [ ] 1. Read the current `11B` roadmap and family-phase references
- [ ] 2. Confirm the live cached-graph seams in `useSpaghettiStore`, Browser state, and the new `11A` persistence surface
- [ ] 3. Lock the narrow `11B` cached-graph lifecycle target
- [ ] 4. Lock saved-graph versus cached-live-graph behavior
- [ ] 5. Lock Browser-owned cached graph entry rules
- [ ] 6. Lock reopen/focus behavior for cached graph entries
- [ ] 7. Lock identity and editor-binding guardrails between `11B` and `11C`
- [ ] 8. Tighten acceptance criteria and likely file surface
- [ ] 9. Finish the `11B` task doc as an implementation-ready execution spec

### Progress Rules

- [ ] keep `11B` focused on cached graph lifecycle, not full editor save/load UX
- [ ] keep Browser-owned cached entries separate from raw saved-file IO
- [ ] build on the shipped `11A` persistence seam instead of bypassing it
- [ ] keep graph runtime state separate from durable file state
- [ ] keep `Open Graph` / `Load Into New Graph` / `Swap Current Editor` policy out of this phase
- [ ] keep project-file ownership out of this phase
- [ ] prefer one honest cached-entry lifecycle before deeper multi-document behaviors

### Done Check

- [ ] `11B` implementation complete

## Doc Header

### Doc History
1. 2026-03-09 22:32: Created this file as the first-pass implementation-planning doc for `[1.2B] GE - Phase 11B - Cached Graph Lifecycle`, carrying the roadmap direction forward from the shipped `11A` persistence core so cached Browser graph entries now have their own task surface before code work starts

### Purpose

This file is the execution spec for:
- `[1.2B] GE - Phase 11B - Cached Graph Lifecycle`

Use it to:
- define how saved graph documents become Browser-owned cached graph entries
- lock cached graph versus live graph behavior
- define reopen/focus behavior for cached graph entries
- keep Browser lifecycle separate from raw save/load file IO

This file is not the implementation plan for:
- `[1.2A] GE - Phase 11A - Graph Document Persistence Core`
- `[1.2C] GE - Phase 11C - Save/Load Interaction With Editors`
- `GE 12`

Those are separate cuts.

### Scope

This file covers:
- cached Browser graph entries
- saved graph versus cached live graph behavior
- reopen/focus behavior for cached graphs
- cached graph identity rules strong enough for later editor coordination

This file does not implement:
- raw graph-document file format work
- editor action policy like `Open In New Editor` or `Swap Current Editor`
- import/merge behavior
- project-file ownership
- final Browser save/load polish

Working scope rule:
- `11B` is the Browser-owned cached-graph lifecycle layer
- it should sit on top of `11A` without absorbing `11C` or `GE 12`

### Refactor Rule For This Phase

This app is not a live production system with active outside users.

Working rule:
- temporary breakage during the refactor is acceptable
- the goal is to land the correct cached-graph lifecycle model
- speed matters, but cached graph identity and Browser ownership need to stay explicit

This phase should prefer:
- one honest cached graph entry model
- clear saved-versus-live graph rules
- Browser-owned lifecycle over ad hoc editor-local file handling
- explicit deferral of editor interaction policy to `11C`

### Source Inputs

This doc is based on:
- `docs/Human-Plans/roadmap/roadmap.md`
- `docs/Phase-Plans/00_Phase-Setup.md`
- `docs/Phase-Plans/01_GE - Phase-Plans.md`
- `docs/CHANGELOG.md`
- `docs/Phase-Plans/Tasks/Future/01.06 - GE - Phase 11A.md`
- `src/app/spaghetti/store/useSpaghettiStore.ts`
- `src/app/panels/BrowserPanel.tsx`
- `src/app/io/graphDocumentPersistence.ts`

## Arch Spec

### Spec Target

This implementation target is only:
- `[1.2B] GE - Phase 11B - Cached Graph Lifecycle`

This cut is intentionally narrow:
- define Browser-owned cached graph entries
- connect loaded graph documents into that cached-entry layer
- define reopen/focus behavior for cached graphs
- keep editor open/swap/import decisions out of the phase

If this cut is wrong, later work has no stable base for:
- `11C` editor save/load coordination
- `GE 12` project-level graph collection ownership
- Browser graph lifecycle beyond one purely in-memory session

### Problem Statement

After `11A`, the app now has:
- a durable `GraphDocument` file contract
- pure graph-document serialization/parsing helpers
- low-level browser file save/load services

But it still does not have:
- a Browser-owned cached graph entry model
- explicit saved graph versus cached live graph rules
- reopen/focus behavior for loaded graphs
- a lifecycle model for loaded graphs that survives beyond one immediate file-open action

Current runtime still behaves too much like:
- Browser and editor are looking straight at live graph documents
- saved files are only raw IO, not Browser-managed cached entries
- there is no explicit cached graph layer between file IO and editor interaction policy

`11B` needs to make cached graph lifecycle real before `11C` starts deciding how save/load actions affect editors.

### Locked Direction

- `11B` owns Browser-side cached graph lifecycle
- `11B` builds on the shipped `11A` persistence seam
- saved graph documents should become Browser-owned cached entries
- cached entries should be focusable/reopenable without deciding editor action policy
- graph identity and cached-entry identity should remain explicit

### First-Pass Cached Lifecycle Direction

Use this first-pass direction:

```ts
type CachedGraphEntry = {
  cachedGraphId: string
  graphDocument: GraphDocument
  source: 'file-load' | 'in-memory'
  isDirty: boolean
  lastSavedAt?: string
}
```

Rules:
- cached entry identity is not the same thing as viewport identity
- `graphDocument.graphDocumentId` remains the graph identity inside the cached entry
- cached entries are Browser-owned lifecycle objects
- runtime buckets and viewport state are not persisted into the cached entry itself
- `11B` may keep the first cached-entry shape simple if the same ownership rules remain explicit

### Locked Saved Versus Cached-Live Rules

- a saved graph file is durable file data
- a cached graph entry is Browser-owned live memory derived from that saved graph data
- cached entries may become dirty after editing
- dirty/live lifecycle rules belong here in `11B`
- how a cached entry is opened into a specific editor viewport belongs later in `11C`

### Locked Browser Ownership Rules

- Browser owns the list of cached graph entries
- Browser owns reopen/focus behavior for those cached entries
- Browser can show cached graphs even before final editor interaction policy is implemented
- editor viewports should not become the long-term owner of loaded graph lifecycle

### Current Seam Findings

#### `src/app/io/graphDocumentPersistence.ts`

Current seam:
- now provides low-level `GraphDocument` save/load services

Implication for `11B`:
- cached lifecycle should build on this service instead of recreating file IO behavior

#### `src/app/spaghetti/store/useSpaghettiStore.ts`

Current seam:
- already owns live graph documents
- already owns Browser/open-focus viewport state

Implication for `11B`:
- this is the likely first home for cached graph entries unless a clearer Browser-local store is introduced
- cached lifecycle must stay separate from graph runtime buckets and viewport state

#### `src/app/panels/BrowserPanel.tsx`

Current seam:
- already exists as the first real Browser shell surface

Implication for `11B`:
- Browser can become the visible owner of cached graph entries without forcing final editor UX decisions

### Deferred Out Of `11B`

- `Open In New Editor`
- `Swap Current Editor`
- `Load Into New Graph`
- import/merge rules
- project-file ownership
- final multi-document project containment

That keeps `11B` from collapsing into:
- `11C`
- `GE 12`

### Likely Files

Primary:
- `src/app/spaghetti/store/useSpaghettiStore.ts`
- `src/app/panels/BrowserPanel.tsx`
- `src/app/io/graphDocumentPersistence.ts`

Possible support files:
- cached graph lifecycle tests
- Browser selector helpers
- `src/app/store/useAppStore.ts` only if a temporary shell bridge is genuinely needed

## Master CheckList For 11B

### Cached Lifecycle Core

- [ ] define saved graph versus cached live graph behavior
- [ ] define the first cached graph entry contract
- [ ] load saved graph documents into Browser-owned cached entries
- [ ] support reopen/focus behavior for cached graphs
- [ ] keep cached graph identity stable enough for later editor switching
- [ ] keep cached entries separate from viewport/editor identity

### Guardrails

- [ ] raw file IO remains in the `11A` persistence layer
- [ ] editor save/load interaction policy is not pulled into this phase
- [ ] project-file ownership is not pulled into this phase
- [ ] viewport/browser geometry is not added to cached graph data
- [ ] runtime graph buckets are not treated as saved/cached graph truth

## Plan.md For 11B

### Recommended Refactor Order

1. confirm the saved graph versus cached live graph distinction
2. add the first cached graph entry model
3. wire file-loaded graph documents into cached Browser entries
4. define reopen/focus behavior for cached entries
5. verify that cached lifecycle stays separate from editor action policy

### Verification

- [ ] one saved graph can become one cached Browser graph entry
- [ ] cached graph identity stays stable enough for later editor binding
- [ ] Browser can reopen/focus cached graph entries
- [ ] cached graph state stays separate from viewport/editor identity
- [ ] raw file IO remains delegated to `11A`
- [ ] editor open/swap/import policy remains deferred to `11C`

### Next After `11B`

- `[1.2C] GE - Phase 11C - Save/Load Interaction With Editors`
  - `Open In New Editor`
  - `Swap Current Editor`
  - `Load Into New Graph`
  - editor coordination rules
- `GE 12`
  - project-file graph collection ownership
  - project-level cached graph lifecycle

# Close Out notes

Use this section after implementation to record:
- whether the cached graph entry model stayed separate from editor identity
- what saved-versus-live graph behavior turned out to matter most
- what still clearly belongs in `11C`
- whether Browser ownership of cached entries feels stable enough to build on later
