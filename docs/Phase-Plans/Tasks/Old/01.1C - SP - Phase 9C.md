# 01.1C - SP - Phase 9C - Graph-Local Compile / Preview Preparation

## Progress CheckList

### Active TaskList

- [x] 1. Read the current `9C` roadmap, decisions, changelog, and Codex notes inputs
- [x] 2. Confirm the live global compile/build and preview-prep seams in `useAppStore`, `bootstrapBuildWiring`, `ViewerHost`, and `useSpaghettiStore`
- [x] 3. Lock the graph-local compile/build memory bundle for `9C`
- [x] 4. Lock the graph-local preview-preparation bundle for `9C`
- [x] 5. Lock the shared worker and shared viewer-shell rule
- [x] 6. Lock the bridge boundary between `9C`, `SP 10`, and later `AS` work
- [x] 7. Tighten the implementation sequence and acceptance criteria
- [x] 8. Finish the `9C` task doc as an implementation-ready execution spec

### Progress Rules

- [x] keep `9C` as a bridge phase only
- [x] move compile/build memory graph-local before full routing redesign
- [x] move preview-prep memory graph-local before Browser-facing output hierarchy work
- [x] keep one shared worker lifecycle
- [x] keep one shared viewer shell
- [x] write down every softened global seam before reconnecting it through graph identity

### Done Check

- [x] `9C` implementation complete

## Doc Header

### Doc History
3. 2026-03-10 00:00: Modernized this completed `9C` task doc into the newer old-task reference format by correcting the `01.1C` numbering, cleaning stale source references, tightening the shipped-scope language, and adding a clearer implemented-end-state summary while preserving the original implementation close-out
2. 2026-03-09 21:43: Implemented the `9C` bridge by moving spaghetti compile/build memory into graph-local runtime buckets in `useSpaghettiStore`, moving preview-preparation memory into a graph-local preparation bundle, reconnecting `useAppStore`, `bootstrapBuildWiring`, `ViewerHost`, `SpaghettiPanel`, and `DebugInspectorDrawer` to that graph-keyed runtime, and verifying the cut with targeted tests plus a full production build
1. 2026-03-09 21:12: Created this file as the implementation-planning doc for `[1.1C] SP - Phase 9C - Graph-Local Compile / Preview Preparation`, grounding it in the locked roadmap, decisions, Codex notes Q1-Q5 resolutions, and the current `useAppStore` / build-wiring / viewer seams so the bridge phase could be implemented without re-deciding its ownership boundaries

### Purpose

This file is the execution spec and historical reference for:
- `[1.1C] SP - Phase 9C - Graph-Local Compile / Preview Preparation`

Use it to:
- record the shipped graph-local compile/build runtime split
- record the shipped graph-local preview-preparation split
- preserve the exact bridge boundary between `9C`, `SP 10`, and later output/Browser phases
- keep a clean reference for later routing and Browser-facing output work

This file is not the implementation plan for:
- `9A.1`
- `9A.2`
- `9A.3`
- `[1.1B] SP - Phase 9B - Multi-Editor Browser Foundation`
- `[1.3] SP - Phase 10 - Graph Aware Worker And Preview Routing`
- `[1.5] SP - Phase 11 - Graphs Panel And Nested Parts`
- `[1.2] GE - Phase 11 - Graph Persistence And Save Load`
- `[1.4] GE - Phase 12 - Multi-Document Graph Ownership`

Those are separate cuts.

### Scope

This file covers the shipped `9C` cut:
- graph-local compile/build memory bundles
- graph-local preview-preparation bundles
- graph-keyed state handoff through shared app services
- the first softening of the old spaghetti-global compile/build path

This file does not cover:
- full dispatcher/worker routing redesign
- full Browser-facing output hierarchy
- final `Component / Assembly / Object / Part` output structure
- Browser UI work
- project ownership work
- build bars / build-control UX
- rich viewer reference/material systems

Working scope rule:
- `9C` is a bridge phase
- it made graph-local compile/build/preview-prep runtime real without trying to finish full routing or final output structure

### Refactor Rule For This Phase

This app is not a live production system with active outside users.

Working rule:
- temporary breakage during the refactor was acceptable
- the goal was to land the correct `9C` end-state
- speed mattered, but every softened global seam still needed a clear graph-keyed reconnect target

This phase preferred:
- graph-keyed runtime buckets
- explicit bridge seams into `SP 10`
- fewer compatibility layers around the old one-global spaghetti compile/build path

### Source Inputs

This doc is based on:
- `docs/CHANGELOG.md`
- `docs/Human-Plans/roadmap/roadmap.md`
- `docs/Human-Plans/Decisions.MD`
- `docs/Human-Plans/CodexNotes/6_CodexChatNotes.md`
- `docs/Human-Plans/CodexNotes/7_CodexChatNotes.md`
- `docs/Phase-Plans/Tasks/Old/01.1B - SP - Phase 9B.md`
- `src/app/store/useAppStore.ts`
- `src/app/bootstrapBuildWiring.ts`
- `src/app/components/ViewerHost.tsx`
- `src/app/spaghetti/store/useSpaghettiStore.ts`

## Arch Spec

### Spec Target

This implementation target was only:
- `[1.1C] SP - Phase 9C - Graph-Local Compile / Preview Preparation`

This cut intentionally sat between the graph-document/browser foundation and the later routing/output phases:
- each graph should keep its own compile/build memory
- each graph should keep its own preview-preparation memory
- shared worker/viewer infrastructure could stay shared
- the app needed to stop collapsing compile/build/preview state into one spaghetti-global bucket

If this cut had been wrong, later work would have had no stable base for:
- `SP 10` graph-aware worker/result routing
- later `AS` output structure work
- Browser-aware graph output ownership

### Implemented End State

What landed in `9C`:
- graph-local compile/build runtime moved into `useSpaghettiStore`
- graph-local preview-preparation moved into graph-keyed runtime state
- shared worker lifecycle remained shared
- shared viewer shell remained shared
- `useAppStore` stayed a bridge instead of the long-term owner of spaghetti runtime truth

Practical outcome:
- the app stopped treating spaghetti compile/build and preview-prep memory as one global bucket
- later routing and Browser-facing structure work gained a real graph-keyed base to build on

### Carry-Forward From `9A` And `9B`

Locked carry-forward rules from `9A` and `9B`:

- `useSpaghettiStore` remained the right home for graph + viewport runtime state
- `useAppStore` stayed shell/bridge-oriented and stopped being the long-term home for spaghetti-global compile/build buckets
- graph-owned truth stayed graph-owned
- viewport/browser-owned state stayed out of graph-owned compile/build truth
- narrow temporary bridges were acceptable if they stayed explicit and small

Working read:
- `9C` built on graph documents and Browser/open-focus structure
- this phase expanded graph-local runtime memory, not Browser UI or output hierarchy

### Problem Statement

After `9B`, the app had:
- graph documents
- Browser/open-focus viewport coordination
- explicit per-viewport graph binding

But it still did not have:
- graph-local compile/build memory
- graph-local preview-preparation memory
- graph-keyed bridge buckets through shared worker/viewer services

Current runtime still behaved too much like:
- one global spaghetti compile/build bucket in `useAppStore`
- one preview-prep path in `ViewerHost`
- one build-wiring branch keyed only by `inputMode === 'spaghetti'`

`9C` needed to make graph-local build/preview memory real without pulling in full routing or Browser-facing output hierarchy.

### Locked Direction

- `9C` is a bridge phase
- each graph owns its compile/build memory bundle
- each graph owns its preview-preparation bundle
- one worker lifecycle can stay shared
- one viewer shell can stay shared
- requests/results/preview-prep must be ready to route by `graphDocumentId`

### Locked Graph-Local Compile / Build Memory Bundle

Use this first-pass graph-local compile/build bundle:

- `lastCompileResult`
- `previousBuildInputs`
- `pendingChangedParamIds`
- `pendingStatsPartKeys`
- `pendingInstances`
- optional `lastBuildSeq`

Rules:
- this bundle is graph-local and graph-keyed
- it no longer lives as one spaghetti-global bucket in `useAppStore`
- it is ready to hand off into later graph-aware request/result routing

### Locked Graph-Local Preview-Preparation Bundle

Use this first-pass graph-local preview-prep bundle:

- graph-local output declaration state
- graph-local preview candidate list
- graph-local source/build part identity mapping
- graph-local build-stats-ready part keys
- graph-local "what this graph is trying to preview" memory

Rules:
- each graph prepares its own previewable outputs locally
- richer Browser-facing output hierarchy comes later
- this phase does not finish final output structure

### Locked Infrastructure Rule

- one shared worker lifecycle can stay shared
- one shared viewer shell can stay shared
- compile/build/preview-prep state still becomes graph-local and graph-keyed

Working rule:
- shared services do not need to fragment into one worker per graph or one viewer per graph
- the state they process should still be graph-scoped and ready to route by `graphDocumentId`

### Current Seams That `9C` Broke

1. `useAppStore` owned one spaghetti-global compile/build bucket
2. `compileSpaghetti()` and `requestSpaghettiBuild()` still acted like one-global-graph bridge actions
3. `bootstrapBuildWiring.ts` still read one global spaghetti pending-state path
4. `ViewerHost.tsx` still used one active-graph preview-prep path
5. preview/build-prep state was not yet stored per graph document

### Current Seam Findings

#### `useAppStore.ts`

Pre-`9C` seam:
- still owned:
  - `spaghettiLastCompile`
  - `spaghettiPreviousBuildInputs`
  - `spaghettiPendingChangedParamIds`
  - `spaghettiPendingStatsPartKeys`
  - `spaghettiPendingInstances`
- `compileSpaghetti()` and `requestSpaghettiBuild()` still implicitly operated on one active graph

`9C` consequence:
- spaghetti compile/build memory moved toward graph-keyed ownership
- `useAppStore` stayed a bridge/shell store instead of the long-term owner of spaghetti runtime buckets

#### `bootstrapBuildWiring.ts`

Pre-`9C` seam:
- still branched off one spaghetti pending-state path based on `inputMode === 'spaghetti'`

`9C` consequence:
- this softened from one spaghetti-global path into graph-keyed providers
- full dispatcher/worker routing still remained deferred

#### `ViewerHost.tsx`

Pre-`9C` seam:
- still prepared preview through one active-graph path
- one viewer shell was correct, but preview-prep memory was not yet graph-local

`9C` consequence:
- one viewer shell stayed intact
- preview-preparation moved toward graph-local ownership before later output structure work

#### `useSpaghettiStore.ts`

Pre-`9C` seam:
- already owned graph documents and viewport binding
- did not yet own the graph-local compile/build/preview-prep bundles that should live alongside graph identity

`9C` consequence:
- graph-keyed compile/build/preview-prep state moved here as the correct runtime home

### Deferred Out Of `9C`

- full dispatcher/worker routing redesign
- full Browser-facing output hierarchy
- final `Component / Assembly / Object / Part` output structure
- Browser UI work
- project ownership work
- build bars / build-control UX
- rich viewer reference/material systems

That kept `9C` from collapsing into:
- `[1.3] SP - Phase 10`
- later `AS`
- `[1.2] GE - Phase 11`
- `[1.4] GE - Phase 12`
- later Browser/workspace lanes

### Implementation Sequence

1. Introduce graph-keyed compile/build memory buckets.
2. Introduce graph-keyed preview-preparation buckets.
3. Move current spaghetti-global compile/build memory out of `useAppStore`.
4. Soften `compileSpaghetti()` and `requestSpaghettiBuild()` into graph-keyed bridge actions while keeping shared services shared.
5. Soften `bootstrapBuildWiring.ts` to read graph-keyed pending state.
6. Soften `ViewerHost.tsx` to consume graph-local preview-prep memory through the active graph/viewport bridge.
7. Validate that graph-local memory is real while full routing and output hierarchy are still deferred.

### Disconnect / Reconnect Tracking

#### Disconnects Allowed In This Phase

- one global spaghetti compile/build bucket
- one global preview-preparation bucket
- one active-graph-only assumption for all spaghetti build memory

#### Required Reconnect Targets

- reconnect graph-local build memory through graph identity
- reconnect preview-prep through graph-local state
- leave a clean follow-on seam for:
  - `SP 10` full request/result routing
  - later `AS` Browser-facing output hierarchy

### Likely Files

Primary:
- `src/app/store/useAppStore.ts`
- `src/app/bootstrapBuildWiring.ts`
- `src/app/components/ViewerHost.tsx`
- `src/app/spaghetti/store/useSpaghettiStore.ts`

Possible tests:
- graph-keyed compile/build store tests
- graph-keyed preview-prep tests
- bridge/wiring tests for active graph pending-state lookup

## Master CheckList For 9C

### Core Contract

- [x] compile/build memory is graph-local
- [x] preview-preparation memory is graph-local
- [x] one shared worker lifecycle remains
- [x] one shared viewer shell remains
- [x] graph-local state is ready to hand off into `SP 10`

### Runtime Cut

- [x] `useAppStore` no longer owns one spaghetti-global compile/build bucket
- [x] graph-keyed compile/build bridge actions exist
- [x] `bootstrapBuildWiring.ts` reads graph-keyed pending state
- [x] `ViewerHost.tsx` no longer depends on one spaghetti-global preview-prep bucket
- [x] preview/build-prep state is graph-keyed without finishing full routing

### Guardrails

- [x] full dispatcher/worker routing redesign is not pulled into this phase
- [x] Browser-facing output hierarchy is not pulled into this phase
- [x] Browser UI work is not pulled into this phase
- [x] project/file ownership is not pulled into this phase
- [x] build bars / build-control UX are not pulled into this phase

## Plan.md For 9C

### Recommended Refactor Order

1. lock the graph-local compile/build bundle
2. lock the graph-local preview-prep bundle
3. move spaghetti-global runtime buckets out of `useAppStore`
4. reconnect shared bridge actions through graph identity
5. reconnect build wiring through graph-keyed pending state
6. reconnect viewer preview-prep through graph-local state
7. validate that only bridge memory moved

### Verification

- [x] each graph keeps its own compile/build memory bundle
- [x] each graph keeps its own preview-prep bundle
- [x] shared worker lifecycle still works
- [x] shared viewer shell still works
- [x] `useAppStore` no longer behaves like the one global spaghetti build bucket
- [x] `bootstrapBuildWiring.ts` reads graph-keyed pending state
- [x] `ViewerHost.tsx` consumes graph-local preview-prep state
- [x] full routing is still deferred to `SP 10`
- [x] Browser-facing output hierarchy is still deferred to later `AS`

### Next After `9C`

- `SP 10`:
  - full graph-aware worker and preview routing
- later `AS`:
  - Browser-facing output hierarchy
  - final `Component / Assembly / Object / Part` output structure

# Close Out notes

What landed cleanly:
- `useSpaghettiStore` is now the clear runtime home for graph-local compile/build and preview-preparation memory.
- `useAppStore` stayed in the bridge role, which was the right constraint for `9C`.
- shared worker lifecycle and shared viewer shell did not need to split apart to make graph-local runtime real.

What was actually harder:
- the hardest seam was not compile itself, it was the request/result bridge around shared worker build sequencing.
- the light `buildSeq -> graphDocumentId` tracking bridge was enough for `9C` without dragging full `SP 10` routing forward.
- preview-preparation was easier once it was treated as a reusable preparation bundle instead of one-off viewer logic.

Carry-forward notes:
- `SP 10` should own full request/result routing and remove the remaining active-graph bridge assumptions around worker results.
- later `AS` work should build on the graph-local preview-preparation bundle instead of recomputing Browser-facing output hierarchy from scratch.
- if future graph mutations start making compile/build buckets feel too stale between edits, that should be handled as a later policy refinement, not retrofitted back into `9C`.
