# Chill Log

## Doc Header

### Doc History
1. 2026-03-08 00:00: Restored the local `Doc History` block in this file for header/rule/structure changes only, while ordinary chill-mode activity remains recorded in the active chill batch

### Purpose

This file is the active chill-mode log used during chill mode.

While chill mode is active:
- small rapid edits should be logged here
- permanent `docs/CHANGELOG.md` entries should wait until the user says `Update changelog`
- routine small chill-mode final responses do not need a `Diff:` line unless the user asks for one or the batch is being closed

### Canonical Note
- this is now the canonical chill-mode file named by `AGENTS.md`
- the canonical chill-mode path is `docs/Chill-Log.md`
- older chill-mode files may still exist for history/reference
- do not delete or archive the older files unless the user explicitly asks

### Batch Rules

- Each chill-mode session should start a new section:
  - `Chill Batch #1`
  - `Chill Batch #2`
  - etc.
- There should be only one active chill batch at a time.
- Each individual edit in chill mode should be logged under the active batch.
- Batch entries should use numbered one-line items in this format:
  - `1. 2026-03-06 20:54 - Entry text`
- Batch entries should be kept newest-first in source, with the newest entry at the top of the active batch.
- When explicit numbering is used in source, the highest number should be at the top and the lowest number at the bottom.
- Each batch entry should include its own `YYYY-MM-DD HH:MM` timestamp so rapid edits can still be reconstructed in order later.
- When the user says `Update changelog`, the active batch can be consolidated into one permanent `docs/CHANGELOG.md` entry.

## Doc Body

<!-- ============================================================ -->
### Chill Batch #8
<!-- ============================================================ -->

Status:
- active

Started:
- 2026-03-19 00:15

Ended:
- active

Mode:
- chill mode active

Consolidated To:
- pending

Current Goal:
- track the current sketch-plane viewport toolbar and transform-control polish pass without creating permanent `docs/CHANGELOG.md` entries for each small UI adjustment

Entries:
84. 2026-03-19 12:59 - Changed the local `Draw Sketch` `PLine` console trigger from `pline` to `pl` in `src/app/console/ConsoleDock.tsx`, including the echoed command text and the in-session help line so the temporary draw-session command surface now reads `line, pl, enter, esc, x, status, help`; focused console/viewer/store tests plus `tsc --noEmit` all pass.
83. 2026-03-19 12:52 - Updated `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md` to mark `3.2B-DrawSketch-1-Cleanup` complete and replace the placeholder cleanup spec with the actual shipped cleanup summary: honest `Line` / `PLine` ghost previews, stable draft-to-commit handoff, marker-drift fixes, sketch-grid readability, the expanded `Sketch Draw` `i Menu` controls, improved start-point defaults, and the new historical `PLine` point-marker controls, while explicitly preserving the cleanup boundaries so `DrawSketch-2` and `DrawSketch-3` remain the next deeper phases.
82. 2026-03-19 12:39 - Reduced the default `PLine` point-symbol size from `0.1` to `0.05` in `src/app/store/uiPrefsStore.ts` so historical polyline points load smaller by default, and updated the matching overlay expectation in `src/app/components/ViewerHost.test.tsx`; focused overlay/viewer/store tests plus `tsc --noEmit` all pass.
81. 2026-03-19 12:38 - Added first-pass `PLine` point-symbol controls and rendering for `Sketch Draw`: `src/app/store/uiPrefsStore.ts` now stores `sketchDrawPlinePointVisible`, `sketchDrawPlinePointSymbolSize`, and `sketchDrawPlinePointSymbolType`, `src/app/viewerBridge.ts` and `src/app/components/ViewerHost.tsx` now pass those prefs into the geometry-sketch overlay VM, `src/app/components/ViewportOverlay.tsx` now exposes `PLine Point Symbols`, `PLine Point Symbol`, and `PLine Point Size` in `Sketch Draw Settings`, and `src/viewer/sketch/GeometrySketchDrawHelper.ts` now renders historical polyline points in a separate muted marker layer while the active last point keeps the existing active marker color. Focused overlay/viewer/store tests plus `tsc --noEmit` all pass.
80. 2026-03-19 12:32 - Reduced the default `Sketch Draw` start-point symbol size from `1` to `0.1` in `src/app/store/uiPrefsStore.ts` so the new default circle marker loads much smaller, and updated the matching overlay expectation in `src/app/components/ViewerHost.test.tsx`; focused overlay/viewer/store tests plus `tsc --noEmit` all pass.
79. 2026-03-19 12:30 - Tightened the `Sketch Draw` start-point defaults: `src/app/store/uiPrefsStore.ts` now clamps `sketchDrawStartPointSymbolSize` down to `0.01` and defaults `sketchDrawStartPointSymbolType` to `circle`, `src/app/components/ViewportOverlay.tsx` now exposes the same `0.01` minimum in the `Start Point Symbol Size` slider, and `src/app/components/ViewerHost.test.tsx` was updated to expect the new default symbol type. Focused overlay/viewer/store tests plus `tsc --noEmit` all pass.
78. 2026-03-19 12:28 - Fixed the remaining `Draw Sketch` start/end crosshair drift by moving the live cursor marker, snap marker, and start-point marker into the sketch-plane local frame in `src/viewer/sketch/GeometrySketchDrawHelper.ts` instead of positioning those symbols as plain world-space XY markers; the sketch line itself was already rendered in the rotated sketch-plane frame, so the old marker path could visually drift against the line on nontrivial plane orientations, and now the markers inherit the same plane orientation/elevation as the drawing surface. Focused viewer/overlay/store tests plus `tsc --noEmit` all pass.
77. 2026-03-19 12:25 - Removed the remaining `Draw Sketch` preview jump on second click by aligning the draft chain/ghost line elevations with the committed sketch component elevation in `src/viewer/geometrySketchOverlay.ts`; the in-progress preview and the final committed line now render on the same projected plane instead of shifting slightly during the commit handoff, and `src/viewer/geometrySketchOverlay.test.ts` was updated to match the new draft elevation. Focused viewer/overlay/store tests plus `tsc --noEmit` all pass.
76. 2026-03-19 12:23 - Fixed the slight `Draw Sketch` ghost-preview drift at the start point by aligning the start-point marker depth with the ghost segment depth in `src/viewer/sketch/GeometrySketchDrawHelper.ts`; the marker and ghost line were previously rendered at slightly different elevations above the sketch plane, which let perspective introduce a small parallax offset, and now both use the same draft-point/ghost elevation so the preview line sticks to the center of the start-point symbol. Focused viewer/overlay/store tests plus `tsc --noEmit` all pass.
75. 2026-03-19 12:04 - Tightened `Sketch Draw` start-point symbol sizing so it can get meaningfully smaller and so the circle variant reads smaller by default: `src/app/store/uiPrefsStore.ts` now clamps `sketchDrawStartPointSymbolSize` down to `0.2` instead of `0.5`, `src/app/components/ViewportOverlay.tsx` now exposes the same lower minimum in the `Start Point Symbol Size` slider, and `src/viewer/sketch/GeometrySketchDrawHelper.ts` now renders the circle start-point symbol at a reduced scale relative to the same slider value so switching to `Circle` immediately reads smaller than `Crosshair`; focused viewer/overlay/store tests plus `tsc --noEmit` all pass.
74. 2026-03-19 12:01 - Expanded the `Sketch Draw Settings` subsection in the sketch draw `i Menu` with two more real start-point controls: `src/app/store/uiPrefsStore.ts` now stores `sketchDrawStartPointVisible` and `sketchDrawStartPointSymbolType`, `src/app/viewerBridge.ts` and `src/app/components/ViewerHost.tsx` now pass those settings through the geometry-sketch overlay VM, `src/viewer/sketch/GeometrySketchDrawHelper.ts` now supports both crosshair and circle start-point markers and respects the start-point on/off toggle, and `src/app/components/ViewportOverlay.tsx` now exposes `Start Point` and `Start Point Symbol` as `ParaSelect` rows under `Sketch Draw Settings`; focused viewer/overlay/store tests plus `tsc --noEmit` all pass.
73. 2026-03-19 11:57 - Expanded the `Sketch Draw` toolbar `i Menu` with a real `Sketch Draw Settings` subsection and wired the controls into live viewer prefs: `src/app/store/uiPrefsStore.ts` now stores draw-session snap enablement, snap distance, crosshair size, and start-point symbol size; `src/app/viewerBridge.ts` and `src/app/components/ViewerHost.tsx` now pass those values into the geometry-sketch overlay VM; `src/viewer/sketch/GeometrySketchDrawHelper.ts` now uses them to enable/disable origin snap, change the origin snap radius, and scale the live cursor/start-point symbols; and `src/app/components/ViewportOverlay.tsx` now adds the new `Sketch Draw Settings` subsection under `UI Customization` with rows for `Snap`, `Snap Distance`, `Crosshair Size`, and `Start Point Symbol Size` alongside the existing `Toolbar Window` controls. Focused overlay/viewer/store tests and `tsc --noEmit` all pass.
72. 2026-03-19 11:51 - Reverted the `Draw Sketch` live cursor/draft-point symbol back to the previous crosshair language in `src/viewer/sketch/GeometrySketchDrawHelper.ts` after the circle change proved too disruptive to the drafting read; `cmd /c npx tsc -p tsconfig.json --noEmit` and `cmd /c npx vitest run src/app/components/ViewerHost.test.tsx src/app/components/ViewportOverlay.test.tsx src/app/spaghetti/store/useSpaghettiStore.test.ts` both pass after the revert.
71. 2026-03-19 11:50 - Added first-pass visual snap feedback for `Draw Sketch` origin snapping: `src/viewer/sketch/GeometrySketchDrawHelper.ts` now renders a visible square snap indicator around the armed snap target when the cursor enters origin-snap range, and the live cursor/gizmo point marker now uses a circle symbol instead of another crosshair so the active cursor point and snap target read as different drafting roles; `cmd /c npx tsc -p tsconfig.json --noEmit` plus `cmd /c npx vitest run src/viewer/geometrySketchOverlay.test.ts src/app/components/ViewerHost.test.tsx src/app/components/ViewportOverlay.test.tsx src/app/spaghetti/store/useSpaghettiStore.test.ts` both pass.
70. 2026-03-19 11:28 - Moved `Draw Sketch` draft preview segments onto the same proven geometry overlay path used for committed sketch lines after the helper-owned ghost line still was not reading honestly in-app: `src/viewer/geometrySketchOverlay.ts` now emits `draftChain` and `draftGhost` polylines during draw mode, `src/viewer/Viewer.ts` now renders them with dedicated draft materials in the main geometry-sketch overlay group, and `src/viewer/sketch/GeometrySketchDrawHelper.ts` has been reduced back to the plane-local grid/origin/cursor-marker responsibilities instead of owning the line/polyline draft segment render path; `src/viewer/geometrySketchOverlay.test.ts` now verifies the draft chain and ghost segment generation, and `cmd /c npx tsc -p tsconfig.json --noEmit` plus `cmd /c npx vitest run src/viewer/geometrySketchOverlay.test.ts src/app/components/ViewerHost.test.tsx src/app/components/ViewportOverlay.test.tsx src/app/spaghetti/store/useSpaghettiStore.test.ts` both pass.
69. 2026-03-19 11:21 - Fixed the actual `Draw Sketch` draft-segment rendering path after the first cleanup attempt only made the start anchor more visible: `src/viewer/sketch/GeometrySketchDrawHelper.ts` now renders the temporary chain, ghost next segment, and draft point markers in world space using the same sketch-plane projection math as the committed sketch overlay, instead of relying on the transformed local helper path for the preview lines; this makes the in-progress line/polyline segment render directly from the locked start/last point to the live cursor projection while preserving the plane-local working grid/origin helper, and `cmd /c npx tsc -p tsconfig.json --noEmit` plus `cmd /c npx vitest run src/app/components/ViewerHost.test.tsx src/app/components/ViewportOverlay.test.tsx src/app/spaghetti/store/useSpaghettiStore.test.ts src/viewer/geometrySketchOverlay.test.ts` both pass after the fix.
68. 2026-03-19 11:17 - Tightened the live `Draw Sketch` line/polyline preview readability for the first cleanup pass: `src/viewer/sketch/GeometrySketchDrawHelper.ts` now lifts the draft chain, ghost segment, origin marker, and live point markers farther off the working grid, disables depth writing on those preview materials so they do not get visually buried, adds a visible draft anchor marker at the current start/last point, and explicitly toggles ghost visibility so the in-progress line/polyline preview reads immediately once drawing starts; `cmd /c npx tsc -p tsconfig.json --noEmit` and `cmd /c npx vitest run src/app/components/ViewerHost.test.tsx src/app/components/ViewportOverlay.test.tsx src/app/spaghetti/store/useSpaghettiStore.test.ts` both pass after the cleanup.
67. 2026-03-19 11:05 - Marked `## [x] - 3.2B-DrawSketch-1 - Viewer-Owned Live Draw Preview` complete in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md` and added a new follow-on `## [ ] - 3.2B-DrawSketch-1-Cleanup - Viewport Draw Workflow Cleanup` section directly underneath it so the first shipped viewer-owned draw pass stays recorded as done while the remaining polish work has its own narrow cleanup bucket for camera/grid/origin readability, ghost-preview clarity, toolbar/session cleanup, and other non-scope-expanding follow-up work.
66. 2026-03-19 11:05 - Implemented `3.2B-DrawSketch-1` as the first honest viewer-owned draw pass: `src/app/spaghetti/store/useSpaghettiStore.ts` now carries a real draw draft/session model for `Line` and `PLine`, `src/app/viewerBridge.ts` and `src/app/components/ViewerHost.tsx` now bridge draw draft/tool/plane state plus viewer callbacks, `src/viewer/sketch/sketchPlaneMath.ts` and `src/viewer/sketch/GeometrySketchDrawHelper.ts` now own sketch-plane projection, camera-aligned draw grid/origin, origin snap, cursor markers, ghost line/polyline previews, and point projection, `src/viewer/Viewer.ts` now drives camera alignment / grid handoff / hover-click-enter-escape routing during draw mode, `src/viewer/geometrySketchOverlay.ts` now renders committed sketch geometry against the active transformed sketch plane, `src/app/components/ViewportOverlay.tsx` now turns the `Draw Sketch` toolbar into a real session/status surface for `Line` and `PLine` instead of numeric form-entry, `src/app/console/ConsoleDock.tsx` now adds the first local draw-session commands (`line`, `pline`, `status`, `help`, `enter`, `esc`, `x`), and the focused viewer/store/overlay tests plus `cmd /c npx tsc -p tsconfig.json --noEmit`, `cmd /c npx vitest run src/viewer/geometrySketchOverlay.test.ts src/app/spaghetti/store/useSpaghettiStore.test.ts src/app/components/ViewerHost.test.tsx src/app/components/ViewportOverlay.test.tsx`, and `cmd /c npm run build` all pass.
65. 2026-03-19 10:32 - Replaced the thin placeholder under `3.2B-DrawSketch-1 > ### Implementation Spec` in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md` with a real buildable first-pass spec: it now defines the locked workflow from `Draw` click through camera alignment, sketch-plane grid/origin setup, `Line` / `PLine` viewer interaction, toolbar/session ownership, temporary local console commands, commit/state rules, implementation seams, acceptance checks, and explicit in-scope/out-of-scope boundaries so the next command can go straight into implementation planning or code.
64. 2026-03-19 10:27 - Tightened and locked the remaining first-cut `3.2B-DrawSketch-1` questions in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md`: `q5` now spells out the viewer-owned live preview contract for `Line` and `PLine` with projected cursor points, ghost segments, endpoint markers, and visible origin snap; `q6` through `q13` are now marked locked around first commit boundaries, temporary-chain `PLine` behavior, toolbar draft mirroring, first-pass hover/origin-snap scope, required temporary console commands, the dedicated viewer-side draw-preview helper seam, explicit out-of-scope boundaries, and always collapsing the active `Spaghetti Editor` when entering `Draw Sketch`.
63. 2026-03-19 10:25 - Locked `3.2B-DrawSketch-1` question `q4` in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md`: the first draw cut is now explicitly click-first and should feel like drawing in a 2D drafting plane after the camera aligns to the sketch plane, with the active `Line` tool placing a first point on the plane, showing a ghost preview line from that first point to the live cursor projection, keeping the line snapped to the chosen plane, and using left click to confirm the second point and complete the line while deferring true typed numeric-entry workflow.
62. 2026-03-19 10:22 - Locked `3.2B-DrawSketch-1` question `q3` in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md`: entering `Draw Sketch` now explicitly aligns the camera to the active sketch-plane normal/opposite, moves the working grid onto the chosen sketch plane, keeps the origin point visible at the center of that grid, projects clicks directly onto that plane through one canonical viewer-side helper, and requires the drawing cursor to be able to snap to the visible origin for first or later endpoints.
61. 2026-03-19 10:19 - Locked `3.2B-DrawSketch-1` questions `q1` and `q2` in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md`: the first honest viewer-owned draw cut is now explicitly limited to `Line` and `PLine`, and the first sketch-draw console commands are now explicitly treated as local session commands (`line`, `pline`, `status`, `help`) rather than a second permanent workflow.
60. 2026-03-19 10:16 - Expanded `## [ ] - 3.2B-DrawSketch-1 - Viewer-Owned Live Draw Preview` in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md` into a real implementation-ready decision surface by adding the missing first-cut questions and suggestions for plane projection, click-vs-typed entry, live preview content, first commit boundary, `PLine` temporary-chain behavior, toolbar draft mirroring, first hover/snap scope, temporary console commands, the viewer-side draw-preview helper seam, explicit out-of-scope boundaries, and the expected collapsed-editor behavior while drawing.
59. 2026-03-19 10:14 - Standardized the `# Sub-Phases` section in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md` so each `##` sketch phase now uses the same scaffold: `### Header`, `### Questions / Decisions`, `#### [ ] - qN`, `##### Suggestion`, and `### Implementation Spec`; the existing phase content was kept and reorganized under those buckets rather than removed, and the newer `3.2B-DrawSketch-*` entries now have the same structure as the sketch-plane and browser/expose phases.
58. 2026-03-19 10:05 - Cleaned the `# Sub-Phases` section titles in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md` without deleting the existing planning content: the subphase map heading now reads as the wider `3.2B` family map, the sketch-plane entries now use the renamed `3.2B-SketchPlane-*` titles, the cleanup subsection titles were retied to `3.2B-SketchPlane-2-Cleanup`, and new `##` entries were added for `3.2B-DrawSketch-1` through `3.2B-DrawSketch-3` so the full sketch family is visible in one scan-friendly phase list.
57. 2026-03-19 09:40 - Cleared the current pre-commit build blockers and got `npm run build` green again: removed an unused sketch-row toggle helper in `src/app/spaghetti/canvas/NodeView.tsx`, fixed the missing `profileIndex` in `src/app/spaghetti/compiler/compileGraph.ts`, tightened managed sketch typing in `src/app/spaghetti/registry/nodeRegistry.ts`, fixed the `readonly` profiles parameter and explicitly typed the collapsed-viewport patches in `src/app/spaghetti/store/useSpaghettiStore.ts`, widened sketch point-key handling in `src/app/spaghetti/ui/features/SketchFeatureView.tsx`, and narrowed the real Three helper test material access in `src/viewer/sketch/SketchPlanePickHelper.test.ts`; after those fixes `cmd /c npm run build` now passes, with only the existing non-blocking Vite warnings about externalized `path` / `crypto` for `occt-import-js` and the large chunk-size advisory.
56. 2026-03-19 09:34 - Added the deeper console-scope rule into the `3.2B-DrawSketch` planning block in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md`: `Draw Sketch` commands are now explicitly framed as a future graph/node-scoped command branch (`graph > node > sketch > draw`) rather than a separate top-level sketch console, while still allowing later convenience aliases on top of that deeper ownership model.
55. 2026-03-19 09:29 - Added explicit temporary console-command planning into the `3.2B-DrawSketch` decision block in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md`: `DrawSketch-1` now has a new question for the first dev-only tool/status commands (`line`, `pline`, `status`, `help`), and `DrawSketch-2` now has a new question for the first confirm/cancel/continue lifecycle commands (`enter`, `esc`, `x`, `undo`) so the viewport-first draw session can follow the same console-assisted debugging seam used earlier for `SketchPlane`.
54. 2026-03-19 09:27 - Tightened the first `Draw Sketch` tool-scope decisions in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md` so `3.2B-DrawSketch-1` now recommends a `Line + PLine` first-cut pair instead of `Line` only, explicitly records the command-behavior difference between one-shot `Line` and continuous chained `PLine`, and updates the later accept/cancel and active-tool questions so `Enter`/`Esc` and tool persistence now account for finishing or cancelling a live polyline command.
53. 2026-03-19 09:25 - Tightened the `3.2B-DrawSketch` decision block in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md` around a `Line`-only first viewport-draw cut: `3.2B-DrawSketch-1` question `1` now recommends proving the viewer-owned draw-session seam with `Line` only before adding other tools, and the later `DrawSketch` questions were adjusted so they no longer assume `Rectangle`, `Circle`, or `BezierSpline` are already in first-cut scope.
52. 2026-03-19 09:22 - Expanded the `## Decisions` section in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md` with a new `### 3.2B-DrawSketch` planning block, breaking the `Draw Sketch` family into `3.2B-DrawSketch-1` through `3.2B-DrawSketch-3` as `####` phase headings and then adding per-phase open decision questions as `#####` items covering first tool scope, viewer-owned preview, point projection, collapse/session rules, commit model, multi-step tool state machines, accept/cancel behavior, tool persistence, spline progression, toolbar draft mirroring, selection/editing depth, highlight language, snapping, and browser-vs-viewer ownership boundaries.
51. 2026-03-19 09:13 - Added the renamed sketch-plane follow-on family under `[3.2B]` in `docs/Human-Plans/roadmap/roadmap.md` as nested `####` entries: `[x] 3.2B-SketchPlane-1` for the shipped source/transform surface, `[~] 3.2B-SketchPlane-2` for the partially landed viewport-first source-pick and sketch-origin-gizmo flow with remaining cleanup, and `[ ] 3.2B-SketchPlane-3` for the future geometry-driven auto-setup/highlighting cut.
50. 2026-03-19 09:12 - Expanded the live `[3.2B] Sketch Operation Authoring` section in `docs/Human-Plans/roadmap/roadmap.md` with three new nested `####` follow-on entries for the viewport-first `Draw Sketch` family: `3.2B-DrawSketch-1` for viewer-owned live draw preview, `3.2B-DrawSketch-2` for multi-step tool sessions and commit rules, and `3.2B-DrawSketch-3` for selection/editing/richer sketch feedback, while leaving the parent `[3.2B]` shipped status intact.
49. 2026-03-19 09:01 - Renamed the user-facing second sketch input row from `SketchEntities` to `SketchDraw` while leaving the internal port/type ids intact: `src/app/spaghetti/registry/nodeRegistry.ts` now labels the port `SketchDraw`, `src/app/spaghetti/canvas/NodeView.tsx` now uses `SketchDraw` in the row-toggle accessibility copy, `src/app/spaghetti/registry/nodeRegistry.test.ts` and `src/app/spaghetti/canvas/NodeView.test.tsx` now expect the new visible label, and `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md` now describes the second pin as `SketchDraw` instead of `SketchEntities`.
48. 2026-03-19 08:58 - Renamed the new sketch viewport-authoring architecture entry in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md` so the overall workflow is now described as `Draw Sketch` instead of `SketchEntities` viewport draw, while keeping `SketchEntities` as the node-level pin/seam name; the recommended subphases are now labeled `DrawSketch-1` through `DrawSketch-3`.
47. 2026-03-19 08:47 - Added a new `SketchEntities` viewport-drawing architecture entry to `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md`, defining the direction that sketch entity authoring should move into the real Three model viewport while the floating `Sketch Draw` toolbar remains only the control surface; the new section also breaks that work into three recommended subphases covering viewer-owned live draw preview, multi-step tool session/commit rules, and later selection/editing/richer viewport feedback.
46. 2026-03-19 08:41 - Swapped the sketch draw tool chooser to icon-first buttons while keeping the tool names on hover/accessibility: `src/app/components/ViewportOverlay.tsx` now renders small inline SVG glyphs for `Line`, `Arc3Point`, `BezierSpline`, `Rectangle`, and `Circle`, reuses a shared `getGeometrySketchToolLabel(...)` helper for the active-tool heading and button labels, and gives each icon button `aria-label`/`title`; `src/app/theme/v15Theme.css` now styles the sketch tool buttons as compact centered icon buttons; `src/app/components/ViewportOverlay.test.tsx` now verifies the icon buttons remain discoverable by accessible name.
45. 2026-03-19 08:38 - Restyled the floating sketch entries toolbar to match the sketch-plane toolbar more closely and keyed it to the `SketchEntities` pin accent: `src/app/components/ViewportOverlay.tsx` now docks `Done` in the sketch draw/review title bar and removes the duplicate body-level `Done` buttons, while `src/app/theme/v15Theme.css` now drives the sketch draw section labels, draft card, tool chips, profile cards, empty states, and session metadata from `--overlay-tool-accent` so the sketch entries toolbar reads like the same shared toolbar family with the `SketchEntities` theme instead of the older neutral blue skin.
44. 2026-03-19 08:32 - Entering `Draw Sketch` now auto-collapses the active spaghetti editor viewport and restores it on sketch-session close through the store-owned geometry sketch session seam: `src/app/spaghetti/store/useSpaghettiStore.ts` now records the active editor viewport on draw-session start, collapses that viewport using the same collapsed-window path as sketch-plane pick, and restores it when `closeGeometrySketchSession()` ends the session; `src/app/spaghetti/store/useSpaghettiStore.test.ts` now verifies draw starts in a collapsed shell and returns the editor to `expanded` on close.
43. 2026-03-19 08:22 - Added the title-bar right-click `i Menu` behavior to the floating sketch draw/review toolbar so it now matches the shared toolbar template more honestly: `src/app/components/ViewportOverlay.tsx` now opens a title-bar `SpaghettiContextMenu` on sketch-session right click, exposes `Open i Menu` / `Close i Menu`, and renders a hidden first `UI Customization` section with `Toolbar Width` and `Toolbar Height` sliders inside the sketch session body; `src/app/components/ViewportOverlay.test.tsx` now verifies the sketch draw title-bar context menu opens and reveals those controls.
42. 2026-03-19 08:19 - Reframed the floating `Draw Sketch` / `Sketch Review` session onto the shared viewport toolbar format instead of the older one-off card stack: `src/app/components/ViewportOverlay.tsx` now gives the sketch session the shared title-bar/density/close chrome, shared resize-handle classes, shared tool-panel body styling, and explicit collapsible body sections (`Session`, `Tool Selection`, `Active Tool`, `Entities`, `Profiles`, `Session Actions`) so the sketch draw workflow now reads much closer to the `SketchPlane` toolbar structure while preserving the existing draw/review behavior.
41. 2026-03-19 08:13 - Added a real second `SketchEntities` input row to the `Geometry/Sketch` node surface: `src/app/spaghetti/schema/spaghettiTypes.ts` and `src/app/spaghetti/schema/spaghettiSchema.ts` now include the new `sketchEntities` port kind, `src/app/spaghetti/canvas/typeColors.ts` gives it a dedicated sketch-content accent, `src/app/spaghetti/registry/nodeRegistry.ts` now declares `SketchEntities` as a second sketch input port, and `src/app/spaghetti/canvas/NodeView.tsx` now renders that port as a managed attached input row owning the `Draw` action plus the entity placeholder/list while removing the old duplicate content-side `Draw` and `Entities` sections; `src/app/spaghetti/registry/nodeRegistry.test.ts` and `src/app/spaghetti/canvas/NodeView.test.tsx` now cover the new row.
40. 2026-03-19 02:23 - Added a new `SketchEntities` note to `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md`, recording the direction that `Sketch` should gain a second input pin named `SketchEntities` alongside `SketchPlane`, with a first visible `Draw` button inside that pin to enter the drawing session and open the sketch drawing toolbar while keeping the main model viewport as the real drawing surface.
39. 2026-03-19 02:20 - Reformatted the new bottom `### 3.2b-3` geometry-driven decision list in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md` so each question now lives in its own collapsible `####` block with a `##### Suggestion` subsection, matching the existing `3.2b-2` planning pattern.
38. 2026-03-19 02:19 - Expanded the bottom `### 3.2b-3` section in `docs/Human-Plans/Architecture/Spaghetti-Editor-Arch/Nodes/Sketch.md` into a real geometry-driven decision list with twelve checklist items plus default suggestions, covering valid source types, orientation inference, ambiguity handling, highlight states, session-shape reuse, and preserved geometry-source metadata for the later `3.2B-3` phase.
37. 2026-03-19 02:14 - Made the sketch-plane keyboard mode shortcuts switch the visibly open active subgroup as well as the underlying gizmo mode: `src/app/components/ViewportOverlay.tsx` now treats `m` as "open Move / collapse Rotate / set translate mode" and `r` as "open Rotate / collapse Move / set rotate mode", so the bordered active box and visible rows stay aligned with the shortcut-driven mode change; `src/app/components/ViewportOverlay.test.tsx` now verifies both keyboard paths flip the active group classes and the visible `Move X` / `Rotate X` rows correctly.
36. 2026-03-19 02:08 - Simplified sketch-plane `M` / `R` keyboard behavior so they now switch gizmo mode without auto-starting a center-handle drag: `src/app/components/ViewportOverlay.tsx` no longer uses the temporary pending shortcut activation path for sketch-plane adjust mode, so `m` now cleanly selects move mode and `r` now cleanly selects rotate mode while waiting for the user’s next actual handle click.
35. 2026-03-19 02:06 - Fixed sketch-plane keyboard gizmo activation so `R` now follows the same pending center-handle activation pattern as `M`: `src/app/components/ViewportOverlay.tsx` now handles sketch-plane adjust-mode `m` / `r` shortcuts, switches the draft gizmo mode through the store, and then triggers the real viewer `activateTranslateCenterHandle()` / `activateRotateCenterHandle()` once the requested mode is live; `src/app/components/ViewportOverlay.test.tsx` now verifies the sketch-plane session switches into rotate mode from the `r` shortcut.
34. 2026-03-19 02:03 - Fixed sketch-plane slider snapping so enabled snap values now change the actual move/rotate slider step instead of only the preview visuals: `src/app/components/ViewportOverlay.tsx` now uses the active move/rotate snap amount as the real `ParaSlider` and compact vec3 step when snap is enabled, and `src/app/components/ViewportOverlay.test.tsx` now verifies dragging `Move X` with `Move Snap` enabled lands on the expected snapped value.
33. 2026-03-19 02:00 - Enabled snap-aware continuous drag preview for the sketch-plane move/rotate sliders so the white value marker can stay on the snapped value while the blue fill follows the mouse: `src/app/components/ParaVec3Slider.tsx` now forwards `showContinuousDragPreview`, and `src/app/components/ViewportOverlay.tsx` now turns that behavior on for sketch-plane move/rotate `ParaSlider` and compact vec3 rows whenever the corresponding snap toggle is enabled.
32. 2026-03-19 01:57 - Added first-pass sketch-plane gizmo snap controls inline with the `Move` and `Rotate` subgroup headers: `src/app/store/uiPrefsStore.ts` now carries tool-owned translate/rotate snap enabled/value settings, `src/app/viewerBridge.ts` extends the sketch-plane overlay VM with a `snap` block, `src/app/components/ViewerHost.tsx` now feeds those snap values into the viewer overlay, `src/viewer/Viewer.ts` now applies and clears translate/rotate gizmo snap from the active sketch-plane overlay, `src/app/components/ViewportOverlay.tsx` now adds inline `Snap` toggles plus `Move Snap` and `Rotate Snap` sliders that appear when enabled, and `src/app/components/ViewportOverlay.test.tsx` plus `src/viewer/sketch/SketchPlanePickHelper.test.ts` now cover the updated overlay contract.
31. 2026-03-19 01:47 - Added a draft-only `Reset Transform` action inline with the sketch-plane `Transform` section header: `src/app/spaghetti/store/useSpaghettiStore.ts` now exposes `resetSketchPlanePickDraftTransform()` to restore the active sketch-plane draft transform to the default zeroed state, `src/app/components/ViewportOverlay.tsx` now docks a `Reset Transform` button on the same row as the `Transform` label, `src/app/theme/v15Theme.css` now supports shared inline section-header actions for that row layout, and `src/app/components/ViewportOverlay.test.tsx` now verifies the inline button resets the draft transform before confirm.
30. 2026-03-19 01:40 - Hardened the shared viewer `TransformGizmo` so sketch-plane arrow dragging no longer gets interrupted by same-state overlay updates: `src/viewer/gizmo/TransformGizmo.ts` now treats same-object `attach(...)` calls as no-ops and skips `setMode(...)` when the requested mode is already active, preventing sketch-plane draft-state churn from clearing the active transform axis mid-drag; `src/viewer/gizmo/TransformGizmo.test.ts` now covers both no-op behaviors.
29. 2026-03-19 02:05 - Changed the shared sketch-plane toolbar height behavior so collapsing or expanding sections hands the outer window back to `auto` height after a manual resize: `src/app/components/ViewportOverlayToolPanel.tsx` now exposes shared `i Menu` open/expand change callbacks, `src/app/components/ViewportOverlay.tsx` now resets the sketch-plane toolbar from `manual` back to `auto` when the title-bar `i Menu`, customization subsections, density mode, or body section/subsection toggles change the visible content height, `src/app/components/ViewportOverlay.test.tsx` now proves a manually resized toolbar drops its explicit height after a section collapse, and `docs/Human-Plans/Architecture/Toolbar.md` now records that collapse/expand should return the outer toolbar to `auto` height.
28. 2026-03-19 01:52 - Split the sketch-plane `UI Customization` section into two subsections and wired real viewer-side sketch UI controls: `src/app/components/ViewportOverlay.tsx` now groups the existing console-style shell controls under `Toolbar Window` and adds a new `Sketch Plane UI` subsection with `Gizmo Size` and `Ghost Plane Size` sliders, `src/app/store/uiPrefsStore.ts` now carries shared sketch-plane UI scale prefs, `src/app/components/ViewerHost.tsx` now passes those values into the sketch-plane overlay VM, `src/app/viewerBridge.ts` extends that VM with a `ui` block, `src/viewer/gizmo/TransformGizmo.ts` now exposes `setSize`, `src/viewer/Viewer.ts` now applies the sketch-plane gizmo size, `src/viewer/sketch/SketchPlanePickHelper.ts` now scales ghost-plane size/offset from the overlay UI settings, `src/viewer/sketch/SketchPlanePickHelper.test.ts` adds coverage for scaled ghost planes, `src/app/components/ViewportOverlay.test.tsx` now checks the new `Toolbar Window` / `Sketch Plane UI` subsection labels and controls, `src/app/theme/v15Theme.css` adds subsection spacing, and `docs/Human-Plans/Architecture/Toolbar.md` now records the recommended `i Menu` subsection split between shared window controls and tool-specific UI controls.
27. 2026-03-19 01:44 - Added shared auto-height versus manual-height behavior for the sketch-plane toolbar outer shell: `src/app/components/ViewportOverlay.tsx` now keeps the toolbar in `auto` height mode by default, switches to `manual` only after explicit outer height edits, and uses a `ResizeObserver` to keep the auto-height panel clamped inside the viewport as sections expand or collapse; `docs/Human-Plans/Architecture/Toolbar.md` now records that auto/manual outer-height rule as part of the shared toolbar template contract.
26. 2026-03-19 01:37 - Added a shared body-level split divider when the toolbar `i Menu` is open: `src/app/components/ViewportOverlayToolPanel.tsx` now wraps the open `UI Customization` section and the lower tool content in a resizable vertical stack with a draggable horizontal separator, `src/app/theme/v15Theme.css` now styles that new body split rule using the shared accent language, `src/app/components/ViewportOverlay.test.tsx` now checks the customization/body split handle appears when the `i Menu` opens, and `docs/Human-Plans/Architecture/Toolbar.md` now records the body-level `i Menu` resize divider as part of the toolbar template contract.
25. 2026-03-19 01:33 - Made the shared toolbar `i Menu` section itself collapsible once opened: `src/app/components/ViewportOverlayToolPanel.tsx` now gives the hidden first body section its own text-label toggle and chevron, reopening the menu expands it by default, `src/app/theme/v15Theme.css` now promotes the text-toggle/chevron styling into reusable template classes instead of sketch-only names, `src/app/components/ViewportOverlay.test.tsx` now verifies `UI Customization` can collapse while staying open, and `docs/Human-Plans/Architecture/Toolbar.md` now records the `i Menu` chevron-collapse rule as part of the shared toolbar template contract.
24. 2026-03-19 01:27 - Expanded the sketch-plane toolbar `i Menu` to mirror the Console Tools control set instead of only width/height: `src/app/components/ViewportOverlay.tsx` now adds `Preset`, `BG Fill`, `Text`, `Font`, `Z Index`, `Fill Type`, and `BG Color` controls alongside the existing width/height sliders, computes the same `default / clear / custom` preset language, and applies those values to the toolbar shell; `src/app/theme/v15Theme.css` now respects the new overlay-tool background, text-opacity, font-size, fill-mode, and z-index variables so the controls visibly affect the shared panel shell; `src/app/components/ViewportOverlay.test.tsx` now checks the full console-style control list appears in the `i Menu`; and `docs/Human-Plans/Architecture/Toolbar.md` now records the console-tools-aligned `i Menu` control set as part of the shared toolbar template direction.
23. 2026-03-19 01:18 - Replaced the sketch-plane toolbar `i Menu` placeholder with real first-pass UI customization controls: `src/app/components/ViewportOverlay.tsx` now exposes `Toolbar Width` and `Toolbar Height` `ParaSlider`s inside the hidden `i Menu` section and routes them through the same clamped overlay-size model the resize handles use, `src/app/theme/v15Theme.css` now adds the small shared customization-rows layout, `src/app/components/ViewportOverlay.test.tsx` now checks the `i Menu` reveals the width/height controls, and `docs/Human-Plans/Architecture/Toolbar.md` now records width/height as first-pass `i Menu` customization content.
22. 2026-03-19 01:14 - Fixed the new toolbar title-bar right-click menu so it actually opens visibly on the sketch-plane toolbar: `src/app/components/ViewportOverlayToolPanel.tsx` now positions the shared `SpaghettiContextMenu` in panel-local coordinates instead of raw viewport coordinates while preserving the forwarded panel ref, and `src/app/components/ViewportOverlay.test.tsx` now checks the title-bar context menu anchors relative to the toolbar panel before opening the hidden `i Menu` section.
21. 2026-03-19 01:09 - Added a shared title-bar right-click menu to the reusable viewport toolbar template and wired the first built-in `i Menu` behavior: `src/app/components/ViewportOverlayToolPanel.tsx` now opens a `SpaghettiContextMenu` from title-bar right-click and toggles a hidden first body section when `iMenuContent` is supplied, `src/app/components/ViewportOverlay.tsx` now supplies the sketch-plane toolbar with a first-pass `UI Customization` section as the first hidden body section, `src/app/theme/v15Theme.css` now styles the accent-tinted toolbar context menu and the hidden `i Menu` section, `src/app/components/ViewportOverlay.test.tsx` now verifies the title-bar context menu opens and reveals the hidden body section, and `docs/Human-Plans/Architecture/Toolbar.md` now records the shared title-bar right-click / `Open i Menu` template rule.
20. 2026-03-19 00:56 - Changed the sketch-plane active preview grid to better match the main model viewport grid recipe: `src/viewer/sketch/SketchPlanePickHelper.ts` now uses a centered layered grid construction instead of the earlier single custom grid, while keeping the ghost planes offset away from the origin; `src/viewer/sketch/SketchPlanePickHelper.test.ts` now targets the named active-grid group and checks that the ghost planes remain offset while the active grid spans both negative and positive coordinates like the main viewer grid.
19. 2026-03-19 00:53 - Re-centered the active sketch-plane preview grid on the origin to match the main model viewer grid while keeping the ghost planes offset away from the center: `src/viewer/sketch/SketchPlanePickHelper.ts` now builds the active plane grid symmetrically around the origin again, and `src/viewer/sketch/SketchPlanePickHelper.test.ts` now asserts that split directly by checking the ghost planes remain offset while the grid spans both negative and positive coordinates.
18. 2026-03-19 00:51 - Reduced the sketch-plane real Three origin gap back down by half: `src/viewer/sketch/SketchPlanePickHelper.ts` now uses a `3` unit gap instead of `6`, and `src/viewer/sketch/SketchPlanePickHelper.test.ts` now matches the smaller plane/grid offsets while the viewer bridge tests still pass.
17. 2026-03-19 00:49 - Fixed the remaining centered-origin read in the real Three preview by shifting the active plane grid into the same positive-quadrant-with-gap model as the ghost planes: `src/viewer/sketch/SketchPlanePickHelper.ts` now builds the active grid from the origin gap outward instead of from negative to positive coordinates, and `src/viewer/sketch/SketchPlanePickHelper.test.ts` now checks the in-plane grid coordinates stay at or beyond the configured gap while the viewer bridge tests still pass.
16. 2026-03-19 00:47 - Increased the real Three ghost-plane origin gap again so the preview planes start farther from the center: `src/viewer/sketch/SketchPlanePickHelper.ts` now uses a `6` unit origin gap instead of `3`, and `src/viewer/sketch/SketchPlanePickHelper.test.ts` now asserts the larger positive-quadrant offsets while the viewer bridge tests still pass.
15. 2026-03-19 00:46 - Added a small gap between the origin and the real Three ghost planes so they no longer start flush at the center: `src/viewer/sketch/SketchPlanePickHelper.ts` now uses a `3` unit origin gap before each positive-quadrant plane begins, and `src/viewer/sketch/SketchPlanePickHelper.test.ts` now verifies the plane meshes sit beyond that origin gap while the viewer bridge tests still pass.
14. 2026-03-19 00:44 - Changed the real Three-rendered sketch-plane ghost previews so they no longer overlap the origin symmetrically: `src/viewer/sketch/SketchPlanePickHelper.ts` now offsets each plane mesh/outline into its positive quadrant instead of centering the plane on the origin, so the `XY`, `XZ`, and `YZ` previews now start at the origin and extend outward more like Fusion-style origin planes; `src/viewer/sketch/SketchPlanePickHelper.test.ts` now verifies the plane mesh offsets for all three planes, and the viewer bridge tests still pass.
13. 2026-03-19 00:42 - Moved the sketch-plane `Done` action from the `Plane Selection` body into the title-bar action cluster beside the tool title/density controls: `src/app/components/ViewportOverlay.tsx` now renders `Done` as a title-bar action and removes the duplicate body-level confirm buttons, while `src/app/components/ViewportOverlay.test.tsx` now targets the title-bar `Done` action and updates the `Plane Selection` collapse assertions so they only check the section body content disappears.
12. 2026-03-19 00:39 - Replaced the sketch-plane `XY / XZ / YZ` chip cluster with a shared `ParaSelect` row in the `Plane Selection` section: `src/app/components/ViewportOverlay.tsx` now uses `ParaSelect` for the plane chooser in both essentials and expanded modes, `src/app/theme/v15Theme.css` now extends the dark `ParaSelect` chrome to `ViewportOverlayToolPanel`, and `src/app/components/ViewportOverlay.test.tsx` now chooses `XZ` through the shared `Plane` row instead of the removed plane chips.
11. 2026-03-19 00:37 - Mirrored the `Move` two-state display model onto the sketch-plane `Rotate` subsection: `src/app/components/ViewportOverlay.tsx` now shows full `Rotate X / Y / Z` rows in expanded mode and the compact reusable vec3 control in collapsed mode, and `src/app/components/ViewportOverlay.test.tsx` now verifies the first `Rotate` click activates the gizmo mode and the second collapses it into the compact vec3 form.
10. 2026-03-19 00:35 - Changed the sketch-plane `Move` subsection to use two display states instead of shown-vs-hidden: `src/app/components/ViewportOverlay.tsx` now shows full per-axis `Move X / Y / Z` rows in expanded mode and the compact reusable `Vec3 ParaSlider` in collapsed mode, while `src/app/components/ViewportOverlay.test.tsx` now verifies the default expanded state shows the full rows and clicking `Move` collapses it to the compact vec3 control.
9. 2026-03-19 00:33 - Added a shared left-side chevron pattern to expandable sketch-plane toolbar labels and recorded it as a toolbar-template rule: `src/app/components/ViewportOverlay.tsx` now renders chevrons on `Plane Selection`, `Transform`, `Move`, and `Rotate` labels with open/closed state tied to the existing expanded booleans, `src/app/theme/v15Theme.css` now styles the reusable chevron and rotates it when expanded, `src/app/components/ViewportOverlay.test.tsx` now verifies the chevron state follows `Plane Selection` collapse/expand, and `docs/Human-Plans/Architecture/Toolbar.md` now says expandable toolbar section/subsection labels should include a left-side chevron reflecting open/closed state.
8. 2026-03-19 00:31 - Added a reusable compact `Vec3 ParaSlider` template and switched the sketch-plane `Move` controls onto it: `src/app/components/ParaSlider.tsx` now supports a shared cap-less mode, `src/app/components/ParaVec3Slider.tsx` is the new reusable three-channel grouped slider template, `src/app/components/ViewportOverlay.tsx` now uses that grouped control for sketch-plane translation instead of separate `Move X / Y / Z` rows, `src/app/theme/v15Theme.css` now styles the cap-less slider variant and the shared three-column vec3 slider layout, `src/app/components/ParaVec3Slider.test.tsx` adds focused coverage for the new template, `src/app/components/ViewportOverlay.test.tsx` now checks the sketch-plane toolbar renders the compact vec3 row, and `docs/Human-Plans/Architecture/Toolbar.md` now records reusable compact grouped controls as part of the toolbar template direction.
7. 2026-03-19 00:27 - Made `Plane Selection` collapsible from its text label in both essentials and expanded sketch-plane toolbar modes: `src/app/components/ViewportOverlay.tsx` now tracks a shared `Plane Selection` expanded state and uses the same text-toggle pattern as `Transform`, `src/app/components/ViewportOverlay.test.tsx` now verifies `Plane Selection` can collapse and expand from its label, and `docs/Human-Plans/Architecture/Toolbar.md` now records the template rule that all toolbar body sections and subsections should be collapsible from their own text labels without needing separate visible disclosure buttons.
6. 2026-03-19 00:25 - Swapped the typography treatments between the top sketch-plane section labels and the inner transform subgroup labels: `src/app/theme/v15Theme.css` now gives `Plane Selection` and `Transform` the stronger orange-toned heading style that `Move / Rotate` were using, while `Move / Rotate` now use the smaller, quieter gray section-label style; verified with the focused viewport-overlay test.
5. 2026-03-19 00:23 - Reworked the sketch-plane transform-mode emphasis so the active state no longer changes the `Move` / `Rotate` text color or glow; `src/app/components/ViewportOverlay.tsx` now marks the active slider-group wrapper instead of the label, `src/app/theme/v15Theme.css` now draws the active state as a subtle orange border around the active `Move` or `Rotate` subsection while leaving the text styling unchanged, and `src/app/components/ViewportOverlay.test.tsx` now verifies the active bordered group flips when `Rotate` is clicked.
4. 2026-03-19 00:23 - Added an active-mode visual state to the sketch-plane `Move` and `Rotate` text toggles so the currently selected live gizmo mode reads as active without reintroducing visible buttons; `src/app/components/ViewportOverlay.tsx` now applies `isActive` from `sketchPlanePickSession.gizmoMode`, `src/app/theme/v15Theme.css` now gives those text labels a brighter accent and subtle glow on the active state plus a light hover response, and `src/app/components/ViewportOverlay.test.tsx` now verifies the active class flips when `Rotate` is clicked.
3. 2026-03-19 00:21 - Simplified the sketch-plane `Transform` section into text-driven collapsible subsections: `src/app/components/ViewportOverlay.tsx` now removes the old `Sketch Origin Gizmo` header copy and `Move / Rotate` action row, makes `Transform`, `Move`, and `Rotate` text labels the clickable collapse targets, and keeps clicking `Move` or `Rotate` able to switch the live viewer gizmo mode while those groups stay text-only; `src/app/theme/v15Theme.css` now includes the shared no-chrome text-toggle reset for those clickable labels; and `src/app/components/ViewportOverlay.test.tsx` now checks the old gizmo header/button row stays gone.
2. 2026-03-19 00:17 - Updated the sketch-plane viewport toolbar so the `Transform` section now shows all six `ParaSlider` rows at once instead of gating them behind the `Move / Rotate` toggle; `src/app/components/ViewportOverlay.tsx` now renders both `Move` and `Rotate` slider groups simultaneously while the toggle still controls the live viewer gizmo mode, `src/app/theme/v15Theme.css` now styles the two grouped stacks with a small divider/label treatment, and `src/app/components/ViewportOverlay.test.tsx` now expects all six sliders to render together after plane selection.
1. 2026-03-19 00:15 - Entered a new chill batch for the current sketch-plane viewport-toolbar polish pass so the next rapid UI tweaks can be logged in `docs/Chill-Log.md` until the user explicitly asks to consolidate them.

<!-- ============================================================ -->
### Chill Batch #7
<!-- ============================================================ -->

Status:
- ended

Started:
- 2026-03-18 16:12

Ended:
- 2026-03-19 00:15

Mode:
- chill mode ended

Consolidated To:
- pending

Current Goal:
- track the current geometry-node shell and node-mode cleanup pass without creating permanent `docs/CHANGELOG.md` entries for each small planning or doc adjustment

Entries:
12. 2026-03-18 16:53 - Added a real middle `Sketch` block to the shared geometry-node shell for `Geometry/Sketch`: `src/app/spaghetti/canvas/GeometryNodeShell.tsx` now supports a labeled top-level content block between `Inputs` and `Outputs`, `src/app/spaghetti/canvas/NodeView.tsx` now mounts the sketch workflow under `contentLabel="Sketch"`, `src/app/theme/v15Theme.css` includes the matching shell-section sizing hook, and `src/app/spaghetti/canvas/NodeView.test.tsx` now asserts the top-level `Inputs / Sketch / Outputs` labels are present in the rendered sketch node.
11. 2026-03-18 16:55 - Tightened note `[180]` in `docs/Human-Plans/CodexNotes/12_CodexChatNotes.md` into implementation-ready shape by locking the first-pass scope to `Geometry/Sketch`, defining the exact `collapsed / essentials / expanded` defaults for the higher-level `Inputs` and `Outputs` blocks, recording the geometry-only block override model, and adding acceptance criteria plus verification targets for the next layout pass.
10. 2026-03-18 16:55 - Added new open note `[180]` in `docs/Human-Plans/CodexNotes/12_CodexChatNotes.md` for the next geometry-shell layout pass: the higher-level `Inputs` and `Outputs` blocks now need their own `collapsed / essentials / expanded` behavior above the individual sketch port-row wrappers, using the same “row mode sets defaults, manual toggles win” model.
9. 2026-03-18 16:46 - Implemented note `[179]` for `Geometry/Sketch` input/output rows: `src/app/spaghetti/canvas/NodeView.tsx` now wraps the sketch `SketchPlane`, `SketchProfiles`, and `SketchProfile` ports in geometry-only collapsible row shells whose default-open state comes from row mode while manual toggles persist without changing node mode; `src/app/theme/v15Theme.css` now styles those row headers and bodies; `src/app/spaghetti/canvas/NodeView.geometryMode.test.tsx` now covers collapsed row opening, essentials defaults, and output-row persistence across a mode switch; `src/app/spaghetti/canvas/NodeView.test.tsx` now checks the new sketch port-row wrappers appear in the geometry template; and note `[179]` in `docs/Human-Plans/CodexNotes/12_CodexChatNotes.md` is now marked complete.
8. 2026-03-18 16:41 - Tightened note `[179]` in `docs/Human-Plans/CodexNotes/12_CodexChatNotes.md` into implementation-ready shape by locking the first-pass scope to `Geometry/Sketch` input/output rows, defining the exact `collapsed / essentials / expanded` defaults for those rows, recording the geometry-only port-row override model, and adding acceptance criteria plus verification targets for the next cleanup pass.
7. 2026-03-18 16:41 - Marked `[178]` complete in `docs/Human-Plans/CodexNotes/12_CodexChatNotes.md` after the `Geometry/Sketch` row-mode cleanup landed, and opened new note `[179]` to track the next matching cleanup surface: input and output port rows still need the same “row mode sets defaults, manual toggles win” treatment.
6. 2026-03-18 16:29 - Implemented note `[178]` for `Geometry/Sketch`: `src/app/spaghetti/canvas/NodeView.tsx` now uses row mode only as the default-open model for sketch sections, removes the temporary row-mode promotion patch, and lets manual section state override those defaults without changing node mode; `src/app/spaghetti/canvas/state/spaghettiUiStore.ts` now preserves explicit `false` values so geometry sections can store manual-open overrides distinct from row-mode defaults; and `src/app/spaghetti/canvas/NodeView.geometryMode.test.tsx` now covers collapsed one-section opening, essentials defaults, and manual-close persistence across a mode switch.
5. 2026-03-18 16:29 - Tightened note `[178]` in `docs/Human-Plans/CodexNotes/12_CodexChatNotes.md` into implementation-ready shape by appending the locked execution decisions, first-pass scope, explicit `collapsed / essentials / expanded` defaults, geometry-only override model, acceptance criteria, and verification targets for the upcoming `Geometry/Sketch` node-mode cleanup.
4. 2026-03-18 16:29 - Normalized the remaining active-note headings in `docs/Human-Plans/CodexNotes/12_CodexChatNotes.md` so entries `[176]`, `[175]`, and `[174]` now also use the new `[ ]` / `[x]` status marker convention instead of mixing marked and unmarked headings in the same note file.
3. 2026-03-18 16:29 - Added explicit `[ ]` / `[x]` status markers to the active entries in `docs/Human-Plans/CodexNotes/12_CodexChatNotes.md`, plus a small top-of-file convention note, so the current geometry-node cleanup notes can distinguish open work from completed or superseded entries at a glance.
2. 2026-03-18 16:26 - Updated `AGENTS.md` so the file-reference rule now matches the verified chat-client behavior in this workspace: prefer clickable markdown links like `[Chill Log](./docs/Chill-Log.md)` and `[AGENTS.md](./AGENTS.md)` in responses, and use absolute filesystem paths only as a fallback when a relative markdown link is known not to work.
1. 2026-03-18 16:12 - Entered a new chill batch for the current geometry-node cleanup pass and locked the forward logging split: code changes go to `docs/CHANGELOG.md`, docs-only changes go to `docs/Doc-Log.md`, and chill-mode work is recorded in `docs/Chill-Log.md` until the user explicitly asks to consolidate it.

<!-- ============================================================ -->
### Chill Batch #6
<!-- ============================================================ -->

Status:
- inactive

Started:
- 2026-03-16 19:32

Ended:
- 2026-03-18 16:12

Mode:
- chill mode closed

Consolidated To:
- pending

Current Goal:
- track the current `02.4C` doc-planning pass around user reference import decisions without creating permanent `docs/CHANGELOG.md` entries for each small planning clarification

Entries:
172. 2026-03-17 13:04 - Reworked the live `[4.1]` roadmap row in `docs/Human-Plans/roadmap/roadmap.md` so each shipped and future console subphase now has its own explicit `####` subsection (`[4.1A]` through `[4.1G]`) instead of only appearing as checklist bullets, while preserving the same checked-off completion state for `[4.1A-D]` and the same open follow-through for `[4.1E-G]`.
171. 2026-03-17 12:46 - Cleared the repo-wide production build blockers so `npm run build` now passes again: tightened null-safe React test teardown and query usage in `src/app/components/ParaSelect.test.tsx` and `src/app/console/ConsoleDock.test.tsx`, removed an unused slider value in `src/app/components/ParaSlider.tsx`, fixed missing `newId(prefix)` calls in `src/app/components/ReferenceTimelineGraph.tsx`, `src/app/references/referenceTimeline.ts`, `src/app/console/useConsoleStore.ts`, and `src/app/store/useAppStore.ts`, loosened the context-menu event types in `src/app/components/ReferenceTimelineGraph.tsx` and `src/app/components/ReferenceTransformToolbar.tsx`, replaced unsupported `.toReversed()` usage in the console surfaces, hardened browser-import and URL test mocks in `src/app/references/importReferenceFile.test.ts` and `src/app/store/useAppStore.test.ts`, and expanded the mocked orbit-controls test shape in `src/viewer/scene/CameraController.test.ts` so the current TypeScript build matches the shipped runtime code again.
170. 2026-03-17 12:39 - Updated the live `4.1` console tracking surfaces so the roadmap row in `docs/Human-Plans/roadmap/roadmap.md` now lists explicit `[4.1A-G]` subphase items with `[4.1A-D]` checked off and `[4.1E-G]` still open, and the parent lane doc `docs/Phase-Plans/Tasks/Future/04.1 - VR-SP-DBG - Console And Layered Transcript.md` now also marks `[4.1C]` and `[4.1D]` complete to match the shipped console state.
169. 2026-03-17 12:34 - Implemented `4.1D` across the console/event surfaces: `src/app/console/consoleTypes.ts`, `src/app/console/useConsoleStore.ts`, `src/app/console/ConsolePanel.tsx`, `src/app/console/ConsoleDock.tsx`, and `src/app/theme/v15Theme.css` now support the three new visible layers (`View`, `Browser`, `Transforms`), isolate/subset transcript filtering, and optional diagnostics pin behavior; publisher coverage was expanded into `src/app/panels/BrowserPanel.tsx`, `src/app/store/useAppStore.ts`, `src/viewer/Viewer.ts`, and `src/app/components/ReferenceTransformToolbar.tsx` so Browser, view, and transform actions emit focused layer-specific transcript lines; and the focused console/transform tests were expanded in `src/app/console/useConsoleStore.test.ts`, `src/app/console/ConsoleDock.test.tsx`, and `src/app/components/ReferenceTransformToolbar.test.tsx` to cover the new layer/filter behavior while preserving the existing console and transform flows.
168. 2026-03-17 12:27 - Rewrote `docs/Phase-Plans/Tasks/Future/04.1D - VR-SP-DBG - Layered Event Feeds And Filtering.md` into an implementation-ready execution spec, locking the exact `View / Browser / Transforms` layer additions, the isolate-plus-subset filtering model, optional diagnostics pin behavior, the first publisher expansion targets, feed-quality rules, the integration seams, locked deferrals, and the concrete acceptance criteria for `4.1D`.
167. 2026-03-17 12:23 - Added `docs/Phase-Plans/Tasks/Future/04.1D - VR-SP-DBG - Layered Event Feeds And Filtering.md` as the next dedicated console subphase planning surface after `4.1C`, and updated `docs/Doc-Index.md` so the docs map now lists `4.1D` alongside the existing `04.1`, `04.1B`, and `04.1C` future console task docs.
166. 2026-03-17 12:18 - Flipped the rendered transcript order in `src/app/console/ConsolePanel.tsx` and `src/app/console/ConsoleDock.tsx` so both the expanded console transcript and the list overlay now show the newest visible console lines at the top and older lines lower down, while the bottom-row summary logic still points at the latest entry.
165. 2026-03-17 12:14 - Implemented the first real `4.1C` console command-input pass across `src/app/console/useConsoleStore.ts`, `src/app/console/ConsoleBar.tsx`, and `src/app/console/ConsoleDock.tsx`: the console now supports explicit `/` focus entry, focused `Up / Down` history recall, `Esc` clear-then-exit behavior in the input, strict narrow-core typed commands (`help`, `console`, `clear` disabled feedback, `history`, `frame`, `zoom`, `move`, `rotate`, `scale`, `snap`, `echo`, `status`), and command-history storage separate from transcript state; in parallel, `src/app/components/ReferenceTransformToolbar.tsx` now treats `X / Y / Z` as secondary-only transform modifiers tied to an active keyboard transform chain and uses `Esc` to cancel the live keyboard transform, while `src/viewer/Viewer.ts` now respects `event.defaultPrevented` and preserves standalone `Z` as zoom-selected (`frameSelected`) outside that chain; the focused console and transform tests were expanded to cover slash focus, command history recall, strict unknown-command reporting, the `X / Y / Z` secondary-only gate, and `Esc` transform cancel.
164. 2026-03-17 11:52 - Rewrote `docs/Phase-Plans/Tasks/Future/04.1C - VR-SP-DBG - Command Input And Keyboard Routing.md` into an implementation-ready execution spec, locking explicit `click/focus` plus `/` text-entry activation, the narrow-core first command set, strict parsing and `Up / Down` history rules, the immediate-versus-secondary transform hotkey split, `Esc` cancel behavior, the first integration shape, locked deferrals, and the concrete acceptance criteria for `4.1C`.
163. 2026-03-17 11:50 - Expanded `docs/Phase-Plans/Tasks/Future/04.1C - VR-SP-DBG - Command Input And Keyboard Routing.md` with a concrete recommended first-command set, naming the initial console commands worth shipping across core console control, view/selection actions, transforms, debug/transcript actions, and build/runtime status while keeping the alias rule scoped to explicit text-entry mode.
162. 2026-03-17 11:48 - Updated `docs/Phase-Plans/Tasks/Future/04.1C - VR-SP-DBG - Command Input And Keyboard Routing.md` so the `Esc` behavior is now explicitly documented as a cancel key for the currently active command/session, including live transform commands, with the clear-input-then-exit fallback kept only for pure console text-entry mode.
161. 2026-03-17 11:44 - Tightened the hotkey model in `docs/Phase-Plans/Tasks/Future/04.1C - VR-SP-DBG - Command Input And Keyboard Routing.md` so `X / Y / Z` are now explicitly documented as secondary axis-constraint modifiers only after an active `M / R / S` transform chain begins, while standalone `Z` remains reserved for zoom-selected behavior outside that chain.
160. 2026-03-17 11:41 - Added `docs/Phase-Plans/Tasks/Future/04.1C - VR-SP-DBG - Command Input And Keyboard Routing.md` as the next dedicated console subphase planning surface for typed command input, history, and keyboard-routing ownership, and updated `docs/Doc-Index.md` so the docs map now lists `4.1C` under the future console task-doc sequence.
159. 2026-03-17 11:40 - Reworked the parent console lane doc `docs/Phase-Plans/Tasks/Future/04.1 - VR-SP-DBG - Console And Layered Transcript.md` so the progress checklist is now split under explicit subphase parents: shipped shell/transcript/tools work lives under `[4.1A]`, floating/pop-out/list work lives under `[4.1B]`, and the still-open command-routing, richer feeds/filtering, result UX, inspector bridge, and hotkey customization work is now tracked under `[4.1C-G]`.
158. 2026-03-17 11:38 - Updated `docs/Phase-Plans/Tasks/Future/04.1 - VR-SP-DBG - Console And Layered Transcript.md` so the parent console lane doc now includes a concrete shipped-work checklist covering the real collapsed shell, expanded transcript, tools surface, floating/pop-out/list modes, and the main later follow-through still deferred into `4.1C+`, instead of reading only like a future planning note.
157. 2026-03-17 11:31 - Moved the additive console `List` transcript out of the dock-owner shell and into its own top-level main-app overlay in `src/app/console/ConsoleDock.tsx`, and changed `src/app/theme/v15Theme.css` so `ConsoleListOverlay` is now a viewport-fixed layer instead of being constrained by whichever console owner is active; `src/app/console/ConsoleDock.test.tsx` now proves that in pop-out mode the list transcript renders in the main viewport while the child pop-out window stays open and does not get its own list overlay.
156. 2026-03-17 11:25 - Refactored console `List` into a true overlay instead of a replacement window owner: `src/app/console/useConsoleStore.ts` now tracks `isListMode` separately from `windowMode`, `src/app/console/ConsoleDock.tsx` now renders the list transcript as an additive overlay while preserving the live `Docked / Floating / Pop-Out` owner underneath, `src/app/console/ConsolePanel.tsx` now drives the list button active state from that overlay flag, and the list overlay stack moved into its own `ConsoleListOverlay` CSS layer in `src/app/theme/v15Theme.css`; the focused console store/UI tests were updated so docked, floating, and pop-out all keep their owner alive while list is on and only hide the list overlay when toggled back off.
155. 2026-03-17 11:25 - Extended the console `List` toggle so it now round-trips cleanly across all window owners instead of only docked mode: `src/app/console/useConsoleStore.ts` now remembers the non-list owner and expanded truth that existed before entering list mode, `src/app/console/ConsoleDock.tsx` now uses that stored owner when the active list button is clicked again, and `src/app/console/useConsoleStore.test.ts` plus `src/app/console/ConsoleDock.test.tsx` now cover returning from list mode back into floating and pop-out flows as well as the existing docked round-trip.
154. 2026-03-17 11:21 - Corrected the console `List` mode ownership seam by making `src/app/console/useConsoleStore.ts` preserve the current expanded state when entering list mode instead of force-collapsing it, and by changing `src/app/console/ConsoleDock.tsx` so clicking the active list button now returns to normal docked mode while keeping the expanded console open; `src/app/console/ConsoleDock.test.tsx` was updated to cover both the preserved expanded-on-enter path and the docked-on-second-click toggle-off behavior.
153. 2026-03-17 11:18 - Refined the console `List` mode re-entry behavior so the header list-mode button now uses the same active visual treatment as the other live mode toggles in `src/app/console/ConsolePanel.tsx`, and `src/app/console/ConsoleDock.tsx` now treats a second click on that button as a no-op when list mode is already active instead of collapsing the expanded panel; `src/app/console/ConsoleDock.test.tsx` now proves the active class and the preserved expanded state.
152. 2026-03-17 11:15 - Refined console `List` mode so the bottom single-row console bar can still drag up into the normal expanded panel without leaving list mode, by keeping `toggleExpanded` inside `list` state, rendering the docked panel on top of the list overlay only when `isExpanded` is true, and expanding `src/app/console/ConsoleDock.test.tsx` plus `src/app/console/useConsoleStore.test.ts` to cover the new list-mode expand path and the corrected collapsed render gate.
151. 2026-03-17 11:11 - Refreshed the live `[4.1]` roadmap row in `docs/Human-Plans/roadmap/roadmap.md` so it now matches the real shipped console state, marking the collapsed/expanded shell, layered transcript, tools surface, and `Floating / Pop-Out / List` presentation work as landed while explicitly carrying the remaining command-routing, richer filtering/result UX, inspector bridge, and hotkey-customization work forward into later `4.1C+` follow-through.
150. 2026-03-17 11:05 - Lowered the dedicated `ConsoleDock--listOwner` stack level in `src/app/theme/v15Theme.css` so the new console list overlay now sits behind the viewport chrome layers like the HUD/view toolbar instead of drawing over them, while leaving the normal docked/floating/pop-out console stack unchanged.
149. 2026-03-17 11:03 - Lowered the console font-size floor from `10` to `1` by updating the shared clamp in `src/app/console/useConsoleStore.ts`, matching the `Font` slider range in `src/app/console/ConsolePanel.tsx`, and tightening `src/app/console/useConsoleStore.test.ts` so the new minimum is explicitly covered.
148. 2026-03-17 11:02 - Added a new `Font` `ParaSlider` to the console `i` tools menu by extending `src/app/console/useConsoleStore.ts` with shared console font-size state/clamping plus preset integration, threading the live font-size CSS variable through `src/app/console/ConsoleDock.tsx`, exposing the slider in `src/app/console/ConsolePanel.tsx`, and updating `src/app/theme/v15Theme.css`, `src/app/console/useConsoleStore.test.ts`, and `src/app/console/ConsoleDock.test.tsx` so the console transcript/list text size can now be adjusted from the tools menu and is covered.
147. 2026-03-17 10:59 - Corrected the new console `List` owner layout in `src/app/theme/v15Theme.css` so the single-row console command bar stays bottom-docked in list mode instead of snapping to the top of the viewport; the list overlay remains absolute while the owner shell now uses the same bottom-anchored flex behavior as the docked console.
146. 2026-03-17 10:57 - Added a new `List` console window mode by extending `src/app/console/consoleTypes.ts`, `src/app/console/useConsoleStore.ts`, and `src/app/console/ConsoleDock.tsx` with a passive transcript-overlay owner that keeps the bottom single-row console bar visible while printing console lines directly over the viewport, added a list-mode header button in `src/app/console/ConsolePanel.tsx`, styled the simple no-fill overlay in `src/app/theme/v15Theme.css`, threaded the live left-dock seam offset from `src/app/AppShell.tsx`, and expanded `src/app/console/useConsoleStore.test.ts`, `src/app/console/ConsoleDock.test.tsx`, and `src/app/AppShell.test.tsx` so list mode, docked fallback, and left-toolbar resize following are covered.
145. 2026-03-17 10:51 - Replaced the console header `Float` and `Pop` text buttons in `src/app/console/ConsolePanel.tsx` with icon-only mode controls so floating now reads as a window-box icon and pop-out now reads as an up-right launch arrow, while keeping the same accessible labels/titles and updating `src/app/console/ConsoleDock.test.tsx` to target the stable aria labels instead of button text.
144. 2026-03-17 10:43 - Added a top-level `Preset` `ParaSelect` to the console `i` tools menu so the lower console appearance controls can be recalled as named presets, wired `Default` to restore the shipped `4.1A` console appearance, wired `Clear` to use `BG Fill = 10` with `Fill Type = Flat`, exposed the preset state through `src/app/console/useConsoleStore.ts` and `src/app/console/ConsolePanel.tsx`, and expanded `src/app/console/useConsoleStore.test.ts` plus `src/app/console/ConsoleDock.test.tsx` so the preset row and its `default / clear / custom` behavior are covered.
143. 2026-03-17 10:36 - Implemented the `4.1B` console window-mode expansion by extending `src/app/console/useConsoleStore.ts` with shared `Docked / Floating / Pop-Out` state plus floating-rect ownership, adding mode-aware dock/floating/pop-out hosting and child-window portal behavior in `src/app/console/ConsoleDock.tsx`, upgrading `src/app/console/ConsolePanel.tsx` and `src/app/console/ConsoleBar.tsx` to serve docked, floating, and pop-out surfaces with shared tools/layer/chrome state, and expanding `src/app/theme/v15Theme.css`, `src/app/console/useConsoleStore.test.ts`, and `src/app/console/ConsoleDock.test.tsx` so the new floating shell, pop-out ownership, and return-to-collapsed behavior are styled and covered.
142. 2026-03-17 10:27 - Rewrote `docs/Phase-Plans/Tasks/Future/04.1B - VR-SP-DBG - Console Floating And Pop-Out Modes.md` into an implementation-ready execution spec, locking the shared `Docked / Floating / Pop-Out` console model, separate `Float` and `Pop-Out` chrome controls, floating rect/session behavior, single-owner pop-out rules, close fallback to collapsed-row-only, styling differences, interfaces, and the acceptance criteria for `4.1B`.
141. 2026-03-17 10:23 - Added `docs/Phase-Plans/Tasks/Future/04.1B - VR-SP-DBG - Console Floating And Pop-Out Modes.md` as the next dedicated console subphase doc for in-browser floating window mode plus full-window browser pop-out mode, and updated `docs/Phase-Plans/Tasks/Future/04.1 - VR-SP-DBG - Console And Layered Transcript.md` plus `docs/Doc-Index.md` so the `4.1` lane now gives those two new console presentation modes a real planning home after the shipped `4.1A` docked shell.
140. 2026-03-17 10:19 - Made the console header `i` and `T` buttons visually reflect their current on-state by adding `isActive` classes in `src/app/console/ConsolePanel.tsx`, extending the existing active button treatment to `.ConsolePanelAction.isActive` in `src/app/theme/v15Theme.css`, and updating `src/app/console/ConsoleDock.test.tsx` so the focused console tests now prove those active-state classes toggle correctly.
139. 2026-03-17 10:17 - Moved the visible-state console chrome toggle back into the title-bar action cluster in `src/app/console/ConsolePanel.tsx`, so `^` now sits anchored right with the other header buttons while the hidden-state `v` remains the only floating restore control above the panel.
138. 2026-03-17 10:15 - Fixed the missing floating console chrome-toggle by changing `src/app/theme/v15Theme.css` so `.ConsolePanel` no longer clips overflow; the transcript keeps its own scroll region, and the external `^ / v` chrome button can now actually render above the panel instead of being hidden by the panel shell.
137. 2026-03-17 10:14 - Moved the console chrome toggle fully out of the title bar in `src/app/console/ConsolePanel.tsx` so the same floating control now always lives above the panel and just flips between `^` and `v` as it hides or restores the chrome; `src/app/theme/v15Theme.css` nudges that external seam slightly higher so it reads as independent from the title bar it controls.
136. 2026-03-17 10:13 - Refined the hidden-state console chrome restore button so `src/app/console/ConsolePanel.tsx` now shows it as a down-arrow when the chrome is hidden, and `src/app/theme/v15Theme.css` now pushes the floating version farther above the panel with a small transition so it reads more clearly as an external restore control rather than still feeling attached to the header.
135. 2026-03-17 10:10 - Refined the new console chrome-hide control in `src/app/console/ConsolePanel.tsx` so it now sits in the header action row to the left of `i` while the console chrome is visible, then becomes the lone floating restore button above the panel once the chrome is hidden; `src/app/theme/v15Theme.css` now splits the shared chrome-toggle skin from the floating positioning rule, and the focused console tests still pass under the updated behavior.
134. 2026-03-17 10:04 - Added a new floating console chrome-toggle button in `src/app/console/ConsolePanel.tsx` that sits just above the expanded panel and hides the full console chrome in one shot: title bar, `i` tools strip, and layer-toolbar row all disappear while the single floating toggle remains available to restore them; `src/app/theme/v15Theme.css` now styles the new floating button and `src/app/console/ConsoleDock.test.tsx` now proves the full-chrome hide/show behavior.
133. 2026-03-17 10:04 - Added a new small header `T` button to `src/app/console/ConsolePanel.tsx` that acts as the visibility control for the `Commands / Shortcuts / App / ...` layer toolbar row, letting the user hide or restore that strip independently from the `i` tools menu; `src/app/console/ConsoleDock.test.tsx` now proves the toolbar row disappears when the new toggle is used.
132. 2026-03-17 10:02 - Brought the open `ParaSelect` dropdown menu closer to the app’s dark native select styling by giving `src/app/theme/v15Theme.css` a dedicated `ParaSelectNative` dark `color-scheme` plus dark `option` / `optgroup` / checked-state colors, so the console tools selector popup no longer falls back to the bright default white menu treatment.
131. 2026-03-17 10:01 - Fixed the console tools slider value boxes in `src/app/theme/v15Theme.css` by extending the same `ParaSliderValueButton` styling used in `.V15Panel` and the reference transform toolbar to `.ConsoleToolbarMenu`, so the slider values no longer render with the browser-default white button face.
130. 2026-03-17 09:59 - Tightened the shared `ParaSelect` value presentation in `src/app/theme/v15Theme.css` so its right-side value text now uses a more slider-like inset/aligned treatment, which brings the console tools selector rows closer to the `ParaSlider` visual language without changing the actual select behavior.
129. 2026-03-17 09:57 - Upgraded `src/app/components/ParaSelect.tsx` so the left/right arrows now wrap through the full option list instead of stopping at the ends, and added a visible option-progress fill/marker bar to the select track to show which choice is active; `src/app/components/ParaSelect.test.tsx` now proves wraparound in both directions plus the new fill-bar position behavior.
128. 2026-03-17 09:54 - Added a second Spaghetti-style selector row to the console tools strip for background color by introducing shared `backgroundColorMode` state in `src/app/console/useConsoleStore.ts`, wiring a new `BG Color` `ParaSelect` into `src/app/console/ConsolePanel.tsx`, and routing the selected color palette into dock-level CSS variables in `src/app/console/ConsoleDock.tsx` / `src/app/theme/v15Theme.css`; console tests now cover the extra selector and the new store setting.
127. 2026-03-17 09:53 - Tightened the console tools control chrome in `src/app/theme/v15Theme.css` by extending the same polished `ParaSliderCap` and `ParaSelectCap` styling used under `.V15Panel` to the console tools menu, so the left/right arrow caps now visually match the Spaghetti editor `i` menu controls instead of keeping the raw default button look.
126. 2026-03-17 09:51 - Added a Spaghetti-style left/right selector row for console background fill type by introducing shared `backgroundFillMode` state in `src/app/console/useConsoleStore.ts`, wiring a new `ParaSelect` control into `src/app/console/ConsolePanel.tsx`, and applying the selected `blur / flat / clear` mode on the dock shell in `src/app/console/ConsoleDock.tsx` plus `src/app/theme/v15Theme.css`; console tests now also prove the tools menu includes the selector and the store accepts fill-mode changes.
125. 2026-03-17 09:48 - Added a third `Z Index` slider to the console tools strip in `src/app/console/ConsolePanel.tsx`, backed by new shared `zIndex` state in `src/app/console/useConsoleStore.ts` and applied at the dock shell level in `src/app/console/ConsoleDock.tsx`, so the user can push the console behind Browser/editor surfaces when needed; `src/app/console/useConsoleStore.test.ts` and `src/app/console/ConsoleDock.test.tsx` now cover the new control.
124. 2026-03-17 09:44 - Reworked the new console tools strip into real visual controls by adding shared `backgroundOpacity` and `textOpacity` state in `src/app/console/useConsoleStore.ts`, driving the dock shell through CSS variables in `src/app/console/ConsoleDock.tsx`, and replacing the placeholder info text in `src/app/console/ConsolePanel.tsx` with two `ParaSlider` controls (`BG Fill` and `Text`) using the same slider template language as the Spaghetti info surface; `src/app/theme/v15Theme.css`, `src/app/console/ConsoleDock.test.tsx`, and `src/app/console/useConsoleStore.test.ts` were updated to style and cover the new controls.
123. 2026-03-17 09:43 - Added a new header `i` button to `src/app/console/ConsolePanel.tsx` that toggles a hidden console tools strip above the layer toggles, styled the new button/menu shell in `src/app/theme/v15Theme.css`, and updated `src/app/console/ConsoleDock.test.tsx` so the console tests now reflect the disabled `Clear` button and prove the new tools menu toggle works.
122. 2026-03-17 09:42 - Removed the remaining viewport fraction caps from the console shell by changing the bar-drag and panel-drag resize math in `src/app/console/ConsoleBar.tsx` and `src/app/console/ConsolePanel.tsx` to allow the full window height, raising the store ceiling in `src/app/console/useConsoleStore.ts`, and updating `src/app/theme/v15Theme.css` so the expanded console can now grow to 100% of the browser viewport height.
121. 2026-03-17 09:41 - Raised the console’s upper resize ceiling by increasing the store max expanded height in `src/app/console/useConsoleStore.ts` from `560` to `780` and matching the panel shell cap in `src/app/theme/v15Theme.css` to `min(780px, 85vh)`, so the transcript can expand much taller before it tops out.
120. 2026-03-17 09:40 - Disabled the expanded console `Clear` action in `src/app/console/ConsolePanel.tsx` without removing it, and added a muted disabled visual state in `src/app/theme/v15Theme.css` so users can still see the control but cannot erase transcript history for now.
119. 2026-03-17 09:38 - Lowered the console’s remaining expanded-height floor from `56` to `10` in both `src/app/console/useConsoleStore.ts` and `src/app/theme/v15Theme.css`, so the user can compress the expanded shell almost fully flat before the existing collapse snap takes over.
118. 2026-03-17 09:34 - Lowered the remaining console visual floor by reducing the store minimum expanded height in `src/app/console/useConsoleStore.ts` to `56` and dropping the panel shell minimum plus some inner toggle/transcript padding in `src/app/theme/v15Theme.css`, so the expanded console can shrink much thinner before it snaps back to the single-row collapsed state.
117. 2026-03-17 09:27 - Reworked the console resize seam across `src/app/console/useConsoleStore.ts`, `src/app/console/ConsoleBar.tsx`, and `src/app/console/ConsolePanel.tsx` so dragging the expanded panel all the way down now snaps back into the single-row collapsed bar, reopening resets to the default expanded height, and the bottom row itself can now be dragged upward to expand without relying only on the arrow button; `src/app/console/useConsoleStore.test.ts` and `src/app/console/ConsoleDock.test.tsx` now cover the new collapse/restore/drag-open behavior.
116. 2026-03-17 09:27 - Lowered the expanded console resize floor in `src/app/console/useConsoleStore.ts` from `180` to `120`, so the bottom-docked transcript panel can collapse much further in height without changing its default or max size behavior.
115. 2026-03-17 09:26 - Tightened the expanded console header row in `src/app/theme/v15Theme.css` by reducing the `ConsolePanelHeader` min-height and vertical padding, so the `CONSOLE` title strip sits closer to the compact Browser-style header height instead of reading too tall.
114. 2026-03-17 09:22 - Removed the duplicate expanded-panel command input from `src/app/console/ConsolePanel.tsx` so the console keeps only one real command line in the always-visible bottom row, and cleaned out the panel’s now-unused input/submit wiring.
113. 2026-03-17 09:21 - Tightened the new console shell dock in `src/app/theme/v15Theme.css` so the bottom bar and expanded panel now hard-snap flush to the left, right, and bottom viewport edges with no outer inset, and removed the outer corner fillets from the console shell so it reads as a true docked strip instead of a floating rounded panel.
112. 2026-03-17 09:18 - Implemented `04.1A` across `src/app/console/`, `src/app/AppShell.tsx`, `src/app/store/useAppStore.ts`, `src/app/buildDispatcher.ts`, `src/app/components/ReferenceTransformToolbar.tsx`, and `src/app/theme/v15Theme.css`, shipping the first bottom-docked console row, expandable transcript panel, shared transcript store, narrow typed-command seam, and first transcript publishers; then moved `docs/Phase-Plans/Tasks/Old/04.1A - VR-SP-DBG - Console Shell And Transcript Core.md` into shipped history and updated `docs/Doc-Index.md`.
111. 2026-03-17 08:58 - Locked `4.1A.Q3` in `docs/Phase-Plans/Tasks/Future/04.1A - VR-SP-DBG - Console Shell And Transcript Core.md`: the first console shell should already show a real visible input field, but `4.1A` can still keep actual typed-command execution narrower than the later richer command-language follow-up work.
110. 2026-03-17 08:57 - Updated `docs/Phase-Plans/Tasks/Future/04.1A - VR-SP-DBG - Console Shell And Transcript Core.md` to lock `4.1A.Q1` to a bottom-docked expanded console, lock `4.1A.Q2` to simple first-pass per-layer toggles, and clarify `4.1A.Q3` so the difference between a visible input seam and full typed-command execution is explicit.
109. 2026-03-17 08:54 - Added the dedicated future task doc `docs/Phase-Plans/Tasks/Future/04.1A - VR-SP-DBG - Console Shell And Transcript Core.md` as the first concrete subphase under the console lane, focusing the initial implementation on the collapsed/expanded shell and shared transcript core, and updated `docs/Doc-Index.md` so the new `4.1A` task doc is discoverable from the docs map.
108. 2026-03-17 08:52 - Expanded `docs/Phase-Plans/Tasks/Future/04.1 - VR-SP-DBG - Console And Layered Transcript.md` with a hotkey-customization note: the console lane now explicitly records a later `[4.1F]` follow-up for user shortcut rebinding plus a `4.1.Q1B` decision note covering conflict detection, restore-default behavior, and the rule that rebinding should not blur the line between reserved text-entry behavior and rebindable tool shortcuts.
107. 2026-03-17 08:51 - Added a console command-entry decision note to `docs/Phase-Plans/Tasks/Future/04.1 - VR-SP-DBG - Console And Layered Transcript.md`: reserved single-key tool hotkeys like `M / R / S` should stay immediate without `Enter`, while longer typed commands like `mirror` should only begin inside explicit console text-entry mode and submit on `Enter`.
106. 2026-03-17 08:48 - Expanded `docs/Phase-Plans/Tasks/Future/04.1 - VR-SP-DBG - Console And Layered Transcript.md` with a suggested `[4.1A-E]` subphase breakdown and a recommended first-pass console layer set, so the console task doc now explicitly captures how the lane could be split and which transcript/debug streams should be treated as required versus later follow-up layers.
105. 2026-03-17 08:41 - Added the dedicated future task doc `docs/Phase-Plans/Tasks/Future/04.1 - VR-SP-DBG - Console And Layered Transcript.md` for the new console/debug lane, using `docs/Human-Plans/Architecture/Console.md` as the architecture source, and updated `docs/Human-Plans/roadmap/roadmapCHECKLISTS.md` plus `docs/Doc-Index.md` so `[4.1]` now counts as having its own plan surface.
104. 2026-03-17 08:40 - Reworked the roadmap structure so the console/debug system now owns the new Lane `[4]` and the older deferred control/build/workspace systems shifted out to Lane `[5]`: `docs/Human-Plans/roadmap/roadmap.md` now gives `Console And Layered Transcript` its own dedicated lane, and `docs/Human-Plans/roadmap/roadmapCHECKLISTS.md` now mirrors the new `[4.1]` console lane plus the renumbered `[5.1]` through `[5.6]` deferred systems with updated totals.
103. 2026-03-17 00:41 - Added the new future task doc `docs/Phase-Plans/Tasks/Future/02.4F - VR - General Content Transform Toolbar.md` as the roadmap home for generalizing the shipped reference-only transform toolbar into a viewer-side transform session for any eligible `Content` object, and synced the supporting trackers in `docs/Human-Plans/roadmap/roadmap.md`, `docs/Human-Plans/roadmap/roadmapCHECKLISTS.md`, and `docs/Doc-Index.md`.
102. 2026-03-17 00:33 - Added per-row keyboard-target highlighting to the reference transform toolbar in `src/app/components/ReferenceTransformToolbar.tsx` and `src/app/theme/v15Theme.css`: `m`, `r`, and `s` now highlight all three `X/Y/Z` rows for their section when the center handle is active, while constrained shortcuts like `m -> x`, `r -> z`, or `s -> y` highlight only the matching row; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves the row-highlight behavior across move, rotate, and scale.
101. 2026-03-17 00:28 - Extended the same-mode keyboard toggle behavior in `src/app/components/ReferenceTransformToolbar.tsx` so plain `r` and `s` now match `m`: first press arms the center handle and highlights the section, second press cancels the live drag, restores the pre-transform baseline, clears the active handle, and deselects the section, and the next press re-arms it again; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves the full `r -> r -> r` and `s -> s -> s` flows.
100. 2026-03-17 00:24 - Made plain `m` use one consistent staged center-handle activation path in `src/app/components/ReferenceTransformToolbar.tsx` instead of mixing direct and deferred activation, so after a second-`m` cancel the next `m` reliably re-arms the move center handle as well as reselecting the `Move` section; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves the `m -> m -> m` flow calls the center-handle activation twice with the section deselected in between.
99. 2026-03-17 00:22 - Added a true disarmed cancel state for second-`m`: `src/viewer/gizmo/TransformGizmo.ts`, `src/viewer/Viewer.ts`, and `src/app/viewerBridge.ts` now expose a clear-handle seam, and `src/app/components/ReferenceTransformToolbar.tsx` now uses local section selection so pressing `m` a second time clears the active gizmo handle, restores the pre-move baseline, and deselects the highlighted `Move` section instead of leaving it visually armed; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves the handle-clear call and the cleared section highlight.
98. 2026-03-17 00:19 - Tightened the same-mode keyboard cancel path in `src/app/components/ReferenceTransformToolbar.tsx` so pressing `m` a second time now explicitly clears any leftover pending center-handle activation before canceling the live drag and restoring the pre-move baseline; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves second-`m` reverts an uncommitted move back to its original transform instead of re-triggering move.
97. 2026-03-17 00:14 - Fixed the remaining keyboard-transform mode-switch bug by adding explicit gizmo drag lifecycle control in `src/viewer/gizmo/TransformGizmo.ts`, `src/viewer/Viewer.ts`, and `src/app/components/ReferenceTransformToolbar.tsx`: pending keyboard transforms now cancel the live gizmo drag before reverting on mode switch, and `Enter` / viewport-click commit now explicitly finish the active gizmo drag instead of only clearing the pending flag; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves the toolbar calls the new cancel/complete seams.
96. 2026-03-17 00:09 - Added a pending keyboard-transform commit layer to `src/app/components/ReferenceTransformToolbar.tsx`: keyboard-started move/rotate/scale changes now keep a baseline until committed, `Enter` or a viewport click commits that pending transform, and switching to a different transform mode before commit reverts back to the pre-transform baseline instead of silently saving the previous keyboard transform; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves revert-on-mode-switch and keep-on-Enter behavior.
95. 2026-03-17 00:06 - Added a new header `i` help toggle to the reference transform toolbar in `src/app/components/ReferenceTransformToolbar.tsx` and `src/app/theme/v15Theme.css`: clicking it shows or hides a compact keyboard-shortcuts section above the `Space / Local` row so the transform hotkeys are visible in-context; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves the help block toggles open and closed.
94. 2026-03-17 00:03 - Fixed the reference-transform `r/s` double-press issue in `src/app/components/ReferenceTransformToolbar.tsx` by staging center-handle activation until after the mode change is actually active, and aligned the plain center-handle targets in `src/viewer/Viewer.ts` so plain `r` now drives the rotate outer-ring handle (`E`) while plain `s` drives uniform scale (`XYZ`) on the first press instead of after a second keypress.
93. 2026-03-16 23:59 - Wired plain `r` and `s` into the gizmo center controls too: `src/app/components/ReferenceTransformToolbar.tsx`, `src/app/viewerBridge.ts`, `src/viewer/Viewer.ts`, and `src/viewer/gizmo/TransformGizmo.ts` now make `r` trigger the rotate center handle (`XYZE`) and `s` trigger uniform scale (`XYZ`), while the existing follow-up `x/y/z` shortcuts still constrain each mode to single axes; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves the center-handle calls.
92. 2026-03-16 23:58 - Removed the hotkey conflict that could make reference-transform `r` fall through to viewer `Scale`: `src/viewer/Viewer.ts` now suppresses the legacy viewer `w/e/r` gizmo mode shortcuts whenever a reference transform session is active, so the reference transform toolbar fully owns those mode keys during reference editing.
90. 2026-03-16 23:55 - Extended the keyboard-driven axis path beyond move: `src/app/components/ReferenceTransformToolbar.tsx`, `src/app/viewerBridge.ts`, `src/viewer/Viewer.ts`, and `src/viewer/gizmo/TransformGizmo.ts` now support `r -> x/y/z` for constrained rotate and `s -> x/y/z` for constrained scale, while leaving plain `r` and `s` as mode switches; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves the rotate/scale axis sequences.
89. 2026-03-16 23:47 - Corrected plain `m` move startup in `src/viewer/gizmo/TransformGizmo.ts` and `src/viewer/Viewer.ts` so center move now begins from the gizmo center’s projected screen position instead of using the cursor as the drag-start anchor, which fixes the “object jumps to my mouse” feel and makes keyboard move initiation behave much closer to clicking the middle move gizmo handle.
88. 2026-03-16 23:45 - Adjusted the new keyboard-driven move behavior so plain `m` in `src/app/components/ReferenceTransformToolbar.tsx` / `src/viewer/Viewer.ts` once again only arms the translate gizmo’s center `XYZ` handle instead of forcing an immediate current-pointer drag, while the follow-up `x`, `y`, and `z` shortcuts still use the direct axis-trigger path that felt correct; `src/app/components/ReferenceTransformToolbar.test.tsx` now reflects the split behavior.
87. 2026-03-16 23:42 - Extended the new keyboard-driven translate path so `src/app/components/ReferenceTransformToolbar.tsx`, `src/app/viewerBridge.ts`, `src/viewer/Viewer.ts`, and `src/viewer/gizmo/TransformGizmo.ts` now support axis-constrained move activation too: `m` starts center `XYZ` move, and while `Move` is active the follow-up `x`, `y`, and `z` shortcuts re-fire the same translate drag path on the matching gizmo axis; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves the `m -> y/x/z` sequence.
86. 2026-03-16 23:40 - Upgraded the new `m` translate shortcut path in `src/viewer/gizmo/TransformGizmo.ts` and `src/viewer/Viewer.ts` so it now tries to fire the gizmo’s real center `XYZ` drag immediately from the current viewport pointer instead of only pre-arming the axis; if there is no live viewport pointer yet it still falls back to the earlier arm-only behavior, and the toolbar shortcut tests/typecheck continue to pass.
85. 2026-03-16 23:38 - Wired the reference transform toolbar `m` shortcut through `src/app/components/ReferenceTransformToolbar.tsx`, `src/app/viewerBridge.ts`, `src/viewer/Viewer.ts`, and `src/viewer/gizmo/TransformGizmo.ts` so it now arms the translate gizmo’s center `XYZ` handle instead of only switching modes, which makes the next viewport click use the same center-move action as clicking the gizmo’s middle handle; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves the shortcut also calls the viewer activation path.
84. 2026-03-16 23:31 - Added toolbar-scoped keyboard shortcuts to `src/app/components/ReferenceTransformToolbar.tsx` so `m`, `r`, and `s` switch the active reference transform mode to `Move`, `Rotate`, and `Scale` whenever the transform toolbar is up, while still ignoring active text/select inputs; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves the shortcut path.
83. 2026-03-16 23:28 - Restyled the `Move / Rotate / Scale` section shells in `src/app/theme/v15Theme.css` so they now follow the flatter Browser `Project Browser > Content` section language instead of the earlier mini-card look, keeping the new select-versus-collapse behavior while making the transform toolbar sections read more like native Browser blocks.
82. 2026-03-16 23:26 - Reworked the reference transform toolbar layout in `src/app/components/ReferenceTransformToolbar.tsx` and `src/app/theme/v15Theme.css` so `Move`, `Rotate`, and `Scale` now render as Browser-style collapsible sections with their own header rows, active-section highlighting, and arrow-only collapse/expand behavior; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves header click selects the section while the arrow only collapses it.
81. 2026-03-16 23:22 - Added full edge-and-corner resizing to the reference transform toolbar in `src/app/components/ReferenceTransformToolbar.tsx` and `src/app/theme/v15Theme.css`, so the floating panel can now be resized from all four edges and all four corners while preserving the new internal body scroll behavior.
80. 2026-03-16 23:20 - Added internal overflow handling to the reference transform toolbar in `src/app/theme/v15Theme.css`, giving `.ReferenceTransformToolbar` a Browser-like viewport height cap and making `.ReferenceTransformToolbarBody` scroll vertically once the timeline controls make the panel taller than the available height.
79. 2026-03-16 23:18 - Fixed the hidden `02.4E` row-mode context menu visibility bug in `src/app/theme/v15Theme.css` by removing the reference transform toolbar shell clipping and raising its overlay stack order, since the `Basic / Timeline` menu was rendering inside the toolbar but getting cut off by `overflow: hidden`.
78. 2026-03-16 23:15 - Fixed the `02.4E` row-mode context menu path so right-clicking the actual transform slider fill bar/track in `src/app/components/ParaSlider.tsx` and `src/app/components/ReferenceTransformToolbar.tsx` now reliably opens the `Basic / Timeline` menu instead of depending on the outer row shell; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves a direct right-click on the slider track shows that menu.
77. 2026-03-16 23:11 - Refined `docs/Phase-Plans/Tasks/Future/03.2D - SP-NI-FS-GE - Loft Foundation.md` `Q2` so the first loft contract still stays at `StartProfile + EndProfile -> SolidBody`, while a later lightweight `Intermediate Profiles` numeric control is now captured as a possible generated-section count that rounds/clamps to whole-number internal sections rather than requiring fully authored intermediate profile inputs on day one.
76. 2026-03-16 23:10 - Implemented `02.4E` across `src/app/components/ReferenceTransformToolbar.tsx`, `src/app/components/ReferenceTimelineGraph.tsx`, `src/app/components/ParaSlider.tsx`, `src/app/components/ViewerHost.tsx`, `src/app/references/referenceTimeline.ts`, and `src/app/store/useAppStore.ts`, adding per-channel `Basic / Timeline` mode, speed sliders, editable curve graphs, cycle modes, live timeline evaluation for reference transform channels and rotate snap, and the supporting docs updates that move `docs/Phase-Plans/Tasks/Old/02.4E - VR - Reference Transform Timelines.md` into shipped history.
75. 2026-03-16 23:05 - Expanded `docs/Phase-Plans/Tasks/Future/03.2D - SP-NI-FS-GE - Loft Foundation.md` with code-backed planning questions plus suggested answers, tightening the first loft cut around `Basic` two-profile input, one `SolidBody` output, reuse of the selected-profile compatibility seam, explicit deferral of `RailGuided`, and the concrete feature/schema/compiler/runtime files a real loft feature will need to touch.
74. 2026-03-16 21:56 - Rewrote `docs/Phase-Plans/Tasks/Future/03.2C - SP-NI-FS-GE - Extrude Foundation.md` into an implementation-ready execution spec, locking the first honest extrude contract around one selected profile plus positive depth, one `SolidBody` output, and a public `ExtrudeType` that contains `Basic` plus disabled/deferred `Twist`; then aligned the parent `docs/Phase-Plans/Tasks/Future/03.2 - SP-NI-FS-GE - Foundational Geometry Node System.md` summary so its `[3.2C]` block matches that finalized first-pass contract.
73. 2026-03-16 22:49 - Tightened `docs/Phase-Plans/Tasks/Future/02.4E - VR - Reference Transform Timelines.md` so `2.4E.Q5` now explicitly says the first-pass timeline graph must support real curve-handle editing, not just point placement plus fixed interpolation presets.
72. 2026-03-16 22:47 - Added the new future task doc `docs/Phase-Plans/Tasks/Future/02.4E - VR - Reference Transform Timelines.md` as the first planning home for the post-`02.4D` `Basic / Timeline` transform-value idea, and synced the roadmap/index trackers so `[2.4E]` now exists in `docs/Human-Plans/roadmap/roadmap.md`, `docs/Human-Plans/roadmap/roadmapCHECKLISTS.md`, and `docs/Doc-Index.md`.
71. 2026-03-16 22:42 - Refined locked-camera scale follow again in `src/viewer/Viewer.ts` and `src/viewer/scene/CameraController.ts` so it now preserves the user’s existing orbit-target offset at lock time instead of forcing scale tracking to re-center on the object anchor, which fixes the remaining “scale up still feels like zoom in” case when the camera was locked from an offset view; `src/viewer/scene/CameraController.test.ts` now proves scaling preserves that target offset while the camera distance scales outward correctly.
70. 2026-03-16 22:38 - Corrected locked-camera scale follow for reference transforms in `src/viewer/Viewer.ts` by anchoring scale tracking to the reference pivot/world position instead of the drifting bounds center, which fixes the “scale up zooms in” feel on off-center references while still preserving the current zoom ratio; `src/viewer/scene/CameraController.test.ts` now includes an off-center pivot regression to prove the camera target stays anchored while distance scales outward correctly.
69. 2026-03-16 22:31 - Added a split-feedback path for rotate snap dragging in `src/app/components/ParaSlider.tsx` and enabled it from `src/app/components/ReferenceTransformToolbar.tsx`: when rotate snap is on, the blue fill bar now follows a continuous wrapped drag preview while the white marker and stored value stay snapped, which makes snapped rotate dragging feel much less sticky; `src/app/components/ParaSlider.test.tsx` now proves the fill can move past the snapped marker while the snapped value remains unchanged until the threshold is crossed.
68. 2026-03-16 22:27 - Corrected snapped rotate dragging in `src/app/components/ParaSlider.tsx` so drag quantization is now based on the delta from the current live value instead of the absolute `-180..180` grid origin, which fixes the jumpy feel when rotate snap is enabled; `src/app/components/ParaSlider.test.tsx` now proves a snapped wrapped drag from `121` with step `5` lands at `126` rather than snapping to the wrong global bucket.
67. 2026-03-16 22:25 - Added wrapped relative scrubbing for rotate rows in `src/app/components/ParaSlider.tsx` and enabled it from `src/app/components/ReferenceTransformToolbar.tsx`, so dragging a rotate slider past `-180/180` now keeps rotating through the range instead of hard-stopping at the clamp; normal wrapped drag uses full-rate motion while `Shift` still drops to fine mode, and `src/app/components/ParaSlider.test.tsx` now proves dragging left from `-180` wraps back through the bar instead of freezing.
66. 2026-03-16 22:20 - Corrected shared slider step-button behavior in `src/app/components/ParaSlider.tsx` so step increments are now relative additions/subtractions from the current live value instead of re-quantizing against the absolute base range, which fixes rotate snap stepping like `121 -> 126` at snap `5` instead of `125`; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves that exact case.
65. 2026-03-16 22:18 - Adjusted `src/app/components/ParaSlider.tsx` so the slider preserves the incoming live value as-is instead of immediately re-quantizing it to the current `step`, which fixes rotate snap changes from normalizing an existing `X/Y/Z` rotation; future edits still quantize to the active step, and `src/app/components/ReferenceTransformToolbar.test.tsx` now proves changing snap from a live `13°` rotation leaves that value untouched until the next edit.
64. 2026-03-16 22:16 - Made the rotate `X/Y/Z` rows in `src/app/components/ReferenceTransformToolbar.tsx` adopt the active rotate snap interval as their shared `ParaSlider` step whenever `Snap` is enabled, so button stepping and drag quantization now land on the chosen snap increment instead of the default `1°`; `src/app/components/ReferenceTransformToolbar.test.tsx` now proves a `22.5` snap causes the rotate `X` row to step straight to `22.5`.
63. 2026-03-16 22:14 - Reworked rotate snap in `src/app/components/ReferenceTransformToolbar.tsx` to use the shared `ParaSlider` template instead of a standalone number box: when `Snap` is enabled, the preset buttons remain and a new `Snap` slider now appears above the `X` row, with the old inline snap input removed and the toolbar test updated to type through the slider’s shared value-edit path.
62. 2026-03-16 22:12 - Tightened the new rotate snap layout in `src/app/components/ReferenceTransformToolbar.tsx` so the numeric snap input now lives inline in the same row as the `Rotate` and `Snap` controls, leaving the expanded snap strip for presets only; matching spacing adjustments landed in `src/app/theme/v15Theme.css`.
61. 2026-03-16 22:11 - Added rotate-only snap controls to `src/app/components/ReferenceTransformToolbar.tsx` using the existing gizmo snap path: a small `Snap` toggle now lives beside `Rotate`, enabling a preset strip (`1, 5, 11.25, 15, 22.5, 30, 45, 90, /2, x2`) plus a right-side numeric input that all drive `getViewer()?.setGizmoSnap({ rotateDeg })`; the new strip is styled in `src/app/theme/v15Theme.css`, and `src/app/components/ReferenceTransformToolbar.test.tsx` now proves toggle, preset, and typed snap updates.
60. 2026-03-16 22:04 - Added inline numeric entry to `src/app/components/ParaSlider.tsx` so clicking the right-side slider value now swaps it into a typed number field in normal mode, committing on `Enter`/blur and canceling on `Escape`; this uses the same fine-step precision as `Shift` drag for manual decimals and is styled in `src/app/theme/v15Theme.css`, with a new direct commit test in `src/app/components/ParaSlider.test.tsx`.
59. 2026-03-16 22:01 - Refined the new `Shift` fine-tune drag path in `src/app/components/ParaSlider.tsx` so it now lowers quantization as well as pointer sensitivity, which lets integer-step transform sliders update in decimal increments like `5.1`/`5.10` during fine adjustment instead of staying snapped to whole numbers; also tightened the proof in `src/app/components/ParaSlider.test.tsx` around an integer-step slider landing on a decimal value.
58. 2026-03-16 21:58 - Added fine-tune slider dragging to `src/app/components/ParaSlider.tsx` so holding `Shift` during a value drag switches to a relative `0.1x` sensitivity path instead of full cursor-to-track remapping, which makes transform sliders easier to micro-adjust without disturbing normal drag behavior; also added a direct proof in `src/app/components/ParaSlider.test.tsx`.
57. 2026-03-16 21:56 - Expanded `docs/Phase-Plans/Tasks/Future/03.2C - SP-NI-FS-GE - Extrude Foundation.md` with the next real extrude-planning questions plus code-backed suggested answers, focusing the phase on the current `depth/taper/offset` UI versus depth-only runtime reality, the first `ExtrudeType` boundary, the selected-profile source seam, the one-`SolidBody` output contract, and the concrete schema/compiler/runtime files `[3.2C]` will need to touch.
56. 2026-03-16 21:52 - Corrected the two remaining locked-camera issues in the reference transform flow: `src/viewer/Viewer.ts` now records the locked object’s center/size without moving the camera when the lock button is clicked, `src/viewer/scene/CameraController.ts` now scales camera distance from the actual camera-to-object-center baseline instead of the orbit target, and `src/app/components/ReferenceTransformToolbar.test.tsx` plus `src/viewer/scene/CameraController.test.ts` now prove both “lock does not frame on click” and the corrected scale-follow math.
55. 2026-03-16 21:49 - Refined locked-camera scale behavior in `src/viewer/Viewer.ts` and `src/viewer/scene/CameraController.ts` so scale changes now preserve the user’s current zoom ratio by scaling camera distance proportionally with object size instead of snapping back to a fresh fit, and added a new `trackScaledObject(...)` proof in `src/viewer/scene/CameraController.test.ts`.
54. 2026-03-16 21:46 - Refined the new reference camera-lock behavior so `Move` now only tracks the object center without changing zoom while `Rotate` and `Scale` still auto-fit: added `trackObject(...)` to `src/viewer/scene/CameraController.ts`, switched the locked-reference viewer path in `src/viewer/Viewer.ts` to choose track-vs-frame based on the active gizmo mode, and expanded `src/viewer/scene/CameraController.test.ts` to prove object tracking preserves camera distance.
53. 2026-03-16 21:43 - Added a new camera-follow lock action beside the magnifier in the floating reference transform toolbar by extending `src/app/viewerBridge.ts` and `src/viewer/Viewer.ts` with `setReferenceCameraLock(referenceId | null)` plus viewer-side auto-framing on reference transform changes, then wiring a new lock header button and glyph in `src/app/components/ReferenceTransformToolbar.tsx` / `src/app/theme/v15Theme.css`; also expanded `src/app/components/ReferenceTransformToolbar.test.tsx` to prove the button locks the camera to the active reference id.
52. 2026-03-16 21:40 - Added a reference-only frame action to the floating transform toolbar by extending `src/app/viewerBridge.ts` and `src/viewer/Viewer.ts` with `frameReference(referenceId)`, then wiring a new magnifying-glass header button into `src/app/components/ReferenceTransformToolbar.tsx` with matching glyph styling in `src/app/theme/v15Theme.css`; also expanded `src/app/components/ReferenceTransformToolbar.test.tsx` to prove the new button frames the active reference id.
51. 2026-03-16 21:36 - Corrected the clamp-mode color roles in `src/app/theme/v15Theme.css` for the floating reference transform toolbar so the value fill stays blue during clamp editing while the clamp-range bar remains the orange layer.
50. 2026-03-16 21:36 - Fixed the clamp-mode fill anchoring in `src/app/components/ParaSlider.tsx` so the active fill bar now always starts from the true zero side of the base range instead of attaching itself to the min clamp edge, and added a new assertion in `src/app/components/ParaSlider.test.tsx` to prove clamp edit mode keeps the fill at `left: 0%` with width based on the real value.
49. 2026-03-16 21:34 - Extended `src/app/components/ParaSlider.tsx` with display-text overrides and used them in `src/app/components/ReferenceTransformToolbar.tsx` so clamp mode now shows the min clamp value on the left and the max clamp value on the right for each transform slider instead of the normal axis/current-value readout; also tightened `src/app/components/ReferenceTransformToolbar.test.tsx` to prove the first clamp-mode row swaps to `-300.00 / 300.00`.
48. 2026-03-16 21:32 - Fixed the reference-toolbar clamp-range visibility regression in `src/app/theme/v15Theme.css` by restoring the orange clamp-range bar only for `Edit Clamp` mode while keeping that layer transparent during normal mode, which brings back the intended clamp-edit visual without the extra dark block during regular editing.
47. 2026-03-16 21:31 - Corrected the reference-toolbar clamp styling in `src/app/theme/v15Theme.css` so clamp mode keeps an orange active fill bar while the separate dark clamp-range block remains removed, matching the intended “orange clamp mode, blue normal mode” split more closely.
46. 2026-03-16 21:21 - Removed the extra clamp-range fill block from the floating reference transform toolbar in `src/app/theme/v15Theme.css` by overriding the toolbar-scoped `ParaSliderClampRange` background to transparent, keeping the blue value fill and orange clamp-end bars without the additional dark range bar.
45. 2026-03-16 21:20 - Reverted the immediately previous reference-transform slider face tweak in `src/app/theme/v15Theme.css`, restoring the right-side numeric readout and the original spaced slider content layout after that simplification turned out to be the wrong direction.
44. 2026-03-16 21:20 - Simplified the reference transform toolbar slider face in `src/app/theme/v15Theme.css` by hiding the right-side numeric readout and left-anchoring the slider content, so clamp mode now shows only the visual range/value treatment instead of duplicating the current value as separate text on every row.
43. 2026-03-16 21:18 - Fixed the missing clamp-bar visuals in the floating reference transform toolbar by widening the `ParaSliderClampHandle` CSS selectors in `src/app/theme/v15Theme.css` so the toolbar inherits the same thick orange end-bar styling as the panel-shell sliders instead of falling through to unstyled clamp buttons.
42. 2026-03-16 21:16 - Restyled the shared `ParaSlider` clamp handles in `src/app/theme/v15Theme.css` from small orange pills into larger orange end bars so clamp-edit mode now shows much clearer grab targets at the left and right edges of the allowed range in both the Spaghetti `i` sliders and the reference transform toolbar.
41. 2026-03-16 21:15 - Ported the Spaghetti-style `Edit Clamp` workflow into `src/app/components/ReferenceTransformToolbar.tsx` by adding a top-row clamp toggle and local per-axis clamp ranges for all `Move / Rotate / Scale` sliders, so the toolbar now enters the same orange clamp-edit mode as the `i` menu sliders; also expanded `src/app/components/ReferenceTransformToolbar.test.tsx` to prove clamp mode switches to `Done Clamp` and exposes clamp handles.
40. 2026-03-16 21:12 - Removed the leftover duplicate bottom `Local / World` and `Reset Transform` section from `src/app/components/ReferenceTransformToolbar.tsx`, leaving the top control row as the only toolbar home for those actions.
39. 2026-03-16 21:09 - Rewrote `docs/Phase-Plans/Tasks/Future/03.2B - SP-NI-FS-GE - Sketch Operation Authoring.md` into an implementation-ready execution spec, locking the first-pass sketch authoring workflow, `Rectangle` / `Circle` authored storage and lowering rules, viewer overlay/rendering ownership, managed `closeProfile` compatibility seam, implementation order, acceptance criteria, and test plan; then aligned the parent `docs/Phase-Plans/Tasks/Future/03.2 - SP-NI-FS-GE - Foundational Geometry Node System.md` summary so its `[3.2B]` block matches the finalized tool set and profile-review compatibility direction.
38. 2026-03-16 21:10 - Tightened the top control row in `src/app/components/ReferenceTransformToolbar.tsx` and `src/app/theme/v15Theme.css` by adding an explicit `Space` label for the `Local / World` toggle and anchoring `Reset Transform` to the far right so the toolbar header controls read more clearly.
37. 2026-03-16 21:09 - Tightened the floating reference transform toolbar again in `src/app/components/ReferenceTransformToolbar.tsx` and `src/app/theme/v15Theme.css` by moving the `Local / World` plus `Reset Transform` controls up under the header, restyling the toolbar buttons into a darker app-matching action language, and adding a scale-lock toggle so changing one scale slider can drive `X / Y / Z` together; also expanded `src/app/components/ReferenceTransformToolbar.test.tsx` to prove the lock updates all three scale axes.
36. 2026-03-16 21:06 - Reworked the floating reference transform toolbar in `src/app/components/ReferenceTransformToolbar.tsx` and `src/app/theme/v15Theme.css` to replace the nine static readouts with live `ParaSlider` controls that match the existing title-bar slider language, wired each `Move / Rotate / Scale` axis slider directly into `setReferenceTransformOverride(...)`, and added `src/app/components/ReferenceTransformToolbar.test.tsx` to prove the first slider updates the active reference transform override in store.
35. 2026-03-16 20:59 - Locked the post-draw `Sketch` row flow across `docs/Phase-Plans/Tasks/Future/03.2 - SP-NI-FS-GE - Foundational Geometry Node System.md` and `03.2B - SP-NI-FS-GE - Sketch Operation Authoring.md`, so confirming draw mode now reopens the spaghetti editor and populates the `Draw` area with the authored entities, while the next `Close / Select Profile` row uses a `Review` action that re-enters the viewer in profile-review mode instead of another add-style `+` control.
34. 2026-03-16 21:00 - Restyled the floating reference transform toolbar in `src/app/components/ReferenceTransformToolbar.tsx` and `src/app/theme/v15Theme.css` into a tighter vertical inspector stack, replacing the three-column matrix with one `Move` / `Rotate` / `Scale` row list and restyling the nine value boxes to borrow the darker bordered Spaghetti number-field look instead of the earlier generic pill treatment.
33. 2026-03-16 20:57 - Tightened the floating reference transform toolbar matrix in `src/app/components/ReferenceTransformToolbar.tsx` and `src/app/theme/v15Theme.css` so it now shows all nine values at once as three vertical stacks under `Move`, `Rotate`, and `Scale` instead of only one mode-dependent `X / Y / Z` strip.
32. 2026-03-16 20:54 - Reworked the floating reference transform toolbar layout in `src/app/components/ReferenceTransformToolbar.tsx` and `src/app/theme/v15Theme.css` so `Move / Rotate / Scale` now sit together with live `X / Y / Z` value readouts under them, `Local` plus `Reset Transform` live in a second control section, and `Close` became a simple title-bar `X` in the dark blue drag header.
31. 2026-03-16 20:51 - Upgraded the floating reference transform toolbar shell in `src/app/components/ReferenceTransformToolbar.tsx` and `src/app/theme/v15Theme.css` so it now has a dark blue Browser-style drag header and local drag state, letting the user reposition the toolbar by its header instead of leaving it fixed in place.
30. 2026-03-16 20:49 - Fixed the new floating reference transform toolbar hit-target in `src/app/components/ReferenceTransformToolbar.tsx` and `src/app/theme/v15Theme.css` by making it a real viewport overlay widget with `pointer-events: auto`, so the toolbar buttons are now clickable instead of rendering as a visible but inert overlay during `2.4D` transform mode.
29. 2026-03-16 20:46 - Locked the first `Draw` row and floating sketch-toolbar plan across `docs/Phase-Plans/Tasks/Future/03.2 - SP-NI-FS-GE - Foundational Geometry Node System.md` and `03.2B - SP-NI-FS-GE - Sketch Operation Authoring.md`, so entering `Draw` now minimizes/collapses the spaghetti editor, opens a floating viewport toolbar with `Line / Arc / Spline / Rectangle / Circle` tools, and explicitly relies on ParaHook viewer-side overlay rendering for sketch lines, points, handles, and preview geometry instead of Replicad display.
28. 2026-03-16 20:45 - Implemented `[2.4D]` across `src/app/store/useAppStore.ts`, `src/app/panels/selectBrowserTreeRows.ts`, `src/app/panels/browserRowActions.ts`, `src/app/panels/BrowserPanel.tsx`, `src/app/components/ViewerHost.tsx`, `src/viewer/Viewer.ts`, `src/viewer/gizmo/TransformGizmo.ts`, `src/app/components/ReferenceTransformToolbar.tsx`, `src/app/components/ViewportOverlay.tsx`, and related tests/styles, shipping right-click `Transform Object` on reference rows, one-at-a-time reference-exclusive gizmo ownership, a floating transform toolbar, session-only reference transform overrides, reset/close behavior, and the matching phase-doc close-out by moving `docs/Phase-Plans/Tasks/02.4D - VR - Reference Transform Controls.md` into `Tasks/Old/` while aligning the `VR` family doc, roadmap, and docs index.
27. 2026-03-16 20:42 - Locked the `SketchPlane` row as a true graph-input row across `docs/Phase-Plans/Tasks/Future/03.2 - SP-NI-FS-GE - Foundational Geometry Node System.md` and `03.2A - SP-NI-FS-GE - Data Types And Sketch Foundation.md`, so the row now explicitly keeps its normal input pin for upstream wired plane assignment while also exposing the right-side `+` action for local viewer plane picking when no wire is driving it.
26. 2026-03-16 20:40 - Locked the first neat-row `Sketch` input shape across `docs/Phase-Plans/Tasks/Future/03.2 - SP-NI-FS-GE - Foundational Geometry Node System.md` and `03.2A - SP-NI-FS-GE - Data Types And Sketch Foundation.md`, so `SketchPlane` now reads as its own input row with a right-side `+` action that collapses/minimizes the editor into viewer plane-pick mode, shows ghost origin-centered `XY` / `XZ` / `YZ` plane-grid previews, and defers arbitrary face-picked planes until later.
25. 2026-03-16 20:35 - Locked the first `Sketch in Viewer` planning direction across `docs/Phase-Plans/Tasks/Future/03.2 - SP-NI-FS-GE - Foundational Geometry Node System.md` and `03.2B - SP-NI-FS-GE - Sketch Operation Authoring.md`, framing viewer sketching as a dedicated sketch-only toolbar plus viewport-edit mode inside the `Sketch` node's `Draw/Edit Curves Inside` section, with one shared sketch state between the node body and viewer and the first-pass tool set limited to line / arc / spline / close profile / profile select plus basic delete/cancel actions.
24. 2026-03-16 20:31 - Locked the user-facing `Sketch` hierarchy across `docs/Phase-Plans/Tasks/Future/03.2 - SP-NI-FS-GE - Foundational Geometry Node System.md` and `03.2A - SP-NI-FS-GE - Data Types And Sketch Foundation.md`, so the `[3.2]` lane now explicitly targets one `Sketch` node shaped as `Inputs -> Draw/Edit Curves Inside -> Close/Select Profile -> Outputs`, with `[3.2A]` owning only the shell plus empty internal authoring surface and `[3.2B]` owning the actual draw/close/select behavior.
23. 2026-03-16 20:18 - Promoted `docs/Phase-Plans/Tasks/Future/02.4D - VR - Reference Transform Controls.md` into `docs/Phase-Plans/Tasks/` and rewrote it into an implementation-ready execution spec, locking the right-click `Transform Object` entry, floating reference-only transform toolbar, one-reference-at-a-time gizmo ownership, hidden-reference auto-show/load behavior, workspace-session transform overrides, and the reset/exit rules; then aligned the `VR` family doc, docs index, and roadmap checklist so `[2.4D]` now reads as the active transform-controls follow-up with decision coverage complete.
22. 2026-03-16 20:19 - Locked the first code-backed `[3.2A]` answers in `docs/Phase-Plans/Tasks/Future/03.2A - SP-NI-FS-GE - Data Types And Sketch Foundation.md`, narrowing the immediate type set to the proven `Plane / Vec2 / profile / solid` path, making `SketchCurves` optional, deferring `Transform2D` and explicit curve-reference modeling, and recording the current schema/registry/feature-stack seams the first `Sketch` shell should wrap.
21. 2026-03-16 20:18 - Created `docs/Phase-Plans/Tasks/Future/02.4D - VR - Reference Transform Controls.md` as the dedicated future planning surface for the next reference-workspace carry-forward, capturing the right-click `Transform Object` entry, floating toolbar direction, and the remaining transform-state questions; then updated `docs/Doc-Index.md` and `docs/Human-Plans/roadmap/roadmapCHECKLISTS.md` so `[2.4D]` now reads as having a real task doc while still keeping its decisions open.
20. 2026-03-16 20:14 - Implemented `[2.4C]` across `src/app/references/importReferenceFile.ts`, `src/app/store/useAppStore.ts`, `src/app/panels/selectBrowserTreeRows.ts`, `src/app/panels/BrowserPanel.tsx`, `src/app/components/ViewerHost.tsx`, `src/viewer/Viewer.ts`, and the related tests, shipping the first workspace-only user reference import flow with a `Content` header `+` import menu, a `References > User References` Browser bucket, direct retry/remove row actions, duplicate-name disambiguation, and object-URL-backed viewer loading/removal; then moved the completed task spec into `docs/Phase-Plans/Tasks/Old/` and aligned the `VR` family/doc-index/checklist docs.
19. 2026-03-16 20:12 - Created dedicated future task docs `docs/Phase-Plans/Tasks/Future/03.2A - SP-NI-FS-GE - Data Types And Sketch Foundation.md`, `03.2B - SP-NI-FS-GE - Sketch Operation Authoring.md`, `03.2C - SP-NI-FS-GE - Extrude Foundation.md`, and `03.2D - SP-NI-FS-GE - Loft Foundation.md`, then updated the parent `03.2` task doc so the foundational geometry lane now has one separate execution-planning surface per subphase.
18. 2026-03-16 20:11 - Expanded the `[3.2] Foundational Geometry Node System` block in `docs/Human-Plans/roadmap/roadmap.md` into explicit checklist breakdowns for `[3.2A]` through `[3.2D]`, so the roadmap now captures the intended acceptance shape for `Data Types / Sketch Foundation`, `Sketch Operation Authoring`, `Extrude Foundation`, and `Loft Foundation` instead of only listing one combined lane checklist.
17. 2026-03-16 20:02 - Extended the top reach of the first root `Content` guide in `src/app/theme/v15Theme.css`, so the left shared vertical line now runs higher into the `Content` chevron area after the user-centered `1.5px` guide alignment tweak.
16. 2026-03-16 20:00 - Shifted the first Browser `Content` guide column itself left in `src/app/theme/v15Theme.css`, so the shared vertical guide under `Content` moves visibly toward the chevron center instead of only adjusting the line inside the same fixed column box.
15. 2026-03-16 20:00 - Nudged the first Browser `Content` tree-guide column left in `src/app/theme/v15Theme.css`, so the shared vertical guide for `References` / assembly rows sits closer to the `Content` chevron center instead of reading slightly too far right.
14. 2026-03-16 19:59 - Removed the extra `Content` summary trunk line in `src/app/theme/v15Theme.css`, so the vertical guide that dropped directly out of the open `Content` chevron is gone while the row-level Browser tree guides remain for the next cleanup pass.
13. 2026-03-16 19:57 - Reverted the prior single-trunk `Content` guide simplification in `src/app/panels/selectBrowserTreeRows.ts` and `src/app/panels/selectBrowserTreeRows.test.ts`, restoring the earlier near-target composed guide state where the outer `Content` parent column carries through the `References` and assembly subtrees.
12. 2026-03-16 19:46 - Promoted `docs/Phase-Plans/Tasks/Future/02.4C - VR - User Reference Import From Disk.md` into `docs/Phase-Plans/Tasks/` and rewrote it into an implementation-ready execution spec, then aligned the `VR` family doc, docs index, and roadmap checklist so `[2.4C]` now reads as the active user-import follow-up instead of a future planning note.
11. 2026-03-16 19:53 - Simplified the `Content` hierarchy-line composition in `src/app/panels/selectBrowserTreeRows.ts` and `src/app/panels/selectBrowserTreeRows.test.ts` so the Browser is back to one parent vertical trunk from `Content`: only the root `References` / assembly rows now branch off that trunk, while deeper rows keep only their own subtree guides instead of carrying a second outer ancestor column.
10. 2026-03-16 19:50 - Reworked the `Content` tree-guide composition in `src/app/panels/selectBrowserTreeRows.ts`, `src/app/theme/v15Theme.css`, and `src/app/panels/selectBrowserTreeRows.test.ts` so the outer `Content` parent column now carries through the full `References` and assembly subtrees instead of only touching the first root rows, and any leaf row can now extend its final elbow through the hidden expand slot into the icon.
9. 2026-03-16 19:46 - Simplified the planned `[2.4C]` imported-row actions in `docs/Phase-Plans/Tasks/Future/02.4C - VR - User Reference Import From Disk.md` so rows use a direct `X` remove button and a conditional retry icon on errored rows, while any richer right-click menu stays only as later overflow capacity if needed.
8. 2026-03-16 19:46 - Replaced the earlier `[2.4C]` `References` row `...` import-entry idea in `docs/Phase-Plans/Tasks/Future/02.4C - VR - User Reference Import From Disk.md` with a cleaner first-pass `Content` box top-right `+` button that opens the accepted-format import menu, keeping the `References` tree itself visually cleaner.
7. 2026-03-16 19:43 - Tightened the new `Content` tree-shell pass across `src/app/panels/selectBrowserTreeRows.ts`, `src/app/panels/BrowserPanel.tsx`, `src/app/theme/v15Theme.css`, and `src/app/panels/selectBrowserTreeRows.test.ts` so the `Content` trunk now reaches up into the opened chevron, direct root children like `References` and `Assembly 1` get real selector-owned `tee` / `elbow` connectors, and root leaf rows can carry that elbow through the hidden expand slot into the icon.
6. 2026-03-16 19:42 - Added a dedicated `Content` section trunk line in `src/app/panels/BrowserPanel.tsx` and `src/app/theme/v15Theme.css`, so the open `Content` chevron now drops one vertical connector down into the root `References` / assembly rows instead of leaving that top tree seam visually disconnected.
5. 2026-03-16 19:40 - Locked the first `[2.4C]` import-entrypoint refinement in `docs/Phase-Plans/Tasks/Future/02.4C - VR - User Reference Import From Disk.md`, so the `References` row right-click / overflow `...` menu is the only first-pass import surface and no secondary toolbar import action is needed in this phase.
4. 2026-03-16 19:38 - Locked the first `[2.4C]` file-type restriction in `docs/Phase-Plans/Tasks/Future/02.4C - VR - User Reference Import From Disk.md`, so user import stays limited to `.step`, `.stl`, `.obj`, and `.glb`, matching the already-supported viewer loaders.
3. 2026-03-16 19:36 - Locked the first `[2.4C]` remove-behavior answer in `docs/Phase-Plans/Tasks/Future/02.4C - VR - User Reference Import From Disk.md`, so remove means true workspace removal while visibility stays a separate row-click toggle like the built-in reference rows.
2. 2026-03-16 19:34 - Added the likely first `[2.4C]` error-handling answer in `docs/Phase-Plans/Tasks/Future/02.4C - VR - User Reference Import From Disk.md`, so failed imports stay visible as retryable `Error` rows instead of disappearing or silently failing.
1. 2026-03-16 19:32 - Locked the first `[2.4C]` duplicate-handling answer in `docs/Phase-Plans/Tasks/Future/02.4C - VR - User Reference Import From Disk.md`, so duplicate imports are allowed in the first pass, both rows stay visible, and Browser labels should auto-disambiguate instead of hard-blocking the second import.

<!-- ============================================================ -->
### Chill Batch #5
<!-- ============================================================ -->

Status:
- closed

Started:
- 2026-03-12 20:32

Ended:
- 2026-03-16 19:32

Mode:
- chill mode active

Consolidated To:
- pending

Current Goal:
- track the current UI cleanup wave around the Browser shell, docked/floating panel behavior, meatball editor presentation, and left-dock workspace polish without creating a permanent changelog entry for every small pass

Entries:
187. 2026-03-16 18:16 - Created the new future task docs `docs/Phase-Plans/Tasks/Future/03.1 - NI - Node System Cleanup And Growth.md` and `docs/Phase-Plans/Tasks/Future/03.2 - SP-NI-FS-GE - Foundational Geometry Node System.md`, seeding both with progress checklists, scope boundaries, source inputs, architecture-spec framing, and the first working breakdown of the new Lane `[3.1]` / `[3.2]` roadmap slices.
186. 2026-03-16 18:07 - Reworked Lane `[3]` in `docs/Human-Plans/roadmap/roadmap.md` so the new foundational geometry-authoring cluster now has its own `[3.2]` roadmap home between generic node cleanup and later wire/driver/part/feature hardening, adding a dedicated `SP / NI / FS / GE` sub-lane for `Data Types`, `Sketch`, `Extrude`, and `Loft` and shifting the later Lane `[3]` numbering down by one.
185. 2026-03-16 17:59 - Added a temporary `First Four Phase Draft` section to the bottom of `docs/Human-Plans/Architecture/Spaghetti-Types.md`, breaking the first four numbered checklist blocks (`Data Types`, `Sketch`, `Extrude`, `Loft`) into staged implementation waves so the foundational node-system work can be discussed in phases without splitting into a separate planning doc yet.
184. 2026-03-16 17:54 - Replaced the deferred `Pattern` node in `docs/Human-Plans/Architecture/Spaghetti-Types.md` with a ParaHook-side `Array` wrapper node and broke it into `Linear`, `Circular`, and `Path` array modes, matching the current plan to treat repetition as a higher-level custom feature rather than a direct Replicad command name.
183. 2026-03-16 17:49 - Expanded the numbered foundational checklist in `docs/Human-Plans/Architecture/Spaghetti-Types.md` with the next feature-node candidates after `Sketch`, `Extrude`, and `Loft`, marking `Boolean`, `Fillet`, `Shell`, and `Sweep` as the strongest first-round additions while leaving `Mirror`, `Pattern`, `TrimSplit`, and `Revolve` deferred.
182. 2026-03-16 17:42 - Added simple hierarchical numbering across the `Simple Sketch / Extrude / Loft Node Checklist` in `docs/Human-Plans/Architecture/Spaghetti-Types.md`, so the data types, node sections, and subitems can now be counted and referenced more easily during planning.
181. 2026-03-16 17:38 - Reworked the `Loft` section in `docs/Human-Plans/Architecture/Spaghetti-Types.md` around the same mode-based user-facing design as `Extrude`, so it now starts with `in:LoftType - [Enum]`, groups the required inputs for `Basic` versus `RailGuided` lofting, and publishes one main `out:SolidBody - [SolidBody]` result.
180. 2026-03-16 17:33 - Reworked the `Extrude` section in `docs/Human-Plans/Architecture/Spaghetti-Types.md` around a mode-based user-facing design, so it now starts with `in:ExtrudeType - [Enum]`, separates the required inputs for `Basic` versus `Twist` extrusion, collapses back to one main `out:SolidBody - [SolidBody]` result, and adds `[Enum]` to the top data-types list to support that selector.
179. 2026-03-16 17:24 - Restyled the `Loft` block in `docs/Human-Plans/Architecture/Spaghetti-Types.md` to match the newer `Sketch` / `Extrude` contract format, so it now uses the same `# / ## / ###` ladder with direction-first `in:` and `out:` rows instead of the older flat `Inputs` / `Outputs` bullet list.
178. 2026-03-16 17:21 - Restyled the `Extrude` block in `docs/Human-Plans/Architecture/Spaghetti-Types.md` to match the newer `Sketch` contract format, so it now uses the same `# / ## / ###` ladder with direction-first `in:` and `out:` rows instead of the older flat `Inputs` / `Outputs` bullet list.
177. 2026-03-16 17:18 - Restyled the `Extrude` block in `docs/Human-Plans/Architecture/Spaghetti-Types.md` to match the newer `Sketch` contract format, so it now uses the same `# / ## / ###` ladder with direction-first `in:` and `out:` rows instead of the older flat `Inputs` / `Outputs` bullet list.
176. 2026-03-16 17:13 - Renamed the top `Param Types` section in `docs/Human-Plans/Architecture/Spaghetti-Types.md` to `Data Types` and added the matching accepted terminology decision to `docs/Human-Plans/Architecture/Terminology-Decisions.md`, so the broader type-system language is now locked in one planning doc and one terminology doc.
175. 2026-03-16 17:08 - Finished restyling the top `Param Types` block in `docs/Human-Plans/Architecture/Spaghetti-Types.md` into the same heading-like ladder the user started, so all type groups now use `- ## Group Name` and every type entry uses `- ### [ ] [Type]` instead of mixing one converted row with older flat bullets.
174. 2026-03-16 17:03 - Restyled the top `Param Types` list in `docs/Human-Plans/Architecture/Spaghetti-Types.md` so the type names themselves now stay in square brackets (`[Plane]`, `[Float]`, `[SketchCurveRef]`, etc.), matching the newer sketch-contract formatting and preserving the preferred orange visual treatment.
173. 2026-03-16 16:58 - Corrected the parent `Sketch` input-row word order in `docs/Human-Plans/Architecture/Spaghetti-Types.md` so those setup entries now read as `in:SketchPlane - [Plane]`, `in:SketchPlace - [Transform2D]`, and so on, matching the same direction-first pattern already used by the output rows and operation mini-contracts.
172. 2026-03-16 16:55 - Removed the backticks from the parent `Sketch` checklist markers in the `Inputs` / `Outputs` block inside `docs/Human-Plans/Architecture/Spaghetti-Types.md` so those rows now visually match the plain checklist treatment used by the `Operation subnodes`.
171. 2026-03-16 16:52 - Corrected the parent `Sketch` `Inputs` / `Outputs` rows in `docs/Human-Plans/Architecture/Spaghetti-Types.md` so they now use `in:` for consumed setup values and `out:` for published sketch results, bringing the section labels back in line with the actual direction of data flow.
170. 2026-03-16 16:46 - Restyled the `Sketch` `Inputs` block in `docs/Human-Plans/Architecture/Spaghetti-Types.md` into the same compact typed-row pattern as the primitive/source block, so the setup helpers now show their produced value directly (`out:[Plane]`, `out:[Transform2D]`, etc.) instead of reading as bare names only.
169. 2026-03-16 16:39 - Tightened the `Operation subnodes` field/type format in `docs/Human-Plans/Architecture/Spaghetti-Types.md` so the sketch-operation rows now consistently read as `in:Field - [Type]` and `out:Field - [Type]`, keeping the compact line style while removing the extra square brackets around field names only.
168. 2026-03-16 16:24 - Removed the square brackets from the type names across the compact `Operation subnodes` mini-contracts in `docs/Human-Plans/Architecture/Spaghetti-Types.md`, so the sketch operation rows now consistently read as `in:[Field] - Type` and `out:[Field] - Type` instead of mixing `Type` with `[Type]`.
167. 2026-03-16 16:19 - Simplified the `Operation subnodes` block in `docs/Human-Plans/Architecture/Spaghetti-Types.md` into the compact two-line mini-contract style, so each sketch operation now reads as `### NodeName` followed by inline `in:[Field] - [Type]` and `out:[Field] - [Type]` rows instead of the larger nested `Inputs` / `Outputs` tree.
166. 2026-03-16 16:14 - Normalized the `Operation subnodes` block in `docs/Human-Plans/Architecture/Spaghetti-Types.md` to the stronger heading style the updated `SketchLine` established, so every sketch operation now uses plain checklist markers, backticked node names, plain `Inputs` / `Outputs` headers, and bracketed field/type labels like `[EndPoint] - [Vec2]`.
165. 2026-03-16 16:08 - Realigned the `Operation subnodes` block in `docs/Human-Plans/Architecture/Spaghetti-Types.md` to follow the newer plain-text `SketchLine` style, so the other sketch operations now use the same no-backticks naming, `#### Inputs` / `#### Outputs` nesting, and plain `[Type]` tags instead of the older mixed formatting.
164. 2026-03-16 15:53 - Added backticks to all operation-subnode names in the simple `Sketch` checklist inside `docs/Human-Plans/Architecture/Spaghetti-Types.md` so the whole `Operation subnodes` block now uses one consistent visual treatment for the `### [ ] - Name` rows.
163. 2026-03-16 15:50 - Restyled the remaining `Operation subnodes` rows in the simple `Sketch` checklist inside `docs/Human-Plans/Architecture/Spaghetti-Types.md` so the later operations (`SketchMirror`, `SketchProject`, `SketchTrim`, `SketchOffset`) now match the same inline `### [ ] - Name` visual pattern already used by the earlier operation entries.
162. 2026-03-16 15:48 - Restyled the `Primitive / source subnodes` portion of the simple `Sketch` checklist in `docs/Human-Plans/Architecture/Spaghetti-Types.md` to match the newer inline visual pattern, so those source entries now read consistently as heading-like checklist rows with their output type shown directly on the line instead of mixing one converted row with older nested-output blocks.
161. 2026-03-16 15:45 - Rebuilt the malformed simple `Sketch / Extrude / Loft` checklist block in `docs/Human-Plans/Architecture/Spaghetti-Types.md`, removing the accidental mixed heading/bullet markup drift and restoring one clean compact nested tree for the `Param Types`, `Sketch`, `Extrude`, and `Loft` planning surface.
160. 2026-03-16 15:39 - Normalized the simple `Sketch / Extrude / Loft` checklist in `docs/Human-Plans/Architecture/Spaghetti-Types.md` to the preferred compact tree style, so node entries now consistently read as `[ ] - NodeName` with nested backticked `Inputs` / `Outputs` labels beneath them and the param-type detail living only on the child lines rather than being repeated inline on every node row.
159. 2026-03-16 15:35 - Re-simplified the simple `Sketch / Extrude / Loft` contract in `docs/Human-Plans/Architecture/Spaghetti-Types.md` back into one continuous nested checklist, removing the internal heading-heavy structure and reformatting node lines as `[ ] - NodeName` while moving the param-type detail into the nested input/output bullets instead of repeating inline type tags on every node line.
158. 2026-03-16 15:28 - Added a dedicated `Sketch Builder` section to `docs/Human-Plans/Architecture/Spaghetti-Types.md`, explicitly recording the architecture split that the `Sketch` composite node should live on the Spaghetti canvas, expose an `Edit Sketch` / `Open Builder` action, open a dedicated sketch-authoring surface likely in the model viewport, and own the editable sketch command stack plus the resulting sketch outputs back in the graph.
157. 2026-03-16 14:55 - Tightened the spacing and indentation inside the simple `Sketch / Extrude / Loft` checklist in `docs/Human-Plans/Architecture/Spaghetti-Types.md`, removing the extra blank-line gaps between headings and child lists so the whole contract now reads more like one continuous nested tree instead of a set of disconnected spaced-out blocks.
156. 2026-03-16 14:52 - Reset the simple `Sketch / Extrude / Loft` checklist in `docs/Human-Plans/Architecture/Spaghetti-Types.md` to start at `#` and use a shallower heading ladder overall, so the fold tree stays usable and no longer buries the contract under the old very-deep heading chain.
155. 2026-03-16 14:43 - Reworked the simple `Sketch / Extrude / Loft` checklist in `docs/Human-Plans/Architecture/Spaghetti-Types.md` into a deeper heading hierarchy so the contract can collapse cleanly in the editor, replacing the inline section labels with real heading levels for param-type groups, node `Inputs` / `Outputs`, and each individual sketch operation mini-contract.
154. 2026-03-16 14:40 - Broke the `Sketch` operation subnodes in `docs/Human-Plans/Architecture/Spaghetti-Types.md` down into nested mini input/output contracts using the current best Replicad-aligned read, so `SketchLine`, `SketchArc3Point`, `SketchBezierSpline`, `SketchCloseProfile`, `SketchProfileSelect`, `SketchMirror`, `SketchProject`, `SketchTrim`, and `SketchOffset` no longer read as loose names only and the more exact versus more provisional wrappers are easier to tell apart.
153. 2026-03-16 14:38 - Corrected the nested `SketchOffset` mini-contract inside `docs/Human-Plans/Architecture/Spaghetti-Types.md` so it now uses the real `[ ] / [~]` checklist markers, fixed the ad-hoc spelling drift, and formats the node's own `Inputs` / `Outputs` as typed checklist lines (`SourceCurve`, `OffsetDistance`, `OffsetCurve`) instead of raw placeholder text.
152. 2026-03-16 14:33 - Reworked the simple `Sketch` checklist in `docs/Human-Plans/Architecture/Spaghetti-Types.md` around the new primitive-versus-operation rule, so the composite now reads as top-level inputs, internal primitive/source subnodes, internal operation subnodes, and top-level outputs (`SketchCurves`, `SketchProfiles`, `SketchProfile`) instead of incorrectly flattening all sketch subnodes into one direct output list.
151. 2026-03-16 14:31 - Applied the same `Inputs / Outputs` sub-indent pattern to the simple `Sketch` and `Loft` checklist blocks in `docs/Human-Plans/Architecture/Spaghetti-Types.md`, so the whole foundational `Sketch / Extrude / Loft` list now reads in one consistent contract-style structure instead of mixing flat subnode lists with the newer input/output grouping.
150. 2026-03-16 14:30 - Restructured the simple `Extrude` checklist in `docs/Human-Plans/Architecture/Spaghetti-Types.md` into explicit `Inputs` and `Outputs` with one more sub-indent for the param types, so the foundational node list can now be reviewed as a real contract-style shape before applying the same pattern to `Sketch` and `Loft`.
149. 2026-03-16 14:36 - Expanded the simple foundational checklist in `docs/Human-Plans/Architecture/Spaghetti-Types.md` to include `Loft` as the next sibling after `Sketch` and `Extrude`, added a matching `CompositeLoft` param-type entry, and seeded the new checklist block with the likely first loft-facing pins (`StartProfile`, `EndProfile`, optional intermediate profiles, rails, and basic boolean-style flags) so the node sequence now reads as the beginning of a broader solid-feature family instead of stopping at extrusion.
148. 2026-03-16 14:30 - Reworked the `Param Types` subsection in `docs/Human-Plans/Architecture/Spaghetti-Types.md` into simple grouped headers with indented child types, so the foundational vocabulary list now separates primitive values, vectors/transforms, spatial references, sketch geometry, solid/body outputs, and composite wrapper types instead of reading as one flat set of labels.
147. 2026-03-16 14:27 - Added a dedicated `Param Types` subsection above the simple `Sketch / Extrude` checklist in `docs/Human-Plans/Architecture/Spaghetti-Types.md`, so the bracketed type hints used throughout that working list now have one local scan-friendly home instead of being implied only inline beside each node/subnode.
146. 2026-03-16 14:25 - Added simple bracketed param-type hints to the condensed `Sketch / Extrude` checklist in `docs/Human-Plans/Architecture/Spaghetti-Types.md`, so each parent node and subnode now carries a quick data/command-type read like `Plane`, `Float`, `Vec2`, `SketchProfile`, or `SolidBody` instead of leaving the checklist as names only.
145. 2026-03-16 14:21 - Tightened the condensed `Sketch / Extrude` checklist in `docs/Human-Plans/Architecture/Spaghetti-Types.md` by indenting all discussed subnodes/options under their parent composite nodes, so the first-pass planning surface now reads as a clearer hierarchy instead of one flat mixed checklist.
144. 2026-03-16 14:18 - Added one condensed `Sketch / Extrude` checklist to `docs/Human-Plans/Architecture/Spaghetti-Types.md`, turning the scattered explanatory notes into one scan-friendly working list of the first foundational composite node plus its discussed subnodes/options, with `[~]` marking the intended first-pass `Sketch` and `Extrude` surfaces and `[ ]` leaving the later sketch helpers, mirror/offset behaviors, and advanced extrusion variants clearly deferred.
143. 2026-03-16 14:13 - Expanded `docs/Human-Plans/Architecture/Terminology-Decisions.md` with a small `Suggested Roadmap Placement` pattern and provisional lane/subphase homes for the current terminology shifts, keeping the roadmap guidance explicitly non-canonical while still giving `Number -> Float`, `parent node -> Composite Node`, and the FeatureStack ownership-direction shift a likely future Lane `[3]` execution home.
142. 2026-03-16 14:12 - Created `docs/Human-Plans/Architecture/Terminology-Decisions.md` as a dedicated architecture-side home for active naming transitions and vocabulary migrations, seeded it with the first current decisions (`Number -> Float`, `parent node -> Composite Node`, and the FeatureStack ownership-direction shift), then added the new doc to the architecture section of `docs/Doc-Index.md` so future renaming work has one canonical place to land before codewide migration starts.
141. 2026-03-16 14:08 - Expanded the future `Extrude` section in `docs/Human-Plans/Architecture/Spaghetti-Types.md` with first-pass input and output type notes so the foundational `Sketch -> Extrude` path now reads consistently: `Extrude` should consume a `SketchProfile` plus visible `Distance` / `Direction` / `Origin` driver pins, and should publish a body-like result first as `SolidBody` with `Part` as the later product-facing/build-chunk read.
140. 2026-03-16 14:04 - Expanded `docs/Human-Plans/Architecture/Spaghetti-Types.md` with a dedicated future `Extrude` section directly after the `Sketch` planning block, listing the main Replicad-aligned extrude options/commands as a checklist and marking `Distance`, `Direction`, and `Origin` as the safest first-pass `Sketch -> Extrude` surface while leaving `TwistAngle`, `ExtrusionProfile`, and the lower-level extrusion helpers for later.
139. 2026-03-16 13:34 - Expanded the future `Sketch` composite section in `docs/Human-Plans/Architecture/Spaghetti-Types.md` with a first-pass internal subnode list so the architecture doc now names the likely explicit subnodes inside a real composite `Sketch`: frame/placement nodes like `SketchPlane` and `SketchTranslate`, geometry nodes like `SketchLine` and `SketchArc3Point`, profile nodes like `SketchCloseProfile`, and local driver/value nodes like `SketchNumber`, `SketchBoolean`, and `SketchVec2`.
138. 2026-03-16 13:28 - Expanded the future `Sketch` composite section in `docs/Human-Plans/Architecture/Spaghetti-Types.md` with a first-pass output-type note so the doc now records that `Sketch` should likely publish `SketchCurves`, `SketchProfiles`, and one selected `SketchProfile`, keeping the raw curve network separate from the closed-profile outputs that downstream solid features will actually consume.
137. 2026-03-16 13:23 - Expanded `docs/Human-Plans/Architecture/Spaghetti-Types.md` with a dedicated `Composite Node Candidates` subsection so the architecture docs now separate likely future graph-facing parent/composite nodes such as `Composite/Sketch`, `Composite/Extrude`, `Composite/Loft`, `Composite/Boolean`, `Composite/FeatureStack`, and the first ParaHook-specific composite shells from both the live registry inventory and the lower-level Replicad command checklist.
136. 2026-03-16 13:18 - Expanded `docs/Human-Plans/Architecture/Spaghetti-Types.md` with a dedicated Replicad-command coverage checklist, marking the current practical coverage of sketch/profile, sketch placement, solid creation, solid modification, and curve/rail operations as `[x]`, `[~]`, or `[ ]` so the architecture docs now show which command families ParaHook already covers, only partially covers, or still needs before the Spaghetti node language can honestly mirror the worker-side geometry vocabulary.
135. 2026-03-16 13:15 - Reorganized `docs/Human-Plans/Architecture/Spaghetti-Types.md` into a cleaner current-vs-future architecture map: kept the same overall content but reduced repetition, grouped the live code vocabulary and identity terms more tightly, separated the current registry checklist from future node ideas, and left the Replicad/sketch direction in its own explanatory section so the doc now reads more like one coherent type-and-node map.
134. 2026-03-16 13:13 - Cleaned up `docs/Human-Plans/Architecture/Spaghetti-Types.md` so the future node checklist stays scan-friendly: moved the longer Replicad/sketch-command explanation out of the checklist itself and into a separate `Replicad Command Direction` section, while keeping `[ ] Sketch` in the node list as the likely first composite Replicad-aligned node.
133. 2026-03-16 13:10 - Expanded `docs/Human-Plans/Architecture/Spaghetti-Types.md` with a more concrete Replicad-aligned sketch-node direction: added `[ ] Sketch` to the future node checklist and clarified that the likely first move is one composite `Sketch` node hosting internal line/arc/bezier/close-profile/profile-select plus plane/place/translate/rotate/mirror controls, rather than immediately exploding every sketch command into a separate top-level graph node.
132. 2026-03-16 12:45 - Expanded the future-node ideas in `docs/Human-Plans/Architecture/Spaghetti-Types.md` with a ParaHook-specific feature subsection, adding likely concrete foothook-build nodes such as `Feature/CloseProfile`, `Feature/RailMath`, `Feature/ToeLoft`, `Feature/HeelLoft`, `Feature/ProfileTrim`, and `Feature/AnchorSpline` so the node checklist now reflects the actual feature concepts needed to build the current ParaHook family rather than only generic future node categories.
131. 2026-03-16 12:43 - Expanded `docs/Human-Plans/Architecture/Spaghetti-Types.md` again with a separate `[ ]` future-node ideas checklist so the new architecture type doc now distinguishes live registered nodes from plausible next-wave node families: first-class `FeatureStack` and feature-operation nodes, structure/composition nodes like `Structure/Assembly` and `Structure/Component`, more general part/publish nodes, and possible material/export nodes later.
130. 2026-03-16 12:41 - Expanded `docs/Human-Plans/Architecture/Spaghetti-Types.md` with a current registry node checklist grouped by family, marking the live node types as `[x]`, `[~]`, or `[L]` using the current planning read: part nodes as mostly evolving, `Part/CubeProof` as legacy/proof-path, `OutputPreview` and `Output/Assembled` as still-evolving output nodes, `Param/*` and `Primitive/*` as closest to the intended authored-node direction, and the `Utility/*` identity helpers as useful but still likely cleanup candidates later.
129. 2026-03-16 12:36 - Created `docs/Human-Plans/Architecture/Spaghetti-Types.md` as a human-readable map of the current Spaghetti graph/editor/output/content type names and identities (`GraphDocument`, `SpaghettiGraph`, `SpaghettiNode`, `SpaghettiEdge`, `OutputPreviewObject`, `viewerKey`, and the planned content ids), then added it to the architecture section of `docs/Doc-Index.md` so the new type-bridge doc is discoverable.
128. 2026-03-16 12:31 - Added a short feature-stack direction note to `docs/Human-Plans/roadmap/Vision-roadmap.md`: the vision now explicitly says Feature Stack should not be permanently trapped as embedded part-node data and should leave room for first-class graph/composite ownership later; also updated that file's local `Doc History` block to add the new newest-first entry.
127. 2026-03-16 12:24 - Updated `docs/Human-Plans/roadmap/Vision-roadmap.md` again to make Replicad explicit in the long-range build story: the worker-side geometry engine is now named directly under the build/generate section, and the vision now says chunked rebuild control, sibling chunk independence, and future parent `SEQ / ALL` exist partly because one giant opaque rebuild flow is not reliable enough with the current engine; also normalized that file's local `Doc History` numbering to newest-first with the highest number at the top.
126. 2026-03-16 11:13 - Compiled the answered `Content`-row reset questions from `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` into `docs/Human-Plans/roadmap/Vision-roadmap.md` in one clean pass: clarified the long-range `Root Assembly -> Sub Assembly -> Component -> Object -> Part` hierarchy, kept `Graph Documents` graph-scoped and `Content` chunk-scoped, captured per-row `Live / Release / Manual` plus future `SEQ / ALL`, separated project-owned policy truth from worker-owned execution truth, required revision-based bar honesty, removed source traceability from the main row surface, and framed the next lane around making `Part` rebuilds real before full `Part` rows land.
125. 2026-03-16 11:12 - Filled in question `26` inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` with the next-lane direction: introduce `Part` rows when part-level rebuild becomes real worker/runtime truth with stable `partId`, independent sibling rebuild, truthful revision/state reporting, and clear source mapping, so `Object` can stop doing double duty and become the clearer parent/orchestration row over child parts.
124. 2026-03-16 11:10 - Filled in question `25` inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` with a roadmap-friendly staged answer: start full build bars on `Assembly` and `Component`, keep `Object` lighter until part-level truth exists, and later bring `Object` and `Part` into the same overall build-row family with `Part` as the clearer leaf bar.
123. 2026-03-16 11:10 - Expanded question `24` inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` so it now includes roadmap-friendly framing in addition to the answer: an end-product description that `Content` rows should read as clean build chunks with structure communicated by hierarchy lines/indentation, plus a likely rollout sequence of no visible source labels first, then explicit `view in graph`, then deeper source/reveal actions in the right-click menu.
122. 2026-03-16 11:07 - Clarified question `24` inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`: source traceability like `Graph 1` should stay off the primary `Content` row surface, because the main row should stay build-focused, the hierarchy lines already carry structural context, and graph/source access should instead live in secondary actions like `view in graph` and the row right-click menu.
121. 2026-03-16 10:45 - Clarified question `23` inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`: keep `Content` chunk-scoped and `Graph Documents` graph-scoped once both surfaces gain stronger bars/policies, so `Content` owns row-level chunk behavior while `Graph Documents` becomes the authoring/impact surface that shows which downstream chunks from a graph now need rebuild without duplicating the whole content tree.
120. 2026-03-16 10:42 - Clarified question `22` inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`: truthful `Content` bars should eventually compare at least `authoredRevision`, `publishedRevision`, and `acceptedBuildRevision` per build chunk so the UI can distinguish current vs stale vs unresolved state from real worker/runtime revision truth rather than decorative fill.
119. 2026-03-16 10:40 - Clarified question `21` inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`: row execution truth should live in worker/runtime, which should own actual `building / ready / unresolved / stale` state plus accepted build revisions, while app/project keeps user policy/order preferences and the Browser renders worker-derived truth instead of inventing it locally.
118. 2026-03-16 10:39 - Clarified question `20` inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`: row policy truth should live in project/app state rather than the graph-owned publish contract or worker/runtime, while graph-owned publish still defines entity identity/source mapping and the worker/runtime still owns actual execution truth.
117. 2026-03-16 10:37 - Clarified question `19` inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`: stable published ids should use one family per entity type (`assemblyId`, `componentId`, `objectId`, later `partId`) and should not split into separate root/sub id kinds, because root-vs-sub should be represented by hierarchy relationships rather than different identity types.
116. 2026-03-16 10:35 - Clarified question `18` inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`: locked the efficient-engine direction that sibling `Content` rows should be true independent build chunks rather than graph-scoped UI abstractions, so row-level rebuild is intended to become real worker/runtime truth where rebuilding `Object 1` does not automatically rebuild `Object 2`.
115. 2026-03-16 10:33 - Added note `[171]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` to capture another future parent-row control for the new `Content` build model: a visible `SEQ / ALL` button so parent rows can choose whether their children rebuild one-by-one in current `Content` order or all together, while explicitly keeping that execution-mode control separate from `Live / Release / Manual`.
114. 2026-03-16 10:23 - Clarified the policy/rebuild block inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`: all `Content` rows now explicitly default to `Live`, `Live / Release / Manual` is captured as a true per-row policy for `Assembly / Component / Object` (later `Part`), parent rows can push policy downward while children can override back and force the parent to show `Mixed`, and `rebuild this row` is now outlined as rebuilding that row's whole branch with a later user-controlled child execution strategy of either all-at-once or one-by-one in current `Content` order.
113. 2026-03-16 10:21 - Added deferred note `[170]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` for a later `Graph Documents` follow-up: keep the authoring/document split intact, but eventually let each graph expose a lightweight rebuild trace of the downstream `Component / Object / Part` chunks it has made stale, without duplicating the entire `Content` tree under the graph section.
112. 2026-03-16 10:20 - Clarified question `7` inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`: keep the split between `Graph Documents` and `Content`, with `Graph Documents` staying the authoring/document surface and `Content` becoming the published-entity execution surface for rebuilds, row policy, build order, and later chunk-level controls.
111. 2026-03-16 10:19 - Clarified question `6` inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`: removed double click from the future `Content` interaction model and replaced it with an explicit `view in graph` action, scoped so `Part` / `Object` rows can open and focus their source graph/node, `Component` rows can open the owning graph, and `Assembly` rows intentionally have no graph-jump action yet because they may span multiple graphs.
110. 2026-03-16 10:17 - Clarified questions `4` and `5` inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`: single click on a `Content` row is now explicitly the rebuild trigger for that row's build chunk, while inspect/highlight moves out of the primary click and into the row's right-click menu on the fill-bar area so `Content` stays execution-first.
109. 2026-03-16 10:14 - Added note `[169]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` to lock another future `Content`-row control: every row should later support a right-click menu on the fill-bar area so deeper build/material/reveal/export/organization actions can live in an overflow surface without bloating the compact one-line row itself.
108. 2026-03-16 10:13 - Added note `[168]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` to lock one more future `Content`-row control: a separate right-side material/color button so users can color parts/objects/components and maybe assemblies later, while explicitly keeping that control distinct from row build policy, row rebuild click, and the row loading/status bar itself.
107. 2026-03-16 10:12 - Clarified question `3` inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`: the bar fill is now explicitly defined as the row's current execution/readiness state for that build chunk, while `Live / Release / Manual` is captured as a separate future-facing policy/control state rather than the fill itself.
106. 2026-03-16 10:00 - Added note `[167]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` to lock the new `Content` vision in note form after the roadmap update: `Content` is a build-chunk hierarchy rather than just a Browser tree, every row eventually becomes a real loading bar/build chunk, all rows default to `Live` on load, list order is the intended build sequence, parent rows can push `Live / Release / Manual` downward, child overrides remain meaningful and aggregate back upward, and row click is the rebuild trigger for that chunk.
105. 2026-03-16 09:54 - Added another clarification block under question `1` in note `[166]` inside `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`: in first-pass terms, `Component` is the publish unit produced by one graph's `OutputPreview` (`1 graph -> 1 OutputPreview -> 1 published Component`), while the longer-range vision should still keep room for one graph publishing multiple components and for assembly-level objects that may eventually live without a component wrapper.
104. 2026-03-16 09:52 - Clarified question `1` inside note `[166]` in `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`: replaced the vague “end state” wording with “target ParaHook content model,” updated the hierarchy to `Root Assembly -> Sub Assembly -> Component -> Object -> Part -> later Sub Part`, and added a short clarification that this question is about ParaHook’s own long-term Browser/content truth rather than a literal 1:1 `.step` export structure.
103. 2026-03-16 09:49 - Added note `[166]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` as the reset design-question list for the new `Content` vision: before more implementation, lock what each row really represents, whether row click now means rebuild, where inspect/highlight moves, how `Live / Release / Manual` inheritance and child override work, what `rebuild this row` means at `Assembly / Component / Object`, what stable published ids/revisions are required, and how `Content` can become a row-level build surface without collapsing back into a duplicate `Graph Documents` list.
102. 2026-03-16 09:46 - Tightened `docs/Human-Plans/roadmap/Vision-roadmap.md` again to make the end-state `Content` area behavior explicit after the Browser/status discussion: every `Content` row is now described as an eventual loading/status bar for a published entity, all rows default to `Live` on load, parent rows can push `Live / Release / Manual` downward while child overrides remain meaningful and aggregate back upward, and row click is now captured as the rebuild trigger for that published row rather than leaving `Content` framed as a passive inspection-only tree.
101. 2026-03-16 09:38 - Tightened `docs/Human-Plans/roadmap/Vision-roadmap.md` after stepping back from the recent Browser/content cleanup so the long-range product direction is more explicit: the Browser must keep `Graph Documents` and `Content` as distinct surfaces, `Content` should not become a second graph list, `OutputPreview` should evolve toward a structured publish payload instead of a flat slot/final-mesh inference seam, and the intended Browser interaction language remains inspection-first (`single click` inspect/highlight, `double click` authoring jump) with calmer one-line rows preceding any louder bar/fill treatment.
100. 2026-03-16 09:31 - Tightened the new content-row shell in `src/app/panels/BrowserPanel.tsx` after visual review: removed the accidental duplicate non-graph row label so `Content` rows collapse back to a single compact line closer to the established `Graph Documents` row language; re-verified with `npm run test -- src/app/panels/BrowserPanel.test.tsx src/app/panels/selectBrowserTreeRows.test.ts` plus `npm run build`.
99. 2026-03-16 09:29 - Implemented note `[165]` across `src/app/store/useAppStore.ts`, `src/app/panels/selectBrowserTreeRows.ts`, `src/app/panels/BrowserPanel.tsx`, and `src/app/theme/v15Theme.css` so `Content` rows now have a passive right-aligned readiness slot: `Assembly 1` aggregates to `Ready` / `Unresolved`, published components show `Ready` / `Unresolved` while keeping source traceability in the normal meta slot, and unresolved object rows now move `Unresolved` out of the main meta slot into the new status surface; updated the touched Browser/store tests and re-verified with `npm run test -- src/app/store/useAppStore.test.ts src/app/panels/selectBrowserTreeRows.test.ts src/app/panels/BrowserPanel.test.tsx` plus `npm run build`.
98. 2026-03-16 09:24 - Added note `[165]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`, turning the next Browser/content follow-up into an implementation-ready pass: add a passive right-aligned readiness slot for `Assembly`, `Component`, and unresolved `Object` rows so `Content` can start showing `Ready` / `Unresolved` state without inheriting graph-document controls, loading bars, or build-policy chrome.
97. 2026-03-16 09:15 - Implemented note `[164]` across the OutputPreview normalization and project-content seams so default published object names now use numbered fallback labels like `Object 1`, `Object 2`, etc. instead of slot/runtime ids like `s001`; `src/app/spaghetti/system/outputPreviewNode.ts` now generates those defaults and also upgrades legacy default `label === slotId` cases during normalization, while authored custom object labels and receive-link component labels stay untouched; updated the touched output-preview, slot-normalization, spaghetti-store, and app-store tests and re-verified with `npm run test -- src/app/store/useAppStore.test.ts src/app/spaghetti/system/outputPreviewNode.test.ts src/app/spaghetti/system/ensureOutputPreviewSlots.test.ts src/app/spaghetti/system/ensureOutputPreviewSingleton.test.ts src/app/spaghetti/store/useSpaghettiStore.test.ts` plus `npm run build`.
96. 2026-03-16 09:08 - Added note `[164]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` to lock the next content-row naming cleanup: object rows should keep authored labels when present, but default fallback names should become numbered `Object N` labels instead of slot/runtime ids like `s001`, while unresolved state stays quiet secondary meta rather than the main identity.
95. 2026-03-16 08:58 - Removed the first-pass component-count subtext from the top content assembly row in `src/app/store/useAppStore.ts`, so `Assembly 1` now renders without `0/1/2 Components` meta while later leaving room for a right-side build/status surface; updated the touched Browser/store expectations and re-verified with `npm run test -- src/app/store/useAppStore.test.ts src/app/panels/BrowserPanel.test.tsx src/app/panels/selectBrowserTreeRows.test.ts` plus `npm run build`.
94. 2026-03-16 08:57 - Simplified the first-pass content assembly label in `src/app/store/useAppStore.ts` so the Browser now shows `Assembly 1` instead of `Assembly Root`, matching the recent content-row naming cleanup and leaving room for a later right-side build-status surface; updated the touched Browser/store selector tests and re-verified with `npm run test -- src/app/store/useAppStore.test.ts src/app/panels/BrowserPanel.test.tsx src/app/panels/selectBrowserTreeRows.test.ts` plus `npm run build`.
93. 2026-03-16 08:54 - Implemented note `[163]` in `src/app/store/useAppStore.ts` so published project-content components now use numbered fallback labels like `Component 1` when the output node still carries the default `Published Component` label, while Browser content-row meta for published components now shows quiet source traceability like `Graph 1` instead of object-count wording; updated `src/app/store/useAppStore.test.ts` and re-verified with `npm run test -- src/app/store/useAppStore.test.ts` plus `npm run build`.
92. 2026-03-16 08:49 - Added note `[163]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` to lock the next Browser naming polish: use authored component labels when present, fall back to numbered labels like `Component 1` instead of `Published Component`, and repurpose the component meta slot for quiet source traceability like `Graph 1` instead of weak object-count wording.
91. 2026-03-16 08:44 - Polished the Browser content tree-guide leaf treatment in `src/app/panels/BrowserPanel.tsx` and `src/app/theme/v15Theme.css` so object rows now carry a row-kind class and their final horizontal connector can continue through the empty expand-placeholder gap into the `O` badge center when the object is a leaf.
90. 2026-03-16 08:41 - Polished the Browser content tree-guide alignment in `src/app/theme/v15Theme.css` so the vertical/elbow connector columns track the center of the `+/-` gutter buttons by depth instead of sitting slightly left of them; this was a CSS-only follow-up to the `[162]` hierarchy-line pass.
89. 2026-03-16 08:36 - Implemented the simple Browser content hierarchy-line pass after note `[162]`: `src/app/panels/selectBrowserTreeRows.ts`, `src/app/panels/BrowserPanel.tsx`, and `src/app/theme/v15Theme.css` now derive and render selector-owned tree-guide metadata so `Assembly Root -> Component -> Object` rows show easy vertical/elbow guide lines in the `Content` gutter without changing store ownership; updated Browser row tests/fixtures accordingly and re-verified `npm run build` plus the touched Browser panel/selector tests.
88. 2026-03-16 08:32 - Added note `[162]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` to lock the suggested Browser hierarchy-line approach after `[161]`: draw `Assembly Root -> Component -> Object` tree guides as Browser presentation only, derive any needed sibling/ancestor connector metadata in `selectBrowserTreeRows`, and keep `useAppStore` flat so the guide rails stay tied to visible tree order and collapsed-state rendering instead of becoming stored project truth.
87. 2026-03-16 00:59 - Fixed the post-`[161]` strict build break across `src/app/panels/browserRowActions.test.ts`, `src/app/panels/selectBrowserGraphRows.ts`, `src/app/panels/selectBrowserTreeRows.test.ts`, and `src/app/store/useAppStore.ts`: added the new `depth` field to Browser row fixtures, rewrote the graph-row selector to use a typed `reduce` instead of nullable-map filtering, restored the missing `GraphRuntimeState` type import, and re-verified both `npm run build` and the touched Browser panel tests.
86. 2026-03-16 00:55 - Implemented note `[161]` across `src/app/panels/BrowserPanel.tsx`, `src/app/panels/selectBrowserTreeRows.ts`, `src/app/store/useAppStore.ts`, and the related Browser/store tests so `Content` now behaves like a real local tree: `Assembly Root` and `Component` rows can collapse/expand their children, `Object` rows render as indented leaves under their parent, the store-facing content selector now uses content-native meta instead of loud `Graph X` wording, and graph-row chrome remains scoped to `Graph Documents`; verified with `src/app/panels/selectBrowserTreeRows.test.ts`, `src/app/panels/BrowserPanel.test.tsx`, `src/app/store/useAppStore.test.ts`, `src/app/spaghetti/store/useSpaghettiStore.test.ts`, and `src/app/spaghetti/selectors/selectNodeVm.test.ts`.
85. 2026-03-16 00:50 - Added note `[161]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`, turning the next `[2.2]` Browser follow-up into an implementation-ready cut: make `Content` a real collapsible tree with Browser-local expand state, `Assembly Root` / `Component` parent toggles, indented `Object` children, and content-native row wording, while explicitly excluding graph-row chrome like policies, save buttons, loading bars, and overflow menus from this pass.
84. 2026-03-16 00:49 - Added note `[160]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` to lock the anti-duplication Browser rule after `[158]`: `Content` should stop speaking in graph language because `Graph Documents` already owns graph identity, build policy, save/export, and editor-routing chrome, so future Browser cleanup should remove loud `Graph X` meta from content rows and keep graph origin only as subtle traceability instead of making `Content` feel like a second graph list.
83. 2026-03-16 00:46 - Added note `[159]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` to lock the recommended Browser polish sequence after `[158]`: do `Content` tree structure next with `Assembly Root` / `Component` expand-collapse controls and indented object children, but explicitly defer graph-row chrome like `Live / Release / Manual`, save buttons, loading bars, and `...` menus on content rows until a later Browser surface pass defines what those controls should mean outside `Graph Documents`.
82. 2026-03-16 00:42 - Polished the `[158]` publish layer so the trailing empty OutputPreview slot stays editable in the graph node but no longer publishes placeholder Browser/project content: `src/app/spaghetti/outputSurface.ts` now filters `empty` slot objects out of the structured published-component surface, `src/app/store/useAppStore.ts` skips creating a published component when that filtered object list is empty, and the updated store/browser expectations were re-verified with `src/app/store/useAppStore.test.ts`, `src/app/panels/BrowserPanel.test.tsx`, and `src/app/panels/selectBrowserTreeRows.test.ts`.
81. 2026-03-16 00:39 - Implemented note `[158]` across the output-node, project-content, Browser, and test seams: `src/app/spaghetti/system/outputPreviewNode.ts`, `src/app/spaghetti/outputSurface.ts`, `src/app/store/useAppStore.ts`, `src/app/panels/BrowserPanel.tsx`, `src/app/panels/selectBrowserTreeRows.ts`, `src/app/spaghetti/canvas/NodeView.tsx`, `src/app/spaghetti/canvas/SpaghettiCanvas.tsx`, and `src/app/theme/v15Theme.css` now lift a structured publish layer above the existing slot-based `GraphOutputSurface`, so each graph can publish one authored component with authored object rows into `Assembly -> Component -> Object` project content while preserving the old slot/runtime seam; verified with `src/app/store/useAppStore.test.ts`, `src/app/spaghetti/store/useSpaghettiStore.test.ts`, `src/app/spaghetti/system/outputPreviewNode.test.ts`, `src/app/spaghetti/system/ensureOutputPreviewSlots.test.ts`, `src/app/spaghetti/system/ensureOutputPreviewSingleton.test.ts`, `src/app/panels/BrowserPanel.test.tsx`, `src/app/panels/selectBrowserTreeRows.test.ts`, and `src/app/spaghetti/selectors/selectNodeVm.test.ts`.
80. 2026-03-16 00:19 - Added note `[158]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`, turning the `[2.2]` output-publish direction into a code-grounded implementation-ready spec: keep the existing slot-based `GraphOutputSurface` seam intact, add authored component/object metadata to the output node, derive a structured graph-owned publish layer above the slot surface, lift `Assembly -> Component -> Object` into project content, and explicitly defer true Browser `Part` rows until the publish/build contracts can represent object-internal part membership honestly.
79. 2026-03-16 00:14 - Added note `[157]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` to lock the next recommended task: make `[2.2]` implement-ready by defining the output-node publish payload and project-content contract for `Assembly -> Component -> Object -> Part`, while explicitly deferring sub-assemblies, Browser drag/drop reorganization, and larger export-parent assembly management.
78. 2026-03-16 00:11 - Added note `[156]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` to lock the Browser content-hierarchy direction for `[2.2]`: first pass should use file-level assemblies with one published component per output node, while true parent/sub-assembly composition plus Browser drag/drop reorganization are explicitly deferred to a later pass for imported-assembly and whole-project export workflows.
77. 2026-03-15 23:54 - Implemented note `[155]` across `src/app/panels/BrowserPanel.tsx`, `src/app/panels/selectBrowserGraphRows.ts`, `src/app/panels/selectBrowserTreeRows.ts`, `src/app/store/useAppStore.ts`, and the related Browser/store tests so first-pass content-leaf Browser interaction now follows the new cleanup rule: single-click on `component` and `published-output` rows selects the row and highlights the viewport target when shared composition is inactive, while double-click opens or focuses the owning graph and focuses the source node when that authoring target exists; verified with `src/app/panels/BrowserPanel.test.tsx`, `src/app/store/useAppStore.test.ts`, `src/app/panels/selectBrowserGraphRows.test.ts`, and `src/app/panels/selectBrowserTreeRows.test.ts`.
76. 2026-03-15 23:40 - Added note `[155]` to `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md`, carrying the new Browser row interaction cleanup back into the active planning surface: single click should become selection plus viewport highlight, double click should become authoring jump, and the note explicitly distinguishes graph-document rows from later content/object/part rows so `[2.2]` does not inherit the wrong click language.
75. 2026-03-15 23:16 - Created the new active planning scratchpad `docs/Human-Plans/CodexNotes/10_CodexChatNotes.md` for the `[2.1E]` left-dock shell pass, seeded it with the first ownership and finish-line notes, and updated `docs/Doc-Index.md` so the `Human-Plans/CodexNotes` series now includes the new active file.
74. 2026-03-15 23:04 - Broke the shipped UI cleanup wave down into labeled roadmap subphases in `docs/Human-Plans/roadmap/roadmap.md`, grouping the implemented work under `[2.1C]`, `[2.1D]`, `[2.1E]`, `[2.3]`, and `[3.3]` so the Browser declutter, editor shell cleanup, dock/floating shell work, graph-row build/save surfaces, and workspace-presentation systems now have clearer lane homes.
73. 2026-03-15 23:00 - Re-read `docs/Human-Plans/roadmap/roadmap.md` against the shipped Browser/Spaghetti UI cleanup wave, then fixed the stale Lane `[2.1]` status lines so `[2.1C]` now reads as shipped, `[2.1D]` reads as active/partial instead of missing, and `[2.1E]` reflects the landed fixed-slot left-dock docking pass instead of the older not-started state.
72. 2026-03-15 22:51 - Reconciled the active planning notes in `docs/Human-Plans/CodexNotes/9_CodexChatNotes.md` against the shipped changelog, chill-batch implementation entries, and current shell tests, then checked off the note ids that are clearly implemented now: `[134]`, `[135]`, `[137]`, `[138]`, `[139]`, `[140]`, `[141]`, `[142]`, `[144]`, `[145]`, `[146]`, and `[148]`.
71. 2026-03-13 12:18 - Fixed the stricter TypeScript mock complaint in `src/app/panels/selectBrowserGraphRows.test.ts` by casting the partial `GraphRuntimeState` fixture through `unknown`, keeping the test helper lightweight without weakening the production runtime types; re-verified `src/app/panels/selectBrowserGraphRows.test.ts`.
70. 2026-03-13 12:08 - Updated the Browser graph-row `Building` visual in `src/app/theme/v15Theme.css` so the yellow build bar now animates from empty to full during the in-flight state and then hands off to the existing green `Done` fill when the selector flips to the accepted-build state; re-verified `src/app/panels/BrowserPanel.test.tsx` and `src/app/panels/selectBrowserGraphRows.test.ts`.
69. 2026-03-13 12:06 - Tweaked `src/app/theme/v15Theme.css` so Browser graph rows in the `Rebuild` state now render the build bar as visually empty instead of blue-filled, leaving `Building` and `Done` unchanged; re-verified `src/app/panels/BrowserPanel.test.tsx` and `src/app/panels/selectBrowserGraphRows.test.ts`.
68. 2026-03-13 12:04 - Implemented Browser graph-row step 2 build freshness across `src/app/spaghetti/store/useSpaghettiStore.ts`, `src/app/panels/selectBrowserGraphRows.ts`, `src/app/panels/BrowserPanel.tsx`, and `src/app/theme/v15Theme.css`: graph runtime now tracks per-document graph revisions plus issued/accepted build revisions, Browser row build state is derived as `Rebuild` / `Building` / `Done` from that runtime truth instead of `isDirty`, the save button remains the separate disk-save state surface, and coverage was updated in `src/app/spaghetti/store/useSpaghettiStore.test.ts`, `src/app/panels/selectBrowserGraphRows.test.ts`, `src/app/panels/selectBrowserTreeRows.test.ts`, `src/app/panels/browserRowActions.test.ts`, and `src/app/panels/BrowserPanel.test.tsx`.
67. 2026-03-13 11:49 - Revised `##### [151]` in `docs/Human-Plans/CodexNotes/9_CodexChatNotes.md` to define the proper step-2 ownership model: store/runtime owns graph revision and accepted-build truth, selectors derive `Rebuild / Building / Done` plus save-state readouts, and `BrowserPanel` stays presentation-only instead of inventing build freshness locally.
66. 2026-03-13 11:44 - Landed stage 1 of the Browser graph-row save-control shell across `src/app/panels/BrowserPanel.tsx`, `src/app/panels/selectBrowserGraphRows.ts`, `src/app/panels/selectBrowserTreeRows.ts`, and `src/app/theme/v15Theme.css`: graph rows now render a dedicated save-state button just left of `...`, that button opens its own small `Export Graph` menu on left or right click, the overflow menu uses the renamed disk action too, and the graph fill bar no longer shows `Dirty` / `Saved` text and only surfaces explicit text while `Building`; added coverage in the Browser panel and selector tests.
65. 2026-03-13 11:30 - Updated `##### [151]` in `docs/Human-Plans/CodexNotes/9_CodexChatNotes.md` to use `Rebuild` instead of `Needs Build` for the future graph-row build-bar wording, keeping the planned build-only row states as `Rebuild`, `Building`, and `Done`.
64. 2026-03-13 11:28 - Revised `##### [151]` in `docs/Human-Plans/CodexNotes/9_CodexChatNotes.md` so the implementation plan now explicitly separates row responsibilities: the graph fill bar should represent build-only states, while the new save button should carry save-only state.
63. 2026-03-13 11:18 - Added a new `##### [151]` phase-cut note to `docs/Human-Plans/CodexNotes/9_CodexChatNotes.md` that splits the Browser graph-row save-control work into two stages: ship the new row button/menu shell and explicit disk-export naming first, then add real local app save semantics plus the `Live / Release / Manual` right-click menu later.
62. 2026-03-13 11:15 - Added future planning note `[150]` to `docs/Human-Plans/CodexNotes/9_CodexChatNotes.md` for the Browser graph-row save-control direction: normal click should become a local app/session save action without opening the disk browser, right click should open a compact menu for `Live / Release / Manual`, and explicit disk persistence should move to a separately named action such as `Export Graph` or `Save Graph To Disk`.
61. 2026-03-13 11:01 - Marked note `[149]` complete in `docs/Human-Plans/CodexNotes/9_CodexChatNotes.md` after landing the left-dock resize bump-and-lock behavior in the shell.
60. 2026-03-13 11:00 - Implemented note `[149]` in `src/app/AppShell.tsx` so the active floating Spaghetti editor now gets bumped right when the live left dock resize bar would overlap it, stays temporarily locked to the dock edge with the shared `25px` gutter for the rest of that resize drag, follows the dock back left during the same gesture, and unlocks on pointer-up; added coverage in `src/app/AppShell.test.tsx` and re-verified `src/app/panels/BrowserPanel.test.tsx` plus `src/app/panels/selectBrowserTreeRows.test.ts`.
59. 2026-03-13 10:52 - Revised planning note `[149]` in `docs/Human-Plans/CodexNotes/9_CodexChatNotes.md` to lock in the recommended first-pass behavior: affect only the active floating Spaghetti editor, preserve a fixed `25px` gutter, follow the left dock both directions during the active resize drag, avoid split/maximized modes, and only enter the temporary lock when real overlap begins.
58. 2026-03-13 10:50 - Added follow-up planning note `[149]` to `docs/Human-Plans/CodexNotes/9_CodexChatNotes.md` for the next shell-interaction feature: when the left dock vertical resize bar expands into a floating Spaghetti editor, the dock should bump the window horizontally, keep its width stable, temporarily lock it to the dock edge for the duration of the resize drag, and release that lock on pointer-up.
57. 2026-03-13 10:47 - Replaced the fixed Browser new-editor spawn offset with a live left-dock anchor in `src/app/AppShell.tsx` and `src/app/panels/BrowserPanel.tsx`, so newly opened floating Spaghetti editors now use the current dock splitter width plus 25px and the same anchor also applies when a graph row has to open a fresh editor because none is active; updated `src/app/panels/BrowserPanel.test.tsx` to cover the anchored spawn paths and verified with `src/app/panels/BrowserPanel.test.tsx`, `src/app/panels/selectBrowserTreeRows.test.ts`, and `src/app/AppShell.test.tsx`.
56. 2026-03-13 10:39 - Moved the shared default floating Spaghetti spawn anchor in `src/app/spaghetti/store/useSpaghettiStore.ts` so new editors now open to the right of the left-dock title-status stack instead of stacking under it, using the updated shared `defaultViewportPosition` for both new floating-editor creation and restore paths; verified with `src/app/spaghetti/store/useSpaghettiStore.test.ts`.
55. 2026-03-13 10:34 - Cleaned up the `Open Editors` controls in `src/app/panels/BrowserPanel.tsx`, `src/app/panels/selectBrowserTreeRows.ts`, and `src/app/theme/v15Theme.css` so the section now has a top-right `Open new editor` button for the current active graph, clicking an editor-session row focuses that editor directly, the focused editor row reads as active, and `Close` moved out of the overflow menu into its own small row button; updated coverage in `src/app/panels/BrowserPanel.test.tsx` and `src/app/panels/selectBrowserTreeRows.test.ts`.
54. 2026-03-13 10:11 - Changed graph-row main-click behavior in `src/app/panels/BrowserPanel.tsx` so clicking the Browser row body now selects that row and routes the currently focused Spaghetti editor to the clicked graph, letting the row pick up the existing open/active highlight state once the focused editor is rebound; added BrowserPanel coverage in `src/app/panels/BrowserPanel.test.tsx`.
53. 2026-03-13 09:50 - Reworked Browser graph rows into a one-line policy/status shell across `src/app/panels/BrowserPanel.tsx`, `src/app/panels/selectBrowserGraphRows.ts`, `src/app/panels/selectBrowserTreeRows.ts`, and `src/app/theme/v15Theme.css`, replacing the low-value `G` icon with a local `Live / Release / Manual` policy cycle button, adding a slim clean/dirty/building state bar, and strengthening open/active row highlighting without introducing the deeper Browser policy architecture yet.
52. 2026-03-13 12:09 - Reduced the compact `View` baseline in `src/app/components/viewToolbarLayout.ts` so the gizmo now loads at the true minimum size instead of the previous mid-sized compact state; the closed right dock now uses the minimum-width presentation by default.
51. 2026-03-13 12:04 - Added a shared compact/expanded `View` shell state in `src/app/store/uiPrefsStore.ts`, then wired `src/app/components/ViewToolbar.tsx`, `src/app/components/ViewportOverlay.tsx`, and `src/app/theme/v15Theme.css` so the viewport axis gizmo now defaults to a much smaller size, the `View` drawer snaps the gizmo and right-dock width back to the larger presentation when opened, and manual gizmo resizing is preserved for the expanded state while closed mode always returns to the compact baseline.
50. 2026-03-13 08:37 - Nudged the meatball focus-node width fit closer to the true safe width limit in `src/app/spaghetti/canvas/SpaghettiCanvas.tsx`, so the docked editor zooms a little more aggressively without losing the right-edge protection added in the prior pass.
49. 2026-03-13 08:35 - Tightened the meatball focus-node framing in `src/app/spaghetti/canvas/SpaghettiCanvas.tsx` so the docked fit path now honors both left and right width bounds instead of only protecting the left edge, preventing the selected node card from clipping off the right side in meatball view.
48. 2026-03-13 08:33 - Added a meatball-specific focus-node fit profile across `src/app/panels/SpaghettiPanel.tsx`, `src/app/spaghetti/ui/SpaghettiEditor.tsx`, `src/app/spaghetti/ui/ExpandedEditor.tsx`, and `src/app/spaghetti/canvas/SpaghettiCanvas.tsx`, so the focus-node cycle buttons in meatball view now fit by width first and anchor higher toward the top instead of using the standard floating-editor framing.
47. 2026-03-13 01:11 - Added floating Spaghetti bottom split docking in `src/app/AppShell.tsx` and `src/app/theme/v15Theme.css`, so dragging the floating blue title bar near the bottom seam now shows a lower split ghost preview and docks the same viewport into split view on mouse-up, with AppShell coverage updated in `src/app/AppShell.test.tsx`.
46. 2026-03-13 00:50 - Re-anchored the left-dock width resize handle to a new `LeftDockPanelStackShell` in `src/app/AppShell.tsx` and `src/app/theme/v15Theme.css`, so the vertical divider now follows the actual toolbar panel stack height on load and only grows into the constrained full-height version when the dock stack is genuinely split-constrained.
45. 2026-03-13 00:48 - Narrowed the left-dock resize-handle full-height behavior in `src/app/AppShell.tsx` so the vertical divider only stretches full-height during the combined split layout; when the Spaghetti editor is floating and only the left dock is splitting the viewport, the handle now collapses back to the toolbar stack height.
44. 2026-03-13 00:46 - Restored the left-dock cutoff rule for plain Spaghetti split mode in `src/app/AppShell.tsx`, so the dock now stops at the horizontal split divider again whenever split view is active without a left-dock viewport split, preventing Browser/Preview/Parts from overlapping the lower split editor.
43. 2026-03-13 00:44 - Extended the split Spaghetti titlebar shortcut in `src/app/AppShell.tsx` so `Ctrl + drag` on the blue split title bar now detaches back to `expanded` and immediately hands off into the existing live floating drag path, including the normal meatball dock-preview behavior on release.
42. 2026-03-13 00:36 - Added a split-only `Ctrl + click` titlebar shortcut in `src/app/AppShell.tsx` so clicking the blue Spaghetti title bar while in split mode detaches back to the normal floating editor, preserving the current left-dock split rules while removing the lower split editor pane.
41. 2026-03-13 00:24 - Removed the left-dock width offset from the horizontal split divider in `src/app/theme/v15Theme.css`, so the resize bar now stays full-width in toolbar-priority mode while only the lower Spaghetti editor pane remains dock-shifted.
40. 2026-03-13 00:22 - Simplified the coordinated split-priority control in `src/app/AppShell.tsx` and `src/app/theme/v15Theme.css` so it always lives on the horizontal split bar at the far left, using `<` in toolbar-priority mode and `>` in editor-priority mode instead of mixing a divider button with a separate lower-pane toggle.
39. 2026-03-13 00:20 - Wrapped the horizontal split resize line in its own relative shell in `src/app/AppShell.tsx` and `src/app/theme/v15Theme.css`, so the split-priority return button now anchors to the actual divider bar instead of the overall split layout and no longer jumps to the top-left of the app.
38. 2026-03-13 00:17 - Moved the coordinated split-priority return control onto the horizontal split divider in `src/app/AppShell.tsx` and `src/app/theme/v15Theme.css`, so the lower editor now shows `<` on its left seam when claiming priority and shows `v` centered over the left-dock width on the divider when returning priority to the toolbar stack.
37. 2026-03-13 00:14 - Changed coordinated split behavior in `src/app/AppShell.tsx` so toolbar-priority now keeps the left dock full-height by default, while only editor-priority cuts the dock at the split divider; this removes the blank lower-left gutter and lets the dock use the full vertical region when it owns priority.
36. 2026-03-13 00:10 - Moved the coordinated split-priority toggle out of the Spaghetti titlebar and into the left edge of the lower split editor pane in `src/app/AppShell.tsx` and `src/app/theme/v15Theme.css`, removing the extra titlebar width pressure while keeping the same toolbar-priority/editor-priority animation behavior.
35. 2026-03-13 00:04 - Added a coordinated split-priority toggle for the Spaghetti editor in `src/app/AppShell.tsx` and `src/app/theme/v15Theme.css`, so when left-dock split and editor split are both active the lower editor can animate between dock-shifted toolbar-priority mode and a new full-width editor-priority mode from a split-only titlebar arrow button.
34. 2026-03-12 23:52 - Restored the shared left-dock width resize bar in `src/app/theme/v15Theme.css` by removing the clipping overflow from `.LeftDockContent`, which had hidden the handle after the split-mode dock scroll-chain fix.
33. 2026-03-12 23:50 - Styled the split-height left-dock stack scrollbar in `src/app/theme/v15Theme.css` to match the app’s dark toolbar scrollbar language, including a dark track, dark thumb, hover state, and a stable 12px gutter.
32. 2026-03-12 23:49 - Fixed the left-dock split scroll chain in `src/app/theme/v15Theme.css` by making `.LeftDockContent` a real `flex: 1` / `min-height: 0` container with hidden overflow, so `.PanelStack` can finally become the constrained scroll owner and show its scrollbar when Browser/Preview/Parts exceed the split viewer height.
31. 2026-03-12 23:46 - Constrained the left dock to the split viewer height in `src/app/AppShell.tsx` by giving `.LeftDock` a split-aware bottom offset, and made `.PanelStack` the internal scroll owner in `src/app/theme/v15Theme.css` so Browser/Preview/Parts now scroll inside the dock instead of continuing down over the lower split editor pane.
30. 2026-03-12 23:42 - Tightened split-mode Spaghetti containment in `src/app/panels/SpaghettiPanel.tsx` by clamping the toolbar against the panel's real available height instead of the current canvas height, and added a stable reserved scrollbar gutter plus a 12px toolbar scrollbar in `src/app/theme/v15Theme.css` so the split toolbar stays bounded to the pane without width jumps when overflow appears.
29. 2026-03-12 23:31 - Added a collapsible Spaghetti titlebar action tray in `src/app/AppShell.tsx` and `src/app/theme/v15Theme.css`, keeping the core `__ / maximize / split / close` controls always visible while the advanced `i / t / c / SP-MB` controls now live behind an animated reveal toggle with per-viewport tray state and maximized mode forcing the full set open.
28. 2026-03-12 23:18 - Restyled the new clamp-aware ParaHook slider fill in `src/app/theme/v15Theme.css` back toward the older left-origin fill-bar language by removing the pill rounding from the blue fill and orange clamp range so the bar now reads more like a straight fill extending from left to right.
27. 2026-03-12 23:11 - Implemented the first `Edit Clamp` pass for the Spaghetti window-settings opacity sliders by extending `src/app/components/ParaSlider.tsx` with clamp-range overlays and draggable clamp handles, adding saved clamp ranges to `src/app/panels/spaghettiWindowAppearance.ts`, wiring a section-wide clamp toggle through `src/app/AppShell.tsx`, and exposing it in `src/app/panels/SpaghettiPanel.tsx`.
26. 2026-03-12 22:47 - Replaced the five native Spaghetti window-settings dropdowns with a new reusable `ParaSelect` control in `src/app/components/ParaSelect.tsx`, matching the slider language with left/right end caps, a condensed dropdown-style center track, and cycle buttons for `Title Bar Color`, `Body Color`, `Font Size`, `Font Type`, and `Padding`.
25. 2026-03-12 22:31 - Split the Spaghetti transparency controls again by adding a dedicated `Title Bar` slider in `src/app/panels/SpaghettiPanel.tsx` and `src/app/panels/spaghettiWindowAppearance.ts`, then pushed the body appearance vars further into the main editor box in `src/app/theme/v15Theme.css` so the settings block, focus controls, toolbar summaries, and canvas toolbar follow the body font/padding model more consistently.
24. 2026-03-12 22:24 - Extended the Spaghetti `Window Fill` styling in `src/app/AppShell.tsx` and `src/app/theme/v15Theme.css` so the body slider now drives the actual non-graph editor chrome surfaces too, including the floating body, collapsed shell, canvas shell, and canvas well instead of mostly reading only on the title bar.
23. 2026-03-12 22:12 - Split the Spaghetti window transparency controls into `Window Fill` and `Graph Content`, wired graph-content opacity to the actual canvas content layer instead of the canvas box, and added a `Ctrl + Middle Mouse` temporary viewer-orbit shortcut through `src/app/spaghetti/canvas/SpaghettiCanvas.tsx`, `src/app/viewerBridge.ts`, and `src/viewer/scene/CameraController.ts`.
22. 2026-03-12 22:03 - Increased the Spaghetti toolbar/canvas divider thickness slightly in `src/app/theme/v15Theme.css` so the horizontal resize line matches the left vertical dock separator more closely.
21. 2026-03-12 22:00 - Overrode the generic `.V15Panel button` skin for the Spaghetti resize dividers in `src/app/theme/v15Theme.css`, removing the stretched button border/fill so only the horizontal resize line remains visible.
20. 2026-03-12 21:56 - Removed the leftover native tooltip/button chrome from the Spaghetti resize dividers in `src/app/panels/SpaghettiPanel.tsx` and `src/app/theme/v15Theme.css` so the seam now behaves like a clean line instead of still exposing the old button-style affordance.
19. 2026-03-12 21:52 - Slimmed the Spaghetti toolbar/canvas divider styling in `src/app/theme/v15Theme.css` so the horizontal resize line now matches the thinner left-dock divider language more closely instead of reading as a thick light-grey bar.
18. 2026-03-12 21:49 - Tightened the Spaghetti toolbar/canvas divider seam in `src/app/theme/v15Theme.css` by removing the old canvas top margin and collapsing the resize bar wrapper to the line itself, so there is effectively no extra top/bottom padding around the glowing horizontal divider.
17. 2026-03-12 21:45 - Replaced the chunky Spaghetti toolbar/canvas resize capsule in `src/app/theme/v15Theme.css` with a slimmer glowing horizontal bar so it matches the dock divider language better and saves vertical space.
16. 2026-03-12 21:39 - Pushed the Spaghetti window appearance settings deeper into `src/app/theme/v15Theme.css` so body tint and typography now apply across the editor body surfaces too, including the floating body shell, canvas shell, collapsed view, debug drawer, toolbar controls, and helper text instead of mostly stopping at the titlebar.
15. 2026-03-12 21:34 - Extended the new Spaghetti window appearance settings in `src/app/theme/v15Theme.css` so the toolbar area now follows the same body/window palette too, including the settings block, `Focus Node` row, toolbar sections, and select controls instead of only tinting the titlebar.
14. 2026-03-12 21:28 - Added a new per-window `i` settings toggle for the Spaghetti editor in `src/app/AppShell.tsx`, a session-only `Window Settings` section above `Focus Node` in `src/app/panels/SpaghettiPanel.tsx`, and viewport-scoped appearance variables in `src/app/theme/v15Theme.css` so opacity, tint, font, and padding can follow the same editor viewport across floating, split, and meatball presentations.
13. 2026-03-12 21:12 - Added a small split toggle button at the top of the left-dock resize bar in `src/app/AppShell.tsx` and `src/app/theme/v15Theme.css`, and made the vertical bar stretch to full height whenever left-dock viewport split mode is active.
12. 2026-03-12 21:08 - Finished the left-dock resize menu action styling in `src/app/theme/v15Theme.css` so the `Default Width` and `Split Viewport` buttons now use the same dark-mode treatment as the rest of the app instead of default light browser controls.
11. 2026-03-12 21:06 - Added a right-click menu to the left-dock resize bar in `src/app/AppShell.tsx` with `Default Width` and `Split Viewport` actions, and wired `Split Viewport` so the model viewport can anchor to the right edge of the left dock instead of living underneath it.
10. 2026-03-12 21:01 - Nudged the shared left-dock resize bar 10px farther to the right in `src/app/theme/v15Theme.css` so the handle sits a bit clearer off the panel stack during the current toolbar-width polish pass.
9. 2026-03-12 20:57 - Added a shared left-dock resize bar in `src/app/AppShell.tsx` and `src/app/theme/v15Theme.css` so the user can drag the vertical handle to change the width of the entire left toolbar stack, with a focused shell test covering the dock-width update.
8. 2026-03-12 20:52 - Moved the Browser creation actions out of the three large top buttons and into the `Graph Documents` header as compact square icon buttons in `src/app/panels/BrowserPanel.tsx`, with matching header-action styling and test updates.
7. 2026-03-12 20:49 - Returned the floating Browser shell to content-driven height in `src/app/AppShell.tsx` and `src/app/theme/v15Theme.css` so opening Browser sections like `Project` can expand the popout naturally instead of being clipped by a stale fixed window height.
6. 2026-03-12 20:47 - Fixed the floating Browser collapse sizing by letting `BrowserPanelRoot` drop out of its forced `height: 100%` shell when collapsed, so the popout now auto-shrinks to the titlebar instead of keeping the old tall window.
5. 2026-03-12 20:45 - Removed the floating Browser resize corner from `src/app/AppShell.tsx` and `src/app/theme/v15Theme.css` so the Browser popout stays draggable and dockable but no longer shows the bottom-right re-adjust handle.
4. 2026-03-12 20:34 - Fixed the meatball-to-spaghetti drag handoff in `src/app/AppShell.tsx` so dragging the docked meatball editor out now continues the same pointer drag on the floating Spaghetti editor instead of requiring a release and second click on the titlebar.
3. 2026-03-12 20:33 - Relaxed the floating Spaghetti editor vertical drag clamp in `src/app/AppShell.tsx` so the titlebar stays reachable while the lower editor body can move past the bottom of the browser window instead of hard-stopping at the viewport bottom.
2. 2026-03-12 20:33 - Darkened the Spaghetti editor toolbar scrollbar in `src/app/theme/v15Theme.css` by styling `.SpaghettiPanelHeaderBlock` directly with a darker track and thumb for the current dark-mode UI pass.
1. 2026-03-12 20:32 - Reactivated chill mode and started `Chill Batch #5` in `docs/Chill-Log.md` for the current Browser / Spaghetti / meatball UI cleanup wave.

<!-- ============================================================ -->
### Chill Batch #4
<!-- ============================================================ -->

Status:
- inactive

Started:
- 2026-03-07 12:40

Ended:
- 2026-03-12 20:32

Mode:
- chill mode inactive

Consolidated To:
- pending

Current Goal:
- continue the current docs-system cleanup, navigation cleanup, and phase/doc-structure alignment work without writing a permanent changelog entry for each small edit yet

Entries:
105. 2026-03-08 00:00 - Normalized the `Foldable Outline Tree` file bullets in `docs/Doc-Index.md` so the doc names now consistently use backticks throughout the tree instead of only `CHANGELOG.md` using the code-style label
104. 2026-03-08 00:00 - Rebuilt the bottom `Foldable Outline Tree` in `docs/Doc-Index.md` so it now matches the current `/docs` structure, including `docs/Agents`, the archived Codex chat/context files, the live roadmap set, the numbered family phase-plan files, and the current `Tasks/Future` plus `Tasks/Old` split
103. 2026-03-08 00:00 - Added a `Planning Mode Rule` to `AGENTS.md` so explicit planning sessions now point to `docs/Agents/Agents-Planning.md` and keep Codex focused on architecture, direction, salvageable/refactor/new/retire classification, and roadmap-oriented reasoning unless code is explicitly requested
102. 2026-03-08 00:00 - Created `docs/Agents/Agents-Planning.md` as the detailed planning-mode rule file, defining how Codex should stay focused on vision tightening, salvageable/refactor/new/retire classification, repo-grounded architecture reasoning, roadmap ordering, and CodexChat capture during explicit planning sessions
101. 2026-03-08 00:00 - Added a `Codex Chat Capture Rule` to `AGENTS.md` so future long-form sessions use the next `docs/Archive/CodexContext/History-Chats/N_CodexChat.md` file as the running notes surface for decisions, architecture clarifications, rule changes, and durable project conclusions, and fixed the stale `OO` family-doc example there to `14_DOC`
100. 2026-03-08 00:00 - Folded the concrete no-legacy roadmap from `docs/Human-Plans/roadmap/LEAVING-legacy.md` into `docs/Phase-Plans/15_LEG - Phase Plan.md`, adding the final target, subsystem-specific removal lanes, practical removal order, and stronger checklist detail so `LEG` can become the main working legacy-removal surface
99. 2026-03-08 00:00 - Reworked `docs/Human-Plans/roadmap/roadmap.md` into a more normal operator roadmap with `Current State / Now / Next / Later`, using the rebuilt family phase files and current app shape to point the next lane toward roadmap consolidation, editor/interaction stabilization, param-input direction, and later legacy removal
98. 2026-03-08 00:00 - Aligned the completed batch state by marking the finished family rows as done in `docs/Archive/CodexContext/4_CodexContext.md`, fixing the nested `3A-3H` indentation there, and updating the stale `DOC - Phase 14F` family-status entries in `docs/Phase-Plans/14_DOC - Phase-Plans.md`
97. 2026-03-08 00:00 - Created, templated, and completed the changelog information pass for `docs/Phase-Plans/11_SP - Phase-Plans.md`, rebuilding reconstructed `SP - Phase 1` through `3`, filling shipped `SP - Phase 4` through `7`, aggregating the multi-entry `SP - Phase 5` layout wave, and marking `SP` complete in both the CodexContext tracker and `DOC - Phase 14F`
96. 2026-03-08 00:00 - Created, templated, and completed the changelog information pass for `docs/Phase-Plans/13_ADV - Phase-Plans.md`, rebuilding reconstructed `ADV - Phase 1` and `2`, keeping later future-systems phases as placeholders, and marking `ADV` complete in both the CodexContext tracker and `DOC - Phase 14F`
95. 2026-03-08 00:00 - Created, templated, and completed the changelog information pass for `docs/Phase-Plans/12_EX - Phase-Plans.md`, rebuilding reconstructed `EX - Phase 1`, keeping later export phases as future placeholders, and marking `EX` complete in both the CodexContext tracker and `DOC - Phase 14F`
94. 2026-03-08 00:00 - Created, templated, and completed the changelog information pass for `docs/Phase-Plans/10_JK - Phase-Plans.md`, rebuilding reconstructed `JK - Phase 1`, leaving later Jake-mode phases as future placeholders, and marking `JK` complete in both the CodexContext tracker and `DOC - Phase 14F`
93. 2026-03-08 00:00 - Created, templated, and completed the changelog information pass for `docs/Phase-Plans/09_DBG - Phase-Plans.md`, rebuilding `DBG - Phase 1` from the shipped debug-inspector entry, keeping later `DBG` phases as future placeholders, and marking `DBG` complete in both the CodexContext tracker and `DOC - Phase 14F`
92. 2026-03-08 00:00 - Created, templated, and completed the changelog information pass for `docs/Phase-Plans/08_VR - Phase-Plans.md`, rebuilding reconstructed `VR - Phase 1`, keeping later viewer phases as future placeholders, and marking `VR` complete in both the CodexContext tracker and `DOC - Phase 14F`
91. 2026-03-08 00:00 - Created, templated, and fully rebuilt `docs/Phase-Plans/07_AS - Phase-Plans.md` from `docs/CHANGELOG.md`, adding reconstructed `AS - Phase 1`, `2`, and `4`, keeping `AS - Phase 3` as an inferred gap phase, then marked `AS` complete in both the temporary CodexContext tracker and `DOC - Phase 14F`
90. 2026-03-08 00:00 - Expanded the temporary normalization workflow in `docs/Archive/CodexContext/4_CodexContext.md` so the `information pass` step now breaks down into concrete sub-steps for changelog scan, phase classification, summary writing, grouped checklist buildout, file-footprint capture, reconstructed labeling, and cautious `L` / `R` marking
89. 2026-03-08 00:00 - Rebuilt `docs/Archive/CodexContext/4_CodexContext.md` into a temporary phase-plan normalization tracker, deleting the older mixed checklists and replacing them with one three-step workflow plus per-file sub-checklists for the remaining family phase-plan files
88. 2026-03-08 00:00 - Added short ownership labels back to the `Future Legacy Updates` reminder list in `docs/Phase-Plans/00_Phase-Setup.md` so the compact existing-prefix guidance still says what `VR`, `DBG`, `DOC`, and `LEG` mean
87. 2026-03-08 00:00 - Pruned the `Future Legacy Updates` section in `docs/Phase-Plans/00_Phase-Setup.md` so only `REF` remains as a plausible future standalone prefix candidate after mapping the other wishlist areas back onto existing canonical families like `VR`, `DBG`, `AS`, and `DOC`
86. 2026-03-08 00:00 - Simplified the `Future Legacy Updates` section in `docs/Phase-Plans/00_Phase-Setup.md` into a compact numbered prefix list so the candidate future families now scan more like the canonical prefix blocks
85. 2026-03-08 00:00 - Added a `Future Legacy Updates` section to `docs/Phase-Plans/00_Phase-Setup.md`, using `docs/Human-Plans/Wish-Features/00 - legacy-Wishes.md` to seed non-canonical candidate prefixes like `GZ`, `SCN`, `REF`, `MAT`, `INS`, `CAM`, `WB`, and `AUD` without promoting them into the active canonical prefix list yet
84. 2026-03-08 00:00 - Added a cautious `L` legacy pass to `docs/Phase-Plans/06_DR - Phase-Plans.md`, marking the old pre-canonical driver ID forms, dual-read alias compatibility, and the still-supported primitive-value compatibility path as likely future removals while leaving the rest of the `DR` checklist history untouched
83. 2026-03-08 00:00 - Rebuilt `docs/Phase-Plans/06_DR - Phase-Plans.md` from `docs/CHANGELOG.md`, promoting `DR - Phase 3` to reconstructed status and adding real summaries, grouped detailed checklists, and file-footprint sections for `DR - Phase 3` and `DR - Phase 7` through `12`, then marked `DR` complete inside `DOC - Phase 14F`
82. 2026-03-08 00:00 - Created `docs/Phase-Plans/06_DR - Phase-Plans.md` in the settled family-file format and seeded it with the canonical `DR` phase ladder from `docs/Phase-Plans/00_Phase-Setup.md`, using placeholder summaries and checklist stubs for the later changelog reconstruction pass
81. 2026-03-08 00:00 - Built `docs/Human-Plans/roadmap/roadmap.md` as a clean short roadmap surface with the immediate three-step direction ordered as `DOC - Phase 14F`, roadmap buildout, then legacy understanding
80. 2026-03-08 00:00 - Trimmed `docs/Phase-Plans/00_Phase-Setup.md` back toward a lean rules-and-registry doc by removing stale workflow sections, duplicated prefix summaries, and the long researched-history interpretation blocks while keeping the canonical family-file rules, prefix list, and compact phase checklist
79. 2026-03-08 00:00 - Added the canonical `LEG` family to `docs/Phase-Plans/00_Phase-Setup.md`, including the meta/docs prefix list, total prefix count, compact checklist row, and researched-family guidance so legacy-removal planning is now part of the active phase system
78. 2026-03-08 00:00 - Rebuilt `docs/Phase-Plans/15_LEG - Phase Plan.md` into the settled phase-plan structure, collapsing the earlier top-level legacy-removal stages into one reusable `LEG - Phase 1` with `1A` through `1F` sub-phases
77. 2026-03-08 00:00 - Built `docs/Phase-Plans/15_LEG - Phase Plan.md` as a real legacy-removal planning file with identification, classification, replacement-path, removal-wave, and verification sections so legacy cleanup now has a structured starting frame
76. 2026-03-08 00:00 - Rebuilt `docs/Phase-Plans/05_PT - Phase-Plans.md` from `docs/CHANGELOG.md`, promoting `PT - Phase 1` and `PT - Phase 2` to reconstructed phases and adding detailed summaries, grouped checklist content, and file-footprint sections for the currently evidenced `PT` family, then marked `PT` complete inside `DOC - Phase 14F`
75. 2026-03-08 00:00 - Replaced the generic grouped placeholder in `docs/Phase-Plans/05_PT - Phase-Plans.md` with the canonical `PT` phase ladder from `docs/Phase-Plans/00_Phase-Setup.md`, so the family file now reflects the real required phase numbers and separates confirmed versus future rows
74. 2026-03-08 00:00 - Created `docs/Phase-Plans/05_PT - Phase-Plans.md` in the settled family-file structure with the canonical header, fold guide, and a temporary grouped placeholder block so the `PT` family now has a proper landing surface for later reconstruction
73. 2026-03-08 00:00 - Restored the local `Doc History` block in `docs/Chill-Log.md` as a structure-only section for header/rule changes, while keeping ordinary chill activity in the active batch
72. 2026-03-08 00:00 - Clarified the `docs/Chill-Log.md` exception in `docs/Doc-Index.md` so ordinary batch activity stays in the active chill batch while real header/rule/structure changes may still use a local `Doc History`
71. 2026-03-08 00:00 - Added the explicit `docs/Chill-Log.md` exception wording to `docs/Doc-Index.md` so the canonical doc-history rule now says this file should log its own changes only in the active chill batch
70. 2026-03-08 00:00 - Removed the local `Doc History` block from `docs/Chill-Log.md` so this file now matches the repo exception rule and records its own changes only in the active chill batch
69. 2026-03-08 00:00 - Updated the canonical checklist rules in `docs/Doc-Index.md` and `docs/Phase-Plans/00_Phase-Setup.md` so `L` now means legacy feature to remove and the new `R` marker means removed
68. 2026-03-08 00:00 - Added a cautious `L` legacy pass to `docs/Phase-Plans/04_FS - Phase-Plans.md`, marking only the explicit FS compatibility aliases, `Option-B` naming debt, debug-preview framing, `CubeProof` proof path, backward-compatible enable behavior, and preserved singleton-key seams as likely future removals
67. 2026-03-08 00:00 - Added the canonical reconstructed-title rule to `docs/Phase-Plans/00_Phase-Setup.md` so confirmed reconstructed family phases now explicitly use the `- Reconstructed` suffix while gap/provisional rows stay separate
66. 2026-03-08 00:00 - Rebuilt `docs/Phase-Plans/04_FS - Phase-Plans.md` from `docs/CHANGELOG.md`, adding detailed summaries, grouped checklist content, and file-footprint sections for `FS - Phase 9` through `FS - Phase 15`, and marked `FS` complete inside `DOC - Phase 14F`
65. 2026-03-08 00:00 - Added the completed `Doc History` cleanup file list from `docs/Archive/CodexContext/4_CodexContext.md` into `DOC - Phase 14G` in `docs/Phase-Plans/14_DOC - Phase-Plans.md` so the canonical docs plan now records the accomplished cleanup targets directly
64. 2026-03-08 00:00 - Added `DOC - Phase 14G - Doc History Standardization` to `docs/Phase-Plans/14_DOC - Phase-Plans.md`, including a short summary and checklist so future doc-history cleanup has a stable tracked home inside the canonical docs buildout
63. 2026-03-08 00:00 - Moved the active remaining family-file checklist from `docs/Archive/CodexContext/4_CodexContext.md` into `DOC - Phase 14` in `docs/Phase-Plans/14_DOC - Phase-Plans.md` so the canonical docs phase plan now carries that working task surface directly
62. 2026-03-08 00:00 - +? -? - Clarified in `docs/Chill-Log.md` that chill-batch entries should stay newest-first in source and re-ordered `Chill Batch #4` so the entry list now follows that rule explicitly
61. 2026-03-08 00:00 - +? -? - Completed the remaining four `Doc History` cleanup files in strict sequence: `docs/Phase-Plans/03_NI - Phase-Plans.md`, `docs/Phase-Plans/04_FS - Phase-Plans.md`, `docs/Phase-Plans/00_Phase-Setup.md`, then `docs/Chill-Log.md`, and checked each one off in `docs/Archive/CodexContext/4_CodexContext.md` immediately after it was done
60. 2026-03-08 00:00 - +? -? - Added a second checklist to `docs/Archive/CodexContext/4_CodexContext.md` tracking which files have had their `Doc History` blocks fixed and which still need explicit renumber cleanup
59. 2026-03-08 00:00 - +? -? - Rebuilt `docs/Archive/CodexContext/4_CodexContext.md` as a compact checklist of the family phase-plan files that still need normalization or historical-content work during `DOC - Phase 14`
58. 2026-03-08 00:00 - +? -? - Re-numbered the `##### Doc History` blocks in `docs/Phase-Plans/01_GE - Phase-Plans.md` and `docs/Phase-Plans/02_VM - Phase-Plans.md` so both files now use explicit newest-first numbering with the highest number at the top
57. 2026-03-08 00:00 - +? -? - Re-numbered the `##### Doc History` block in `docs/Phase-Plans/14_DOC - Phase-Plans.md` so the source now uses explicit newest-first numbering with the highest number at the top
56. 2026-03-08 00:00 - +? -? - Corrected the `docs/CHANGELOG.md` `Doc History` numbering direction so the newest top entry now carries the highest number while the list still stays newest-first
55. 2026-03-08 00:00 - +? -? - Re-numbered the `Doc History` block in `docs/CHANGELOG.md` so the source now explicitly matches the newest-first ordering rule instead of relying on markdown auto-numbering
54. 2026-03-08 00:00 - +? -? - Re-numbered the `Doc History` block in `docs/Doc-Index.md` so the source now explicitly matches the newest-first ordering rule instead of relying on markdown auto-numbering
53. 2026-03-08 00:00 - +? -? - Reformatted `docs/Phase-Plans/04_FS - Phase-Plans.md` to the settled family-file layout, removing `Doc Body`, moving phase titles to `##`, adding the header fold pattern and `Fold Mode Guide`, and using the overview-side fold wrapper consistently
52. 2026-03-08 00:00 - +? -? - Added the direct reconstructed provenance label to the DOC family where it is currently true, so `DOC - Phase 1` in `docs/Phase-Plans/14_DOC - Phase-Plans.md` now says `- Reconstructed` in the heading
51. 2026-03-08 00:00 - +? -? - Added direct reconstructed provenance labels to the reconstructed phase titles in `docs/Phase-Plans/01_GE - Phase-Plans.md` and `docs/Phase-Plans/02_VM - Phase-Plans.md`, matching the same heading-level pattern now used in NI
50. 2026-03-08 00:00 - +? -? - Added direct reconstructed provenance labels to the NI phase titles where it is actually true, so the early gap rows in `docs/Phase-Plans/03_NI - Phase-Plans.md` now read as reconstructed in the heading itself
49. 2026-03-08 00:00 - +? -? - Restored detailed NI phase checklists, added file-footprint sections, and strengthened the summaries in `docs/Phase-Plans/03_NI - Phase-Plans.md` by mining the matching `NI - Phase 3` through `NI - Phase 5` entries from `docs/CHANGELOG.md`
48. 2026-03-08 00:00 - +? -? - Reformatted `docs/Phase-Plans/03_NI - Phase-Plans.md` to the settled family-file layout, removing `Doc Body`, moving phase titles to `##`, adding the overview-side fold wrapper, and adding the header `Fold Mode Guide`
47. 2026-03-08 00:00 - +? -? - Updated `DOC - Phase 14` in `docs/Phase-Plans/14_DOC - Phase-Plans.md` so its notes, decision list, and checklist now reflect the actual completed normalization work across `GE`, `VM`, `DOC`, the setup rule, and the locked family-file format decisions
46. 2026-03-08 00:00 - +? -? - Strengthened the `Phase Summary` blocks in `docs/Phase-Plans/02_VM - Phase-Plans.md` so they now describe the actual VM boundary shifts and why each phase mattered, not just the high-level result labels
45. 2026-03-08 00:00 - +? -? - Restored detailed VM phase checklists and file-footprint sections in `docs/Phase-Plans/02_VM - Phase-Plans.md` by mining the matching `VM - Phase 1` through `VM - Phase 5` entries from `docs/CHANGELOG.md`
44. 2026-03-08 00:00 - +? -? - Added the new `Fold Mode Guide` header section to `docs/Phase-Plans/01_GE - Phase-Plans.md`, `docs/Phase-Plans/02_VM - Phase-Plans.md`, and `docs/Phase-Plans/14_DOC - Phase-Plans.md` so the quick-fold system is documented directly in each active family file
43. 2026-03-08 00:00 - +? -? - Added a family-file header-note rule to `docs/Phase-Plans/00_Phase-Setup.md` so each `Phase-Plans.md` file can include a short fold-mode guide for later readers using the current `Ctrl+2 / Ctrl+3 / Ctrl+4` labels
42. 2026-03-08 00:00 - +? -? - Tightened the header description in `docs/Phase-Plans/00_Phase-Setup.md` so the family-file rule now explicitly matches the live `Fold Hack 3` / `Fold Hack 4` header pattern with the real housekeeping sections at `#####`
41. 2026-03-08 00:00 - +? -? - Tightened the header area in `docs/Phase-Plans/02_VM - Phase-Plans.md` to the clearer `Fold Hack 3` / `Fold Hack 4` pattern, with the real header sections sitting at `#####`
40. 2026-03-08 00:00 - +? -? - Reformatted `docs/Phase-Plans/02_VM - Phase-Plans.md` to the settled family-file layout first, removing `Doc Body`, moving phase titles to `##`, adding the overview-side fold wrapper, and leaving checklist content directly visible for the next content pass
39. 2026-03-08 00:00 - +? -? - Updated `docs/Phase-Plans/14_DOC - Phase-Plans.md` to the settled family-file layout by flattening the header, adding the overview-side fold wrapper, and keeping checklist content directly visible under each phase checklist
38. 2026-03-08 00:00 - +? -? - Updated `docs/Phase-Plans/00_Phase-Setup.md` so the family phase-plan rule now matches the settled `GE` layout exactly: overview-side fold hack only, direct-open checklist section, and the new `Ctrl+3 / Ctrl+4 / Ctrl+5` behavior
37. 2026-03-08 00:00 - +? -? - Removed the checklist-side fold wrapper from every phase in `docs/Phase-Plans/01_GE - Phase-Plans.md` while keeping the overview-side fold wrapper, so `Ctrl+4` now opens checklist content directly but still hides notes and file-footprint sections
36. 2026-03-08 00:00 - +? -? - Added the in-phase `#### Fold Hack` wrapper pattern across every phase in `docs/Phase-Plans/01_GE - Phase-Plans.md` so `Ctrl+4` stays on the compact phase surface and the notes, file-footprint sections, and detailed checklist content now live one fold level deeper
35. 2026-03-08 00:00 - +39 -2 - Expanded `GE - Phase 5` through `GE - Phase 7` in `docs/Phase-Plans/01_GE - Phase-Plans.md` using the reconstructed gap details and changelog bodies, adding likely file-touch sections plus grouped detailed checklist structure for the vertical-slice, affected-part-routing, and early-modern-baseline phases
34. 2026-03-08 00:00 - +54 -3 - Expanded `GE - Phase 8` through `GE - Phase 10` in `docs/Phase-Plans/01_GE - Phase-Plans.md` using the concrete implementation detail from `docs/CHANGELOG.md`, adding grouped detailed checklists plus file-change sections for the runtime bridge hardening, graph command kernel, and contract-lock phases
33. 2026-03-08 00:00 - +2 -58 - Cleaned `docs/Phase-Plans/01_GE - Phase-Plans.md` so `GE - Phase 1` through `GE - Phase 3` each keep one main detailed checklist surface instead of duplicating a second short flat checklist, and moved Phase 1 file-footprint sections into the notes area to match the newer phase layout
32. 2026-03-08 00:00 - +5 -6 - Re-formatted `docs/Phase-Plans/01_GE - Phase-Plans.md` to match the new flatter family-file structure, with the lighter header fold-hack, direct `##` phase titles, and `###` overview/checklist buckets
31. 2026-03-08 00:00 - +19 -14 - Rewrote the `Family Phase-Plan Format Rule` in `docs/Phase-Plans/00_Phase-Setup.md` so it now documents the exact flatter format being kept in `DOC - Phase-Plans.md`, including phase titles at `##`, `Overview` / `CheckList` at `###`, and the lighter header fold-hack
30. 2026-03-08 00:00 - +3 -19 - Removed the old `DOC - Phase 13F` follow-up block from `docs/Phase-Plans/DOC - Phase-Plans.md` after its scope was effectively absorbed by `DOC - Phase 14`
29. 2026-03-08 00:00 - +0 -1 - Re-formatted `docs/Phase-Plans/DOC - Phase-Plans.md` as a test pass to flatten the heading structure, removing the extra family wrapper so canonical phase titles now sit at `##` and `Overview` / `CheckList` blocks sit at `###`
28. 2026-03-08 00:00 - +12 -0 - Added a `Phase 14A` decision list to `docs/Phase-Plans/DOC - Phase-Plans.md` covering the format choices that still need to be locked before the family phase-plan files can be normalized consistently
27. 2026-03-08 00:00 - +101 -0 - Expanded `docs/Phase-Plans/GE - Phase-Plans.md` so `GE - Phase 1` through `GE - Phase 3` now include grouped high-detail checklist buckets and estimated worked-on/added file sections compiled from `docs/Archive/History/0 - History-TaskLog.md`
26. 2026-03-08 00:00 - +12 -1 - Compiled DOC-family evidence from `docs/Archive/History/0 - History-TaskLog.md` into `docs/Phase-Plans/DOC - Phase-Plans.md`, strengthening `DOC - Phase 3`, `DOC - Phase 11`, and `DOC - Phase 14B` with roadmap handoff detail, restored-checklist structure detail, and an explicit primary archived reconstruction source
25. 2026-03-08 00:00 - +9 -5 - Updated `docs/Phase-Plans/DOC - Phase-Plans.md` so `DOC - Phase 14` now reflects that `00_Phase_Log.md` and `TaskHistoryCompilation.md` are archived, marks `00_Phase-Setup.md` complete under the rename sub-phase, and shifts the remaining checklist items to the current live-versus-archived file state
24. 2026-03-08 00:00 - +17 -12 - Moved the repo-wide checklist marker/structure standard into `docs/Doc-Index.md` as `Global CheckList Rule` and reduced `docs/Phase-Plans/00_Phase-Setup.md` to a shorter phase-specific checklist reference block
23. 2026-03-08 00:00 - +21 -20 - Updated `docs/Phase-Plans/00_Phase-Setup.md` so the canonical docs/meta prefix references now use `DOC` instead of `OO`, including the family-file example, prefix list, compact checklist block, and researched family section
22. 2026-03-08 00:00 - +1 -1 - Marked the `docs/CHANGELOG.md` item complete under `DOC - Phase 14E` in `docs/Phase-Plans/DOC - Phase-Plans.md` after the changelog title layer was hand-updated from `OO` to `DOC`
21. 2026-03-08 00:00 - +2 -1 - Cleaned `docs/Doc-Index.md` so its foldable docs map now points to `DOC - Phase-Plans.md` instead of the old `OO - Phase-Plan.md` family file reference
20. 2026-03-08 00:00 - +10 -0 - Expanded `DOC - Phase 14` with a new `14E` sub-phase for the `OO` to `DOC` prefix migration, including a required-file checklist for `CHANGELOG`, `00_Phase-Setup`, `00_Phase_Log`, and `Doc-Index`
19. 2026-03-08 00:00 - +2 -2 - Renamed `docs/Phase-Plans/OO - Phase-Plans.md` to `docs/Phase-Plans/DOC - Phase-Plans.md`, switched the internal family prefix from `OO` to `DOC`, and fixed the file title so the docs/meta family can start moving to the clearer `DOC` prefix
18. 2026-03-08 00:00 - +15 -0 - Expanded `OO - Phase 14A` in `docs/Phase-Plans/OO - Phase-Plans.md` with one checklist item per required canonical family `Phase-Plans.md` file, marking the existing family docs complete and the missing prefix docs as still needing dedicated files
17. 2026-03-08 00:00 - +45 -37 - Re-formatted `docs/Phase-Plans/GE - Phase-Plans.md` into the live family-file structure, adding the fold-oriented header nesting, a single family `##` section, and normalized per-phase `Overview` / `CheckList` buckets while preserving the existing GE phase content
16. 2026-03-08 00:00 - +45 -0 - Added `OO - Phase 14 - Canonical Family Phase-Plan Buildout` to `docs/Phase-Plans/OO - Phase-Plans.md` in the current family-doc format, covering normalization, reconstruction, checklist migration, and roadmap support
15. 2026-03-08 00:00 - +2 -23 - Removed the duplicated local `Doc History` rule from `docs/Chill-Log.md` and marked `docs/Doc-Index.md` as the canonical home for that repo-wide docs rule
14. 2026-03-08 00:00 - +12 -8 - Cleaned the top of `docs/Chill-Log.md` into a proper `Doc Header` / `Doc Body` layout, renamed stale rule headings, and updated old historian/phase path references to the current docs structure
13. 2026-03-07 14:51 - +132 -131 - Batch-normalized the numbered title lines in `docs/CHANGELOG.md` to the new dashed header format, keeping modern entries date-based and reconstructed entries source-tag-based while leaving entry bodies unchanged
12. 2026-03-07 14:24 - +157 -5 - Added normalized `HUMAN SUMMARY:` lines across every numbered entry and reconstructed marker block in `docs/CHANGELOG.md`, using entry-body context and keeping summaries directly under each wrapper for folding-friendly scanning
11. 2026-03-07 13:44 - +4 -0 - Added the new headerless `HUMAN SUMMARY:` rule to `docs/CHANGELOG.md` so future entries include a compact 1-3 sentence readable summary directly under the entry wrapper without adding another fold level
10. 2026-03-07 13:39 - +5 -3 - Tightened the forward changelog-title rule so new entries should use the backticked canonical title shape like ``### [119] 2026-03-05 23:09 `SP - Phase 6 - Resizable Debug Inspector Drawer` `` without requiring a mass rewrite of older entries
9. 2026-03-07 13:36 - +13 -0 - Populated `### Changelog Rules` in `docs/CHANGELOG.md` so the doc header now describes the actual entry wrapper and heading format used in the body
8. 2026-03-07 13:34 - +6 -0 - Tightened `docs/CHANGELOG.md` changelog rules so new permanent entries now explicitly require canonical phase-prefix naming in the entry title when the work belongs to a canonical phase family
7. 2026-03-07 13:31 - +31 -4 - Added a `Changelog Format` block to the `docs/CHANGELOG.md` doc header so the file now documents its own entry wrapper, numbering rule, and heading-level structure
6. 2026-03-07 13:27 - +? -? - Shifted the main `docs/CHANGELOG.md` entry headers from `##` to `###`, shifted per-entry section headings from `###` to `####`, and updated the header rule text so the current changelog folding structure matches the new layout
5. 2026-03-07 13:21 - +17 -2 - Updated the top of `docs/CHANGELOG.md` to use a proper `Doc Header` and `Doc Body` split while keeping the existing changelog entry format and numbering intact
4. 2026-03-07 13:18 - +10 -8 - Re-structured `docs/Phase-Plans/00_Phase-Setup.md` to follow the `Doc-Index.md` doc rules with `Doc Header` and `Doc Body`, and corrected the stale phase-plan lifecycle path references in the top guidance
3. 2026-03-07 12:55 - +24 -0 - Expanded the `Human Docs Map` in `docs/Doc-Index.md` so the `Archive/` branch now includes archived files as well as archive folders
2. 2026-03-07 12:40 - +? -? - Consolidated the docs meta-system into `docs/Doc-Index.md`, rebuilt it as the central docs navigation + rules file, added the `/docs` hierarchy and canonical source guidance there, and removed the now-redundant `docs/Documentation-Rules.md`
1. 2026-03-07 12:40 - +16 -0 - Reactivated chill mode and started `Chill Batch #4` in `docs/Chill-Log.md`

<!-- ============================================================ -->
### Chill Batch #3
<!-- ============================================================ -->

Status:
- compiled

Started:
- 2026-03-06 20:53

Ended:
- 2026-03-07 10:37

Mode:
- chill mode inactive

Consolidated To:
- `docs/CHANGELOG.md` `[104]`

Current Goal:
- rapid repo edits under the canonical chill log without writing a full permanent changelog entry for each small change

Entries:
1. 2026-03-06 20:53 - +22 -0 - Reactivated chill mode and started `Chill Batch #3` in `docs/Chill-Log.md`
2. 2026-03-06 20:54 - +7 -2 - Updated the chill-batch instructions so each entry line now includes its own timestamp in addition to the inline diff
3. 2026-03-06 20:57 - +40 -0 - Added two explicit gap-story sections to `docs/Phases/00_Phase_List.md` so the phase narrative now explains the missing early repo-execution window and the missing handoff into the first shipped changelog wave
4. 2026-03-06 20:59 - +10 -0 - Normalized the `## Phase explination` section headers in `docs/Phases/00_Phase_List.md` so sections `4` through `8` now use the same full-width divider bars above and below the heading as the earlier sections
5. 2026-03-06 21:12 - +? -? - Ran a best-effort HTML-comment cleanup pass on `docs/History/8 - COPY.md`, wrapping likely ChatGPT response blocks while keeping visible user prompt lines and several pasted repo/code seams readable for later history extraction
6. 2026-03-06 21:17 - +? -? - Reformatted the visible ChatGPT block markers in `docs/History/8 - COPY.md` so the start label is followed by a real `<!--` line and the end label is preceded by a real `-->` line
7. 2026-03-06 21:22 - +? -? - Rebuilt the prompt labels in `docs/History/8 - COPY.md` so the file now shows `## total user prompts` as `61`, starts with `## user prompt #1`, and inserts a new incremented `## user prompt #N` immediately after each `<!-- CHATGPT BLOCK END -->`
8. 2026-03-06 21:29 - +? -? - Updated `docs/History/0 - compiled-HISTORY.md` so conversation `8` now explicitly records the preserved seam from pre-repo planning, to the repo-setup Codex prompt, to the later visible jump where the next preserved ask is the `3 sliders` box vertical slice
9. 2026-03-06 21:59 - +? -? - Rebuilt `docs/History/9 - COPY.md` into the new history-reading format with a short top summary, numbered `## User Prompt [N]` sections, backticked short prompts, double-indented pasted prompt/code blocks, and HTML-commented ChatGPT response blocks keyed off `Architectural refactor progress:`
10. 2026-03-06 22:02 - +? -? - Added a `First 10 Achievements` block near the top of `docs/History/9 - COPY.md` so the first routing/planning prompt bands now read like a milestone list instead of only raw transcript sections
11. 2026-03-06 22:07 - +? -? - Extended `docs/History/9 - COPY.md` under `## Prompt/respone Summary` with one-line achievement summaries for prompts `11` through `30`, covering file verification, routing-plan amendments, bootstrap hardening, rename planning, and the move into the instance-aware parts phase
12. 2026-03-06 22:17 - +? -? - Finished `docs/History/9 - COPY.md` under `## Prompt/respone Summary` with one-line achievement summaries for prompts `31` through `46`, covering Spaghetti Editor naming, graph-first direction, the `6A`/`6B` split, and Phase `6B` implementation/validation
13. 2026-03-06 22:31 - +? -? - Created `docs/History/MISSINGTASKS.md` as a best-effort reconstruction doc for the early `/20/` history gap, separating stronger inferred missing tasks from lower-confidence possibilities between conversations `8` and `9`
14. 2026-03-06 22:40 - +? -? - Created `docs/History/12 - COPY.md` in the same readable history format as `9 - COPY.md`, with a top summary, prompt/response summary, one numbered user prompt band, and the assistant reply wrapped in HTML comments
15. 2026-03-06 22:47 - +? -? - Updated `docs/TaskHistoryCompilation.md` by breaking the likely repo-start Codex prompt from `docs/History/12 - COPY.md` into a concrete checklist under the app-creation foundation block
16. 2026-03-06 22:55 - +? -? - Added a new top `Canonical Accepted Phases` section to `docs/TASKLIST.md`, consolidating the accepted canonical phases in chronological order from `docs/Phases/00_Phase_List.md` while leaving the older detailed task blocks intact below
17. 2026-03-07 00:03 - +? -? - Normalized the visible phase prefixes and phase names in `docs/TaskHistoryCompilation.md` to the canonical system by collapsing `CK` and `CT` into `GE`, collapsing `DC` into `OO`, and removing the older `-0A/-0B/-0C` label style from the visible headings
18. 2026-03-07 00:12 - +? -? - Added the remaining restart-era completed task blocks from `/docs/History/` into `docs/TaskHistoryCompilation.md` under `## COMPLETED PHASE TASKLISTS`, covering `AS`, `PT`, `JK`, `VM`, `VR`, `EX`, and `ADV` above the clean-restart `GE` foundation block
19. 2026-03-07 00:19 - +? -? - Created `docs/DOCindex.md` as a raw `/docs` inventory showing the current file count and per-file line counts across the documentation tree
20. 2026-03-07 00:20 - +? -? - Corrected `docs/DOCindex.md` so its self-entry reflects the file's actual current line count instead of the initial pre-write scan value
21. 2026-03-07 00:23 - +? -? - Reformatted the `docs/DOCindex.md` inventory rows to the bracketed zero-padded count style like `[0000] - path`, matching the new raw-scan display format
22. 2026-03-07 00:28 - +? -? - Added separate summed totals to `docs/DOCindex.md` for non-conversation docs and conversation docs, replaced the placeholder main total row with the real non-conversation total, and corrected the file's updated self-count
23. 2026-03-07 00:31 - +? -? - Reformatted all displayed counts in `docs/DOCindex.md` to the bracketed comma style like `[0,000]`, including the separated conversation totals and the main file inventory rows
24. 2026-03-07 00:46 - +? -? - Rebuilt `docs/Phases/00_Phase_Log.md` so it now matches the current canonical `59`-phase completed list from `docs/Phases/00_Phase_List.md`, including the later `OO` phases and the legacy-marked `NI - Phase 3` row
25. 2026-03-07 01:05 - +? -? - Rebuilt `docs/Phases/00_Phase_Log.md` again with fuller checklist-style detail across all canonical `59` phases after the prior compressed rewrite removed too much task-level information
26. 2026-03-07 01:11 - +? -? - Added two explicit speculative `Gap Phase` blocks to `docs/Phases/00_Phase_Log.md` for the missing restart-execution bridge and the missing first-shipped-wave handoff, using `[?]` checklist items and keeping them outside the `59` completed-phase count
27. 2026-03-07 01:16 - +? -? - Cleaned the top of `docs/Phases/00_Phase_Log.md` so it now has a clearer purpose block, reading instructions, canonical rules, and a proper legend for `[x]`, `[~]`, and `[?]`
28. 2026-03-07 01:27 - +? -? - Rebuilt `docs/Phases/00_Phase-CheckList.md` into a real completed-and-future canonical phase checklist, keeping the prefix organization, adding roadmap-derived future phases, and separating speculative gaps from planned work
29. 2026-03-07 01:34 - +? -? - Folded the compact canonical completed-and-future checklist into `docs/Phases/00_Phase-Setup.md` near the top so setup now carries the active prefixes, rules, and grouped phase inventory in one place
30. 2026-03-07 01:36 - +? -? - Aligned the top prefix descriptions in `docs/Phases/00_Phase-Setup.md` to the same ownership wording used by the canonical checklist so the master prefix list and checklist headings now describe each prefix the same way
31. 2026-03-07 01:40 - +? -? - Tightened the top guidance in `docs/Phases/00_Phase-Setup.md` and `docs/Phases/00_Phase_Log.md` to make the split explicit, then added a matching `Phase Docs Rule` to `AGENTS.md` so future phase work follows the same setup-vs-log workflow
32. 2026-03-07 09:50 - +? -? - Rebuilt `docs/repo.md` as a cleaner repository snapshot with a proper header, explicit snapshot timestamp, a smaller core hierarchy, and a medium hierarchy that shows the current `src/` layout
33. 2026-03-07 09:53 - +? -? - Added `Repository Hierarchy (High Detail)` to `docs/repo.md`, expanding only the architecture-defining files across app, Spaghetti, worker, viewer, shared contracts, and core docs
34. 2026-03-07 09:56 - +? -? - Expanded the `Snapshot Info` section in `docs/repo.md` with a readable summary of what the three hierarchy levels mean and what the main repo folders and selected high-detail files are responsible for
35. 2026-03-07 09:57 - +? -? - Added a `Golden Rule` section under the core stack in `docs/repo.md` to restate the main ownership boundaries between app, Spaghetti, worker, viewer, shared contracts, geometry, and docs
36. 2026-03-07 10:02 - +? -? - Relaxed the chill-mode diff rule in `AGENTS.md` and `docs/Chill-Log.md` so routine small chill-mode edits no longer need a `Diff:` line unless requested or during batch closeout
37. 2026-03-07 10:06 - +? -? - Added a formal `Phase Loop Rule` to `AGENTS.md` covering idea classification, future phase-file creation, planning, execution readiness, post-implementation logging, and promotion into the canonical completed phase docs

<!-- ============================================================ -->
### Chill Batch #2
<!-- ============================================================ -->

Status:
- compiled

Started:
- 2026-03-06 09:46

Ended:
- 2026-03-06 20:47

Mode:
- chill mode inactive

Consolidated To:
- `docs/CHANGELOG.md` `[102]`

Current Goal:
- build a prefix-aligned historical task map from the early restart conversations so the engine foundations and surrounding system decisions are easier to understand later

Entries:
1. +0 -0 - Chill mode reactivated and `Chill Batch #2` started
2. +3 -2 - Updated chill-mode batch-entry format so each entry includes its inline diff as `+A -B - Entry text`
3. +19 -0 - Added a top-level current phase-prefix list and local `Doc History` block to `docs/Phases/01_Phase-Setup.md`
4. +3 -0 - Tightened the chill-mode batch rule so entries use real `+A -B` counts when reliable and `+? -?` when a clean count is not possible
5. +? -? - Added a quick prefix cheat sheet to `docs/Phases/01_Phase-Setup.md` mapping common Spaghetti editor, node, driver, feature, viewer, and Jake-mode work to the phase prefixes they should usually use
6. +? -? - Added `Prefix Cheat Sheet 2` to `docs/Phases/01_Phase-Setup.md`, organized by prefix with bullet lists of the kinds of work that usually belong under each phase code
7. +43 -0 - Added `docs/Plans/README.md` as a navigation index linking the main architecture, wish-feature, Spaghetti, decision, and temporary planning docs
8. +? -? - Re-formatted `docs/Plans/README.md` so the link labels begin with `/docs/...` paths and are easier to scan from the repo root
9. +? -? - Removed the duplicate markdown link targets from `docs/Plans/README.md` so the index now shows clean `/docs/...` path lines only
10. +135 -0 - Added `docs/roadmap/ROADMAP.md` as the go-forward project roadmap, grouped by stable phase prefix with planned phases and sub-phases across graph, Spaghetti, drivers, assembly, viewer, Jake mode, debug, and export
11. +? -? - Grouped `docs/roadmap/ROADMAP.md` into named Sections under each prefix so the roadmap can expand without reusing or resetting phase numbers
12. +? -? - Re-formatted the prefix titles in `docs/roadmap/ROADMAP.md` into divider-wrapped code-style labels so the roadmap reads more clearly in raw Markdown while keeping the `###` Section headings
13. +? -? - Expanded the divider lines around the prefix titles in `docs/roadmap/ROADMAP.md` so they read more like horizontal separators in the raw markdown view
14. +? -? - Standardized every prefix divider in `docs/roadmap/ROADMAP.md` to the exact long separator line so all roadmap prefix blocks use the same visual width
15. +? -? - Normalized the rest of `docs/roadmap/ROADMAP.md` to match the `GE` block layout with a compact section list in each prefix header and indented detailed sections and task bullets underneath
16. +? -? - Converted the remaining section labels in `docs/roadmap/ROADMAP.md` to the same backticked code-style format used in the `GE` block so all prefix sections now render consistently
17. +? -? - Changed only the compact section lists in the roadmap prefix headers back to blue `###` headings so they match `GE`, while leaving the detailed section labels underneath in orange backticks
18. +? -? - Added a bottom `Achieved So Far` section to `docs/roadmap/ROADMAP.md` summarizing the major completed phase groups already landed in the repo
19. +? -? - Re-formatted the `Achieved So Far` section in `docs/roadmap/ROADMAP.md` to match the main roadmap block style and changed the completed entries to checked `[x]` items
20. +? -? - Reworked the `Achieved So Far` section so each completed block uses descriptive section names and grouped older completed prefixes under the closest current ownership areas instead of a generic `Completed` label
21. +? -? - Added a `Lost History` block under `Achieved So Far` in `docs/roadmap/ROADMAP.md` for older completed phases that do not map cleanly into the current go-forward prefix system
22. +167 -0 - Compiled the raw ChatGPT conversation in `docs/History/1 - chatgpt.md` into `docs/History/0 - compiled-HISTORY.md` as a structured summary of the early architecture discussion, one-truth principle, rebuild/worker model, and v18 restart ideas
23. +12 -0 - Recreated `docs/History/0 - compiled-HISTORY.md` as a short compiled-history index and added conversation 1 with an estimated `v18 planning era` label and a compact milestone/decision summary
24. +55 -0 - Added a structured summary block to the top of `docs/History/2 - chatgpt.md` covering the node-based input system direction, spaghetti wire rendering idea, gizmo migration strategy, and composite vec2 port UX decisions
25. +? -? - Added conversation 2 to `docs/History/0 - compiled-HISTORY.md` with an estimated `early v20 / Spaghetti planning era` label and a short summary of the node-graph, spaghetti-wire, gizmo-migration, and composite-port decisions
26. +92 -40 - Added a `Historian Mode Rule` to `AGENTS.md` defining the workflow for processing `docs/History/# - chatgpt.md` files into a structured top-of-file summary and a short compiled entry in `docs/History/0 - compiled-HISTORY.md`
27. +56 -0 - Added a structured summary block to the top of `docs/History/3 - chatgpt.md` covering the modern v20 architecture layout, parts-list decision, viewer-only control rules, vertical-slice path, and early real-toe roadmap
28. +? -? - Added conversation 3 to `docs/History/0 - compiled-HISTORY.md` with an estimated `early-to-mid v20 buildout era` label and a short summary of the concrete app/viewer/worker split, parts-list direction, and viewer-only control decisions
29. +? -? - Moved the structured summary to the top of `docs/History/3 - chatgpt.md` and wrapped the full raw transcript below it in an HTML comment so the file renders like a readable summary instead of a giant pasted conversation
30. +50 -0 - Added a structured summary block to the top of `docs/History/4 - chatgpt.md` covering the repo reinitialization, first public v20 Git baseline, and GitHub Pages/Actions recovery decisions
31. +? -? - Added conversation 4 to `docs/History/0 - compiled-HISTORY.md` with an estimated `first public v20 repo baseline` label and a short summary of the Git recovery, force-push baseline, and deployment restoration decisions
32. +108 -40 - Tightened the `Historian Mode Rule` in `AGENTS.md` so the workflow now explicitly requires a roughly 100-line Markdown summary at the top of each history file and wrapping the full raw conversation underneath in an HTML comment
33. +? -? - Brought `docs/History/1 - chatgpt.md`, `2 - chatgpt.md`, and `4 - chatgpt.md` into the historian format by adding clean top summaries and wrapping their raw transcripts in HTML comments
34. +? -? - Added a structured historian summary to `docs/History/5 - chatgpt.md` covering current v20 validation, Zod at the worker boundary, Spaghetti editor maturity, and Feature Stack v1 planning, then wrapped the raw transcript in an HTML comment
35. +? -? - Added conversation 5 to `docs/History/0 - compiled-HISTORY.md` with an estimated `mid v20 / Spaghetti + Feature Stack maturation era` label and a short summary of the validated v20 baseline and Feature Stack becoming the next major system
36. +0 -0 - Audited `docs/History/1 - chatgpt.md` through `5 - chatgpt.md` and confirmed they now all have visible `Doc History` + `Purpose` sections at the top and HTML-commented raw transcripts underneath
37. +? -? - Added a structured historian summary to `docs/History/6 - chatgpt.md`, framed it around the product-module architecture plan, toe recovery pivot, tail-intent rules, and Simple Toe v1 rollout, and wrapped the raw transcript underneath in an HTML comment
38. +? -? - Added conversation 6 to `docs/History/0 - compiled-HISTORY.md` with an estimated `mid v20 / architecture refactor paused for toe recovery` label and a short summary of the architecture plan, toe recovery decisions, and the first Simple Toe v1 direction
39. +? -? - Reordered `docs/History/0 - compiled-HISTORY.md` into estimated project chronology and converted the conversation labels into a realized `/20/` version ladder from `20.1` through `20.6` while keeping the original conversation numbers
40. +? -? - Updated the `Timeline Rule` in `docs/History/0 - compiled-HISTORY.md` so realized versions are generation-aware, allowing older last-gen conversations to use ladders like `18.1`, `18.2`, and `18.3` instead of forcing everything into `/20/`
41. +? -? - Reclassified conversations `1` through `6` in `docs/History/0 - compiled-HISTORY.md` into a mixed last-gen/next-gen timeline: conversations `1-3` now use realized versions `18.1` to `18.3`, and conversations `4-6` now use realized versions `20.1` to `20.3`
42. +? -? - Added an `Active History Note` to `docs/History/0 - compiled-HISTORY.md` recording that we are still searching for the exact restart conversation and starter prompt that should become realized version `20.0`
43. +? -? - Added a structured historian summary to `docs/History/7 - chatgpt.md`, focused on the Feature Stack mental model, feature-level driver promotion, sketch-to-extrude references, and the first concrete Feature Stack v1 plan, then wrapped the raw transcript underneath in an HTML comment
44. +? -? - Added conversation 7 to `docs/History/0 - compiled-HISTORY.md` and re-ordered the next-gen timeline so conversation `7` is realized version `20.3` and conversation `6` becomes `20.4`
45. +? -? - Added a structured historian summary to `docs/History/8 - chatgpt.md`, focused on the clean-slate `/20/` restart architecture, Parts List decision, perfect repo hierarchy, selectors guidance, and param-routing roadmap, then kept the raw transcript underneath in an HTML comment
46. +? -? - Added conversation 8 to `docs/History/0 - compiled-HISTORY.md` as the current best `20.0` candidate and updated the timeline plus active history note to treat it as the clean-slate `/20/` restart conversation
47. +? -? - Reordered the visible conversation entries in `docs/History/0 - compiled-HISTORY.md` so they now follow realized-version order with the newest versions at the top and the oldest at the bottom, while still keeping conversation `8` marked as `20.0`
48. +? -? - Added `Current Goal` text to `docs/chill-mode-ChangeList.md` so this history pass is explicitly framed around building a prefix-aligned map of the early restart conversations and engine foundations
49. +? -? - Created `docs/History/00 - History-Task.md` as the first prefix-aligned historical task map, using conversation `8 - chatgpt.md` to organize the clean `/20/` restart work into phase-style sections under the current prefix scheme and noting the likely future need for a dedicated runtime prefix
50. +? -? - Added a top-level `Legacy Cleanup Checklist` to `docs/History/00 - History-Task.md` so the restart history also points at the main old systems, assumptions, and ownership patterns that should likely be retired
51. +? -? - Created `docs/History/0 - History-TaskList.md` as the short ordered index of phase prefixes and section names copied from `0 - History-TaskLog.md` so the historical task map now has a simple navigation layer
52. +? -? - Reformatted `docs/History/0 - History-TaskList.md` so the phase titles use the orange backtick block style and all section entries use blue `###` headings consistently
53. +? -? - Normalized the rest of `docs/History/0 - History-TaskList.md` to match the new `GE` layout with divider line, phase title, spacing, and indented one-line `Prefix - Section N - Name` entries
54. +? -? - Added a second flat ordered list to `docs/History/0 - History-TaskList.md` using bracketed sequence numbers like `[01]` so all historical sections can be scanned and referenced as one-line entries in order
55. +? -? - Added a new `Historical Task Checklist` section to `docs/History/0 - History-TaskList.md` with a short summary and checked entries for every ordered section so the file also functions like a restored milestone log of the early `/20/` restart
56. +? -? - Broke out `GE - Section 1 - Clean Restart Architecture` in `docs/History/0 - History-TaskLog.md` into a concrete `First Codex Prompt Task List` based on the original master setup prompt from conversation `8 - chatgpt.md`
57. +? -? - Added a note in `docs/History/0 - History-TaskList.md` pointing to the deeper first-prompt checklist now stored under `GE - Section 1` in the history task log
58. +? -? - Normalized the section labels in `docs/History/0 - History-TaskLog.md` to the same prefix-first convention used in `0 - History-TaskList.md`, such as `GE - Section 1 - Clean Restart Architecture`
59. +? -? - Added a compact sub-checklist under `GE - Section 1 - Clean Restart Architecture` in `docs/History/0 - History-TaskList.md` so the historical checklist now shows the main setup tasks inside the first restart phase
60. +? -? - Added a `Historian Mode > Restore Checklist Mode` rule to `docs/chill-mode-ChangeList.md` so old conversations can be reconstructed into `0 - History-TaskLog.md` and then condensed into the `Historical Task Checklist` in `0 - History-TaskList.md`
61. +? -? - Restored `GE - Section 2 - Runtime And Rebuild Rules` in `docs/History/0 - History-TaskLog.md` with a fuller historical checklist covering warm-worker persistence, latest-only scheduling, stale-drop protection, render-vs-build intent classification, canonical state separation, and shared protocol boundaries
62. +? -? - Added the reduced `GE - Section 2 - Runtime And Rebuild Rules` sub-checklist to `docs/History/0 - History-TaskList.md` so the short historical checklist now shows the main runtime/rebuild rules established in conversation `8 - chatgpt.md`
63. +? -? - Converted the `Historical Task Checklist` in `docs/History/0 - History-TaskList.md` to a dual-status legacy format where the first checkbox tracks current-system status and the second checkbox tracks whether the task was achieved historically
64. +? -? - Restored sections `3-13` in `docs/History/0 - History-TaskLog.md` with fuller historical checklists covering the restart engine roadmap, Parts List replacement, deterministic part ordering, independent-part thinking, param ownership, Jake Mode structure, selector discipline, viewer ownership, export shape, future-ready systems, and roadmap ordering from conversation `8 - chatgpt.md`
65. +? -? - Added reduced dual-status sub-checklists for sections `3-13` to `docs/History/0 - History-TaskList.md` so the short historical checklist now summarizes the full clean-restart milestone set from conversation `8 - chatgpt.md`
66. +? -? - Restructured all sections in `docs/History/0 - History-TaskLog.md` into grouped subcategories so the detailed history now reads like a cleaner restored phase checklist, including a condensed grouped rewrite of `GE - Section 1 - Clean Restart Architecture`
67. +? -? - Added a `Restored Checklist Format` section near the top of `docs/History/0 - History-TaskLog.md` so the file now defines how the grouped historical reconstruction format is supposed to be read
68. +? -? - Added a bottom `Humanized Task Summary` section to `docs/History/0 - History-TaskList.md` with plain-English summaries of all 13 restored tasks from the clean `/20/` restart conversation
69. +? -? - Grouped the `Humanized Task Summary` in `docs/History/0 - History-TaskList.md` into numbered `Part` blocks so related sections are clustered by ownership area, starting with `Part 1` for the three `GE` sections
70. +? -? - Added descriptive names to the humanized-summary `Part` headers in `docs/History/0 - History-TaskList.md`, including the prefix and full ownership label for each block
71. +? -? - Added an `Estimated Files Created` block to `GE - Section 1 - Clean Restart Architecture` in `docs/History/0 - History-TaskLog.md` so the restored clean-restart phase now also shows the likely starter file footprint from conversation `8 - chatgpt.md`
72. +? -? - Renamed the main restored-history units in `docs/History/0 - History-TaskLog.md` from `Section` to `Phase`, including the format description, headings, and local doc-history references
73. +? -? - Added global `[001]`-style execution numbers to the restored phases in `docs/History/0 - History-TaskLog.md` and synchronized `docs/History/0 - History-TaskList.md` to the same `Phase` wording and chronological ids
74. +? -? - Added `Estimated Worked On Files` and `Estimated Added Files` blocks across the restored phases in `docs/History/0 - History-TaskLog.md` so each phase now shows a likely file-touch footprint based on conversation `8 - chatgpt.md`
75. +? -? - Rewrote the `Legacy Cleanup Checklist` in `docs/History/0 - History-TaskLog.md` so it now reflects the restored `/20/` restart lessons more precisely: Parts List over scrubber, one warm worker, no second geometry truth, part-owned params, selector discipline, and canonical-state history rules
75. +? -? - Added a summary of the surviving Git commit chain to `docs/History/0 - GIT.md`, covering the four visible commits and what each one most likely represented in the `/20/` repo timeline
75. +? -? - Created `docs/History/0 - GIT.md` as the starting history doc for Git-related repo events, resets, recovery notes, and public-baseline milestones
76. +? -? - Updated `docs/Phases/01_Phase-Setup.md` with engine-birth lessons from the restored `/20/` restart history, confirming the prefix list still stands while sharpening `GE`, `VM`, `DR`, `AS`, `VR`, and `JK` around warm-worker rules, part-owned routing, Parts List direction, selector discipline, and no-second-truth boundaries
77. +? -? - Created `docs/roadmap/02_Vision-Roadmap.md` as the new high-level product and architecture vision, defining Spaghetti as the canonical authoring system, Jake mode as the simplified end-user layer, drivers/control-viz as the bridge between them, and the long-term direction for parts, viewer, debug, and multi-document growth
78. +? -? - Expanded `docs/roadmap/02_Vision-Roadmap.md` with a new `Next Gen Plans - Parametric Parts` section so the vision now reflects the longer-range goal of evolving beyond ParaHook into a typed parametric-parts platform with stronger value/object contracts and reusable graph-engine architecture
79. +? -? - Added a 10-name idea list to the end of the `Next Gen Plans - Parametric Parts` section in `docs/roadmap/02_Vision-Roadmap.md`, exploring public/open-source next-gen naming directions around Parametric Parts, Spaghetti, ParaHook, ParaFootPad, and custom part systems
80. +? -? - Created `docs/roadmap/03_Spaghetti-Studio.md` as the future platform concept doc, capturing what Spaghetti Studio could become after ParaHook, the typed-graph and multi-product ideas behind it, and the guardrail rules current ParaHook work should follow to avoid a major future restructure
81. +? -? - Rebuilt `docs/Plans/Wish-Features/Spaghetti-Editor/01.0 - Master Spaghetti.md` as the top-level Spaghetti system doc, organizing it around graph-first ownership, current editor layers, spawnable node families, and the future multi-graph architecture needed for multiple loaded graph files, graph-aware worker builds, and a Graphs panel above Parts
82. +? -? - Created `docs/Plans/Wish-Features/Spaghetti-Editor/01.5 - Muliple Spaghetti Bowls` as the master plan for the future multi-graph Spaghetti editor, covering graph/document ownership, graph-aware worker and viewer routing, the likely Graphs panel above Parts, and the rules current work should follow so multi-graph support stays feasible later
83. +? -? - Added a concrete phased rollout to `docs/Plans/Wish-Features/Spaghetti-Editor/01.5 - Muliple Spaghetti Bowls.md`, breaking the future multi-graph feature into `SP-7` through `SP-12` across graph-document foundations, graph-aware worker/preview routing, a Graphs panel, shared viewport composition, multi-window editing, and multi-graph debug/polish
84. +? -? - Created `docs/Plans/Wish-Features/WISHLIST.md` as the short master wishlist checklist, collecting the main future feature buckets across Spaghetti editor upgrades, Jake mode, control-viz, multi-graph, Feature Stack, viewer parity returns, export growth, and the longer-range Spaghetti Studio direction
85. +? -? - Created `docs/roadmap/Focused-Roadmap.md` as the short execution-order roadmap and then reformatted it into a numbered phase sequence covering what should happen next across Spaghetti stability, wires/preview/debug, drivers, part hardening, Jake mode, viewer parity, export, and later multi-graph expansion
86. +? -? - Created `docs/Bugs/0_Bug_Report.md` as the structured running bug report, grouping the known Spaghetti window/split issues, the Cube-to-OutputPreview render regression, toolbar fragmentation, and wires-visibility problems into one clean current bug list with status, likely ownership, and related docs
87. +? -? - Populated the new `## Bug List` section in `docs/Bugs/0_Bug_Report.md` with one-line entries for Bugs 2 through 6 so the file now has a short bug index above the detailed writeups
88. +? -? - Re-formatted the `## Priority Order` section in `docs/Bugs/0_Bug_Report.md` to match the one-line bug-list style, ranking the current bugs by `Bug N` labels instead of a separate prose numbering scheme
89. +? -? - Added status tags to all items in the `## Bug List` section of `docs/Bugs/0_Bug_Report.md` so the short bug index now shows `[open]`, `[investigating]`, or `[planned]` at a glance without changing the separate ranked priority list
90. +? -? - Re-formatted the `## Bug List` lines in `docs/Bugs/0_Bug_Report.md` so each entry now reads in the order `Bug N`, then status key, then the note text
91. +? -? - Began unifying `docs/TaskHistoryCompilation.md` with the restored history system by folding the engine-birth phases from `docs/History/0 - History-TaskLog.md` into `Phase A`, adding the clean `/20/` restart architecture, runtime/rebuild rules, and engine-roadmap foundation decisions to the legacy app-creation block
92. +? -? - Replaced the legacy `Phase A-L` labels in `docs/TaskHistoryCompilation.md` with retrofitted prefix-based historical IDs like `GE-0A`, `NI-0A`, `PT-0A`, `FS-0A`, `DR-0A`, `DC-0A`, and `GE-0D`, while preserving the old changelog ranges for traceability
93. +? -? - Created `docs/Phases/00_Phase_List.md` as the unified accomplished-phase history, merging the restored engine-birth phases from `docs/History/0 - History-TaskLog.md` with the shipped product/system phases from `docs/CHANGELOG.md` into one chronological prefix-based checklist with entries `[001]` through `[038]`
94. +? -? - Added a `Transition Note` between `[013]` and `[014]` in `docs/Phases/00_Phase_List.md` clarifying that the remaining seam is a source-transition seam between restored engine-birth history and shipped changelog-derived implementation phases
95. +? -? - Added a `List Of Conversations Compiled` section to `docs/History/0 - compiled-HISTORY.md` so the file now shows a quick index of the currently included conversation summaries and their realized versions before the detailed entries
96. +? -? - Processed `docs/History/9 - chatgpt.md` in historian mode by adding a structured summary at the top, estimating its placement as early next-gen Spaghetti `S3` integration work, and wrapping the raw conversation in an HTML comment
97. +? -? - Added conversation `9` to `docs/History/0 - compiled-HISTORY.md`, placed it into the realized-version ladder as `20.2`, and updated the visible conversation order and timeline mapping so conversations `5`, `7`, and `6` now read as `20.3`, `20.4`, and `20.5`
98. +? -? - Reclassified conversation `6` in `docs/History/0 - compiled-HISTORY.md` from `20.5` to `18.4`, updated its era summary to reflect late legacy-to-next-gen toe recovery work, and reordered the compiled conversation list accordingly
99. +? -? - Added a short `Direct User Prompt Clues` quote list near the top of `docs/History/8 - chatgpt.md` so the clean-restart file now preserves some of the direct user commands that revealed the restart intent and early product direction
100. +? -? - Tightened the extracted-user-lines section in `docs/History/8 - chatgpt.md` into a short numbered in-order list of the lines most likely spoken by the user, instead of a broader mixed prompt dump
101. +? -? - Reworked the `Extracted User Lines` section in `docs/History/8 - chatgpt.md` into a numbered question-only list pulled from the raw conversation so it now reflects the user’s actual asks more cleanly
102. +? -? - Restored the broader mixed `Extracted User Lines` list in `docs/History/8 - chatgpt.md` and added a separate numbered `User Questions` section so the file now keeps both views of the same conversation
103. +? -? - Expanded `Direct User Prompt Clues` in `docs/History/8 - chatgpt.md` with the more directive repo-setup and Codex-command asks, including the explicit `give me full codex command to set up repo` line
104. +? -? - Added restored-history context to `docs/tasks/001 - GE - Phase 1 - Clean Restart.md` so the file is clearly framed as a reconstruction from `docs/History/8 - chatgpt.md` rather than a guaranteed original plan document
105. +? -? - Added top-level `Summary`, `Core Concepts`, and `Golden Rules` sections to `docs/tasks/001 - GE - Phase 1 - Clean Restart.md` so the restored first-phase task reads more like a proper historical phase doc
106. +? -? - Added a `Phase List` section near the top of `docs/History/9 - chatgpt.md` mapping the conversation to the restored `/20/` phase buckets, including completed groundwork and the newly defined routing and Spaghetti `S3` bridge work
107. +? -? - Added a `Phase List` section near the top of `docs/History/4 - chatgpt.md` showing that the conversation reads more like repo/public-baseline recovery and deployment work than a direct core-engine follow-up phase
108. +? -? - Expanded the `Ordered Phase List` in `docs/History/0 - History-TaskList.md` with the phases learned from conversation `9` and inserted explicit open `[ ]` bridge rows for the still-missing setup/vertical-slice/artifact-baseline phases between the clean restart and later Spaghetti integration work
109. +? -? - Reformatted the full `Ordered Phase List` in `docs/History/0 - History-TaskList.md` so every line now shows phase id, completion status, source tag like `Conv 8` or `Gap`, and the phase name in one consistent shape
110. +? -? - Added a historian header and top-level phase breakdown to `docs/History/10 - chatgpt.md`, framing conversation 10 as a later `/20/` checkpoint where canonical layout, the graph system, Feature Stack v1, and expose-fields were treated as complete and baseplate geometry was the next milestone
111. +? -? - Added conversation `10` to `docs/History/0 - compiled-HISTORY.md`, placed it into the realized-version ladder as `20.5`, and inserted its summary into the visible compiled conversation order
112. +? -? - Expanded the `Ordered Phase List` in `docs/History/0 - History-TaskList.md` with the phases learned from conversation `10`, including canonical layout, the Spaghetti graph system, Feature Stack v1, expose-fields, and the open next milestone for `Baseplate Geometry v0`
113. +? -? - Compared conversation `10` against `docs/CHANGELOG.md` and added a `Likely CHANGELOG References` list under `## Conversation 10 - ChatGPT` in `docs/History/0 - compiled-HISTORY.md`, including the strongest matching Feature Stack / Expose Fields entries and notes for missing exact matches
114. +? -? - Compared conversation `7` against `docs/CHANGELOG.md`, added a phase/changelog overlap note near the top of `docs/History/7 - chatgpt.md`, and added a `Likely CHANGELOG References` list under `## Conversation 7 - ChatGPT` in `docs/History/0 - compiled-HISTORY.md`
115. +? -? - Compared conversation `5` against `docs/CHANGELOG.md`, added a phase/changelog overlap note near the top of `docs/History/5 - chatgpt.md`, and added a `Likely CHANGELOG References` list under `## Conversation 5 - ChatGPT` in `docs/History/0 - compiled-HISTORY.md`
116. +? -? - Added `Best Guess Changelog Range` subsections under conversations `5`, `7`, and `10` in `docs/History/0 - compiled-HISTORY.md`, including split primary/later-echo ranges where the conversation seems to span both initial landing and later maturation
117. +? -? - Reworked the `Ordered Phase List` in `docs/History/0 - History-TaskList.md` so each row now carries the best current conversation anchor and a short note describing what that phase actually achieved, including overlap-aware tags for conversations `5`, `7`, and `10`
118. +? -? - Inserted additional explicit `Gap` rows into the `Ordered Phase List` in `docs/History/0 - History-TaskList.md` for the likely missing first-landed Spaghetti graph UI, Feature Stack v1 implementation, and Expose Fields implementation steps before the later mature checkpoint conversations
119. +? -? - Added `## Conversation 9 Task Log` to `docs/History/9 - chatgpt.md`, breaking conversation 9 into restored phase/task buckets for canonical part identity, instance-aware VM baseline, Spaghetti `S1` and `S2`, param routing v20, worker affected-part routing, and the `S3` compile-to-build bridge, with estimated worked-on and added file lists
120. +? -? - Simplified `## Phase List` in `docs/History/9 - chatgpt.md` into a one-line phase-title index so the top of the file now matches the detailed restored task-log phases below
121. +? -? - Added `## Conversation 5 Task Log` to `docs/History/5 - chatgpt.md`, restoring detailed phase/task buckets for engine validation, advanced Spaghetti editor UX, Feature Stack v1 app-layer planning, worker execution planning, and expose-fields direction with estimated worked-on and added file lists
122. +? -? - Populated `## Conversation 5 Phase List` in `docs/History/5 - chatgpt.md` with a one-line phase-title index that matches the restored conversation-5 task log below it
123. +? -? - Added `## Conversation 7 Phase List` and `## Conversation 7 Task Log` to `docs/History/7 - chatgpt.md`, restoring the conversation into phase/task buckets for part-node CAD identity, Feature Stack structure, driver-promotion design, and Sketch-to-Extrude reference rules with estimated worked-on and added file lists
124. +? -? - Added `## Conversation 10 Phase List` and `## Conversation 10 Task Log` to `docs/History/10 - chatgpt.md`, then reconciled `docs/History/0 - History-TaskList.md` so conversations 5, 7, and 10 now occupy one coherent restored phase sequence from `[024]` through `[037]`
125. +? -? - Updated `docs/History/0 - compiled-HISTORY.md` with the current best gap estimate between conversations 8 and 9, noting that the missing middle now looks like a small dense `2-4` conversation implementation burst rather than a large unknown era
126. +? -? - Added a classification heuristic to `docs/History/0 - compiled-HISTORY.md` that HookSpec-heavy conversations should be treated as last-generation by default unless there is strong explicit `/20/` evidence
127. +? -? - Processed `docs/History/11 - chatgpt.md` in historian mode by adding a planning summary, changelog overlap/range notes, a phase list, and a detailed task log, then updated `docs/History/0 - compiled-HISTORY.md` to place conversation `11` before conversation `10` in the realized `/20/` ladder
128. +? -? - Rebuilt `docs/Change-List-COMPILED.md` into one unified historical change index with compiled-order ids, reconstructed `18.x` + pre-`CHANGELOG.md` rows, explicit `Gap` entries, and the real shipped changelog continuing from source id `[001]`
129. +? -? - Reset the first compiled-order ids under `# Change List - compiled` in `docs/Change-List-COMPILED.md` so the separated `/20/` block starts from `[001]` again after the `# Last gen` section and updated the shipped-boundary note to match
130. +? -? - Added the remaining seam gaps to `docs/Change-List-COMPILED.md` after restored phase `[023]`, including an explicit restored-history-to-first-shipped-changelog handoff row and a first-landed graph/Feature-Stack/expose-fields implementation-wave row, then shifted the real changelog boundary down to `[026] [001]`
131. +? -? - Added a leading `[x]` or `[ ]` marker to every row in `docs/Change-List-COMPILED.md` to show whether each entry likely involved real file edits, and corrected restored `GE - Phase 1 - Clean Restart Architecture` to `[x]`
132. +? -? - Added a second vision-alignment marker to every row in `docs/Change-List-COMPILED.md` so each entry now shows both likely file-edit status and whether it still fits the long-term Spaghetti / ParaHook direction, marking obvious legacy scrubber/baseplate UI experiments and explicit gaps with `[ ]`
133. +? -? - Added a legend under `# Change List - compiled` in `docs/Change-List-COMPILED.md` explaining the meaning of the first and second row markers
134. +? -? - Collapsed `docs/Change-List-COMPILED.md` from the two-marker row format into one four-state marker `[x]`, `[~]`, `[?]`, or `[ ]`, and updated the header legend and format notes to match
135. +? -? - Replaced the restored `/20/` row labels in `docs/Change-List-COMPILED.md` from `est. 2026-03-03 pre-CHANGELOG` to direct source tags like `Conv 8`, `Conv 9`, or `Gap`
136. +? -? - Rebuilt `docs/Phases/02_Phase-Setup.md` as the history-aware phase map, covering canonical prefixes, historical alias mapping, researched phase families, and the known remaining gaps between restored history and shipped changelog history
137. +? -? - Added `docs/Documentation-Rules.md` as the central docs-maintenance rules file and recorded that `docs/Phases/00_Phase-Setup.md` is now the canonical source for phase setup decisions
138. +? -? - Consolidated the phase-setup rule so `docs/Phases/00_Phase-Setup.md` now clearly reads as the single active canonical phase-setup source and `docs/Documentation-Rules.md` now says older copied setup docs are reference-only
139. +? -? - Renamed the shipped `docs/Change-List-COMPILED.md` block into prefix-first phase-style entries so the real changelog rows now match the restored pre-changelog history format
140. +? -? - Updated `docs/Phases/00_Phase-CheckList.md` so `## Current Canonical Prefix Phases` now uses the modern canonical prefixes and maps older shipped `CK`, `CT`, `IN`, and `UI` entries into those current ownership buckets
141. +? -? - Added an ordering note and short per-prefix labels to `docs/Phases/00_Phase-CheckList.md` so the canonical prefix checklist now explains why `GE`, `VM`, `NI`, and the later buckets are arranged in that architectural order
142. +? -? - Tightened `docs/Phases/00_Phase-CheckList.md` so the canonical checklist stays compact and each shipped row now uses the `PREFIX - Phase N - Name` format instead of the older mixed short-code labels and longer per-prefix clutter
143. +? -? - Rebuilt `docs/Phases/00_Phase-CheckList.md` from `docs/Change-List-COMPILED.md` so it now groups the full merged history into compact checklist sections for the canonical prefixes plus explicit `Gaps`, `OO / Docs Meta`, and `Last Gen Conversation Anchors`
144. +? -? - Added a short legend under `## Current Canonical Prefix Phases` in `docs/Phases/00_Phase-CheckList.md` clarifying what `[x]`, `[~]`, and `[ ]` mean inside that grouped canonical checklist
145. +? -? - Condensed `docs/Phases/00_Phase-CheckList.md` so repeated rows with the same phase number now collapse into one phase line, removing duplicate phase numbers across the grouped checklist
146. +? -? - Created `docs/Phases/00_Phae_List.md` as the chronological version of the canonical phase checklist by taking the entries from `docs/Phases/00_Phase-CheckList.md` and ordering them by historical sequence instead of by prefix grouping
147. +? -? - Added ordered `[NNN]` numbers to every row in `docs/Phases/00_Phae_List.md` so the chronological canonical phase list can be referenced as a stable numbered sequence
148. +10 -2 - Updated `docs/Chill-Mode-Edits.md` so its instructions now match the current `AGENTS.md` chill-mode workflow and clearly mark this file as the canonical chill log without deleting or archiving older chill files

<!-- ============================================================ -->
### Chill Batch #1
<!-- ============================================================ -->

Status:
- compiled

Started:
- 2026-03-05 23:57

Ended:
- 2026-03-06 02:35

Mode:
- chill mode inactive

Consolidated To:
- `docs/CHANGELOG.md` `[101]`

Entries:
1. Chill mode activated for rapid-edit session
2. Added `docs/Wish-Features/radio-Sampler.md` based on the old `/14/replicad-app` radio feature and its per-control stable-random Keota cue behavior
3. Added `docs/Wish-Features/04 - Gizmo.md` with a parity plan to bring `/20/parahook` back to the old `/15.1` top-right clickable gizmo style and add the missing visual controls for line opacity, text, and text height
4. Added `docs/Wish-Features/11 - Environments.md` from the verified old Scenes system in `/14/replicad-app` and `/18/src/viewer/viewer.ts`, including the full Stars, Nebula, and Swarm control list
5. Renamed `docs/Wish-Features/11 - Environments.md` to `docs/Wish-Features/11 - Scenes.md` so the wishlist uses the original `Scenes` naming consistently
6. Added `docs/Architecture/History.md` with a plain-English per-repo timeline from `/1/` through `/20/`, covering the shift from the earliest worker-driven ParaHook prototype to the classic monolithic generator era, the `/18/` layered-engine reset, and the modern `/20/` React + Spaghetti architecture
7. Added `docs/Wish-Features/old-Wishes.md` as the master consolidated wishlist of notable old-app features and UX qualities missing, reduced, or only partially restored in `/20/`, including gizmo, scenes, radio, reference workspace, layers/materials, section-cut inspection, camera/framing, build/status UX, and the old all-in-one workbench feel
8. Added `docs/Architecture/System-Map.md` as the current `/20/` one-page architecture map covering the app shell, stores, selectors, Spaghetti compiler, build wiring, build dispatcher, worker runtime, viewer host, viewer, preview pipeline, and identity boundaries
9. Added `docs/Architecture/Glossary.md` defining the main `/20/` architecture terms including `PartArtifact`, `partKey`, `partKeyStr`, `slotId`, `viewerKey`, build identity, preview identity, Feature Stack, Spaghetti, OutputPreview, ViewerHost, and worker runtime
10. Added a chill-mode `Docs Header Rule` requiring docs created or edited during chill mode to maintain a local `Doc History` section near the top so stale docs are easier to spot
11. Numbered the entries in `Chill Batch #1` and updated this file to use its own local `Doc History` header
12. Updated the chill-mode docs rule so `Doc History` entries should be numbered newest-first and exempted `docs/chill-mode-ChangeList.md` from needing its own local `Doc History` section
13. Added numbered newest-first `Doc History` blocks to the chill-batch docs under `docs/Plan/Architecture` and `docs/Plan/Wish-Features`
14. Updated the doc history format to include `HH:MM` timestamps and applied that format to the chill-batch docs
15. Cleaned up `docs/Plans/File-List.md` into a structured reference doc with headers, grouped sections, a future-doc checklist at the top, and a local numbered `Doc History`
16. Added plain-English documentation and technical examples to `docs/Plans/Wish-Features/Spaghetti-Editor/01.4 - Nodes.md` explaining what nodes currently are in the Spaghetti system
17. Added a master Spaghetti inventory to `docs/Plans/Wish-Features/Spaghetti-Editor/01.0 - Master Spaghetti.md`, including the main editor elements, current node families, current part nodes, and the node types that can currently be spawned on the canvas
18. Added `docs/roadmap/LegacyList.md` as a practical cleanup map of likely dead ends, legacy seams, structural leftovers, and decisions needed across legacy mode, Spaghetti compatibility, node types, feature compatibility, and docs/folder drift
19. Cleaned up the formatting of the new `/00/` entry in `docs/Plans/Architecture/History.md` so it matches the rest of the repo timeline structure
20. Added `docs/Plans/Wish-Features/Jake-Mode.md` describing Jake mode as the simplified end-user editing layer on top of the Spaghetti-defined model, including drivers, control-viz handles, vec2 controls, multi-driver bindings, and future equations
21. Added `docs/Plans/Decisions.MD` as a decisions backlog covering unresolved product, architecture, Spaghetti, Jake mode, driver, control-viz, identity, viewer, multi-window, and docs/process choices
22. Added a `Decisions Made` section to `docs/Plans/Decisions.MD` and copied the first two resolved decisions there so settled choices are separated from the open backlog
23. Cleaned up `docs/Plans/Wish-Features/Spaghetti-Editor/01.3 - Wires ui` into a structured feature note for active wire visualization, render-path toggle behavior, selector-driven active-path derivation, and compile/build pulse animation
24. Added local `Doc History` blocks to the remaining Spaghetti editor docs so every file in `docs/Plans/Wish-Features/Spaghetti-Editor` now has one
25. Drafted `docs/Plans/Temp-CHANGELOG-Entry.md` as a temporary consolidated `CHANGELOG.md`-style entry for `Chill Batch #1` without touching the real permanent changelog
26. Added a safe final-response diff-summary rule to `AGENTS.md` requiring `Diff: +A / -B` for files Codex changed in the current task, or `Diff: unavailable` when a reliable per-task count cannot be computed
27. Added a header to `docs/change-List.md` explaining that it should remain a fast one-line index of completed changes, not a second full changelog
28. Added `docs/CodexContext/1_CodexContext.md` as a compact handoff of the main product direction, architecture rules, history reconstruction state, canonical doc sources, phase system, and workflow rules established in this conversation
29. Added `docs/CodexContext/History-Chats/1-CodexChat.md` in the structured summary-first chat-history format, using `## Conversation`, `### User Prompt`, and `#### Codex Response` to organize this docs-consolidation session
6. 2026-03-07 13:31 - +31 -4 - Added a `Changelog Format` block to the `docs/CHANGELOG.md` doc header so the file now documents its own entry wrapper, numbering rule, and heading-level structure
7. 2026-03-17 18:23 - +97 -42 - Rewrote `docs/Phase-Plans/Tasks/Future/04.1A - VR-SP-DBG - Console Shell And Transcript Core.md` from an open-question planning note into an implementation-ready execution spec, locking the first console shell behavior, input scope, transcript model, required layers, first publishers, test plan, and non-goals for `4.1A`
